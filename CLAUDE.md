# CLAUDE.md

**このファイルにルールは書かない。**

このリポジトリは Claude 以外の AI（Codex 等）も共同で触る。
ルールが2箇所にあると必ず食い違うので、**正は [`AGENTS.md`](./AGENTS.md) だけ**にしてある。

## 従うもの

[`AGENTS.md`](./AGENTS.md) に従え。それ以外に従うものは無い。

`.claude/` `*.claude.json` など特定の AI だけが読む設定は**このリポジトリに置かない**。
機械で強制したいことは、**どの AI でも実行できるコマンド**にして `npm run check` に落とす。
