# CLAUDE.md

**このファイルにルールは書かない。**

このリポジトリは Claude 以外の AI（Codex 等）も共同で触る。
ルールが2箇所にあると必ず食い違うので、**正は [`AGENTS.md`](./AGENTS.md) だけ**にしてある。

## 従うもの

[`AGENTS.md`](./AGENTS.md) に従え。それ以外に従うものは無い。

`.claude/` `*.claude.json` など特定の AI だけが読む設定は**このリポジトリに置かない**。
機械で強制したいことは、**どの AI でも実行できるコマンド**にして `npm run check` に落とす。

## 最初に見るもの（道しるべ。ルールではない）

**セッションを始めたら、まず大計画を見る。**

- @docs/ops/roadmap.md — **大計画**（スタートからゴールまでの全体像）
- @docs/ops/phase — 現在地（1行）
- @AGENTS.md — 従うルールの正

ここに置くのは**場所の案内だけ**で、ルールは `AGENTS.md` にある（上の方針どおり）。
案内が無視されても止まるように、**`npm run check` の1本目（`plan-read`）が
「このセッションが大計画を読んだか」を見て、読んでいなければ赤で止める**。
印を書くのは `node scripts/guard/checkin.mjs`。
