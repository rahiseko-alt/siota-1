# Session Handoff (docs/handoff.md)

> **運用ルール**
> - セッション間の揮発的な作業状態の引き継ぎファイルです。
> - セッション開始時 (`In`) に読み込み、セッション終了時 (`Out`) に更新します。
> - 恒久的な設計情報はここではなく `docs/design.md` に記載してください。

> ## ⚠️ このファイルの読み方（2026-08-25 追記）
>
> **「## 0」より下は過去の記録で、現在の状態ではない。**
> とくに `verify:m6 11/11` `verify:roundtrip 15/15` のような **PASS の数字は、
> すべて 2026-08-23 以前の実測**。これらの検査は F1（UI とバックエンドの隔離）で
> **削除済み**で、いま実行すると `npm error Missing script` で EXIT 1 になる。
> 消した経緯と作り直す時期は `AGENTS.md` の STEP 5 と `docs/ops/plan.md`（放置リスト）#8 に書いた。
>
> **数字だけを見て「通っている」と読まないこと**（`F-20260821-21` の型）。
> いま実行できるものと、その結果は、すぐ下の「## 0」に書いてある。

> ## ⚠️ 大計画は `docs/ops/plan.md` 1本（2026-08-29 統合・マスター指示）
>
> **`docs/ops/delivery-plan.md` と `docs/deferred.md` は廃止した。** 中身はすべて
> `docs/ops/plan.md` に統合済み（第9章 P-1〜P-3・第10章 C-1〜C-12・第12章 放置リスト）。
> **セッション開始時は `node scripts/guard/checkin.mjs` を必ず実行すること**——
> `npm run plan` の出力（進捗表・「いまやる番」）を画面に出し、読んだ印 `.plan-read` を書く。
> **この印が無いと `npm run check` が EXIT 1 になる**（`scripts/guard/plan-read.mjs`）。
> Claude Code では `.claude/settings.json` の SessionStart フックが自動でこれを起動する。
>
> **セッション終了時は、`docs/ops/plan.md` の「いまやる番」の行を必ず書き換えること。**
> `checkout.mjs` の7項目目が、このセッションで変わったかを機械で見る。

---

## 0-AD. いちばん新しい（2026-08-31・その27）— **`0-AC`の修正後も「まだ出てこない」。サブ3体の敵対検証で真因（の可能性が高いもの）を発見・修正**

`0-AC`の「前回を複製」修正をデプロイ後もマスターから「出てこない」と再報告。
マスター指示「ゼロベースでサブに徹底調査させろ、3体で」を受け、独立した3体の
サブエージェントを並行起動（画面遷移の全経路調査／バックエンドの実機再現テスト／
配信パイプライン調査）。

**パイプライン班**: ビルド・wrangler設定・キャッシュヘッダに問題なし。`0-AC`の修正は
本番に正しく反映済みと確認。

**画面遷移班**: 前回の修正（「前回を複製」）は正しく効いているが、**より確度の高い
真因**を発見——④店舗設定（管理画面）の「使用オプションを保存する」ボタンが、
`/api/shop`の読み込み完了を待たずに押せる状態だった。`backend/js/supabase-admin.js`の
`appendGroomingOptionsEditor()`は`names = []`で初期化した直後に非同期でfetchを投げるが、
保存ボタンは最初から有効。読み込みが終わる前（表示上は一瞬「空」に見える状態）に
保存を押すと、`PATCH /api/shop { groomingOptions: [] }`が飛び、**店舗の使用オプション
一覧を空配列で丸ごと上書きしてしまう**。④カルテ作成画面の選択欄が「1件も無ければ
隠す」という正しい仕様どおりに空になっていた——つまりUIの表示バグではなく、
**データそのものが空になっていた**可能性が高い。

同エージェントはもう1件、`createNewKarte()`（②一覧の「＋ 新規カルテを作成する」
ボタン）もF2仮データ専用のまま実データでも表示され続けており、押すと
`selectKarte('新規わんちゃん', ...)`という固定のダミー値だけで実データの初期化
（使用オプション取得含む）を一切せず進むことを発見。ただし**backendへの保存も
一切されない**（`context.petId`が無いため`commitReport()`は`goToStep(4)`するだけで
何も保存しない）ため、実際に踏んでいれば「新規わんちゃん」という偽の名前に
すぐ気づくはずで、今回の報告（「使用オプションが出ない」だけ）とは症状が
一致しない。**このセッションでは修正していない**——`前回を複製`と同型の
未修理の危険地帯として次回へ持ち越す（放置リストへの追加を検討）。

**やったこと**: `backend/js/supabase-admin.js`の`appendGroomingOptionsEditor()`で、
`/api/shop`の読み込みが終わる（または失敗する）まで保存ボタンを`disabled`にした。
読み込み失敗時も、手元の状態（空のまま）が実際の値と合っている保証が無いため
保存させない。

**検査**: `npm run build`/`check`。CIと同じ14本の`verify:*`を`db reset`直後に実行
（`verify:delete`/`verify:draft`が1回ずつ一時的に落ちたが、単独再実行では
全て緑——バックグラウンドで並行稼働していたサブエージェントとDocker資源を
取り合った一時的な競合と判断。実害の証拠は無い）。使い捨てスクリプトで
「読み込み中は保存ボタンがdisabled、読み込み後はenabled」を実測確認（削除済み）。

**まだ確認できていないこと**: **本番の`grooming_options`が実際に空になっているかは
確認できない**（Claude Codeは本番Supabaseの認証情報を持たない・`A-1`）。この修正を
デプロイしても、既に空になっている一覧は自動では戻らない——マスターに④店舗設定の
画面を開いてもらい、一覧が空なら手動で追加し直してもらう必要がある。

## 0-AC. いちばん新しい（2026-08-31・その26）— **「使用オプション」が②一覧から出ない実害を発見・修正（`0-AB`の続き）**

マスターより「本番に出ていない」と報告。デプロイ済みのHTML/JSを直接確認したところ
コード自体は正しく配信されていた（`curl`で`options-grid`/`renderOptionChips`の存在を確認）
ため、②犬一覧からの導線を洗い直した。

**原因**: ②一覧のカードにある「前回を複製」ボタン（`cloneAndCreate()`）は、F2（仮データ）
時代専用の導線で、実データの犬にも表示され続けていた。押すと「複製完了」というアラートを
出すが**実際には何も複製しておらず**（`selectKarte()`を直接呼ぶだけ）、実データの初期化
一式（下書きの再開・**今回の使用オプションの一覧取得**）を丸ごと飛ばして④カルテ作成画面へ
進む。`scripts/verify-screens.mjs`のコメントに「実際にそれで一度落ちた」と書かれており、
検査側は昔からこの場所を避けて犬の名前を直接押す実装になっていた——**既知の危険地帯だが
直っていなかった**。マスターが実際にこのボタンを押していたかは確認していないが、
②一覧から④へ入る経路のうち唯一使用オプションが出ない経路であり、他に説明が付かない。

**やったこと**: `src/js/ui.js: renderDogs()` で、実データの犬（`dog.id`が在る）には
「前回を複製」ボタンを出さない（`btn-invite`と同じ判定基準で`.remove()`）。実データでは
カード・犬の名前を押せば同じ画面に正しい初期化つきで入れる（`openPet()`）ため、
機能が失われることはない。

**検査**: `npm run build`/`check`/`test`、CIと同じ14本の`verify:*`を`db reset`直後に
全緑で確認。実データの犬一覧で「前回を複製」ボタンが0件になったことを使い捨てスクリプトで
確認（削除済み）。

**まだ残っている（次のセッションへ）**: 「前回を複製」自体は仮データの犬（`window.DUMMY`。
`/`の未ログイン経路のみ）にはまだ出る——これは元々の仕様どおりで、実際に壊れるのは実データ
だけだったので、実データからボタンごと消すだけに留めた。**「前回を複製」を実データで
本当に使えるようにする（前回のカルテ内容を実際に読み込む）**機能自体は未実装のまま
——アラート文言が嘘をついていた問題は実データでは解消したが、機能そのものを作るかは
マスター判断待ち。

---

## 0-AB. いちばん新しい（2026-08-31・その25）— **「使用オプション」を本番へ出した。PR #54 マージ・migration適用・デプロイまで完了**

`0-AA` の続き。マスター指示で以下を実施:

1. PR #54 をドラフト解除して squash merge（`master`・`6215f7f`）
2. マスターが本番Supabase（`bcodloqwnrhcuvevfguy`）のSQL Editorで
   `202608310011_grooming_options.sql` を実行、「成功した」と報告
3. `deploy.yml` を `master`（`6215f7f`）に対して起動、成功（run #8）
4. 再デプロイ後、`curl https://trimmer-system.kouheikosehira.com/api/pets` が
   通常どおり `401 authentication required` を返すことを確認（`0-W`のような
   `502`/`42703`ではない）——犬関連APIは正常に稼働している

**「使用オプション」機能はいま本番で使える。** ただし**実際に⑤⑥画面を人の目で
見て確認したのはローカル環境のみ**（`0-AA`参照）——本番そのものでの目視確認は
まだ。`0-Y`から持ち越しているPR #52分（ピンチ修正・次回来店時期）の本番目視確認と
合わせて、次のセッションでマスターに確認してもらうこと。

---

## 0-AA. いちばん新しい（2026-08-31・その24）— **「使用オプション」を復活させた（マスター指示）。PRはまだドラフト・本番未反映**

マスターより「以前あったオプション選択が消えているから復活させろ」との指示。調べたところ
`design-samples/ponchi-v2.html`（F1で削除済みの古い試作。いまの src/ とは無関係）にあった
「今月の使用オプション」トグルカード（アメージング／シセルリノ／ジュエルT／ジュエルM／
NAMAKERA／キラトリ／グロスチャー／皮膚spa／被毛spa をON/OFFで選び、名前を追加もできる）
と特定。`AskUserQuestion` で「これですか？」「どこに出すか」「一覧は店舗ごとに管理者が
編集できるようにするか」を確認してから実装した（旧UIはいまのデータ構造に無いので作り直し）。

**やったこと**:
- 新規 migration（`202608310011_grooming_options.sql`）: `shops.grooming_options`
  （`text[]`・既定は旧デザインの9件）を新設。CHECK制約はサブクエリを直接書けないため
  `private.grooming_option_names_ok()` 関数に切り出した。**本番 Supabase にはまだ
  当たっていない**（`A-1`。マスターの手作業が要る——`0-W`のデプロイ事故と同じ轍を踏まないため）
- `worker/`: `updateShopSchema`・`getShop`/`updateShop` を拡張（`defaultRevisitDays`と
  `groomingOptions`はどちらか片方だけでもPATCHできる。渡された列だけ書く）
- `src/js/ui.js`（④）: 店舗の一覧からチップを動的に組み立て（`renderOptionChips`）、
  複数選択トグル（`toggleOption`）。`applyReport`/`extractReport`にも配線
- `backend/js/magazine-view.js`（⑤⑥共有）: 選んだ名前を「カット」カードにタグで表示
  （`renderOptionTags`）
- `backend/js/supabase-admin.js`（④店舗設定）: 一覧の追加・名前変更・削除・保存UI
- `docs/ops/key-parity-F3.md`に`options`キーを追記、`verify-report-roundtrip.mjs`に
  `8b.`/`16b.`を追加（STEP 5の指示どおり）。**実際に`renderOptionTags`呼び出しを
  コメントアウトして赤になることを確認してから緑に戻した**証拠を`proof-of-red.md`に
  記帳（**`## 証明済み`の節は`## F4を閉じる範囲`より前で終わる**——過去に複数回
  踏まれた罠だが、今回も一度踏んで直した。次の人も同じ場所に足すこと）

**検査（すべて実測・EXIT 0）**: `npm run build`/`check`/`test`。CIと同じ14本の`verify:*`
（stack/portal/edit/xss/roundtrip/empty/screens/delete/draft/invitation/m6/admin/photo/revisit）
を`db reset`直後に全部実行し全緑。`npm run walk`の絵で④の新しいチップ・⑤⑥のタグ表示・
④店舗設定の編集UIを目視確認済み（管理者が1件追加→トリマーが選択→確定→⑤⑥両方に
タグとして届くところまで、使い捨てスクリプトで実ブラウザ操作して確認、削除済み）。

**まだやっていないこと（次のセッションへ）**:
1. **本番 Supabase へのmigration適用はマスターの手作業が要る**（`A-1`）。適用前にデプロイ
   すると`F-20260829-61`と同型の事故になるので、**migration適用を先に確認してから**
   `deploy.yml`を起動すること
2. マスターへの完了報告（日本語で）と、実際の見た目の確認・承認

## 0-Z. いちばん新しい（2026-08-31・その23）— **`0-Y` の宿題（実ブラウザ目視確認）を、ローカル環境で代替実施。本番そのものの確認はまだ**

このコンテナは Docker が使えた（`dockerd &` → `npx supabase start` で全マイグレーション
適用込みの土台が上がった）。`0-Y` が次回に指示した2件を、**本番ではなくローカルの
実データ相当の環境**で確認した。

1. **`npm run verify:revisit`（11/11 PASS）・`npm run verify:roundtrip`（32/32 PASS）**
   をクリーンな `supabase start` 直後に実測。`10.` 飼い主向けメッセージの末尾に
   「次回のおすすめご来店時期」の一文が付くことも機械で確認済み。
2. **`npm run walk` の絵**（`.human/correct/09-08_飼い主に届いたカルテ.png`）で、
   案内文末尾の一文と、下の黒帯ボックス（revisit-box）が両方とも自然に表示されて
   いることを目視確認した。
3. **ピンチ誤動作の修正（`openAnnotate()` の pointerId 区別）は、実機の2本指ピンチが
   無いので、使い捨てスクリプトで2本指を模した。** `App.photos.teeth` にダミー画像を
   差し込み `openAnnotate('teeth', 0)` を呼び、`pointerId=1` で水平線を引きながら
   同時に `pointerId=2` で縦方向へ大きく動かす `PointerEvent` を発火。結果:
   キャンバスに描かれたのは水平線のみ（中央行に赤画素82、指2の経路上に混入0）。
   スクリーンショットでも一直線であることを確認。修正は効いている。
   （このスクリプトは使い捨てで、確認後に削除済み。恒久検査としては残していない
   ——`verify-revisit`/`verify-roundtrip` と違い、正UIの動線からではなく `App` を
   直接呼ぶ手当てのため、`AGENTS.md` D-9 等との整合を取ってから正式な検査に
   するかはマスター判断とする）

**まだ確認していないこと**: **本番そのもの**（`https://trimmer-system.kouheikosehira.com`）
での⑤⑥実画面の目視確認。Claude Code は本番の認証情報を持たない（`A-1`）ため、
ここだけはマスターの手作業が要る。上記1〜3で「コードは正しく動く」ことは
強く裏付けられたが、「本番の実データ・実ブラウザで同じか」は別の確認である。

### 次にやること

マスターに本番で⑤⑥画面を一度見てもらい、問題なければ `docs/ops/plan.md` 第6章の
残る5件（`#5` `#6` `#11` `#22` `#35`）を `AskUserQuestion` で改めて問う。

---

## 0-Y. いちばん新しい（2026-08-30・その22）— **PR #52 マージ済み。2件のバグ修正・機能追加。実ブラウザでの目視確認はまだ**

このセッションの作業（`claude/checkin-sjegvz` → `master`、squash merge、`cabfa5e`）:

1. **写真書き込みキャンバスのピンチ誤動作を修正**（マスター報告: 「写真にピンチすると線がバーってなる」）。
   `src/js/ui.js` の `openAnnotate()`（口の写真などへのフリーハンド書き込み）が
   `pointerId` を区別せず、2本指ピンチで両方の指の座標が同じ線に混ざって暴れていた。
   1本目の指の `pointerId` だけを追跡し、2本目以降の指を無視するよう修正。
   なお実装は**Canvas 2D**であり、報告にあった「SVG」ではない（訂正済み）。
2. **次回のおすすめご来店時期を顧客ページの案内文に表示**（マスター指示:
   「次回のおすすめ時期は顧客ページの案内文に表示しろ」）。`backend/js/magazine-view.js`
   の `renderMagazine()` で、顧客ページ（⑥、`opts.onRevisitDaysChange` が渡らないビュー）
   限定で「担当トリマーからのメッセージ」の文末に「次回のご来店は◯◯頃をおすすめします。」
   を追記。専用の黒帯ボックス（revisit-box）表示はそのまま残した。編集は引き続き
   ⑤スタッフ側限定。
   - この変更で `verify:roundtrip` の「10. 飼い主: 担当からの一言」が一度赤くなった
     （CI・自分のPR自身が原因）。期待値を新仕様に合わせて更新し、
     `docs/ops/proof-of-red.md` の台帳エントリ名も合わせて直した。

**確認したこと**: ローカルの `npm run build` / `check` / `test` は全緑。CI（`check` /
`verify` / `walk`）も全緑（`mutate` は skipped）。

**まだ確認していないこと（次のセッションへ）**: このセッションは**Dockerが使えず**
ローカルSupabaseを起動できなかったため、`npm run verify:revisit` や
`npm run verify:roundtrip` を手元では実行できていない（CIでのみ確認）。
2件とも、実際の本番/本番相当環境で**人の目**による確認はまだ:
   - ピンチ操作で線が暴れなくなったか（スマホ実機・2本指ピンチ）
   - 顧客ページ（⑥）の案内文の末尾に、次回のおすすめご来店時期の一文が
     自然な形で表示されているか

---

## 0-X. いちばん新しい（2026-08-30・その21）— **`F-20260829-61` は復旧完了。次のセッションは「動作確認から」始めること（マスター指示）**

`0-W` に書いたデプロイ事故は解決済み。マスターが本番Supabase（`bcodloqwnrhcuvevfguy`）のSQL Editorで
migration（`202608290010_revisit_interval.sql`の内容）を実行し、「完了」と報告。以下を実測して確認した:

1. `curl https://bcodloqwnrhcuvevfguy.supabase.co/rest/v1/shops?select=id,default_revisit_days` /
   同 `pets?select=id,revisit_days_override` とも、事故時の `42703 column does not exist` ではなく
   通常の `401 permission denied`（未認証・想定どおり）に変わったことを確認
2. `deploy.yml` を `master`（`fbe7169`・`D-20260829-58`の全コードを含む）に対して再度起動し、成功（run #7）
3. 再デプロイ後、同じ curl 確認に加え `https://trimmer-system.kouheikosehira.com/api/pets` が
   通常どおり `401 authentication required` を返すことを確認——犬関連APIは正常化している

**「次回のおすすめご来店時期」機能は本番で正しく稼働している（はず）。**

**マスター指示（このチェックアウトの直前）: 「チェックアウト、次回は動作確認から」。**
→ **次のセッションは、他の作業（残る5件の質問など）に入る前に、まず本番で実際に
この機能が人の目で見て正しく動いているかを確認すること。** このセッションでは
`curl` によるAPI層の確認までしかしておらず、⑤（スタッフ）⑥（飼い主）の実画面を
本番で開いて確認する「動作確認」はまだ行っていない。`npm run walk` はローカルの
検証環境向けなので、本番を見るには `PROD_URL` を向けた確認か、実際にブラウザで
本番URLを操作すること。

---

## 0-W. いちばん新しい（2026-08-29・その20）— **⚠️ デプロイ事故と応急ロールバック（`F-20260829-61`）。次のセッションは必読**

**現在の真の状態**: `master` には `D-20260829-58`（次回のおすすめご来店時期）のコードがマージ済みだが、
**本番 Worker はそのコードを含まない1つ前の版（`6ad1b3f`）にロールバック済み**。
理由: この機能は新しい DB 列（`shops.default_revisit_days` / `pets.revisit_days_override`）を
使うが、**本番の Supabase（hosted プロジェクト `bcodloqwnrhcuvevfguy`）にはこの migration が
まだ当たっていない**。マージ直後にそれを確かめずデプロイしてしまい、犬の一覧・詳細のAPIが
軒並み502になる事故を起こした（詳細は `docs/failures.md` の `F-20260829-61`）。

**やったこと（応急）**:
1. 事故に気づいた直後、`curl https://bcodloqwnrhcuvevfguy.supabase.co/rest/v1/shops?select=...` で
   `42703 column does not exist` を実測して原因を特定
2. `master` の1つ前のコミット（`6ad1b3f`）から一時ブランチ `rollback-pre-revisit` を切り、
   `deploy.yml` を再起動して本番Workerをロールバック（`run #6`・成功）
3. ロールバック後、同じ curl で `401`（未認証・正常）に戻ったことを確認——本番は復旧している

**次のセッション・次の作業者へ（最優先）**:
1. **マスターに、`supabase/migrations/202608290010_revisit_interval.sql` の中身を
   本番 Supabase（`bcodloqwnrhcuvevfguy`）の SQL Editor で実行してもらうこと。**
   Claude Code は本番 Supabase の認証情報を持たない（`A-1` に準ずる）ため、この一手だけは
   マスターの手作業が要る
2. 適用後、`curl https://bcodloqwnrhcuvevfguy.supabase.co/rest/v1/shops?select=id,default_revisit_days`
   （認証無しで叩いてよい。`401` なら列は在る＝OK、`42703` ならまだ当たっていない）で確かめてから、
   `deploy.yml` を `master`（`df7a51f` 以降）に対して再度起動し、`D-20260829-58` を正式に本番へ出す
3. 一時ブランチ `rollback-pre-revisit` は役目を終えたら消してよい
4. **恒久対策は未決**: `docs/failures.md` の `F-20260829-61`「How to prevent」にある2案
   （`verify:prod` に本番スキーマの実測を足す／`supabase db push` を `deploy.yml` に機械化する）
   をマスター判断で決めること。決まるまで、**DBスキーマを伴う変更のデプロイは、
   migration適用を先に本番相手に確かめてから行う**

---

## 0-V. いちばん新しい（2026-08-29・その19）— **第6章 `#3`「次回のおすすめご来店時期」を実装（`D-20260829-58`）**

マスターが「デフォルトは30日後、別途修正できるようにする。修正はデフォルト自体の
修正も犬ごとの修正も可能とする。」と回答（`D-20260829-58`）。これに沿って
新規 migration・API・UI・検査を一式実装した。

**やったこと**:
- `supabase/migrations/202608290010_revisit_interval.sql`: `shops.default_revisit_days`
  （既定30・1〜3650）と `pets.revisit_days_override`（nullable・1〜3650）を新設。
  RLS は書き込みを管理者限定（`shops_admin_update`）に、⑥飼い主の読み取りを
  自分の犬が居る店舗に限定（`shops_customer_select`）で許可。列を絞った GRANT
  （`202608270009` と同じ多層防御パターン）も両方の新列に追加
- `worker/src/api-schemas.js`: `updatePetSchema` に `revisitDaysOverride`、新規
  `updateShopSchema` を追加
- `worker/src/data-stores/supabase-data-store.js`: pet の SELECT 列に
  `revisit_days_override` を追加、`updatePet()` のマッピングを拡張、
  `getShop()`/`updateShop()`/`getShopDefaultRevisitDays()` を新設
- `worker/src/index.js`: `/api/shop` の GET/PATCH を新設。⑥飼い主向けレポート
  応答に `pet.revisitDaysOverride` と `shopDefaultRevisitDays` を追加
- `backend/js/magazine-view.js`: `renderMagazine()` に「次回のおすすめご来店時期」の
  実描画を新設（来店日＋（犬の上書き ?? 店舗既定）日数を計算・表示）。編集欄
  （`opts.onRevisitDaysChange` が渡っているときだけ表示＝スタッフ限定）も同居させた
- `src/js/ui.js`（⑤側）・`backend/js/supabase-staff.js`（`/api/shop` 取得・
  `mapPet()` に `revisitDaysOverride` 追加）・`backend/js/supabase-auth.js`（⑥側）を配線
- `backend/js/supabase-admin.js`: 管理者画面に「④ 店舗設定」を新設（既定日数の編集）
- 新設 `scripts/verify-revisit-interval.mjs`（12 `check()`）。`mutate-run.mjs` に
  壊し方8種を追加し、実測で11件を赤にした（残り1件は未証明のまま理由つきで記帳）

**検査（すべて EXIT 0 / green）**: `npm run build && npm run check && npm test`。
`verify:revisit`（11/11）。`gate.mjs --end`/`delivery-ready.mjs` とも通過。

### 次にやること

第6章のマスター判断待ちは残り5件（`#5` `#6` `#11` `#22` `#35`）——`#3` は今回で完了。
master の「再度質問しろ」指示（`#3`/`#5`/`#6`/`#11`/`#22`/`#35`）のうち `#3` のみ
本回答で消化済み。残り5件を `AskUserQuestion` で改めて問うこと。マージ後は
`D-20260829-57` によりデプロイの都度確認不要——ただしマスターへの完了報告は
日本語で毎回行うこと（マスター指示・2回繰り返し）。

---

## 0-U. いちばん新しい（2026-08-29・その18）— **C-8: 全文明朝体に再指示（`D-20260829-56`）**

マスターより「次回のおすすめご来店時期」ブロックのフォントを問われ、見出しは明朝体・
本文はゴシック体という混在があることを回答したところ、「全文明朝体で進めろ。明朝体にも
種類あるだろ」と再指示。

**やったこと**: `--font-sans`/`--font-en` を `--font-serif`
（`"Hiragino Mincho ProN", "Yu Mincho", YuMincho, "Times New Roman", serif`）と同じ
フォント列に揃え、4ファイル（`index.html`/`my.html`/`admin.html`/`magazine-view.js`）＋
Canvas直書き2箇所（`ui.js`）を明朝体で統一。**外部通信ゼロ**（`D-20260823-14`）と
**日本語フォント非同梱**（`docs/ASSET-PROVENANCE.md`）という既存の制約上、実際に
描画されるのは端末のシステム明朝体（Mac/iOS: Hiragino Mincho ProN、Windows: Yu Mincho、
Android等: ブラウザ既定serifへフォールバック）であることをマスターに明記して報告した。

**検査（すべて EXIT 0 / green）**: `npm run build && npm run check && npm test`。
`verify:*` 全12本をクリーンな `db reset` 後に実測（208/208 PASS）。`gate.mjs --end` /
`delivery-ready.mjs` とも通過。`npm run walk` の絵で全画面が明朝体になったことを確認。

### 次にやること

クライアント指示12件（C-1〜C-12）は完了済み。本番へのデプロイはこのセッションで
`workflow_dispatch`（`deploy.yml`）を使い、マスター承認のうえ実施した実績がある
（`F-20260829-60` の修正）。今回のフォント変更もマスター承認を得てから同様にデプロイする。
残るは第6章のマスター判断待ち6件（`#3` `#5` `#6` `#11` `#22` `#35`）。

---

## 0-T. いちばん新しい（2026-08-29・その17）— **本番でマスターが実際に踏んだ404を直した（`F-20260829-60`）**

マスターが本番 `https://trimmer-system.kouheikosehira.com` を操作中、`/edit/p/d1` で
404 に遭遇したと報告。調べたところ **`d1` は本物の犬IDではなく、`src/js/dummy.js`
（F1〜F2時代の仮データ）の1頭が持っていた仮の `id`** だった。`ui.js: renderDogs()`
は「`id` を持っていれば実データ」という契約でURL遷移するかを判定しているが、
**仮データ自身がこの契約に違反して `id` を持っていた**ため、バックエンドの読み込みが
何らかの理由で間に合わない一瞬に仮データ（「ポンチ」等）が描画されると、タップした
先で実在しない `/edit/p/d1` へ遷移して404になっていた。

**やったこと**: `dummy.js` の5頭すべてから `id` を削除（契約どおり、仮データは画面内
だけで進むようにした）。新設 `test/dummy-no-id.test.mjs` で赤→緑→赤を実測し、
`docs/failures.md` に `F-20260829-60` として記帳。

**検査（すべて EXIT 0 / green）**: `npm run build && npm run check && npm test`。
`verify:*` 全12本をクリーンな `db reset` 後に実測（208/208 PASS）。`gate.mjs --end` /
`delivery-ready.mjs` とも通過。

### 次にやること

クライアント指示12件（C-1〜C-12）は完了済み。残るは `docs/ops/plan.md` 第6章の
マスター判断待ち6件（`#3` `#5` `#6` `#11` `#22` `#35`）。新しい指示が来るまで
自発的な着手はしない。

---

## 0-S. いちばん新しい（2026-08-29・その16）— **C-8（フォント統一）を解消。クライアント指示12件、全件完了**

`0-R` で保留していた C-8 に着手した。**「統一先はマスターに画面を見せてから指示を仰ぐ」
という当初の合意は、複数の妥当な候補から選ぶ判断が要る場合の話だった。実測したところ
そうではなかった**——`my.html`/`admin.html`/`backend/js/magazine-view.js` の3箇所は
最初からシステムフォント（Hiragino/Yu Gothic/Meiryo系）で一致しており、`index.html` だけ
"Noto Sans JP"/"Noto Serif JP" を指定していたが、フォントファイルを同梱していないため
実機ではどのみちシステムフォントに落ちていた（`npm run walk` の絵で見た目が変わらない
ことを確認済み）。つまり選ぶ判断ではなく、**効いていない指定を実態に合わせるだけ**の
客観的な不一致だったので、その場で4箇所を揃えた。Canvas に直書きしていた2箇所
（`ui.js` の犬体図ラベル・印の文字。CSS 変数を参照できないため）も同じ列に直した。

### 検査（すべて EXIT 0 / green）

`npm run build && npm run check && npm test`。`verify:*` 全12本をクリーンな
`db reset` 後に CI 順で実測（208/208 PASS）。`gate.mjs --end` / `delivery-ready.mjs`
とも通過。見た目の回帰が無いことは `npm run walk` の絵で確認。

**環境メモ**: このセッションの後半で Docker デーモンが一度落ちていた
（`dockerd` を手で起動し直して復旧）。`db reset` 直後に Kong が古い IP を
キャッシュして 502 になる既知の事象（`scripts/lib/local-stack.mjs` に記載済み）も
再度発生し、`docker restart supabase_kong_trimmer-system` で解消した。

### 次にやること

クライアント指示12件は全件完了。次は `docs/ops/plan.md` 第12章（放置リスト）の巡回、
または新たにマスターから指示が来ればそちらを優先する。

---

## 0-R. いちばん新しい（2026-08-29・その15）— **クライアント指示12件のうち11件を実装・検査・証明まで完了**

### やったこと（詳細は `docs/ops/plan.md` 第10章）

`0-Q` で計画にだけ載せていた C-1〜C-12 を実装した。**C-8（フォント統一）だけは、
マスターの画面提示待ちという意図的な理由で未着手**（受入条件・禁止事項に反しない）。

- `src/index.html` / `src/js/ui.js`（`extractReport()` / `applyReport()` / 新設の
  `selectEarLevel()` / `onBestWeightChange()` / `openAnnotate()` 等） / `backend/js/magazine-view.js`
  の4点セットを、12件まとめて改修
- `scripts/verify-report-roundtrip.mjs` に⑤⑥両方への `check()` を7本追加（`3b/3c/3d/9b/9c/9d/21`）
- `scripts/mutate-run.mjs` に対応する壊し方4本を追加し、**すべて手元で実測して赤にした**
  （このコンテナは Docker が動くので CI 往復なしで実測できた）。
- `docs/ops/proof-of-red.md`「24回目」に、壊し方と実際に出た赤の出力を記帳
- `scripts/walk-human.mjs` に新フィールドを記入する `fillNewFields()` を追加し、
  `npm run walk` の絵に C-1/C-4/C-5/C-6/C-9 が実際に写ることを確認（受入条件 #7）
- `test/ui-revise-keeps-trimming.test.mjs` を削除（C-12 でカットスタイル選択欄自体が
  無くなったための正当な削除。カットスタイル関連コードが本当に消えたかは敵対検証3で裏取り済み）

### 敵対検証（マスター指示・サブ3体）で1件、実害のある穴が見つかった

検証1（要求充足）・検証2（届くか／`applyReport()` 回帰／恒真検査）・検証3（既存回帰／
台帳整合／`A-4`）を並行展開。**検証2 が C-3 の実害を発見**——`backend/js/magazine-view.js`
の日付表示が「確定を押した日（`report.reportDate`）」を優先する並びのままで、⑥飼い主の
画面には来店日ではなく確定日が出続けていた（⑤トリマー確認は別経路のため気づかれな
かった）。**その場で直した**（優先順位を来店日優先に変更）。証拠:
`scripts/verify-report-roundtrip.mjs` に `3e.`/`9e.` を追加、`mutate-run.mjs` に
`report-date-confirm-wins` を追加して実測（`docs/ops/proof-of-red.md` 25回目）。

検証3 はさらに2件の軽微な指摘: ①フルの `mutate-run.mjs` を並行実行した際、
`backend/js/supabase-admin.js` の `admin-owner-delete-not-persisted` 相当の壊しが
一瞬 working tree に残っていた（自分の敵対検証プロセス自身が起こした一時的な
競合で、確認して復元済み・製品コードの実害ではない）。②`docs/ops/key-parity-F3.md`
の追記が「来店日・目標体重の入力は既にP-2で完了」と誤記していた（実際は今回
C-3/C-4 で新設）——修正済み。検証1は指摘0件（カットスタイルの意匠モック文言の
消し残し1箇所のみ・非表示のため実害なし）。

**3体とも指摘を出し切ってから、実害のある1件（C-3の日付逆転）を直し、再度
`npm run check`/`npm test`/`verify:*` 全12本を green にしてからコミットした。**

### 検査（すべて EXIT 0 / green・敵対検証の指摘を直した後の最終計測）

`npm run build && npm run check && npm test`。`verify:*` 全12本を CI と同じ順で
クリーンな `npx supabase db reset` 直後に実測（208/208 PASS）。
`gate.mjs --end` / `delivery-ready.mjs` とも通過。

**副産物のブロッカー修正（D-21）**: `scripts/guard/checkout.mjs` の「まっさらな
作業場で build/check/test」検証が、`plan-read.mjs`（前セッションで新設した関所）
のせいで**常に赤になる**バグを見つけて直した。フレッシュな `git worktree` では
`.plan-read` が無いのは当然だが、そこで動く `npm run check` に `CI=true` を渡して
おらず、本物の CI なら通る状況でここだけ毎回止まっていた。`checkout.mjs` の
`run()` に `env: { ...process.env, CI: 'true' }` を渡すよう1行直した
（範囲: `checkout.mjs` のこの1関数のみ）。

### 次にやること

C-8（フォント統一）は `npm run walk` で撮った絵（`.human/correct/`）をマスターへ提示し、
統一先の指示を受けてから着手する。それ以外は放置リスト（第12章）の巡回に戻る。

---

## 0-Q. いちばん新しい（2026-08-29・その14）— **大計画を1本化し、読み書きを機械強制にした。クライアント指示12件を計画に載せた**

### 背景（マスター指示）

「毎回進まない。納品できない。致命的だ」という指摘を受けた。実測で原因を3つ特定した:
①大計画は自動では一切読まれない（自動注入は `CLAUDE.md` 1枚だけ） ②大計画が2枚に
割れていた（`plan.md` / `delivery-plan.md`） ③`scripts/plan.mjs` にバグがあり、
`docs/ops/phase=F4` になった瞬間から**完了済みの F1〜F3 まで全部「未着手」と表示**していた
——大計画の進捗表示そのものが嘘をついていた。

### やったこと（詳細は `docs/ops/plan.md` 第8.5節）

1. `docs/ops/delivery-plan.md`・`docs/deferred.md` を `plan.md` に統合し、旧2ファイルを廃止
2. `scripts/plan.mjs` の進捗バグを直した（`test/plan.test.mjs` で赤→緑→戻して赤を実測）
3. `plan.md` に「いまやる番」を新設。`checkin.mjs` が毎回強制表示し `.plan-read` を書く。
   **無いと `npm run check` が EXIT 1**（`scripts/guard/plan-read.mjs`。CI では見ない）
4. `checkout.mjs` に7項目目（「いまやる番」を更新したか）
5. `.claude/settings.json` に SessionStart フック（`checkin.mjs` を自動起動するだけ）
6. `AGENTS.md` D-21（ブロッカー規則）を新設、D-15 に `.claude/settings.json` の例外を追記
7. **クライアント指示12件（C-1〜C-12）を `plan.md` 第10章に載せた**——体重の評価軸・ベスト体重・
   耳の6段階と写真・コース選択・口の写真とお絵描き・フォント統一・カットスタイル削除 等。
   実装はまだ**していない**（この PR には含まれない）。次のセッションはここから着手する

### 検査（すべて EXIT 0）

`npm run build` / `npm run check`（`plan-read` 含め15本）/ `npm test`（**110件**）/
`gate.mjs --end` / `delivery-ready.mjs`。

### 次にやること

`docs/ops/plan.md` の「いまやる番」のとおり、**C-7 の確認から着手**し、続けて C-4〜C-12 を
実装する。各項目の受入条件・敵対検証の設計は第10章に書いてある。

---

## 0-P. いちばん新しい（2026-08-29・その13）— **マスターの返答2件を記帳した。上げていた宿題は両方とも閉じた**

### 1. 「マスター判断 A」を決定の帳面に記帳した → `D-20260828-54`

`0-O` で上げていた「引き継ぎにしか無い判断」。マスターより「**判断Aは記帳しろ**」。

**発言は引用していない。** A を選んだときのマスターの言葉が手元に無く、あるのは私の要約だけ
なので、文面を作れば創作になる（`F-20260825-29` の型）。**「A を選んだ」という事実と、
記帳が遅れた理由**だけを書いた。歯止め4つと、境界（**ここに足してよいのは「埋めるべきでない
もの」だけ**）も一緒に落としてある。

### 2. CI が全滅した件は決着 → `D-20260829-55`、`F-20260828-57` は解決済みへ

マスターより「**気にするな、設定を変えたら間違えたからだ。解消すみだし、再発しない**」。

**原因はマスターの設定変更の誤り。すでに解消済みで、再発しない。**
`F-20260828-57` の Status を `未解決` → `解決済み` にした。**この件はもう持ち出さない**
（`D-20260827-52` と同じ扱い——決定であって宿題ではない）。

> **失敗そのものは消していない。** 「確かめていない前提の上に切り分けを積んだ」ことの
> 再発防止は、原因が分かっても価値が変わらないので `How to prevent` は書き換えていない。
> `failure-check-F4-end.md` の「原因は分かっていない」も**取り消し線で残した**——
> **当てられなかったこと自体が記録の中身**である。

### まだマスターの返事を待っているもの（1件）

**フェーズの札（`docs/ops/phase`）を F4 から進めるか。**
F4 は**閉じてよい状態**（`gate.mjs --end` が EXIT 0）だが、札は **F4 のまま**にしてある。
- **何が起きるか**: F4 の次が定義されていないので、札を進めると**範囲外を止める見張りが
  全部黙る**（`scope.mjs` / `gate.mjs` はどちらも「知らない札なら何もしない」）
- **戻せるか**: 戻せる（札を `F4` に書き戻すだけ）
- **間違えたとき何が失われるか**: 範囲外の変更が止められずに入る
- **おすすめ**: 札は F4 のままにして、**次にやることを決めてから動かす**。
  次の段は `docs/ops/delivery-plan.md`（P-1〜P-3）にある。それを札にするなら
  `scope.mjs` に段を足すのが先

### 残っている宿題（次のセッションへ）

1. **`verify-photo-roundtrip` の写真添付**（手つかず）。検査の名前が実行時に決まる
   （`${kind}` を含む）ため台帳の鍵と一致しない。`verify-xss` の18件を3件にまとめ直したのと
   同じ手当てが要る
2. **`F-20260828-55` / `-56` は機械の直しだけで「壊して赤」を見ていない**（台帳の未証明40件に含む）
3. **記録の書き方を機械が縛っていない。** `decisions.md` の1件が見出しの形だけ違って
   **数える命令から見えていなかった**（直した）。同じ崩れに次も気づけるとは限らない

## 0-O.（2026-08-29・その12）— **F4 を閉じてよい状態にした。招待の検査も実測で赤にした**

### やったこと（2本を並行）

**1. F4 の ③再発防止（完了）を書いた** → `docs/ops/failure-check-F4-end.md`

`gate.mjs --end` に足りなかったのは**この1件だけ**だった。失敗 60件・決定 55件を全件照合し、
根拠は**実行した命令の出力**だけにした。

```
node scripts/guard/gate.mjs --end
【関所】F4 は閉じてよい状態です。
```

**2. `verify-invitation :: 5.` を実際に赤にした**（前回の宿題1）

`applyMutation` の `edits` に**編集ごとの `file`** を許し、2層（`pets` の RLS ＋
`bootProtectedPortal()` の関門）を**同時に**剥がす壊し方 `invitation-both-layers-open` を作った。
SQL と JS の両方を触る回は `db reset` と `build` の**両方**が回るようにした（片方だけだと
「壊したつもりで何も壊れていない」になる）。台帳 **142 → 143 / 183**。

### この回の実測（すべて EXIT 0）

| 命令 | 結果 |
|---|---|
| `npm run build` | 0（36件） |
| `npm run check` | 0（`proof-of-red` 143/183） |
| `npm test` | 0（**194件**） |
| `node scripts/guard/delivery-ready.mjs` | 0（「F4 を閉じてよい」） |
| `node scripts/guard/gate.mjs --end` | 0 |
| CI run #191（`master` `7c18c46`） | `check` / `verify` / `walk` **全部成功** |

### ⚠️ マスターに上げること（2件）

**1. フェーズの札を進めるか。**
F4 は**閉じてよい状態**になったが、`docs/ops/phase` は **F4 のまま**にしてある。
F4 の次が定義されていないので、札を進めると**範囲外を止める見張りが全部効かなくなる**
（`scope.mjs` / `gate.mjs` はどちらも「知らない札なら何もしない」）。
- **何が起きるか**: 見張りが黙る。触ってよい場所の制限が無くなる
- **戻せるか**: 戻せる（札を `F4` に書き戻すだけ）
- **間違えたとき何が失われるか**: 範囲外の変更が止められずに入る
- **おすすめ**: **札は F4 のままにして、次にやることを決めてから動かす**。
  次の段は `docs/ops/delivery-plan.md`（P-1〜P-3）に書いてあるので、それを札にするなら
  `scope.mjs` に段を足すのが先

**2. 「マスター判断 A」が決定の帳面に無い。**
前回入れた判断（`delivery-ready` に1項ごとの理由欄を足す）は、**この引き継ぎにしか無い**。
`docs/decisions.md` に項が無く、番号も付いていない。**引き継ぎは次の人が読まない**ので、
このままだと無いのと同じになる。
- **私が文面を作らない理由**: 決定の記録には**マスターが実際に言った言葉**が要る。
  手元にあるのは「A を選んだ」という私の要約だけで、私が書けば創作になる
- **お願い**: 帳面に残してよいか、残すなら文言をいただきたい

### 残っている宿題（次のセッションへ）

1. **`verify-photo-roundtrip` の写真添付**（前回の宿題2・手つかず）。検査の名前が実行時に決まる
   （`${kind}` を含む）ため台帳の鍵と一致しない。`verify-xss` の18件を3件にまとめ直したのと
   同じ手当てが要る
2. **`F-20260828-57`（CI 即死）の原因が未特定。** 現象は消えている（run #191 が全 job 成功）が、
   **「直った」のではなく「いまは出ていない」**。最有力候補（Actions の無料枠 / spending limit）は
   **マスターのアカウントからしか残量が見えない**。納品条件 7-1 に直接効く
3. **`F-20260828-55` / `-56` は機械の直しだけで「壊して赤」を見ていない**（台帳の未証明40件に含む）
4. **記録の書き方を機械が縛っていない。** 今回 `decisions.md` の1件が見出しの形だけ違って
   **数える命令から見えていなかった**（直した）。同じ崩れに次も気づけるとは限らない

### 環境（このコンテナ）

**Docker が動く。** `nohup dockerd &` → `npx supabase start` で土台が上がり、
`verify:*` も `mutate-run.mjs` も**手元で実測できた**。
`failure-check-F4-start.md` の「この環境には Docker が無い」は**この容器では古い**。
ただし次の容器で在るとは限らないので、納品条件 7-2（運用に Docker を要求しない）は変えていない。

## 0-N.（2026-08-28・その11）— **マスター判断 A を入れた。`delivery-ready` が「F4 を閉じてよい」を返す**

### やったこと

`delivery-ready.mjs` に **「1項ごとに埋められない理由」** を足した（マスター判断 A）。
除外はそれまで**ファイル単位**でしか書けず、「この項だけは埋められない」を機械に
伝える場所が無かったので、理由を台帳に書いても数え続け、**F4 が構造上閉じられなかった**。

```
node scripts/guard/delivery-ready.mjs
  ✅ F4 を閉じてよい。
    客に当たる経路 11本 … 全件証明済み
    判定できない 2本 ／ F4 の後に回す 1本 … 理由つきで除外
    1項ごとに埋められない 13件 … 理由つきで外した（一覧が出る）
```

### **黙らせる道具にしないための歯止め**（どれも検査で証明済み）

| 歯止め | 動き |
|---|---|
| 理由が **20字未満** | 理由と認めない（短い言い訳で外せない） |
| **実体に無い検査**を指している | 「古い」として赤 |
| **すでに証明済み**の項に理由が付いている | 「矛盾」として赤 |
| — | **何件をどの理由で外したかは、通ったときの出力に必ず出る** |

`test/delivery-ready.test.mjs` に5件追加（15件すべて緑）。

> **ここに足すのは「埋めるべきでないもの」だけ。** 埋められるのに面倒だから、は
> 該当しない。迷ったら、まず壊し方を書いて実測すること。
> **台帳の数字（142/183）は動かしていない**——理由つきで外すことは「証明した」ことではない。

### 残っている宿題（次のセッションへ）

1. **`verify-invitation :: 5.`** は層を両方とも特定済み（DB の RLS ＋
   `bootProtectedPortal()` の関門）。**2枚同時に剥がせば判定できる**が、
   いまの `applyMutation` は `m.file` を1つしか扱えず、この2層は別ファイルに
   またがるため表現できない。**`edits` に編集ごとの `file` を許せば作れる**（小さい）
2. **`verify-photo-roundtrip` の写真添付**は名前が変数で紐付けられない。
   `verify-xss` の18件を3件にまとめ直したのと同じ手当てが要る

## 0-M.（2026-08-28・その10）— **台帳 142/183。残り13件は全部「埋めるべきでない」か「1件だけ」**

> `0-L` に書いた**目的の話をまず読むこと**（「赤にすることが目的ではない」）。

### 残り13件を**全部**実測して仕分けた。埋める作業は実質1件しか残っていない

| 型 | 件数 | 中身 |
|---|---|---|
| **待ち・サーバ検証を言い直しているだけ** | 6 | `verify-admin 7.`／`verify-delete 1.`／`同 2.`／`verify-m6 ⑤.`／`verify-photo-roundtrip 4.`／`同 5.` |
| **二重の守りの結果を見ている**（1枚剥がしでは赤にならない） | 1 | `verify-invitation 5.` |
| **いまの画面では起こせない漏れの見張り** | 2 | `verify-draft 4.`／`verify-empty-pet 7.`（下書きの**中身**が漏れていない） |
| **名前が変数** | 1 | `verify-photo-roundtrip` の写真添付 |
| **症状が起きないので緑が正しい** | 1 | `verify-report-roundtrip 20.` |
| **壊すと検査が全部死ぬ** | 2 | `verify-portal 1.`／`同 3.` |

**残り1件**（`verify-invitation 5.`）も、やるなら**アプリ側のどの層が招待未消化を
弾いているかを先に読んでから**。当てずっぽうで2枚目を剥がすのは発明であって証明ではない。

### 見つけた一般則（次の人にいちばん効く）

> **「〜できた」を確認する行が、その動作の成功に依存する待ちの後ろにあるとき、
> その行は単発の壊しでは赤にできない。** 待ちが先に落ちるから。
> **守り自体は正しいが、その行は待ちを言い直している。**

上の表の6件はこれ1つで説明がつく。**同じ形を見つけたら、埋めようとせず理由を書く。**

### ⚠️ F4 を閉じるには、機械の直しかマスターの判断が要る

`delivery-ready.mjs` の除外は**ファイル単位**でしか書けない。上の12件は**1項ごとに
理由を台帳へ書いてあるが、機械はそれを読めず数え続ける**。したがって
**このままでは F4 は構造上閉じられない。** 選択肢:

- **A（おすすめ）**: 機械に**1項ごとの理由欄**を足す。理由は既に全件書いてあるので機械側だけの作業
- **B**: 該当の検査そのものを直す。ただし製品の守りに触るうえ、検査を減らす方向なので `A-4`（検査を消して緑にしない）と紛らわしい

**マスターへ上げてある。返事待ち。**

### 環境の注意（このコンテナ）

`dockerd` と Supabase は**よく落ちる**。落ちたら `dockerd &` → `npx supabase start`。
再起動直後は `status=401` のような一過性の失敗が出るので、**壊し方を当てない素の
状態を1回走らせて土台が正常なことを確かめてから**測ること（実際に1度ひっかかった）。

## 0-L.（2026-08-28・その9）— **目的を取り違えていた。仕分け直した結果と、残り18件の型**

### ⚠️ まずこれを読む — 目的は「赤にすること」ではない

このセッションの途中、マスターから **「赤にすることが目的ではない。目的はなんだ？」**
という指摘を受けた。**そのとおりで、私は数を追っていた。**

目的は `docs/ops/plan.md` の冒頭にある:

> マスターが実店舗で1頭分のカルテを最後まで作り、飼い主がスマホでそれを見られる。
> **そして渡した後も、壊れたら壊れたと分かる。** それが完成。

**台帳は「死んだ検査を見つけ出す道具」であって、埋めるノルマではない。**
`132/183` を `183/183` にすること自体には価値が無い。

**目的を見失った合図はひとつ。**「検査の行を赤にするために、現実には起きない
壊れ方を発明し始めたとき」。このセッションでは `commit-report-id-truncated`
（カルテ番号を8文字に切る）がそれで、当然のように通らなかった。

**正しい入り方**は、検査の側から **「この行は、実際に壊れたとき客を何から守るのか」**
を読むこと。そうすると、当てるべき壊し方は**たいてい既に手元にある**——
`★`（戻れるか）には画面の状態が狂う壊し方、`⑤b/⑥b`（書いた一言が届くか）には
文字が出ない壊し方が、どちらも実在の欠陥として既に作ってあった。
**17回目は新しい壊し方を1つも書かずに3件を実証した。**

### 残り18件の仕分け（型で分けた。数で追わないこと）

| 型 | 件数 | どう扱うか |
|---|---|---|
| **(a) 守っている・狙える** | 約6 | 壊し方を書いて確かめる（`verify-report-roundtrip 1.` 記入欄の欠落など） |
| **(b) 直前の待ちやサーバ検証が既に保証しているだけ** | 4 | **埋めない。理由を書く。**`verify-delete 1.`／`verify-admin 7.`／`verify-m6 ⑤.`／`verify-photo-roundtrip 4.` はどれも「確定後の URL の最後が36文字のカルテ番号か」を見ているが、**番号が壊れていればページ取得の時点で HTTP エラーになり、そこに到達しない**（実測済み・台帳に記録） |
| **(c) 名前が変数で、紐付けに手当てが要る** | 3 | `verify-xss :: label #2/#3` と `verify-photo-roundtrip` の1件。**~~`verify-xss` は18件が赤になったのに数に入っていない~~ ← これは誤りだった**（19回目で訂正）。18件は既に**3件として証明済み**に入っている。残るのは「細工が届かなかったときに理由を出す分岐」で、性質が違う。`label`（89行目）は実証済み。`#2`（カルテを作れず）`#3`（確定に失敗）はそれぞれ別の壊し方が要る |
| **(d) 現在の画面では起こせない漏れを見張っている** | 2 | `verify-draft 4.`／`verify-empty-pet 7.`（下書きの**中身**が漏れていない）。いまの飼い主ページは一覧しか描かないので、RLS を開けても本文は出ない。2箇所同時に壊す（`edits`）なら狙えるが、まず「この行は何を守るのか」をマスターと確認したい |
| **(e) 症状が起きないので緑が正しい** | 2 | `verify-report-roundtrip 20.` など。埋めない |

> **⚠️ 記憶で報告しない。** 上の (c) について、私は「18件が数に入っていない」と
> マスターに報告したが、**台帳を読み直したら既に3件として入っていた**。
> `F-20260828-57`（確かめていない前提の上に積む）とまったく同じ型を、
> **同じセッションの中で繰り返した。** 報告する前に台帳を引くこと。
>
> **機械の限界を1つ見つけた。** `delivery-ready.mjs` の除外は**ファイル単位**でしか
> 書けない。上の (b)(c)(d) のような**1項ごとの「埋められない理由」を置く場所が無い**
> ので、理由を台帳に書いても機械は数え続ける。**F4 を閉じるには、この機械を
> 直すか、該当の検査そのものを直すか、マスターの判断が要る。**

### 台帳を触るときの落とし穴（2回踏んだ）

`delivery-ready.mjs` は除外リストの節を**見出しの前方一致**で探す。新しい節に
その書き出しを使うと**本来の節を乗っ取る**。さらに、その注意書きを本文に書くとき
**見出しの文字列をそのまま書いてもう一度乗っ取った**（`indexOf` は本文と見出しを
区別しない）。どちらも `npm test` が捕まえた。**節の名前は見出しにも本文にも書かない。**

### このセッションで客に効いたこと（数ではなく、これが成果）

1. **`F-20260828-59`** — 確定が「番号の無いカルテ」を返すと、**保存できていないのに
   画面が進んでいた**（`D-2` の防波堤にすり抜ける道があった）。マスター指示で修正、
   赤→緑→戻して赤の3出力つき。検査 `test/report-commit-guard.test.mjs`（7件）
2. **`F-20260828-58`** — 壊し方を1つ足したら、**製品は無傷なのにテストが3回に2回赤**に
   なった。`mutate-run.test.mjs` が実リポジトリを書き換えており、`node --test` の
   並行実行と競合していた。使い捨てのコピー上で行うよう直した
3. **`F-20260828-57`** — 前回の引き継ぎ `0-J` の断定2つが誤りだった（private を public と
   思い込んで枠切れ説を外していた）。**CI 即死の原因は private の Actions 無料枠切れ**で、
   マスターの public 化で復旧した

## 0-K.（2026-08-28・その8）— **`0-J` の断定は2つとも誤り。CI を待たずに台帳が進むことが分かった**

> **`0-J` より下は過去の記録。`0-J` は根拠が誤っているので、そのまま読まないこと**（下の「訂正」）。

### 台帳 **123 / 183**（10件増えた。**すべて CI ではなく手元で証明した**）

F4 を閉じる範囲の未証明は **43 → 32件**。増やし方は「新しい壊し方を書く」——
CI が既に走らせた44個は結果が反映済みなので、フル実行を繰り返しても1件も増えない
（それを確かめたのが下の②）。この回で壊し方を **8個**足した:

| 壊し方 | 埋めた項 |
|---|---|
| `edit-dummy-dogs-leak` | verify-edit 9 |
| `edit-breed-mock-refill` | verify-edit 10 |
| `empty-photo-attr-page-url` | verify-edit 15 / report-roundtrip 16 |
| `letter-section-always-shown` | verify-edit 16 |
| `skin-image-blank` | report-roundtrip 8, 15 |
| `weight-prefilled-sample` | report-roundtrip 19 |
| `portal-content-shown-logged-out` | verify-portal 7 |
| `portal-sample-image` | verify-portal 8 |

**`empty-photo-attr-page-url` は `F-20260828-54` が残した宿題そのもの。**
「`getAttribute('src')` が実際に壊れた値を返す形の壊し方が要る」と書き残して
あったとおりに作ったら、狙った2件が両方赤になった。**記録が次の回を助けた例。**

> ⚠️ **壊し方を足したら、`npm test` を必ず複数回走らせること**（`F-20260828-58`）。
> `test/mutate-run.test.mjs` は壊し方を1つずつ実際に当てるので、**別のテストが
> assert している文字列を消す壊し方**を足すと、そのテストが確率的に落ちる。
> 実ツリーを触らないよう直したが、同種の当たり方は他にもあり得る。

### ① `0-J` の訂正 — 書いてあった2つの断定は、どちらも間違いだった

`F-20260828-57` に記録した。次の人が `0-J` の「次にまず確認すること」を
そのまま実行すると**外れた方向に進む**ので、先にここを読むこと。

| `0-J` の記述 | 実際 |
|---|---|
| 「このリポジトリは **public**。だから分数枠切れ説は**否定された**」 | **private だった**（当時）。GitHub API の `"private": true` と Claude 側のリポジトリ一覧の `visibility` が独立に一致。**いちばん有力な仮説を、確かめずに候補から外していた** |
| 「残る仮説は `workflow_dispatch` の**連発**によるレート制限」 | **合わない**。run #161 は `pull_request`、#162 は master への `push` で、連打ではないのに同じ3秒で全ジョブ失敗している |
| （症状の根拠）「**課金時間 0ms**」 | **手がかりではない**。`get_workflow_run_usage` はこのリポジトリでは**成功した run でも `total_ms: 0`** を返す（run #153 は 414 秒走って success だが 0ms）。**正常時の値を見ないまま異常の証拠にしていた** |

ジョブの実体は `runner_id: 0` / `runner_name: ""` / ステップ0件 / check-run の
output が空——**runner が一度も割り当てられていない**（起動前に拒否された）形。

**マスターが 2026-08-28 13:50 にリポジトリを public に変えた**（API で
`"private": false` を確認済み）。private の無料枠切れが原因だったなら、これで
直るはず。**次の push / dispatch の結果を見て確かめること。**

### ② いちばん大きい発見 — **この作業場では Docker が動く。`mutate` は CI 専用ではない**

`.github/workflows/ci.yml` と `scripts/mutate-run.mjs` は、どちらも
「**手元では走らない（Docker が無い）。CI が唯一の実行場所**」と書いている。
**この前提は、いまの作業場では成り立たない。**

```
dockerd &                      → 起動する
npx supabase start             → EXIT 0（全サービス起動）
npm run verify:stack           → 4/4 PASS
npm run verify:portal          → 15/15 PASS
node scripts/mutate-run.mjs …  → 走る
```

Playwright の chromium も `/opt/pw-browsers` に入っているので
`npx playwright install` も要らない。**CI が死んでいても F4 は進められる。**

> **注意**: `dockerd` は自分で起動する（既定では動いていない）。
> セッションごとに `dockerd &` → `npx supabase start` が要る。

### ③ その場で1件証明した — `F-20260828-56` の2つ目の直しは**効いていた**

`0-J` が「**この2つ目の直しの効果はまだCIで確認できていない**」と残した件。
手元で走らせて決着した:

```
node scripts/mutate-run.mjs pet-purge-broken
  ✅ pet-purge-broken   verify-admin.mjs   赤   1件
    18. 消した犬の写真が Storage に残っていない
```

**CI で3戦3敗だったものが、直して初めて赤になった。**
`verify-admin.mjs :: 18.` を証明済みへ移した（112 → **113**）。

### ④ 台帳を触るときの注意（今回踏んだ）

`docs/ops/proof-of-red.md` の `## 証明済み` の節は、**見た目より早く終わっている**。
`proof-of-red.mjs` は次の `## ` 見出しまでしか読まないので、節の途中に在る
`## F4 を閉じる範囲` と `## ⛔ 毒見の天井` **より下に足した行は数えられない**。
証明済みを足すなら `## F4 を閉じる範囲` **より上**に置くこと。
（機械は「台帳に無い検査」として止めてくれるので、黙って落ちることはない）

### 次のセッションがやること

1. **CI が直ったか見る**（public 化の効果）。直っていれば、`ids` を付けない
   フル実行を1回走らせて `docs/ops/mutate-run-result.md` を更新する
   ——`delivery-ready.mjs` の条件3が見るのはこのファイルだけで、**いま古い**
2. **直っていなくても止まらない。** ②の手順で手元で `mutate` を走らせる。
   残り **70件**（うち F4 を閉じる範囲は `node scripts/guard/delivery-ready.mjs`
   が名指しで出す）を、CI の復旧を待たずに埋められる
3. 残っている未証明の大半は**土台の設営そのもの**（`0.`/`1.` のような「用意できた」系）と
   `verify-m6`・`verify-portal`・`verify-report-roundtrip` の個別項目。
   1個の壊しでまとめて狙うのが難しく後回しにしてきたもの（`0-I` に一覧）

## 0-J.（2026-08-28・その7）— **台帳 112/183。CI が原因不明で即死し始めた**

> **`0-I` より下は途中経過。いまの正は下の表と `docs/ops/proof-of-red.md`。**

### 進捗

台帳は **112/183**（毒見21＋1件ずつ壊す91）。`## F4 を閉じる範囲` の11本のうち
未証明は **43件**（`node scripts/guard/delivery-ready.mjs` が名指しで出す）。
残っているのは、大半が**土台の設営そのもの**（`0.`/`1.` のような「用意できた」系）か
**verify-m6・verify-portal・verify-report-roundtrip の個別項目**——1個の壊しで
まとめて狙うのが難しく、後回しにしてきたもの。

### 見つけた実在の欠陥（`F-20260828-56`・直したがCI未検証）

`verify-admin.mjs :: 18.`（消した犬の写真が Storage に残っていない）が、
**2つの独立した理由**で `pet-purge-broken`（実際に `purgePetAssets` を壊す）を
CI で3回連続検出できなかった:

1. Storage 一覧 API の fetch に `apikey` ヘッダが無く常に失敗、失敗を「空」に
   丸めて常に合格していた → ヘッダを足し、失敗を `throw` するよう直した
2. 1つ目を直しても直らなかった。直前の「⑤ カルテ1枚削除」が、この犬の唯一の
   写真をすでに Storage から消していたため、`purgePetAssets` に届く時点で
   対象が0件だった → 削除の直後にもう1枚カルテを確定させ、写真を残すよう
   `verify-admin.mjs` を直した

**この2つ目の直しの効果はまだCIで確認できていない**（下の「CIが原因不明で即死」参照）。

### 一般的な不安定さ（test-harness flakiness）を確認した

同じ壊し方でも run ごとに検出/非検出が入れ替わる項目が複数見つかった
（`invite-reusable`・`rls-drafts-leak`・`app-throws-runtime-error` 系など）。
**1回の0件だけでは恒久的な欠陥と判定しない**——2回以上連続で0件が出て初めて
実欠陥（`pet-purge-broken` がそうだった）と疑う運用にしている。1回だけの0件は
`docs/ops/proof-of-red.md` に「不安定さの範囲として扱い、証明済みは動かさない」旨を
書いて次回に持ち越す。

### ⚠️ CI が原因不明で即死し始めた（未解決・次のセッションへの引き継ぎ）

このセッションの終盤、`workflow_dispatch` で CI を起動しても
**全ジョブ（`check`/`verify`/`walk`/`mutate`）が5〜6秒で「失敗」し、
課金時間 0ms・ログも一切残らない**状態になった（run #156〜160・少なくとも5回連続）。

調べて分かったこと・分からなかったこと:

- **手元の検証（`build`/`check`/`test`/`mutate --dry-run`）はすべて EXIT 0**。
  直前のコミットで `.github/workflows/ci.yml` は変えていない。コードの問題ではない
- **一番軽い `check` ジョブ**（Supabase もブラウザも使わない・普段1分）**まで
  即死している**——重いジョブのランナー容量不足という説明は成り立たない
- [githubstatus.com](https://www.githubstatus.com) は「All Systems Operational」
- **このリポジトリは public**（マスター確認済み）。public リポジトリの
  GitHub-hosted runner は Actions の分数無制限——**月間分数の枠切れ説は否定された**
- 残っている仮説: **このセッションが短時間に `workflow_dispatch` を連発した
  （数時間で10回近く）ことで、GitHub 側に何らかのレート制限・クールダウンが
  掛かっている**。ただし API のレスポンスにその旨のエラーは出ておらず未確認
- PR #31 に2回コメントで状況を記録済み

**次のセッションでまず確認すること**:
1. GitHub の Actions タブをブラウザで直接見て、API に出てこないエラー表示が
   無いか確認する（organization の設定・runner の状態・レート制限の通知など）
2. しばらく（できれば数時間〜半日）間隔を空けてから `workflow_dispatch` を
   1回だけ試す。**連発しない**——この現象が「短時間の連発」で起きた疑いが強いため
3. 直れば、まず `pet-purge-broken` の2つ目の直しが効いているか
   （`verify-admin.mjs :: 18.` が赤になるか）を確認してから、残り43件の
   未証明に進む

## 0-I. いちばん新しい（2026-08-28・その6）— **台帳を「客に当たる経路」まで埋める作業中**

> **`0-H` より下は計画時点の数字。いまの正は下の表と `docs/ops/proof-of-red.md`。**

マスター判断（2026-08-28）: 台帳は129件すべてではなく**客に当たる経路まで**で
F4 を閉じる。あわせて**受け入れ基準を機械にした**——
`scripts/guard/delivery-ready.mjs`（`gate.mjs --end` から呼ぶ。理由は
`checkout.mjs` はセッション終了ごとに走る汎用の関所で、F4 未完了でも通す
必要があるため、そちらではなく `gate.mjs --end` に繋いだ）。

範囲は `docs/ops/proof-of-red.md` の「## F4 を閉じる範囲」節にファイル名で
固定してある: 客に当たる経路11本（100件）／ 判定できない8件（`verify-production`・
`verify-stack` 残り）／ F4 の後に回す21件（`verify-screens`）。

### いまの進捗（1〜6回目まで壊し方を追加・push 済み）

| 回 | 狙い | 状態 |
|---|---|---|
| 1 | `verify-admin`（10個） | **CI run #133 で実測。13件赤（11件が新規）。`admin-menu-title-lost` だけ結果が謎**（狙いは2/3/4/13だが、集計に載った2件の名前が特定できていない。次の回に単体で再実行して切り分け中） |
| 2 | `verify-edit`（5個） | CI 未実測（run #139 で実行中） |
| 3 | `verify-portal` + `verify-m6`（4個） | CI 未実測（run #139 で実行中） |
| 4 | `verify-empty-pet` + `verify-invitation`（5個 + 既存1個拡張） | CI 未実測（run #139 で実行中） |
| 5 | `verify-report-roundtrip` / `photo-roundtrip` / `delete` / `draft`（4個 + 既存1個拡張） | CI 未実測（run #139 で実行中） |
| 6 | 「アプリ由来のエラーが無い」系をまとめて（2個） | **まだ CI に出していない**（push 済みだが dispatch 前） |

**CI run #139**（`ids`: `admin-menu-title-lost` 単体再実行 + 2〜5回目ぶん全部、
計20個・うち2個SQL）が実行中。**このセッションが終わる前に結果を見て、
台帳へ反映すること。**

### 実測から分かった注意点（次のセッションへ）

1. **CI の「結果を枝にコミットする」段に `F-20260828-53` があった**（直し済み・
   `git status --porcelain` は無視ファイルの差分を黙って見せない）。run 133 の
   結果は一度コミットされず、成果物 zip から復元して手で台帳へ移した。
   **同じ理由で他の回の結果もコミットされていない可能性がある**——
   `git log` に `mutate の結果を記録する（run N）` が無ければ、成果物を疑うこと
2. **`mutate-run.mjs` は絞って（`ids`）走らせると `docs/ops/mutate-run-partial.md`
   へ書く**（`.gitignore` 登録・`git add -f` で追跡）。全体の記録は
   `docs/ops/mutate-run-result.md` のまま（**古い**——ラウンド1〜6の新しい
   壊し方をまだ含んでいない）。**締めに1回、`ids` を付けずに全部を走らせて
   `mutate-run-result.md` を更新すること**（`delivery-ready.mjs` の条件3が見るのは
   こちらだけ）
3. **`admin-menu-title-lost` の謎を解決すること。** 狙いは
   `strong`→`span` で `menuTitles()` が空配列になり `2./3./4./13.` が赤になる
   はずだが、run 133 の集計にそれらの名前が1つも出なかった。単体再実行の
   結果（run #139）を見て、①本当に2件しか赤にならないのか（なぜ4件ではないのか）
   ②その2件の名前は何か、を確認すること

### 費用の実測（1〜6回目の合計）

壊し方 **41個**で **上限52件**（見込み）を狙っている。台帳はいま
**65 / 183件**（run #133 の11件を反映済み）。run #139 の結果が乗れば
さらに増える見込み。

### まだ手をつけていない、残る「客に当たる経路」の項目（見込み）

- **土台の設営そのもの**（0/1/2 のような「用意できた」系）: `verify-delete` 0,1,2 /
  `verify-empty-pet` 0b,1 / `verify-invitation` 1 など。個別に狙うと壊れる範囲が
  広すぎるため後回しにしている
- **portal の残り**（1,2,3,4,7,8,12）: `portal-login-panel-dead` が
  5,6,9,14,15 を狙っているが、実測前は他の項目への効果も未確認
- **m6 の残り**（②b,③,★,⑤,⑤b,⑥b）: 個別の壊しが要る
- **verify-xss の3件**（`label` `label #2` `label #3`）: 早期脱出の分岐で、
  この壊しでは通らない
- **verify-photo-roundtrip の label 系・4・5**: 土台の設営に近い

## 0-H. いちばん新しい（2026-08-28・その5）— **残作業の計画**

> **`0-G` より下は、この日の途中経過。数字はすべて古い。** いまの数は
> `docs/ops/proof-of-red.md` の表と `node scripts/guard/proof-of-red.mjs` の出力だけが正。

### いまの状態（実測）

| | |
|---|---|
| フェーズ | **F4**（棚卸しと納品の仕上げ・作る段ではなく**閉じる段**） |
| 関所 `gate` / `build` / `check` / `test` | **すべて EXIT 0** |
| CI（run #126） | **success**（`check` / `verify` / `walk` / `mutate` 全部） |
| 台帳（壊して赤を見た） | **54 / 183件**。未証明 **129件** |
| 枝 `claude/checkin-4rxus2` | **master より 22 コミット先行**（PR #31・未マージ） |
| F4 バッドシナリオ 10件 | **全件クローズ済み**（該当した 5 ／ 該当せず 5） |

### 残っているのは3つだけ

**① 台帳の 129件を埋める**（本命・マスター判断 A の続き）
**② F4 を閉じる**（納品の手続き）
**③ マスター判断が要る 6件**（下）

---

### ① 台帳の 129件（未証明の内訳・実測）

```
 21 /  22  verify-screens.mjs        各画面に在るべきものが在るか
 20 /  22  verify-admin.mjs          管理者の作成・修正・削除
 19 /  20  verify-edit.mjs           トリマーの記入画面
 13 /  16  verify-portal.mjs         飼い主の入口
 10 /  24  verify-report-roundtrip.mjs
  9 /  12  verify-empty-pet.mjs        4 /   7  verify-delete.mjs
  9 /  14  verify-m6.mjs               4 /   4  verify-production.mjs
  7 /   9  verify-invitation.mjs       4 /   6  verify-stack.mjs
  5 /  12  verify-photo-roundtrip.mjs  3 /   8  verify-draft.mjs
                                       3 /   7  verify-xss.mjs
```

**上の3本（`screens` / `admin` / `edit`）だけで 60件——残りの半分**。ここから行く。

**費用の実測**: 壊し方 11個で 33件が埋まった（**1個あたり約3件**）。
CI の `mutate` 1回は **約13分**（`supabase start` 3分 ＋ 本体 9分）。
129件を全部埋めるには壊し方が **40個前後**要る見込みで、**CI 5〜8往復**。

**順番**（客に当たる経路から・マスター指示）:

| 回 | 狙い | 壊し方の例 |
|---|---|---|
| 1 | `verify-admin` 20件 | 顧客作成／ペット作成／カルテ削除の RPC を1つずつ空にする |
| 2 | `verify-edit` 19件 | 記入 → 下書き保存 → 確定の各段を1つずつ止める |
| 3 | `verify-screens` 21件 | `src/index.html` の画面ブロックを1つずつ落とす |
| 4 | `verify-portal` `verify-m6` `verify-empty-pet` `verify-invitation` 38件 | 一覧・招待・空のカルテ |
| 5 | 残り（写真・削除・下書き・stack） | |

**先に分かっている壁が2つ**:

1. **`verify-production` 4件は壊せない。** 本番サイトに向かって出ていく検査なので、
   壊す対象がこのリポジトリに無い。**別の方法が要るか、「判定できない」と台帳に明記するか**
   のどちらか（毒見の天井と同じ扱い）。
2. **単発では判定できない項がある。** `17.` がそうだった——守りが二重で、1枚剥がしても
   漏れない。`edits`（2か所同時）は作ったが、**どの項がそれに当たるかは壊してみるまで
   分からない**。赤 0件が出るたびに1回ずつ調べることになる。

**この工程の産物は数字ではない。** これまでに**読んでも出なかった穴が3件**出ている
（`F-20260828-48` `-50` `-51`）。埋める過程でまた出る前提で見積もる。

### ② F4 を閉じる

| | やること | いま |
|---|---|---|
| 1 | `docs/ops/failure-check-F4-end.md`（`failures.md` 全件照合） | **未着手**。`-start` は在る |
| 2 | `docs/ops/template.md` を2択・過去形で埋める | 未 |
| 3 | `node scripts/guard/checkout.mjs`（まっさらで `npm ci`→`build`→`check`→`test`） | 未 |
| 4 | 共通ゲート＋**CI が緑** | ✅ 満たしている |

**②は①が終わってからでよい**が、**①を全部終わらせる必要は無い**——台帳は
「まだ確かめていない」を名指しで残す造りなので、**途中で閉じても嘘にならない**。
どこで閉じるかはマスターが決める（下の判断6）。

### ③ マスター判断が要る 6件

| | 何を決めるか | 決めないとどうなるか |
|---|---|---|
| 1 | **PR #31 をマージするか**（22コミット） | 今日の成果が全部 master の外にある |
| 2 | `deferred` **#3**: 「次回のおすすめご来店時期」を器に足すか | **残すと決めた節が、飼い主に届く器に無い**（飛んでいる）。足すなら「日をどう決めるか」も要る |
| 3 | `deferred` **#5/#6**: 未参照ファイル7点（`konva.min.js` 含む）を消すか | `AGENTS.md` の確定スタック表が名指ししている。消すとルールと実体が食い違う |
| 4 | `deferred` **#35**: バックアップ | **実顧客データを入れる前に必須**。Supabase の画面を見ないと決まらない（コードでは決められない） |
| 5 | `verify-production` 4件をどう扱うか | 上の壁1 |
| 6 | **台帳をどこまで埋めるか** | 129件全部か、客に当たる経路だけか。全部やるなら CI 5〜8往復 |

**推奨**: 1 → マージする（成果を失わない）。6 → **客に当たる経路（`admin` `edit` `portal`）を
優先し、100% は目標にしない**——台帳は埋まっていない分を名指しで残すので、
途中で止めても誤読されない。それが最初からこの造りの狙いだった。

## 0-G. いちばん新しい（2026-08-28・その3）— **毒見を作った。Docker は要らなかった**

### 何を作ったか

`docs/ops/proof-of-red.md` の182件を1件ずつ手で壊すのは無理なので、**土台ごと壊す**。

| | |
|---|---|
| `scripts/lib/poison-stack.mjs` | 「形だけ合っていて中身が空」の Supabase。ログインは通す（通らないと検査が入口で死ぬ）。データは必ず空 |
| `scripts/poison-run.mjs` | `SUPABASE_LOCAL_URL` をそこへ向けて `verify:*` を走らせ、PASS/FAIL を数える |

**Docker は要らなかった**（`supabase start` の代わりだから）。当初 CI に置くつもりだったが、
**この環境で走る**——`wrangler dev` も Chromium（`launchChromium()` が在るものを探す）も動く。

### 結果（全14本・`docs/ops/poison-run-result.md`）

| | |
|---|---|
| 赤になった（＝壊すと落ちると確かめた） | **25件** |
| 緑のまま残った | **18件** |
| **判定行すら出なかった** | **139件** |

### 18件を「全部壊れている」と読むのは誤り

- **本物の欠陥（`verify-stack`）** → 直した（下）
- **判定できない16件** … `verify-portal` 10 / `verify-screens` 4 / `verify-m6` 2。
  「`/my` が配信される」「未ログインでログイン導線が出る」など**静的配信と未ログイン画面**。
  DB が空でも**緑が正しい**。毒見はこれらを判定できない

### 直した実欠陥 — `F-20260828-50`

**「マイグレーションが当たっている」が `HTTP 200 / 0件` で緑だった。**
`F-20260825-35`（401 でも PASS）を直したときに「200 かつ配列」へ変えたが、
**空の配列も配列**。**同じ検査が2度、違う形で同じ嘘をついた。**
→ **seed にしか無いもの**（犬 X を id で名指し）を引く形にした。毒見で赤を実測。
あわせて `check('Supabase が起きている', true)`（**恒真の直書き**）も応答を見る形に。

### いちばん大きい学び

**毒見の判定力は「その検査の対象が、毒に依存しているか」で決まる。**
空のデータで判定できたのはデータを見る検査だけ。139件が判定行すら出ないのは、
土台が空だと「0. 検査用の犬を登録できた」で落ちて**そこで死ぬ**ため。
**「1回で182件埋まる」は楽観的すぎた。毒の種類を増やさないと残りは埋まらない。**

### 台帳のいま

| | |
|---|---|
| 検査 | **182件** |
| 壊して赤になったところを見た | **17件** |
| まだ見ていない | **164件** |

毒見の赤は25件だが、**移せたのは17件**。残り8件（`verify-xss`）は
`check(label, …)` と**名前が変数**で、台帳の鍵（静的な名前）と結び付かない。
しかも `check(label, …)` は**3か所あるのに台帳では同じ鍵に潰れている**。
→ **台帳の鍵の付け方に欠陥がある**（動的な名前を扱えない）。直すまで未証明のまま置く。

### 機械自身の欠陥を2つ直した

1. `passConditions` が**コメントの中の `check(…)` を数えていた**。
   `verify-stack` の「恒真だった」ことを説明するコメントを書いた直後、
   その説明文が1件の検査として台帳に載った（名前は `…`）。
   **数える機械が数える対象を取り違えた3度目**（`F-20260828-49` の続き）。
   `stripComments` を足した。検査2件
2. `poison-run` が**一部だけ走らせた回で全体の記録を上書き**していた（`W-8` の型）。
   範囲指定の回は `poison-run-partial.md` に書く

### 実測（すべて手元）

```
npm run build   EXIT 0
npm run check   EXIT 0   ← empty-pass / proof-of-red を含む14本
npm test        EXIT 0   ← 163件
node scripts/poison-run.mjs   赤25 / 緑18（Docker 不要・約6分）
```

### そのあと（同じ日・0-G の続き）

**1 と 2 は着手して、途中まで進んだ。**

**台帳の鍵**: 同名には出現順の番号（`label #2` `#3`）を付けて分けた。
182件のうち**4件が、数えられてはいるのに区別できていなかった**状態は解消。
ただし**実行時の名前と台帳の鍵を結び付ける問題は残る**——台帳はソースを静的に読むので
`label` としか書けず、毒見の出力は `犬の名前（見出しへ入る）` などになる。
直すなら `check()` が行番号か固定の id を一緒に印字するしかなく、**182か所の書き換え**。
**マスター判断に回す**（`docs/ops/proof-of-red.md` に明記）。

**毒の2種類目**: `--flavor=noauth`（ログインが通らない世界）を作った。

| 毒 | 赤 | 緑のまま |
|---|---|---|
| `empty`（データが空） | 25 | 18 |
| `noauth`（ログイン不可） | 16 | 15 |

`empty` で緑のまま残っていた `verify-stack :: seed のアカウントで実ログインできる` と
`verify-m6` の2件が、`noauth` で赤になった。**毒を変えると判定できる範囲が変わる。**

**両方の毒でも緑のまま残るのは15件**（`verify-portal` 10 / `verify-screens` 4 /
`verify-stack :: Supabase が起きている`）。**これらは緑が正しい**——静的配信と
未ログイン画面の検査で、土台が空でもログインできなくても中身は変わらない。
判定するには毒ではなく**配信物そのものを壊す**必要がある（例: `dist/` を空にする）。まだ無い。

### 台帳のいま（この日の最終）

| | |
|---|---|
| 検査 | **182件** |
| 壊して赤になったところを見た | **19件**（`empty` 17 ＋ `noauth` 2） |
| まだ見ていない | **163件** |

### 説明文が嘘をつく型を、今日3回踏んだ

1. `worker/src/index.js` のコメント7か所（`bad-scenarios-F4` #1 が発見）
2. `verify-stack.mjs` の「いまは 200 かつ配列を見る」——直した直後に古いまま残した
3. `poison-run.mjs` が「`poison-run-result.md` に書いた」と表示しながら
   実際は `-noauth.md` へ書いていた（書き先を文字列で直書きしていた）

3 は**書いた先を、書いた先から出す**形に直した。1 と 2 も直済み。
**同じ日に3回出る型は、注意では止まらない。**

### ⛔ 毒見は天井に当たった（この日の最終結論）

3つ目の毒 `nodist`（配信物も空・`worker/wrangler.poison.toml`）まで作った。

| 毒 | 赤 | そのうち新規 |
|---|---|---|
| `empty`（データが空） | 25 | **17** |
| `noauth`（ログイン不可） | 16 | **2** |
| `nodist`（配信物も空） | 26 | **2** |

**182件中 21件で止まる。毒を増やしても解けない。** 理由は構造的:

> **検査 N を判定するには、検査 1〜N-1 が通っていなければならない。**

`verify:*` は上から順に走る。毒で最初の1件が落ちると**そこで終わり**、
2件目以降は**実行すらされない**。`empty` で139件が判定行すら出なかったのも、
`nodist` が狙った15件のうち各1件しか判定できなかったのも同じ理由。

**「まとめて壊せば一気に埋まる」は成立しなかった。** 見込みが外れた。

### 毒見が価値を出した場所

**証明の道具としてではなく、欠陥の発見器として。** 本物を2件掘り出した
（`F-20260828-50`）——「200 かつ配列」で DB が無くても緑だった件と、
`check('Supabase が起きている', true)` の恒真の直書き。
**この2件は、毒見が無ければ出なかった。**

### マスター判断は A（1件ずつ壊す）。着手して 30/182 まで来た

`scripts/mutate-run.mjs` ＋ CI の `mutate` ジョブ（手動実行）。
**土台は本物のまま**、製品を1か所だけ壊す。検査は最後まで走り、気づいた項だけが赤になる。

初回（CI run #111）は**客に当たる経路3つ**から:

```
  ✅ delete-assets      verify-delete.mjs              赤   2件
  ✅ delete-assets      verify-admin.mjs               赤   1件
  ✅ hydrate-assets     verify-photo-roundtrip.mjs     赤   6件
  ✅ upload-assets      verify-photo-roundtrip.mjs     赤   1件
  ✅ upload-assets      verify-delete.mjs              赤   1件
  赤になった: **10件**（うち新規9件） / 製品のファイルに差分なし
```

**3つの壊し全部に検査が気づいた。新しい欠陥は出なかった。**
写真が届く・消したものが消える——この経路の検査は**仕事をしている**ことが実測できた。

| | |
|---|---|
| 毒見（3種） | 21件 |
| 1件ずつ壊す（4回・壊し方8個） | **+25件** |
| **いま** | **46 / 183** |

**この工程の目的は赤を出すことではなく、緑が本物かを確かめること。**

**2回目（run #114）で穴が1つ出た**——`F-20260828-51`。`renderWeightGraph` を空にしても
どの検査も気づかなかった。数字（`weight-pill`）とグラフは別の関数が描くのに、
検査は数字しか見ていなかった。体重の項は `7.` と `14.` の2つ在るので、
**人が読んでも「体重の検査は在る」としか見えない**。壊して初めて出た。

`14b. 飼い主: 体重のグラフが描かれている` を足し、**3回目（run #116）で
同じ壊し方が赤になることを確かめた**（赤0 → 赤≥1 の差が証拠）。

**XSS は本物だった。** `setText` を `innerHTML` に変える（`F-20260821-17` の
stored XSS そのもの）と、8つの細工すべて・3つの観点すべてが赤。
ただし出力の名前が実行時の値なので、**18件は確かめられたのに台帳へ移せない**——
鍵の付け方の問題（マスター判断待ち）。

### 残り152件（旧: 161件をどう埋めるか）

安い道は無い。見えている選択肢は3つ:

| | 何をする | 費用 | 得られるもの |
|---|---|---|---|
| **A** | **1件ずつ狙って壊す** | 161通りの壊し方を作る。1件あたり数分〜 | 161件すべて。いちばん確実で、いちばん重い |
| **B** | **`check()` の作りを変える** — 合格条件を関数で受け（`check(name, () => cond)`）、機械が毒を注入して1件ずつ評価する | 182か所の書き換え ＋ 仕組み | 自動で埋まる。ただし**書き換え自体が W-1 の型を踏む危険**がある |
| **C** | **ここで止める** | 0 | 21件のまま。**161件は「未確認」と名指しで残り続ける**——嘘はつかない |

**C も正直な選択肢である。** 台帳は数を良く見せられない造りなので、
21/182 のまま置いても「確かめた」と誤読されることはない。

私の見立て: **A を全部やる価値は無い**。`verify:*` の中には
「押せた」を見るだけの軽い項も多く、161件が等価ではない。
**客に当たる経路（削除・写真の往復・RLS）から順に A を少しずつ**、が現実的だと思う。
ただしこれは**優先順位の判断なので、マスターが決めること**。

### ⛔ アプリを壊しても届かない場所（残り137件の内訳に効く）

**下書きが飼い主に見えないことと RLS は、アプリではなく DB（RLS）が守っている**
（`worker/src/data-stores/supabase-data-store.js:167`）。
つまり `verify-draft :: 3. 下書きは飼い主に見えない` や
`verify-portal :: 13. 他人の犬（Q）は見えない（RLS）` は、
**アプリのコードをいくら壊しても赤にならない**。

証明するには**マイグレーションの SQL を壊して `supabase db reset`** が要る。
いまの `mutate-run.mjs` には無い仕組みで、**これを作らないかぎり
RLS が守っている分は永久に埋まらない**。次の工程の本命。

### 次にやること

1. **RLS を壊せるようにする**（マイグレーションを壊して `db reset`）← 本命
2. 壊し方を足し続ける（客に当たる経路から）
3. **マスター判断**: `check()` に行番号か id を印字させるか。
   `verify-xss` の18件は**確かめられたのに台帳へ移せない**まま
4. **マスター判断**: 上の A / B / C
2. **マスター判断**: `check()` に行番号か id を印字させるか（`verify-xss` の8件が移せる）
3. `docs/watch.md` の W-2〜W-8
4. `docs/deferred.md` の残りを閉じる

---

## 0-F.（2026-08-28・その2）— **「規則にする」を却下され、定義を置いた**

### マスター判断（`D-20260828-53`）

W-1 を `AGENTS.md` の規則へ昇格させるか諮ったところ、**規則にすること自体を却下**された:

> 「もはやルールじゃダメだろ。**16回中16回盛れない方法にしろ。**
>  監視以前にそういう事をやる時点の整備が必要だろう、監視は機械がダメなら
>  フックにするか毎回見に行くか、**壊してみて赤にならないなら検査自体が壊れてるとする**か」

**規則は「気をつける」の言い換え**で、16回のうち何回止まるかを言えない。
形を見る機械（`empty-pass`）でも **5/16** までで、残り11回は形が正常なまま嘘をついていた。

### 置いたもの — **定義**

> **「壊して赤になったところを見ていない検査は、壊れているものとして数える」**

形に依存しないので**どんな嘘の付き方をしても逃げられない**——嘘をついている検査は、
定義上「壊したら赤になった証拠」を出せない。

| | |
|---|---|
| `scripts/guard/proof-of-red.mjs` | 機械。`npm run check` から呼ばれる |
| `docs/ops/proof-of-red.md` | 台帳。**182件を1件ずつ名前で**持つ |
| いまの数字 | 検査 **182件** / 赤になったところを見た **0件** |

**182件すべてが未確認。** `verify:*` が全部緑であることは、いまのところ
**それ自体が何も保証していない**——その緑が嘘でないことを一度も確かめていないため。

**盛らせない造り**（3方向とも実測で赤になることを確かめた）:

| やろうとすると | |
|---|---|
| 証拠なしで検査を足す | 赤（「台帳に無い検査」と名指し） |
| 台帳から行を消して黙らせる | 赤（同上） |
| 検査ごと消して数を減らす | 赤（「台帳が実体に無い検査を指している」） |

数ではなく**名前**で持つのは、数だけの上限だと**別の検査を消して枠を空ければ通る**ため。

### ついでに出た欠陥 — 数える機械が、数える対象を取り違えていた

`F-20260828-49`。「合格条件196件」は**誤りで、正しくは182件**だった。
`\bcheck\s*\(` が **`function check(name, pass, detail)` の宣言そのもの**を
検査として数えていた（14件）。名前も引用符の位置で千切れていた。

**W-1 の型を、W-1 を止めるために作った機械の中で踏んだ。**
気づけたのは、台帳に**名前で並べた**から——`:: name` という在りもしない検査名が
15本ぶん並んで初めて出た。**数だけを見ていたら気づかなかった**（196 も 182 も、
それ単体では何の違和感も無い）。

### 実測（すべて手元）

```
npm run build   EXIT 0
npm run check   EXIT 0   ← empty-pass / proof-of-red を含む14本
npm test        EXIT 0   ← 161件（+4）
```

### 次にやること

1. **毒見（poison run）を作る** ← 182件を一気に埋める唯一の現実的な道。
   接続先は `SUPABASE_LOCAL_URL` で差し替えられる（`scripts/lib/local-stack.mjs:37`）。
   土台とアプリの間に**中身を空にして返す層**を挟み、その世界で `verify:*` を全部走らせる。
   **何も動いていない世界なのだから、全部赤にならなければおかしい**——
   緑のまま残った検査が、壊れている検査。Docker が要るので **CI に置く**
2. 毒見で出ない型（「動いている世界で、別のものを見ている」`F-20260827-43`）は
   1件ずつ壊すしかない。台帳がその残りを名指しで残す
3. `docs/watch.md` の W-2〜W-8
4. `docs/deferred.md` の残りを閉じる

### W-1 の役割が変わった

「何回起きたかを数える」から「**182件のうち何件を確かめたかを数える**」へ。
数える対象が、**事故ではなく残りの仕事**になった。

---

## 0-E.（2026-08-28）— **W-1 を数え直したら、生きた欠陥が2件出た**

### 何が起きたか

`docs/watch.md` の **W-1**（検査が実際の仕組みと違う所を見る）を数え直した。
**7回ではなく16回だった**（`docs/watch.md` 末尾に全16回の表と出典）。
数え方が半分しか数えていなかった——(a) 型の名前になった `F-20260825-35`/`-36` 自身、
(b) **緑のまま通った項**、(c) **F 番号が付かなかった回**、が丸ごと落ちていた。

数える前に「機械で判る1形だけでも止められないか」を試したところ、
**`origin/master` に入っている生きた欠陥が2件出た**（`F-20260828-48`）:

```
scripts/verify-admin.mjs
  16. ペットが実際に消えた   !(petsLeft.pets   || []).some(…)
  17. 顧客が実際に消えた     !(ownersLeft.owners || []).some(…)
```

`petsLeft.pets` が `undefined`（API が落ちた・鍵が切れた・形が変わった）でも
`|| []` が空配列にし、`!false` で **PASS**。**何も消えていなくても、そもそも
一覧を引けていなくても緑。** 「客のデータが消えたか」を見る検査だった。

**いちばん重いのはここ**: 2026-08-27 の `65a4f04` は**まさにこの型を直したコミット**で、
`15.` に「サーバに数え直させて待つ」を入れ、理由を12行のコメントで書いていた。
**そのコメントのすぐ下**に、同じ欠陥が2つ残っていた。CI も緑で通した（落ちようがない）。
→ **直した直後がいちばん見落とす。同じ型を直したら、同じファイルの兄弟を数える。**

### 入れたもの

| | 何を |
|---|---|
| `scripts/guard/empty-pass.mjs` | **新しい機械。** `scripts/verify-*.mjs` の `check()` 第2引数だけを読み、「否定 ＋ 空フォールバック ＋ 錨なし」を止める。15本・合格条件196件で**誤検知0・実欠陥2件**。`npm run check` に入れた |
| `test/empty-pass.test.mjs` | 機械自身が落ちる形かを見る7件。`npm test` に入れた |
| `scripts/verify-admin.mjs` | 16/17 を直した。(1) `Array.isArray` で**一覧が引けたこと**を錨にし、(2) `15` と同じく**サーバに数え直させて消えるまで待つ**。落ちたら画面の表示を行に出す |
| `docs/failures.md` | `F-20260828-48`（上の件）／**`F-20260827-44` の本文**——番号だけ採られてコードが2か所から参照しているのに、**本文が一度も書かれていなかった**（`bad-scenarios-F4` #1 が見つけた） |
| `docs/ops/solved-F4.md` | **新設。** `#1` `#5` `#9` の3出力 |
| `docs/watch.md` | W-1 を書き直し＋全16回の表 |

### 作業場を開けた

F4 の関所は「バッドシナリオの未解決が減っていない（5件）」で閉じていた。
実施済みだった **`#9`**（runbook 2-D）と **`#1`**（記録の照合）を 3出力つきで締め、
**`#5`** は `種別: 回避` にした（Docker はこの環境にもマスターの Windows にも入らない）。
いま `node scripts/guard/gate.mjs src/index.html` は **EXIT 0**。

### 実測（**すべて手元**。CI 判定はこの PR で出る）

```
npm run build   EXIT 0
npm run check   EXIT 0   ← empty-pass を含む13本
npm test        EXIT 0   ← 157件（+7）
```

**`verify:admin` の 16/17 は手元で走っていない**（Docker が無い）。**CI 判定待ち。**

### 次にやること

1. **`docs/watch.md` の W-2〜W-8 を1件ずつ閉じる**（W-1 は今回閉じ方を作った）
2. **マスター判断待ち**: W-1 を `AGENTS.md` の規則へ昇格させるか。昇格の4条件のうち
   3つを満たし、4つ目（機械判定可能）が**一部**になった——`empty-pass` は16回のうち
   **5回分の形**しか止められない。文案は `docs/watch.md` 末尾
3. `docs/deferred.md` の残りを「直す／放置」で閉じる
4. **フェーズを閉じるには**、バッドシナリオ `#2` `#3` も `solved-F4.md` に要る
   （`node scripts/guard/solved.mjs F4` が名指しする）。どちらも「危険な条件がそろっている
   ことの確認」で直す対象が無いので、`種別: 回避` で書く
5. `consume_rate_limit` の工事は**この環境では手元で緑にできない**が、**やれる**——
   PR を上げて CI（Docker 在り）に判定させる。約8分／回

### この環境について（訂正）

前回「Docker が無いからできない」と書いたが、**書き方が誤っていた**。正しくは
**「手元では試せないので CI 経由になる（遅い）」**。CI のランナーには Docker が在り、
`verify` job が15本を走らせている（2026-08-27 の master で全緑・run #98）。
マスターの PC にも私のコンテナにも Docker は**要らない**。

---

## 0-D.（2026-08-27・深夜）— **棚卸しをして F4 に入った**

**F3 は閉じた。いまのフェーズは `F4`（棚卸しと納品の仕上げ）。**（`docs/ops/phase`）

### 客に当たる欠陥を1つ見つけて直した

**「カルテ修正」で、カットの長さとスタイルが黙って消えていた**（`deferred` #26）。
`applyReport()` の呼び出し元は `resumeDraft` だけではなく、管理者画面の「② カルテ修正」も
通る。選び直さずに保存すると `trimming.comment` が出ず、**すでに飼い主に届いていた
内容が消える**。記録は「下書きから選び直しになるだけ」と書いており、**重さの見積もりが
間違っていた**（`F-20260827-46`）。

### そのほか直したもの

| | 何を |
|---|---|
| `#36` | **同姓同名の取り違え**。一番危ないのは削除ではなく**新規ペット登録の飼い主選び**だった——間違えると別の飼い主のポータルにカルテが出る。行と選択肢に「飼っている子の名前 ＋ 登録日」を出す |
| `#27` | 下書きの読み込みが、打っている文字を上書きしていた |
| `#35` | ⑤の器の見本数値（タブ「04」で描画を経ずに出る） |
| `#1`/`#14` | 空の解剖図の枠。歯は繋ぎ、**爪はマスター判断で繋いだ**（`D-20260827-47`・後日差し替える） |
| `#2` | 段のタブは**残す**（マスター「使う」）。使うなら、と「04」の空の雑誌を塞いだ |
| `#4` `#19` `#21` `#3` | PWA 結線／actions v5／死んだ注入6個／固定の来店日 |
| `#32` `#18` | **`.gitignore` に鍵3種**／`.env.example` に1行（F4 で範囲に入った） |

### 仕組みを1つ足した — 画像は1命令で差し替えられる

```
node scripts/swap-image.mjs                        ← 一覧（どこに出る・誰が見る）
node scripts/swap-image.mjs login-photo 新しい写真.png
```

拡張子が違っても**参照している画面を全部書き換える**。台帳は `scripts/lib/image-roles.mjs`、
**台帳に無い画像は画面に出せない**（`npm run check` の `image-inventory`）。
出どころ（`ASSET-PROVENANCE`）は自動で書き換えない——人が直す（`A-3`）。

### マスターが決めたこと（5件・すべて記録済み）

| | 決定 |
|---|---|
| 爪の基準図 | **そのまま繋ぐ**（画質を落とさない）。後日差し替える（`D-47`） |
| ログイン画面の写真 | **マスターが撮影**した → `VERIFIED`（`D-48`）。`D-20260823-U1` の最後の1件が片づいた |
| 段のタブ | **使う**（`D-49`） |
| スタンプ帯の横スクロール | **保留・このまま**（`D-50`） |
| フェーズ | **進める** → F4 へ（`D-51`） |
| バックアップ | **クライアントの意向で決着。再提起しない**（`D-52`） |

### F4 のバッドシナリオ10個は実施済み（該当した4 / 該当せず6）

`#1` が欠陥を掘り出した——`worker/src/index.js` の**コメント7か所**が、もう存在しない注入と
削除済みのテンプレート名を説明したままだった。**コードは正しく、説明文だけが嘘をついていた。**

`#2` は消す手を止めた。未参照ファイルを「記録が名指ししている側」から数えたら
`konva.min.js` が **9件**（`AGENTS.md` の確定スタック表を含む）。

### 次にやること

1. **`docs/watch.md` の W-1〜W-8 を1件ずつ閉じる**。特に **W-1**（検査が別の仕組みを見る・
   **7回で減っていない**）は回数を数え直す
2. `docs/deferred.md` の残りを「直す／放置」で閉じる
3. **`consume_rate_limit` の工事は、この環境ではできない**——SQL は実際に PostgreSQL へ
   流すまで緑にならず、**ここに Docker が無い**。札を手で書き換えるのは `A-4` 違反

### この環境について（次のセッションへ）

**Docker が無い。** `verify:*` **15本**も `verify:migrations` も**1本も手元で走らない**。
緑を主張するときは、**その緑を出した場所（手元か CI か）を必ず書く**こと。
手元で走るのは `build` / `check` / `test`（unit 59件を含む150件）まで。

---

## 0-C. いちばん新しい（2026-08-27・夜）— **本番が実顧客を迎えられる状態になった**

| やったこと | 実測 |
|---|---|
| F3 完了版を本番へ | `verify:prod` **4/4 PASS** |
| **本番のダミー掃除** | **飼い主・犬・カルテの3つとも一覧が空**（マスターが管理者画面から実施） |
| 写真を入れられるように | `verify:photo` **13/13**（入れた色と届いた色が一致） |
| RLS の検査 | **一度も走っていなかったのを動くようにした**（15項目・`F-20260827-45`） |
| 本番の見張り | `verify:prod` を毎朝 09:10 に自動実行 |

**ダミー掃除に service_role の再発行は要らなかった。** 管理者のログインだけで消せる
（当初の記録「新しい秘密鍵が要る」は誤りだった）。

### （この節は 0-D で更新された）

ここに挙げていた2件は、**どちらも 0-D の時点で片づいている**。
同姓同名は直した。バックアップは**クライアントの意向で決着済み**——
`D-20260827-52`「**再提起しない**」。実顧客投入前の一覧は `docs/runbook.md` 2-D が正。

---

## 0-B. いちばん新しい（2026-08-27）— **本番に F3 完了版を出した。`verify:prod` 4/4**

**お客さんが開く URL が、いま master に在るものを返している。** F1 以前の版はもう配られていない。

| 見たもの | 結果 |
|---|---|
| `npm run verify:prod`（本番に向けて実測） | **4/4 PASS** |
| 配信物 | 39/39 本がバイト一致 |
| 削除済みの旧UI | 9本すべて 404（`ponchi-app.js` も消えた） |
| `/edit` | 正UI（手元2本 ＋ worker が注入する3本） |

**出し方は GitHub の画面のボタン**（Actions → `deploy` → Run workflow）。鍵は Secrets に入っており、
**AI は受け取っていない**（`A-1`）。手順は `docs/runbook.md` の A-1。

**初回は2回落ちた。どちらも私の誤りで、本番は無傷だった。**
1. `deploy.yml` の順番（`check` を `build` の前に置いた）→ デプロイの手前で止まった
2. `verify:prod` が **worker の注入を知らなかった**（`F-20260827-43`）→ **配布は成功していたのに検査が落ちた**

`plan.md` 第7章は全項目 ✅、`deferred` #30 も完了。

### この先の計画（2026-08-27・敵対検証レポートへの対応）

**`docs/ops/delivery-plan.md`**（3フェーズ）。P-1 掃除と day1 の欠陥 → P-2 写真を v1 に →
P-3 運用基盤と隙間。レポートの誤り2点（鍵は不要／#84 は実在せず）は同文書の
「0. 事実確認」に記録済み。**写真を v1 に入れるのはマスター決定。**

---

## 0-A. （2026-08-27）— **管理者画面を master に入れた（PR #17 マージ済み）**

**master は `a0809b8`。** 下の「## 0」は `d48811b` 時点の記述なので、差分はここに書く。

- **[PR #17](https://github.com/rahiseko-alt/siota-1/pull/17) はマージ済み**（CI 3 job とも緑・`checkout.mjs` の6項目すべて ✅）。PR #16 はこれに引き継いで閉じた
  - 中身: **管理者画面**（①リピーター→カルテ作成/カルテ修正 ②新規→顧客/ペット作成 ③削除→顧客全/ペット全/カルテ1枚）と、確定済みカルテを直す `revise_report`（migration `202608260008`）
  - 機械: **`npm run verify:admin` = 21/21 PASS**（CI の `verify` に入っている）
- **`npm test` は 116件**（master は 115件）

### 止まっていた1件は、product ではなく検査だった（`F-20260826-41`）

`verify:admin` は `11. 中身が直っている` **1件だけ**が落ちていた。押した先を疑う前に、
**CI のログの時刻を引き算した**——`8.` → `9.` が **51ms**。同じ実行で本当に保存している
`7.` は約 **3.6秒**。**保存を1つも待っていなかった。**

修正で入る URL は `/edit/p/{犬}/{カルテ}?revise=1` で、待ちの
`waitForURL(/\/edit\/p\/[0-9a-f-]+\/[0-9a-f-]+/)` は**押す前から合っている**。
その結果 `9.`（2枚目を作らない）も `10.`（1枚のまま）も**何も起きていなくても通る恒真**で、
中身を見る1件だけが古い値を読んで落ちていた。`?revise=1` が落ちるのを待つように直したら
**335ms かかって 21/21**。**product のコードは1行も変えていない。**

> **緑の項目も疑う。** 落ちた1件の陰で、2件が「何も起きなくても緑」のまま並んでいた。
> `docs/watch.md` W-1 は6回目として数え直してある。

### 次の人がやること

**鍵3種のローテーションは 2026-08-27 に済んだ**（マスターが画面で実施・`D-20260827-45`）。
これで `plan.md` 第7章の形式的な条件は**全項目 ✅**。残るのは順に:

1. **デプロイ**（`deferred` #30・マスター判断）。前提だった「鍵が先」は解けた。
   デプロイ直後に `npm run verify:prod` を走らせ **4/4 PASS** を見ること
2. **本番のダミー犬3頭と孤児 owner の掃除**（新しい秘密鍵をその場で作って使う）
3. 削除の入口の置き場所の確認（管理者画面 ③削除 が実装済み）

下の「次に効いてくること」は 2026-08-26 時点の記述で、1と2の順番だけが上に置き換わった。
削除の入口をどの画面に置くかは、管理者画面（③削除）が実装済みなので**その置き場所でよいかの確認**になった。

---

## 0. 最新（2026-08-26）— **F3 の完了条件を満たした（D-14 の2問が実データで両方 ○）**

### いま実行できるものと、その結果（すべて実測）

| 命令 | EXIT | 中身 |
|---|---|---|
| `npm run build` | **0** | 35件 |
| `npm run check` | **0** | guard / 隔離（条件A）/ parity / design / 削除順序 / 繋ぎ方 / 検査台帳 / キー対応 / 見本文 |
| `npm test` | **0** | **112件** |
| **GitHub Actions** | **緑** | **3 job**: `check`（約20秒）/ `verify`（実 Supabase・約6分）/ `walk`（絵を撮る・約5分） |
| `npm run verify:prod` | **1** | **赤で正しい。** デプロイ済みの本番が F1 以前の版のまま（下の「次に効いてくること」1・`deferred` #30）。CI には入れていない（`#31`） |

**CI の `verify` は11本走る**（土台2本 ＋ 飼い主に届く中身を見る9本）。
最後に観測した版は `d4f35f6`（master）:

| 検査 | 結果 | 見ているもの |
|---|---|---|
| `portal` | 14/14 | `/my` が起動し、自分の犬だけ見える |
| `xss` | 24/24 | 保存された値が飼い主のブラウザで実行されない（D-11） |
| `roundtrip` | **20/20** | **書いたものが同じ値で届く**（D-12・最重要）。`1b` が表示＝保存値も見る |
| `empty` | 11/11 | カルテ0件に架空の履歴を出さない（D-10） |
| `screens` | 20/20 | 各画面に在るべき入口が在り、押すと着く |
| `delete` | 6/6 | 削除で写真が実体として消える（service_role で数える） |
| `draft` | 7/7 | 記入が黙って消えない |
| `invitation` | 8/8 | 新規のお客様が自分のカルテを見られるまで |
| `m6` | **13/13** | 動線①〜⑥。`⑤c` が「確定後に④へ戻るとどの子か分かる」を見る |
| `edit` | 19/19 | `/edit` が正UI を配れているか。`17` が④の入力欄が空で始まることを見る |

### F3 の状態

- `node scripts/guard/gate.mjs --end` → **「F3 は閉じてよい状態です」**
- バッドシナリオの未解決 **0件**
- **D-14 の2問が実データの絵で両方 ○**（`docs/ops/walk-D14-F3.md`）
  - 問1 最後まで到達できるか → ○（飼い主に届いたカルテまで）
  - 問2 間違えても2タッチ以内に完了できるか → ○（当初 1件 ✕ → `deferred` #28 を直して撮り直し）
- **`docs/ops/phase` は `F3` のまま。** 次の段へ進めるのはマスターの判断。

### `npm run walk` は実データを撮る（2026-08-26 に変えた）

**以前は静的配信（`serve-ui.mjs`）で仮データを撮っていた。** 結線が終わったあとも
絵だけ F2 のままで、D-14 に実データで答えた絵は1枚も無かった。いまは実 Worker +
ローカル Supabase に向いており、**仮データへ黙って落ちない**（土台が無ければ止まる／
一覧に `DUMMY` が居ても止まる）。

**手元に Docker が無い環境では撮れない。** 撮るのは CI の `walk` job で、成果物
`human-walk` として上がる。`verify` とは**土台ごと別 job**——同じ土台に置いたら
検査の残骸が19頭並んで判定に使えなかった。

### 次に効いてくること（クライアントに渡す観点）

`plan.md` 第7章の形式的な条件は **7-3（鍵3種のローテーション）だけが ❌**。
ただし形式の外に、渡す前に潰すべきものが残っている:

1. ~~**デプロイ済みの実物が一度も確認されていない。**~~ → **確認した（2026-08-26）。**
   結果は **`docs/ops/prod-check-F3.md`**。**本番は F1 より前のデプロイのまま動いていて、
   いま master に在るもの（F3 完了版）は一度もデプロイされていない。**
   `/js/ponchi-app.js`（削除済み・リポジトリに無い）が 200 で 85,576B 返り、
   正UI の `/js/ui.js` は 404。`/edit` は `502` ではなく **200 で旧UI** を配っていた。
   機械にした → **`npm run verify:prod`**（本番 **0/4 FAIL** ／ いまの dist を配る器に向けると 4/4 PASS）。
   **デプロイ自体はマスター判断**（`deferred` #30）。鍵のローテーション（次項）が先に見える
2. **鍵3種のローテーション**（`.srkey` は RLS を丸ごと無視する。実顧客データ投入前に必須）
   → **調べた（2026-08-26）。** 鍵3種は**ツリーにも git 履歴にも入っていない**
   （`git log --all` が3本とも 0 件）。ただし **`.gitignore` が塞いでいなかった**
   （`*.key` は `.srkey` に当たらない）。`.gitignore` は F3 の範囲外なので、
   **範囲内でより強い側**に機械強制を置いた → **`scripts/guard/no-secrets.mjs`**（`npm run check` と CI が毎回止める）。
   **再発行そのものはマスターの作業**——`D-20260824-31` のとおり私はこの3本を生成・表示・受領しない（A-1）
3. **本番に残っているダミー犬3頭と孤児 owner** → **着手できない。** 消すには `.srkey`（service_role）が要り、
   それは **2 のローテーション後**に使うべきもの。**2 が先**
4. **削除の入口がどの画面にも無い**（`#25`・サーバ側は実装済み）
   → **判断材料を出した（2026-08-26）**: `docs/ops/delete-scope.md`。
   マスターの問い「何を削除するのか具体例で言え」に、**3段階を実例で**答えてある
   （段階1 カルテ1件 / 段階2 犬1頭＝履歴ごと / 段階3 飼い主1人＝犬全頭ごと）。
   **置き場所の決定はまだマスター待ち**——思いつきで足すのは `D-18` 偽-7
5. 過去カルテを開く導線（`#23`）→ **直した（2026-08-26・マスター指定「③で犬を選んだら過去一覧を出す」）。**
   ④カルテ作成の見出し直下に、その犬の**確定済み**カルテを新しい順で並べた。
   **1件も無ければ帯ごと出さない**（`D-10`）。機械: `test/ui-past-reports.test.mjs`
   ／届く日付（`#29`）→ **マスターが「下書きを作った日」を仕様と決めた（2026-08-26）。**
   **バグではなく仕様。実装変更なし。** walk が `2026.07.16` を撮ったのは
   `seed.sql` の draft fixture（`report_date '2026-07-16'`）を `findDraft` が拾って
   再開し確定したためで、本番の挙動ではない。原因の詳細は `deferred` #33

### 自分についての申し送り

**`F-20260825-35`/`-36` の型（検査が実際の仕組みと違う所を見る）を F3 だけで5回出した。**
うち1回は**恒真**で、CI では捕まらなかった（自分で読み直して見つけた・`F-20260825-40`）。
`docs/watch.md` W-1 で回数を追っている。**検査を書いたら、合格条件の左辺と右辺が
別々の場所から来ているかを確かめること。**

`docs/watch.md` は 2026-08-26 に新設した。`plan.md` が第5章・第6章で指しているのに
**F1 から一度も存在しなかった**（計画が指す成果物が実在しない状態で3フェーズ進んでいた）。

## 1. 今回やったこと (Completed in this session)

> **これ以降は過去の記録。** 現在の状態は「## 0」を見ること。

**移設 plan（M0〜M9）完了。統合フェーズは P0（エンジン抽出）と P8-a（飼い主のマイページ再結線）まで。`docs/failures.md` の OPEN は 0 件。**

### 最新セッション（P8-a）

- **`/my` が一度も起動していなかったのを直した（F-22）**。`src/my.html` には `bootProtectedPortal()` が探すフック5種も起動条件 `data-portal="customer"` も無く、`supabase-auth.js` を読み込んでさえいなかった。器として作り直し、`supabase-vendor.js` →`supabase-auth.js` の順で結線した。
- **同じページに出ていた架空のカルテを撤去した（D-10）**。犬名「ポンチ くん」・体重 2.79kg・来店日 2026.08.15・他所の犬の写真7枚・担当トリマーの文面が入った静的モックで、Supabase 有効化と同時に、ログインしていない誰にでもそう見える状態だった。意匠は `design/mock-4step.html` の `#screen-4` に同じものがあるので失っていない（実データからの描画は P6 の `renderMagazine`）。
- **機械強制を2本足した**。`test/supabase-auth.test.mjs` が `bootProtectedPortal()` のソースから `querySelector` の引数を抜き出し、その全部が `my.html` に在ることを要求する（フックを足せば HTML も要求される）。`npm run verify:portal` は実ブラウザで `/my` を開き、10項目を見る。どちらも、外したら落ちることを実際に確認した。
- **その `verify:portal` が2回目の実行で落ちるのを直した（F-23）**。自分で立てた Worker を `npx` にだけ SIGTERM していて、下の wrangler と workerd がポートを掴んだまま残っていた。プロセスグループごと止めて exit を待つようにし、**連続3回とも 10/10・EXIT 0** になることを確認した。1回しか回さなければ気づけない類で、push 後に見つけた。
- **`/my` の見た目は P6 まで素っ気ない。** 犬の一覧・カルテは `supabase-auth.js` の既存レンダラが素の DOM で出す。架空の中身を見せないこととの交換で、意匠は P6 で実データに乗せる。

### Supabase 有効化（2026-08-23・マスター立ち会いで実施）

**動いている**: ただし入口は独自ドメイン `https://trimmer-system.kouheikosehira.com`（`workers.dev` のほうは出ない・`deferred` #34）

- **Supabase プロジェクト `shiota1`（`bcodloqwnrhcuvevfguy`）を有効化した。** マイグレーション5本を適用し、テーブル12個・RLS 全11テーブル有効・`report-assets` バケット（非公開/10MB/jpeg,png,webp）を確認。Google OAuth を有効化（Client ID/Secret はマスターが直接 Supabase へ入力。私は経由していない）。
- **Worker `shiota0823` を `workers.dev` へデプロイした。** 独自ドメインには紐付けていない。**現行本番（`wrangler.toml` / `trimmer-system.kouheikosehira.com` / KV モード）には一切触れていない**——別物として並走している。`RATE_LIMIT_IP_PEPPER` は 48文字を生成して `wrangler secret` へ投入。
- **マスターの Google ログインが通ることを実機で確認した**（`auth.users` に行が出来ている）。`/api/session` も 200 を返す。
- 途中で**実物のバグを2件見つけて直した**。どちらも「机上では見えない」型で、`docs/failures.md` の `F-20260821-24`・`F-20260821-25` に記録した。
  - **F-24**: マイグレーションが予約語 `window` を別名に使っていて、実際の PostgreSQL では一度も通らなかった。→ この環境に PostgreSQL 16 を入れ、Supabase の土台のスタブを書いて5本を実流しして確認。
  - **F-25**: `SupabaseDataStore` が Workers の `fetch` をレシーバ付きで呼んでいて、**本番の Supabase 通信が全滅**していた（`Illegal invocation`）。認証だけ別経路で無事だったため切り分けにくかった。→ `bind` して、レシーバを直接検査するテストを追加。
- **残り**: 最初のトリマー（管理者）の登録。招待制の設計上1人目だけ手で入れる必要があり、**マスターがどの Google アカウントを管理者にするかの決定待ち**。

### 以前のセッション（M0〜M9 / P0）

- **M5**: `npm run check` の親モノレポ参照を自リポジトリへ。`--test-isolation=none`（node 22 で `bad option`）を除去。実体を移設していない `predeploy-check` と、それを指す `wrangler.toml` / `runbook.md` の記述を是正。`playwright@1.59.1` / `wrangler@4.92.0` を devDependencies に宣言。lockfile を追加。
- **M6**: `npm run preview`（KV local）で実際にアプリを動かし、plan の受入基準9項目を全て通した。検証は `scripts/verify-m6.mjs`（`npm run verify:m6`）として残してあるので、次回以降は手作業でなく再実行できる。
- M6 の過程で見つけた不具合3件を修正（`/api/config` の KV モード欠落 / デモ月の無効 fetch / favicon 404）。
- **M7**: 眠っていた Supabase 系3スイート（storage 6 / store 23 / auth 10 = 39件）を `test:supabase:static` に配線し、`npm test` が全6スイート **61 pass** を実行するようにした。`scripts/design-isolation-guard.mjs` を新規作成して `npm run check` に組み込み、`design/README.md` を追加。
- **カルテ往復の修復（最重要）**: マスター指摘を受けて機能そのものを検査したところ、**トリマーが記入した所見の大半が飼い主に届いていなかった**。皮膚の種類・変化と歯の状態は保存されるのに復元で静かに消え（`cssAttrSafe` が日本語の値を空にしていた）、耳・爪・歯のコメントは `data-field` が無く保存対象ですらなく、担当からの一言は保存されないうえ既定文が飼い主に届いていた。犬の名前も肉球画面に一度も出ていなかった。全て修正し、`npm run verify:roundtrip` で常時検査する。詳細は `docs/failures.md` の F-20260821-11〜14。
- **カルテ0件の犬の見え方を是正（F-15）**: 施術を一度もしていない犬の公開ページに、架空の月ラベル5つ・他所の犬の Unsplash 写真5枚・他の犬の体重と担当コメント入りのデモカルテが、その犬の名前で出ていた。飼い主には見本を一切出さず「まだカルテがありません」を表示する。トリマー側の中央パッドだけ「＋ 新規カルテ」として残す（1件目を作る唯一の導線のため）。`npm run verify:empty` で検査。
- **stored XSS を発見して修正（F-17・Critical）**: 認証不要の `POST /api/reports` に細工した `weights[].ym` を入れるだけで、飼い主の公開ページで任意の JS が実行された。実ブラウザで実証済み。`ymShort()` が数字以外を返せないようにし、`SET_WEIGHTS` でも形を検査する。`npm run verify:xss` が6経路に撃ち込んで常時検査（修正前 5/6 → 修正後 6/6）。
- **vibe-base への依存を打ち切り（F-09 / F-10）**: どちらも「移設元を見ないと決着しない」と書いていたが、F-09 は注入シンク31箇所の棚卸しでこのリポジトリだけで解決（→ F-17）。F-10 は受け入れ基準#14 を「移設元から8件を転記」から「**このリポジトリで起きた失敗が再現手順つきで記録されていること**」に読み替えた。以後 vibe-base は参照しない。
- **M8（F-07 / F-08 / PII）**: 外部 CDN 参照を全廃した。Unsplash 10件は**同梱ではなく撤去**（全て写真アップロードまでの仮置きで、他人の犬を顧客の犬として見せるのは F-15 と同じ問題になる）。フォントは Latin 3ファミリを同梱し、日本語4ファミリは容量（Noto 2種で 70MB）を理由にシステムフォントへ寄せた。PWA manifest を4ファイルに結線し、dist でのパス変化も build に置換を足して塞いだ。`docs/ASSET-PROVENANCE.md` を新設。実在顧客名と個人アドレスを除去し、`docs/runbook.md` の移設元パス6箇所も是正。
- **M9（ルール改訂）**: `AGENTS.md` の Golden Stack をテンプレートの鉄板構成（Next.js / Tailwind / Prisma / Vitest）から**本リポジトリの確定スタック**へ差し替え。MECHANICAL CHECK と REAL VERIFICATION に実際のコマンドを明記し、LEVEL D に事故から昇格した13条を記載。`docs/design.md` は空テンプレートだったので実態で埋めた。**移設元の凍結は行わない**（vibe-base への依存を打ち切ったため）。
- **P0（統合フェーズ・エンジン抽出）**: `ponchi-v2.html` のインライン `<script>` 985行を `src/js/ponchi-engine.js` へ**逐語**で切り出した。切り出し前後を突き合わせて**完全一致**を確認済み（位置だけが変わった）。HTML は 2154行 → 1168行。これで HTML を差し替えてもエンジンが消えなくなった——P1 以降の貼り替えの前提。
- `docs/failures.md` に **20 件**を記録（**CLOSED 20 / OPEN 0**、+ F-06 の Root Cause 訂正）。

## 2. 現在の状態 (Current State)

> **注記（2026-08-25・F1）**: 以下の表の `verify:*` は **7本とも存在しない。**
> F1 で UI とバックエンドを分けたとき、実行に Supabase と旧画面（`#screen-paw` 等）を
> 必要とするため一緒に削除された（`git show --stat 6685df5`）。いま叩くと
> `npm error Missing script`・EXIT 1。**PASS の数字は 2026-08-23 以前の実測であって、
> 現在の状態ではない。** 作り直すのは F3（`docs/deferred.md` #8・復元元のコマンド付き）。
> 現在実行できるものと結果は本ファイル冒頭の「## 0」にある。
>
> **注記（2026-08-23・F5）**: 以下の verify:* の件数・実行方法（`npm run preview` を別端末で）は
> KVモード時代のもので、**古い**。当時の Supabase モード版の実行方法・件数は
> 「F5 の結果」セクション（本ファイル後半、F4の結果の次）を見ること。

### 機械検証

| コマンド | 結果 |
|---|---|
| `npm run build` | EXIT 0 |
| `npm run check` | EXIT 0（src↔dist parity + design/ isolation の2本） |
| `npm test` | EXIT 0・**66 pass / 0 fail**（全6スイート。P8-a で +5） |
| `npm run verify:m6` | **11/11 PASS・EXIT 0** |
| `npm run verify:roundtrip` | **15/15 PASS・EXIT 0**（記入→保存→公開ページの往復） |
| `npm run verify:empty` | **8/8 PASS・EXIT 0**（カルテ0件の犬に架空の履歴が出ないこと） |
| `npm run verify:xss` | **6/6 PASS・EXIT 0**（保存データが飼い主のブラウザで実行されないこと） |
| `npm run verify:portal` | **10/10 PASS・EXIT 0**（`/my` が起動し、未ログインでログイン導線が出ること） |

**アプリは外部へ一切通信しない。** `verify:m6` 項目9 の外部リクエスト失敗が 24件 → **0件**。

`npm run verify:all` で verify 系5本をまとめて回せる。

`verify:m6` / `verify:roundtrip` / `verify:empty` / `verify:xss` は別端末で `npm run preview` を起動してから実行する。
`verify:portal` だけは自分で Worker を立てる（`/my` は Supabase モードでしか配信されず、`preview` は KV モードのため）。
playwright 管理外の chromium を使う場合は `M6_CHROMIUM=/path/to/chrome` を渡す。

**`verify:roundtrip` がこのリポジトリで一番重要な検査。** 画面が出るか・押せるかではなく、
トリマーが書いた所見が飼い主の公開ページに同じ値で届くかを、13項目突き合わせる。
入力欄を足したら必ずこの検査にも足すこと。足さなければ、その欄は
「保存されていない」状態に戻っても誰も気づかない。

### 動くことが確認できているもの

**導線（`verify:m6`）**: ログイン画面 / 検索一覧 / 飼い主・犬の新規登録（POST 200）/
肉球画面（中央パッド1・指4）/ 犬体図への Konva 描画（ペン・色変更・undo）/
写真アップロード（7スロット）/ カルテ保存（`POST /api/reports` 200）/
公開ページ `/p/{slug}` での閲覧モード再現 / アプリ由来のコンソールエラー 0 件。

**中身（`verify:roundtrip`）**: 犬の名前 / 皮膚1の部位・大きさ・種類・変化 / 歯の状態 /
耳（右・左）/ 爪のレベル / 耳・爪・歯のコメント / 担当からの一言 —— 13項目すべてが、
トリマーの記入どおりの値で飼い主の公開ページに現れる。

**空（`verify:empty`）**: カルテ0件の犬には、架空の月ラベルも見本写真もタップ導線も出ない。
飼い主には「まだカルテがありません」。トリマーの中央パッドからは1件目を作成できる。

**安全（`verify:xss`）**: 保存されたカルテのデータ6経路（体重の年月・犬名・担当の一言・
耳のコメント・皮膚の部位）に細工を撃ち込んでも、飼い主のブラウザで実行されない。

導線が通ることと中身が届くことは別物で、**後者は一度も検査されていなかった**。
`verify:m6` が 9/9 だった時点で、実際には所見の大半が消えていた。

外部ホスト（`images.unsplash.com` / `fonts.googleapis.com`）への接続失敗 22 件は
実行環境の egress 制限によるもので、アプリの不具合ではない。ただし**外部 CDN に
依存していること自体**は D-4 違反として `F-20260821-07` に OPEN で記録した。

### 未着手

**移設 plan（M0〜M9）の工程は無い。** 残るのはコードでは解けない1件と、次の工程。

### 未解決（`docs/failures.md` の OPEN）

**0 件。**

ただし `docs/ASSET-PROVENANCE.md` に**マスター確認待ちが残っている**。素材20件のうち
AI 生成が5件（C2PA 署名から OpenAI / Google と特定）、残る15件は出所不明で `UNVERIFIED`。
実写に見える4件（`photo-dog-ear` / `photo-dog-skin` / `guide-nail-state` / `guide-teeth-state`）が優先。
飼い主に配るページに載るため、第三者の犬が写っている場合はその飼い主の同意も要る。
消すと状態ガイドが空欄になるので、差し替えるまで残してある。

`docs/runbook.md` は M8 で是正済み（絶対パス6箇所・個人アドレス・存在しない A版ファイルへの指示）。

## 2-b. 飼い主の導線（2026-08-21 マスター決定）

**飼い主はログインしてマイページへ行き、そこにある自分の飼っている犬の一覧から選んでカルテを見る。**

無認証の公開URL `/p/{slug}` を廃止したあとの代替導線がこれ。plan には「`/p/` `/o/` 廃止」としか
書かれておらず代替が空白だったので、ここで確定した。

### 調べたら、この導線はほぼ実装済みだった

| 段階 | 実装 | 状態 |
|---|---|---|
| ログイン | `bootProtectedPortal()` + Google OAuth（`supabase-auth.js`） | ✅ ある |
| マイページ = 犬の一覧 | route `pets` = `/my` → `renderPets()` が犬カードを並べる | ✅ ある |
| 犬を選ぶ | `/my/pets/{petId}` | ✅ ある |
| カルテを見る | `/my/pets/{petId}/reports/{reportId}` | ✅ ある |
| 自分の犬だけ見える | RLS `pets_customer_select` = `is_owner_user(owner_id)` | ✅ ある |
| 確定済みだけ見える | RLS `reports_customer_select` = `status = 'final'` | ✅ ある |
| **`my.html` の DOM フック** | `data-portal="customer"` / `data-portal-content` / `data-login-panel` / `data-google-login` / `data-sign-out` / `data-portal-status` | ✅ **P8-a で結線**（`npm run verify:portal`） |
| カルテの意匠 | `renderMagazine`（`src/js/magazine-view.js`） | ❌ **P6 で作る**。今は素の DOM |

`supabase-auth.js:280` が `document.body.dataset.portal === 'customer'` を条件にしているため、
**ポータルは一度も起動していなかった**（統合 plan のリスク#3）。P8-a で結線し、実ブラウザで
未ログイン状態の起動を確認した（`verify:portal` 10/10）。**ログイン後の表示は未確認**——
Google OAuth も犬一覧も RLS も Supabase 有効化後でないと動かせない。

### ⚠️ 順序を P8 → P1 に変えた理由

この導線は**全部 Supabase の Google OAuth 前提**で、Supabase はまだ有効化されていない。
そして **KV モードには飼い主のログインが存在しない**——`/p/{slug}` が無認証なのは、
それが飼い主向けの唯一の経路だからである。

**P1 で先に `/p/` を消すと、Supabase が有効になるまで飼い主はカルテを一切見られなくなる。**
置き換え先を動く状態にしてから古い経路を消す順序にする。

もう1つ、P1 は「画面の改名」ではない。肉球画面の撤去（`grep -c "paw" src/js/ponchi-app.js` は現在 31）と
`src/search.html` の削除を含み、**`verify:*` 4本はすべて `#screen-paw .pad` を経由している**ので
4本とも書き直しになる。検査の作り直しを含む一塊の作業として見積もること。

---

## 3. 次回やること (Next Steps)

**2026-08-23 にマスターと完成までの計画を確定した。次回セッションはこの順で読むこと。**

1. `docs/handoff.md`（このファイル）— 直近の状態
2. **（旧計画・削除済み）** — 完成までのフェーズ F0〜F6・受け入れ条件・動線図
3. **`docs/decisions.md`** — マスター決定・私の判断・未決事項の台帳（口頭で流さず必ずここに記録する運用に変更した）

**現在地: F0〜F6 すべて完了。計画の全体受け入れ条件7項目を達成した（2026-08-24）。**

**`https://trimmer-system.kouheikosehira.com` は Supabase 版を配信している。**
同ドメインで①〜⑥を実機で一周させ **9/9 PASS**。旧 KV 版 Worker
（`saltydog-report-worker`）はルートを外して残してあり、切り戻せる（手順は
`worker/wrangler.toml` のコメント）。

**コードで解ける作業は残っていない。** 残るのは素材の出所確認
（`docs/ASSET-PROVENANCE.md` の UNVERIFIED 15件・`docs/decisions.md` D-20260823-U1）で、
これはマスターの手作業。

**資格情報の在り処（重要・次セッションで探し回らないこと）**: `CLOUDFLARE_API_TOKEN` と
Supabase の Management API token は、セッションの scratchpad
（`/tmp/claude-0/-home-user-siota-1/<session-id>/scratchpad/` の `.cftoken` / `.sbtoken`、
どちらも `chmod 600`）に置いてある。Management API token からは
`GET https://api.supabase.com/v1/projects/bcodloqwnrhcuvevfguy/api-keys?reveal=true` で
service_role key を取得できる（`.srkey` に保存済み）。SQL は同ディレクトリの
`sbq.py` で流せる。**「無い」と判断する前に深さ制限なしで探すこと**——一度
`find -maxdepth 5` で見落として、あるものを「無い」と報告した（D-20260823-22）。

**ローカルでの実機検証手段（重要・F5でも使う）**: `CLOUDFLARE_API_TOKEN` もホスト済み
Supabase プロジェクトの service role key も無いコンテナでも、実ログイン・実DB・実RLSの検証が
できる。`supabase start`（Docker）でローカルに実 Postgres・実 Auth・実 PostgREST・実 Storage を
起動し（`supabase/config.toml` の `[edge_runtime] enabled=false` はこのサンドボックスでの
rlimit エラー回避のため追加済み）、`worker/wrangler.local.toml`（新規・秘密情報なし）でそれを
指す `wrangler dev --config worker/wrangler.local.toml --port 8787` を立てる。ログインは
`supabase/seed.sql` のテスト専用アカウント（`staff@local.test` 等・password login）で
password grant のアクセストークンを取得し、`window.TrimmerAuth.setSession()` に注入する
（本番UIはGoogle認証のみを表示するので、この注入はテスト専用の裏口）。詳細は
`docs/decisions.md` D-20260823-18。ホスト済みプロジェクトへの実デプロイ確認だけは
`CLOUDFLARE_API_TOKEN` が無いとできない（D-20260823-U2、格下げ済み・低リスク）。

要点だけ書く（詳細は上記2ファイル）:

- **動線はマスター指定でモックが正**: URL → ログイン → 犬を選ぶ → カルテ作成 → 確認 → 顧客ページ（確認と顧客ページは同一レンダラ、`design/mock-4step.html` の `#screen-4`）。V1 現状の「飼い主ページ→ワンちゃんページ→肉球画面」は動線に無い層で、F2 で撤去する。
- **完成 = 独自ドメイン `trimmer-system.kouheikosehira.com` で6段階が一周すること**（D-20260823-01, 02）。現行 KV 版本番のデータは全てダミーで、移行はしない（D-20260823-03）。
- 切替は F5 の検査が全部通ってから（`docs/decisions.md` D-20260823-08、私の判断）。それまでは `https://shiota0823.rahiseko.workers.dev` 上で作業し、現行本番には触れない。
- `verify:m6` / `verify:roundtrip` / `verify:empty` / `verify:xss` の4本は **KV モード・`#screen-paw` 前提**なので F2 で全て壊れる。F5 で Supabase 版へ作り直す（見積もり済み、書き直しを1工程として計画に含めてある）。
- 素材の出所確認（`docs/ASSET-PROVENANCE.md` の UNVERIFIED 15件）は未決のまま `docs/decisions.md` の D-20260823-U1 に転記した。コードでは解けない。

### F1 の結果（2026-08-23・実機確認済み）

ダミーデータ（飼い主2件・犬3件、`rahiseko@gmail.com` を飼い主にも紐付け）を入れて、①〜⑥を実ブラウザで辿った。

- ①②は通る。③は「犬を選ぶ」の前に「飼い主を選ぶ」層が挟まる（動線に無い層、F2 で撤去対象）。
- ⑤（確認画面）自体は正しく動くが、**④（カルテ作成）が構造的に必ず失敗する**ことを実機で確認した。
  日付フィールドの抽出が、年/月/日 3分割表示のうち月だけしか取れておらず（`publish-client-ponchi.js:141`）、
  Supabase 側の検証（`ponchi-app.js:1316-1319`）が求める `YYYY/MM/DD` に絶対になり得ない。
  `docs/failures.md` の **`F-20260823-26`（OPEN・F3で修正）** に詳細を記録した。
- ⑥（顧客ページ）は④が塞がっているため未到達。ただし `renderReport()`（`supabase-auth.js:132`）が
  皮膚・爪・耳・歯などの主要項目を描画していないことはコードレビューで確認済み（F4 の対象）。
- 検証方法の注意（`docs/decisions.md` D-20260823-10）: `rahiseko@gmail.com` は店舗管理者でもあるため
  `pets_staff_all` ポリシーで店舗の全ペットが見える。「他人の犬が見えない」の検証には
  スタッフ権限のない別アカウントが要る（F5 で対応）。

### F2 の結果（2026-08-23・実機確認済み）

`飼い主ページ → ワンちゃんページ` の2層と肉球画面を撤去し、`/edit` が犬の一覧を直接出すようにした。

- **サーバ側**: `listPetsWithOwner()`（`supabase-data-store.js`）と `GET /api/pets`
  （`worker/src/index.js`）を新設。PostgREST の `owners(name)` embed で、店舗の犬を飼い主名つき
  1回の問い合わせで取得する
- **クライアント側**: `/edit` は `GET /api/pets` を叩き、`renderFlatPetList()`（`ponchi-app.js` 新設）
  が犬を直接並べる。クリックで `/edit/p/{petId}` へ直接遷移し、肉球を経由せず全カルテ一覧へ進む。
  「＋ 新規カルテを作成する」で犬名・飼い主名を1画面から受け取り、飼い主のいない状態からでも
  1件目を作れる
- **肉球画面を全面撤去**: `grep -c "paw" src/js/ponchi-app.js` = 0（実施前31）、HTML/CSSからも撤去。
  行き先が1つになったので「戻るドロワー」（3択のガラスパネル）も撤去し直接遷移に簡略化
- `src/search.html` を削除
- **実機で確認したこと**（`https://shiota0823.rahiseko.workers.dev`）: ログイン直後に犬3頭が
  直接並ぶ／犬をクリックすると肉球を経由せず全カルテ一覧に着く／「戻る」で犬の一覧に戻る／
  新規カルテ作成フォームで実際に飼い主・犬を作成し、その犬の編集画面へ進めることまで確認
  （DB にも実際に行が増えることを確認、テストデータは削除済み）
- `npm run build` / `check` / `test` は EXIT 0・67 pass のまま変化なし
- **scope 変更**: section id の `list/edit/view` 改名は行わなかった。KV/Supabase 両モード共有の
  id なので改名は別作業として大きく、今回の実害（動線に無い層・肉球画面）を消すのに必須でなかった
  ため。詳細は （旧計画・削除済み） の F2 セクションに記録
- **意匠はまだ乗っていない**。今の犬の一覧は素の DOM（`createListItem`）。意匠は F4 相当で乗せる

### F3 の結果（2026-08-23・実機確認済み）

**scope 変更**: 計画では「Supabase 経路を結線する」想定だったが、**経路は既に結線済みだった**。
`extractReport`/`applyReport` の Supabase 分岐も、写真アップロード（`TrimmerSupabaseStorage`）も
実装済みで、そのまま動いた。実際に必要だった修正は F1 で見つけた `F-20260823-26` ただ1件。

- **`F-20260823-26`（日付結合の欠落）を修正**。`extractReport()` に `isoDate` キーを新設し、
  `#heroDateInput`（`<input type="date">`、常に `YYYY-MM-DD`）の値をそのまま返すようにした。
  `ponchi-app.js` の公開検証は `report.date`（3分割表示の月だけ）ではなく `report.isoDate` を見る
- **修正の過程でもう1件発見（`F-20260823-27`）**: `clearReport()` が `#heroDateInput` 自体を
  リセットしておらず、新規カルテは日付ピッカーに触れない限り常に HTML の静的な既定値
  （2026年12月5日）で保存されるところだった。今日の日付で初期化するよう修正
- **実機で確認したこと**（`https://shiota0823.rahiseko.workers.dev`）: 日付ピッカーに**一切触れず**、
  犬を選ぶ→記入→確定→プレビュー→公開まで通し、「公開しました！」に到達。DB で
  `reports.status = 'final'`・`report_date` が実行日と一致・`staff_note` が記入値と一致・
  写真4件が `report_assets` に保存されることを確認（テストデータは削除済み）
- 公開処理は写真アセットの数だけ直列アップロードするため体感15〜20秒かかる。遅いが
  今回のスコープの不具合ではないため保留
- `npm run build` / `check` / `test` は EXIT 0・67 pass のまま変化なし

### F4 の結果（2026-08-23・実装完了／実機は一部未確認）

`src/js/magazine-view.js` を新設した。`renderMagazine(container, report, opts)` が
マスター承認のマガジン意匠（4ステップ意匠モックの `#screen-4`）へ、実データを一方向で投影する。
トリマーの確認画面（`/edit` の公開直前プレビュー・Supabase モードのみ）と `/my` の飼い主画面が、
**この1本の関数を共有する**。

- **scope 変更（D-20260823-12）**: 計画は「マガジンの静的HTMLを `ponchi-v2.html` に書く」想定
  だったが、`/my` は別ファイル（`src/my.html`）のため、静的HTMLを2ファイルに複製すると
  ズレるリスクがあった（このセッションで直してきたバグと同じ再発条件）。代わりに
  `magazine-view.js` 1本のテンプレート文字列を両画面に注入する方式にした。機械チェックは
  静的 grep から、実ブラウザでの `[data-view]`/`[data-field]` 件数チェックへ差し替えた
- 皮膚（10行）・爪・耳（左右）・歯・体重推移（実データから計算した折れ線）・カット写真・
  担当からの一言・過去レポートのタイムライン（実リンク）を実データから描画。**モックの創作文・
  次回来店提案・トリマーバッジは、対応するデータフィールドが無いため実装しなかった**
  （D-20260823-13。飼い主に架空の解説文を見せないため）
- Google Fonts の CDN 読込は追加していない（D-20260823-14）。`src/my.html` が既に持っている
  ローカル同梱フォントのトークンをそのまま使い、「アプリは外部へ一切通信しない」を維持した
- KV モード（現行本番）の確認画面（`showLegacyPreview`）は一字一句変更していない
  （D-20260823-15）。マガジン化は Supabase モードだけの分岐
- **実機で確認したこと（2段階）**:
  1. `renderMagazine()` を Playwright（chromium）で合成データを使って単体検証し、27/27 PASS。
     `data-view` 35件・`data-field` 0件、記入なし項目は非表示（架空データを出さない）、
     `<script>` 注入が実行されない（XSS安全性）、アコーディオン/クイックジャンプ/ライトボックス/
     タイムラインリンク/戻るボタンの実動作、コンソールエラー0件
  2. **ローカル Supabase（`supabase start`）+ ローカル `wrangler dev` で一気通貫の実機検証**
     （D-20260823-18）。19/19 PASS。実ログイン（`staff@local.test`）→犬「X」選択→新規カルテに
     皮膚・爪・耳・歯・担当からの一言を実際にクリック/入力→確定→**確認画面
     （`#screen-magazine`）に記入どおりの値が出る**ことを確認→公開→別ブラウザコンテキストで
     飼い主(`owner-a@local.test`)としてログインし直し、**`/my/pets/{id}/reports/{id}` に
     同じ値が同じレンダラで届く**ことを確認。さらに `owner-b@local.test`（他人）は同じURLへ
     アクセスしても中身が出ない（RLS実証・全体受け入れ条件3の前倒し確認）ことも確認
- **未確認のまま残っていること**: 写真アップロードを含む往復（今回はテキスト項目のみ）、
  ホスト済み Supabase プロジェクト（`shiota0823.rahiseko.workers.dev`）への実デプロイ後の
  動作（`CLOUDFLARE_API_TOKEN` が要る・D-20260823-U2・低リスクと判断）
- `npm run build` / `check` / `test` は EXIT 0・67 pass のまま変化なし

### F5 の結果（2026-08-23・実機確認済み）

F4のローカルSupabase実機検証（D-20260823-18）を `scripts/lib/local-stack.mjs`（新設）として
共通化し、`verify:m6`/`verify:roundtrip`/`verify:empty`/`verify:xss`/`verify:portal` の
**5本すべて**をこれで動く形に書き直した。

- **実行方法が変わった**: `npm run preview` を別端末で立てておく前提を廃止した。今は
  `npx supabase start`（ローカルの実Postgres/Auth/PostgREST/Storage）だけを事前に立てておけば、
  各 `npm run verify:*` は自分で `wrangler dev --config worker/wrangler.local.toml` を
  起動・停止するところまで自己完結する。ログインは `supabase/seed.sql` のテスト専用
  アカウント（`staff@local.test` / `owner-a@local.test` / `owner-b@local.test` 等、
  password login）で自動化する
- **`verify:m6`（12/12）**: ①ログイン導線→③犬の一覧（飼い主選択層なし）→画面からの
  新規飼い主・犬登録→④カルテ作成→Konva描画（ペン/色変更/undo）→写真アップロード→
  保存POST→⑤確認画面（マガジン意匠・F4）→⑥公開URL到達→コンソールエラー0件
- **`verify:roundtrip`（19/19・最重要）**: 実UI操作でカルテを記入→保存→別ブラウザ
  コンテキストで飼い主としてログインし直し、13項目すべてが同じ値で届くことを確認。
  他人（owner-b）には見えないこと（RLS）も同じ実行内で確認
- **`verify:empty`（7/7）**: カルテ0件の犬に見本画像・タップ可能なリンクが出ないこと、
  「まだカルテがありません」が出ること（`src/js/supabase-auth.js` の `renderPet()` に追加。
  今回のUIコード変更点はこれのみ）、トリマー側の新規作成導線が生きていることを確認
- **`verify:xss`（7/7）**: DOM/extractReportのサニタイズを迂回し、スタッフAPI経由でDBへ
  直接細工データを書き込んでから飼い主画面で描画させる、より厳しい「出口が安全か」の検査。
  pet/staffNote/skin.loc/ear.comment/nail.comment/teeth.status・comment/weights[].ym の7経路
- **`verify:portal`（14/14）**: ログイン前10項目は無変更のまま維持し、ログイン後4項目
  （自分の犬だけ見える／ログアウトボタン表示／他人の犬は見えないRLS／サインアウトで
  ログイン画面に戻る）を追加
- **クリーンな状態から確認**: `npx supabase db reset` → `npm run verify:all` で
  **59/59 PASS**（12+19+7+7+14）。`npm run build`/`check`/`test` も EXIT 0・67件のまま

### F5後の追加作業（2026-08-23）

一時「F6は資格情報待ち」として止めていたが、**その前提が誤りだった**（後述）。
止まっていた間、および資格情報が見つかったあとに実施したこと。

- **`test/e2e/*.cjs` を削除した（訂正）**: F5直後、「これらはKVモードの検査として今も
  正しいので対象外」と記録したが誤りだった。`#backDrawer`/`openBackDrawer`/`#screen-paw`
  はF2でKV/Supabase共有のソースから完全に削除済みで、この2ファイルはどちらのモードにも
  存在しないものを検査していた。書き直すと`verify:*`5本と重複するだけなので、削除し、
  唯一そこにしか無かった検査（体重の新規登録・使用オプションのオン/オフ）だけを
  `verify-m6.mjs`（6b・6c、12→14項目）へ移植した。`playwright.config.cjs`と
  `package.json`の`test:e2e`も削除。詳細は`docs/decisions.md` D-20260823-20
- **`code-review`スキルでF0〜F5の差分（PR #4）全体を見直し、実バグ1件を発見・修正した**:
  `supabase-auth.js`の`bootProtectedPortal()`で、セッション確認後にトークンが失効/破損
  すると「Googleでログインしてください」と出るのにログインボタンが押せない詰み状態に
  なっていた（signed-out分岐でしかボタンを結線していなかったため）。壊れたトークンを
  実際に注入して再現し、`signOut()`してから1回だけ`reload()`する形に直して、
  実機で解消を確認した。他に確認画面のfetch並行化・未使用コードの整理も実施。
  詳細は`docs/decisions.md` D-20260823-21

### ホスト済み環境での実機確認（2026-08-23・①〜⑥ 8/8 PASS）

**「資格情報が無い」は私の誤りだった**（D-20260823-22）。`.cftoken`/`.sbtoken` は
前セッションの scratchpad に残っていたのに、`find -maxdepth 5` で探して見落とし、
「無い」と報告していた。また「Google OAuth が本番公開済みか確認できない」も誤りで、
`/auth/v1/settings` を叩けば `"google":true` と即座に分かった。**「無い」「できない」と
言う前に、実際に探す・実際に叩くこと。**

資格情報が見つかったので、F4/F5 のコードを `shiota0823.rahiseko.workers.dev` へ
デプロイし、ホスト済み環境（実 Supabase `shiota1`）で①〜⑥を実機で一周させた:

- ①URLを開く → ②ログイン → ③犬の一覧（ポンチ/ムギ/レオが直接出る・飼い主選択層なし）
  → ④カルテ作成 → ⑤確認ページ（F4のマガジン意匠） → 公開 → ⑥顧客ページに同じ値が
  同じレンダラで出る、まで **8/8 PASS**
- ホスト済み DB 側でも `status='final'`・`report_date` が実行日・`staff_note` が記入値と
  一致・`report_assets` 4件（Storage への実アップロード成功）を確認
- テストデータは Storage のオブジェクトごと削除済み（reports 0件・assets 0件・
  Storage 実ファイル0件を確認）
- 詳細は `docs/decisions.md` D-20260823-23

### その検証中に見つけた不具合を修正（D-20260823-24）

**スタッフが未ログインで `/edit` を開くと、ログイン後に飼い主画面へ着いてトリマー画面に
戻れなかった。** `safeReturnPath()` が `/my` 以外の戻り先を全て `/my` に潰しており、
`bootStaffPortal()` が積んだ `post_auth_return='/edit'` が消えていた。さらに
**マスターのアカウントは staff かつ owner（D-20260823-06）** なので、`/my` 側の
「memberships があり ownerLinks が無ければ /edit へ」という救済分岐にも入らず、
飼い主画面に取り残されていた。つまり D-06 の副作用をマスター自身が最も受ける状態。

`safeReturnPath()` が `/my` と `/edit` の両方を通すよう修正（`/editorial` のような
前方一致や `//evil.example/edit` は引き続き `/my` に潰す）。ブラウザ側
（`supabase-auth.js`）と Worker 側（`auth-context.js`）の同名・同契約の関数を
両方直し、両方を対象にした回帰テストを追加（テスト 67→68件）。ホスト済み環境で
「未ログインで `/edit` → ログイン → `/edit` に戻り犬一覧3件」を実機確認済み。

### F6 の結果（2026-08-24・独自ドメインで実機確認済み・9/9 PASS）

**独自ドメイン `trimmer-system.kouheikosehira.com` を Supabase 版へ切り替えた。**

- **Supabase の Site URL / Redirect URLs を独自ドメインへ**変更（Management API 経由）。
  許可リストには workers.dev も残した——切り戻すときに設定を戻す手間を作らないため
- **Cloudflare のルートはダウンタイムゼロで付け替えた**。`wrangler deploy` は既存の
  ルートを削除しないので、設定ファイルの `[[routes]]` を書き換えるだけでは
  切り替わらない（新 Worker のデプロイが `A route with the same pattern already
  exists` で弾かれる）。Cloudflare API の
  `PUT /zones/{zone}/workers/routes/{route_id}` で**ルートを消さず向き先だけ差し替えた**。
  次に同じことをする人はこの方法を使うこと（`docs/decisions.md` D-20260823-26）
- **旧 Worker は削除せず残っている**（D-20260823-09）。ルートを持たないのでドメインからは
  呼ばれない。切り戻し手順は `worker/wrangler.toml` のコメントに書いた
- Google OAuth 同意画面（D-20260823-07）は**既に本番公開済み**だった。Google 側の
  Authorized redirect URI は `https://<project>.supabase.co/auth/v1/callback` 固定なので、
  ドメイン変更の影響を受けない
- **実機確認 9/9 PASS**: ①ドメイン応答（`/api/config` が `backend:"supabase"`）→②ログイン
  →③犬の一覧（ポンチ/ムギ/レオが直接出る）→④カルテ作成→⑤確認ページ（マガジン意匠）
  →公開→⑥顧客ページに同じ値が同じレンダラで出る／編集用フック0件／コンソールエラー0件。
  DB 側も `status='final'`・`report_date` が実行日・`staff_note` 一致・`report_assets` 4件。
  テストデータは Storage のファイルごと削除済み
- **切り替え直後の1回目は `/edit` で「表示できません。」と落ちたが、再実行すると 9/9 で
  通った**（その間コードもデータも変えていない。Node から直接 `/api/session`・`/api/pets`
  を叩くとどちらも 200）。エッジ側の一時的な不安定さと判断した。**本番切替の直後に1回
  落ちても、まず再実行して切り分けること。**

### 本番公開後の点検（2026-08-24）

**出所不明の犬の写真9件が、画面から参照されていないのに配信されていた。** 削除して配信を止めた。

- `docs/ASSET-PROVENANCE.md` が「特に確認が要る」と名指ししていた実写4件
  （`photo-dog-ear` / `photo-dog-skin` / `guide-nail-state` / `guide-teeth-state`）を含む7件が、
  F2（肉球撤去）・P8-a（架空カルテ撤去）・F4（マガジンの実データ化）を経て**参照ゼロ**に
  なっていたのに、`dist/` へコピーされ本番で 200 を返していた
- 画面に出ないのに配信だけされている状態に残す利益が無いので削除（未参照のAI生成2件も併せて9件・3.1MB）。
  `git rm` なので履歴から復元できる。デプロイ後、9件が 404・使用中の素材が 200 であることを確認済み
- **残る問題は `photo-trim-action.jpg` 1件**。`UNVERIFIED` のままログイン画面
  （`src/index.html:226`）で使用中で、**ログイン前の誰にでも見える**ため露出が最も高い。
  消すと意匠が壊れるのでマスター判断が要る（選択肢は `docs/ASSET-PROVENANCE.md` 5章）

### 本番DBに残っている検証用データ（実店舗開始前にマスター判断が要る）

| データ | 内容 | 備考 |
|---|---|---|
| owners | ダミー飼い主A / ダミー飼い主B / **「あ」** | 「あ」は招待テストの残骸（犬0・ユーザー0・未消化の招待1件を保持） |
| pets | ポンチ / ムギ / レオ | D-20260823-03 で「新たにダミーデータを入れて検証」と決めたもの |
| reports | 0件 | 検証で作ったカルテは Storage のファイルごと削除済み |
| auth.users | `rahiseko@gmail.com` のみ | staff（admin）かつ owner |

実店舗で使い始めるときに、これらを消すかどうかはマスターの判断。消す場合は
犬 → 飼い主 → 招待 の順で削除しないと外部キーで止まる。

### 本番公開後の三方向レビューで、実店舗を止める欠陥が4件見つかった（2026-08-24）

**計画の受け入れ条件7項目は満たしていたが、実店舗では使えない状態だった。** サブエージェント
3体に(1)運用リスク(2)未検証経路(3)セキュリティを並行で調べさせ、報告はすべて自分で再現・
確認してから対処した。

| # | 欠陥 | 状態 |
|---|---|---|
| 1 | **旧 KV Worker が無認証APIを workers.dev で公開したままだった** | ✅ 塞いだ |
| 2 | **招待フローが UI から到達不能** = 新規客は永久に自分のカルテを見られない | ✅ 直した |
| 3 | **空の写真スロットにページURLが保存され、飼い主に壊れた画像が届く** | ✅ 直した |
| 4 | **スタッフが2人になると管理者の機能が全部 409 で死ぬ** | ✅ 直した |

**4件とも本番へ反映済み（2026-08-24・Version `2911e465`）。** 直してコミットしただけでは
実店舗には届かないので、`npx wrangler deploy --config worker/wrangler.supabase.toml` で
`shiota0823` を更新し、**ローカルの検査結果を本番の根拠にしない**ため
`https://trimmer-system.kouheikosehira.com` に実ブラウザで入って直接確かめた（**8/8 PASS**）——
スタッフのログイン / 犬の一覧 / **犬の行3件すべてにQRボタンが在る**（行=3 ボタン=3）/
押すと実際に招待URL（64桁）が発行される / QRが画像で出る / 空スロットがページURLを返さない /
`/api/staff` が 200 / コンソールエラー0。旧KV版と新版の `workers.dev` が両方 404 であることも
併せて確認した。検証で発行した招待1件は本番に残さないよう削除済み（D-20260824-32）。

**1. 旧 KV Worker の無認証API（最重要）**
F6 でカスタムドメインの route は Supabase 版へ移したが、**workers.dev のサブドメインは
route とは別物**で塞がれていなかった。`saltydog-report-worker.rahiseko.workers.dev/api/customers`
が 200 で顧客一覧を返し、`DELETE /api/owners/{slug}` のカスケード削除も無認証で通った。
返ってきたデータに「コセヒラ」という実在の姓が含まれていた（D-20260823-03 の「全てダミー」は
姓については当てはまらない）。`workers_dev = false` / `preview_urls = false` を追加して
デプロイし、全経路 404 を確認。Worker 自体は切り戻し用に残してある。
**KV に入っているデータ自体はまだ残っている**（Worker から叩けなくなっただけ）。

**2. 招待フローの不通**
RLS（`pets_customer_select` → `is_owner_user`）は `owner_users` 経由でしか通らず、そこへ
行を入れられるのは `claim_invitation` だけ。ところが QR発行ボタンは **F2 で撤去した
「飼い主を選ぶ層」の画面に乗っていた**ため本番から消えていた。D-20260823-05 は
「QR発行・スタッフ管理は残す」と決めていたのに F2 でその決定を破っていた（私の見落とし）。
飼い主選択層は復活させず、**犬の一覧の各行に QR ボタン**を置いて塞いだ。
スタッフ管理も同じ理由で消えていたので、犬の一覧の末尾（管理者のみ）に置いた。

**3. 写真スロットのページURL混入**
`img.src`（プロパティ）は `<img src="">` のとき空文字ではなく**現在のページURL**を返す
（実ブラウザで確認: `img.src=''` → `http://host/edit/p/{petId}`）。`photo()` がこれを
読んでいたため、使わなかったスロット全部に `https://.../edit/p/{petId}` が保存され、
飼い主のマガジン画面に壊れた画像が出ていた。耳と歯は最初から `src=""` なので**常に**、
ヒーローは hero-1 が空だと hero-2 の実写真より優先されて壊れる。ほぼ全カルテで起きるのに
画面もコンソールも何も言わない。`getAttribute('src')` と `data-empty` を見る形に直した。

**4. `getStaffShopId()` の user_id 欠落**
RLS の `memberships_authorized_select` は `user_id = auth.uid() or is_shop_admin(shop_id)`
なので**管理者には店舗の全メンバー行が返る**。`user_id` で絞っていなかったため、スタッフが
2人になった瞬間に2行取れて `409 shop_selection_required`。飼い主の新規作成・招待の発行と
一覧・スタッフ管理（＝退職者の停止）が全滅する一方、日々のカルテ作成はこの関数を通らない
のでしばらく気づけない壊れ方だった。

**なぜ既存の検査が気づけなかったか**: 5本すべてが、招待を通らずに済むよう `seed.sql` の
`owner-a`（最初から `owner_users` 済み）を使って書かれていた。`verify-report-roundtrip.mjs`
のコメント自身が「新規の飼い主を作ると孤児owner になるため fixture を使う」と明記しており、
**穴を避ける形で検査が書かれていた**。避けた先が実店舗の必須経路だった。
写真も同様で、`verify:roundtrip` は写真を1枚も検査していなかった。

**追加した検査**: `npm run verify:invitation`（新設・`verify:all` に組込）は「実店舗に
新しいお客様が来た」ところから始める——fixture の飼い主を使わず、画面のフォームで新規の
飼い主と犬を作り、カルテを公開し、招待前は見られないことを確認してから、犬の行の QR ボタンで
招待を発行し、別アカウントがそれを消化して自分のカルテを読めるまで通す。消化済み招待の
使い回しも試す。`verify:roundtrip` にも写真の検査を足した（1枚だけ入れて「入れたものは
届く／空スロットにページURLが混ざらない」を同時に見る）。回帰テストは 68→72件。

統合に入る前に `docs/design.md` を読むこと。`finalize_report` が**4条件で黙って `null` を返す**ことなど、
書き直しを選ばない理由が実物の確認つきで書いてある。

なお F-11 と F-16 は自分の進め方の失敗として記録した。要点は2つ——受入基準を満たすことを
成果と取り違えないこと、そして**片方がクライアントに実害を出す選択は「判断」ではなく不具合**
なので、聞かずに直してから報告すること。

### 続けて、記入が黙って消える4経路を塞いだ（2026-08-24・D-20260824-33）

D-20260824-30 に並べた10件のうち、**実害順の上から、設計判断が要らない4件**を直した。
共通点は「画面もコンソールも何も言わないまま、打ち込んだ内容だけが失われる」——
現場では気づけないので、気づけるようにするのではなく**失われないようにした**。

| # | 何が起きていたか | どう直したか |
|---|---|---|
| 1 | 下書きが一切残らず、**戻るの誤タップ1回で数十分の記入が消える** | 入力が止まって1.2秒で `localStorage` に保存。未確定のまま離れようとしたら確認 |
| 2 | 公開に失敗 → 直して再確定、で**直した内容が捨てられ「公開しました！」と出る** | `replaceDataUrlAssets` を再試行判定の外へ。使い回すのは draft 行だけ |
| 6 | 失敗理由が全部「もう一度お試しください」に潰れる | HEIC・10MB超・429・ログイン切れを見分け、何をすれば通るかまで出す |
| 7 | 確定済みカルテが**書けるのに保存できない** | 上書き保存は3層すべてで塞がれている設計なので、書けない側に揃えた |

**1 の設計**: 保存するのは**文字と選択だけ**。写真と手描きは1カルテ十数MBで localStorage の
枠（約5MB）に入らないうえ、`exportImage()` は描画アコーディオンを開いて reflow させるので
入力中には走らせられない。`applyReport` が空の画像キーを触らない性質にそのまま乗せたため、
復元しても既にある写真は消えない（実測 1.1KB）。復元を尋ねるのは**今クリアしたときだけ**
——プレビューから「やり直す」で戻った場合に今の内容を上書きしないため。確定できたら消す。

**検証**: `npm run verify:draft` を新設（13項目・`verify:all` に組込）。
「入力が下書きに残る／写真が混ざらない／離脱時に確認が出る／戻ると続きから書ける／
確定で消える／確定済みは打ち込める場所が0」までブラウザで確認済み。回帰テスト 72→76件。

**まだ触っていないもの**: D-20260824-30 の 3（削除時の Storage 残留）・4（写真の縮小）・
5（レート制限）・8〜10。いずれも設計判断か運用の好みが絡むため、勝手に決めていない。

### 写真が積み上がる／消したのに残る、も直した（2026-08-24・D-20260824-34）

**調べたら D-20260824-30 の 4 の見立ては半分ずれていた。** 「Konva 4面を毎回 PNG 化するので
16MB」と書いたが、実測は**写真が9割・Konva 4面は1割強**。主因は iPhone の 12MP JPEG
（2〜4MB）が無加工で入ることで、縮小するコードはリポジトリに存在しなかった。

| # | 何が起きていたか | どう直したか | 実測 |
|---|---|---|---|
| 4 | 原寸の写真がそのまま入り、1カルテ約16MB | 取り込みの1箇所で長辺1600px・JPEG 0.82 に縮小 | 7.4MB→1018KB |
| 3 | 犬・飼い主を消すと写真が残り、**誰からも触れなくなる** | DB 行を消す**前に** Storage を片付ける | 5件→0件 |

**3 は順序が全てだった。** Storage のポリシー `private.storage_path_staff` は
「その `reports` 行が存在すること」を条件にしている一方、`DELETE /rest/v1/pets` は
FK カスケード（pets → reports → report_assets）でその行を先に消す。犬を先に消すと
写真は残るのに SELECT が false になり、**スタッフも飼い主も見ることすらできない**。
回収は service_role でしかできず、どれが孤児かを示す `report_assets.storage_path` も
同時に消えている。片付けに失敗したら DELETE を送らずに止める——中途半端に消えるより、
何も消えずにもう一度押せるほうがよい。

**検証の肝**: `npm run verify:delete` は **RLS 越しに見てはいけない**。削除後は
「残っていても見えない」ので、RLS 経由の確認は必ず合格してしまう——まさに直したい不具合を
見逃す。service_role で実体を数えている。

**まだ触っていないもの**: Konva 4面の書き出し（未描画面を飛ばす／WebP 化）は、
**飼い主に見えていた素図が消える／配信画質が変わる**という見た目の変更になるため、
マスターの判断が要る。容量の寄与も1割強にとどまる。
残る未着手は D-20260824-30 の 5（レート制限）・8（`owners.active=false`）・
9（招待の解除）・10（中断した削除の復旧）。

### まとめ入力の詰まりと、途中で止まった削除の復旧（2026-08-24・D-20260824-35）

| # | 何が起きていたか | どう直したか |
|---|---|---|
| 5 | `asset_metadata` 60回/10分 = **10分あたり5〜7カルテ**。まとめ入力で 429 | 300回/10分へ（約27カルテ）。マイグレーション 0006・本番適用済み |
| 10 | 削除が途中で失敗すると**一覧から消えるだけで写真ごと残り**、再試行に到達できない | 隠すのをやめ、「削除が途中で止まっています」の行＋やり直す導線を出す |

**5 の補足**: 本来の直し方はアセット登録をまとめて1リクエストにすること（11回→1回）で、
そうすれば上限に触れなくなる。ただし API 契約・store・`finalize_report` の孤児検出ガードまで
巻き込むので、実店舗を止めている今は上限を広げる側を先に出した。後で置き換えられる。

**10 の補足**: 飼い主側は別経路（RLS が `status='final'` を要求する）なので、
スタッフの一覧に出しても飼い主には見えない。`mark_report_deleting` は既に `deleting` の行も
受け付けるので、やり直しは何度押しても壊れない。

**検査自身の不具合も1件直した**: `verify-delete.mjs` が写真スロットを `scrollIntoView`
せずに直接 `click` していて、確定バーに重なった瞬間に30秒でタイムアウトしていた（2回踏んだ）。
`verify-m6` と同じ形にし、**3回連続で通ること**を確認した——1回通っただけで
「通った」と書かない（F-23 と同型）。

### 飼い主側のアクセスを、スタッフ側の見え方と一致させた（2026-08-24・D-20260824-36）

| # | 何が起きていたか | どう直したか |
|---|---|---|
| 8 | 退会扱い（`owners.active=false`）にしても、**その飼い主は `/my` で今まで通り読めた** | 飼い主側の判定4本すべてに `owners.active` を追加 |
| 9 | 招待リンクは**最初にクリックしたアカウントに恒久的に紐付き**、外す手段が無かった | `revoke_owner_link` RPC と、QRダイアログ内の「解除」 |

**8 の要点**: 入口が4本ある——`is_owner_user`（owners/pets）・`can_read_pet`（reports）・
`can_read_final_report`（report_assets）・`storage_path_customer`（Storage）。
**1本でも漏らすとそこだけ見え続ける**ので、機械検査も4本まとめて見る形にした。

**9 の要点**: 誤送信・転送で第三者が先に開くと、以前はその人が永久に読めた。
復旧は飼い主ごと削除しかなく、犬もカルテも写真も道連れになる。解除ボタンは
**QRを出すのと同じダイアログ**に置いた（間違いに気づく場所と、直す場所を同じにする）。
`owner_users` に直接 delete は与えず、他店舗の行に触れないことを関数側で保証する。

**検証**: `verify:invitation` を 11→19項目に拡張。実ブラウザで別アカウントを使い、
退会扱い・その取り消し・紐付けの解除の前後を通した。

### D-20260824-30 の10件の現在地（2026-08-24 時点）

| # | 内容 | 状態 |
|---|---|---|
| 1 | 下書きが残らない | ✅ D-20260824-33 |
| 2 | 再試行で修正が捨てられる | ✅ D-20260824-33 |
| 3 | 削除しても写真が残り触れなくなる | ✅ D-20260824-34 |
| 4 | 写真の縮小をしていない | ✅ 写真は解決（D-20260824-34）／**Konva 4面の書き出し形式は未決** |
| 5 | まとめ入力がレート制限に当たる | ✅ D-20260824-35 |
| 6 | 公開失敗の理由が伝わらない | ✅ D-20260824-33 |
| 7 | 確定済みカルテが書けるのに保存できない | ✅ D-20260824-33 |
| 8 | `owners.active=false` が効かない | ✅ D-20260824-36 |
| 9 | 招待の紐付けを外せない | ✅ D-20260824-36 |
| 10 | 中断した削除が復旧できない | ✅ D-20260824-35 |

**残る未決は 4 の一部だけ**——Konva 4面（犬体図・歯の図など）を「描かれていなければ
書き出さない」または「WebP にする」。どちらも**飼い主に見えるものが変わる**
（素図が消える／配信画質が変わる）ため、マスターの判断が要る。容量の寄与は1割強。

### トリマーが自分の作業画面へ行けなかった（2026-08-24・D-20260824-37・マスター指摘）

マスターが本番にログインしたら、犬の名前が3つ並ぶだけで**入力欄が無い**画面に着いた。
私は「一番下の入力欄に…」と案内していたが、それは `/edit` の話で、着いていたのは `/my`。
**これは案内ミスではなく画面の欠陥**だった。

`supabase-auth.js` は `memberships > 0 && ownerLinks === 0` のときだけ `/edit` へ自動で
飛ばす。**スタッフかつ飼い主のアカウントはこの条件を外れて `/my` に留まる**。ところが
`/` にも `/my` にも `/edit` へのリンクが**0箇所**で、URL を手打ちしない限り仕事を
始められなかった。しかもその組み合わせは D-20260823-06 で私がそう決めた形で、
**本番で実際に使う唯一のアカウントがちょうどその穴に落ちていた**。

**なぜ検査5本が素通りしたか**（ここが本題）:

1. どれも「この操作をすると、この結果になる」を見る作りで、決め打ちの手順をなぞる。
   **画面に何が乗っているか・そこから他のどこへ行けるかを一度も見ていなかった**
2. fixture に「スタッフかつ飼い主」のアカウントが無く、この組み合わせを一度も通していなかった

**入れた検査**: `npm run verify:screens`（新設・23項目・`verify:all` の先頭）。
全画面を1枚ずつ開き、**押せるもの・入力欄・打ち込める場所を数えてスクショを残す**
（`.screens/` に出力）。見るのは**在るか無いか**だけ。
fixture にも `staff-owner@local.test`（スタッフかつ飼い主）を足して本番と同じ形にした。

**同じ型の欠陥は3回目**——D-20260824-29（招待QRボタンが画面から消えた）と同じで、
どちらも「機能は生きているのに、どこからも押せない」。1回目・2回目は偶然見つかり、
3回目はマスターに「全ページのスクショを撮ればわかる事だろう」と指摘されて見つかった。

---

## 2026-08-25 チェックアウト — 判定を自分の手から外す仕組みを入れ、計画を1本にまとめた

### 今回やったこと

**1. 整理整頓プラン（F1→F2→F3）を確定し、逸脱を機械で止めた**

マスター指定の5ルール（①現状あるものだけ ②納品物の完成≠フェーズの完成 ③目的は整理整頓
④見つけた問題は原則あと回し ⑤あと回しは記録して F3 後に判断）を全フェーズに適用。
`scripts/guard/` に3本を置き、`npm run guard`（`npm run check` が呼ぶ）で機械強制した。

| | 役割 | 確認済みの挙動 |
|---|---|---|
| `scope.mjs` | 現在フェーズの範囲外を止める | `supabase/`・`worker/`・`npm install`・`rm design/…` すべて EXIT 1 |
| `gate.mjs` | ②③の成果物が揃うまで作業場を開けない／`--end` でフェーズを閉じさせない | ②の承認印が無い間、`src/` の書き換えを停止 |
| `solved.mjs` | 「解決」の主張に赤→緑→戻して赤の3出力を要求 | 「該当した」→「該当せず」の書き換え逃れを **git 履歴の全版**で捕捉（実測で8件とも停止） |

**2. ルールの正を `AGENTS.md` 一本にした（マスター指定）**

このリポジトリは Codex 等も触るため、`.claude/`（settings.json・agents 3本）を削除し
`.gitignore` で再流入を止めた。`CLAUDE.md` は「AGENTS.md に従え」だけにした。
Hook（Claude 専用）を廃し、**どの AI でも実行できるコマンド**に置き換えた。

**3. ルールを全部「人間向け1行／AI向け1行」の2行に書き直した（マスター指定）**

LEVEL A の5項＋ D-1〜D-18 ＋ 整理整頓の重要ルール5つ、計29件。専門語は AI 行にだけ置く。
新設: **D-15**（正は AGENTS.md 一本）／**D-16**（整理整頓プラン）／**D-17**（2行で書く）／
**D-18**（調べた・直した・解決したの三層／真解決の定義／偽解決12種カタログ）。

**4. F1 のバッドシナリオ10個を実測した → 私の報告は誤りだった**

マスター承認後に実測し、**該当8件／該当せず2件**（`F-20260825-28`）。
「F1 はほぼ完了」という私の報告は、隔離を何も保証しない緑を根拠にしていた。

**5. 外部と照合して、判定を自分の手から外す形を固めた**

消去法的帰納（ベーコン1620）＝ ACH／除外診断／内的妥当性の脅威／eliminative argumentation／
プレモーテム／強推論。共通の掟は4つ（リストは本人がその場で作らない／過去形/矛盾で数える／
潰しても暫定）。3つの帳面（ひな形＝固定・外部既製リスト／発見帳＝failures・deferred・watch／
事後記入）と、当てる時点はフェーズ境目のみ、照合は2択・過去形・事実の言葉、と決めた。

**6. 計画を2ファイルにまとめた（マスター指定）**

- `docs/ops/plan.md` 全面改稿 — **完成までの実装計画（正・1本のみ）**。
  ゴール定義／現在地の残高／F1 の8件の直し方／F2 の具体策／**F3 の結線表**／F3 後の棚卸し
- `docs/reference/ai-development-harness.md` 新設 — **リポ非依存の汎用資料**（10章）。
  リポ固有名詞ゼロを grep で確認済み

**7. マスター判断を1件記録した**

「次回のおすすめご来店時期」は**残す**（`D-20260825-38`）。私が「載せない」と決めた
`D-20260823-13` を一部撤回。以後これを違反として数えない。

### 現在の状態

| 検査 | 結果 |
|---|---|
| `npm run build` / `check` / `test` | すべて **EXIT 0** |
| `node scripts/guard/gate.mjs --end` | **EXIT 1**（F1 未閉鎖。正しい状態） |

- 現在フェーズ: **F1**（`docs/ops/phase`）
- `src/` = UI のみ（`index.html` / `js/ui.js` / `js/dummy.js` / `manifest.json` / `assets`）
- `backend/` = Supabase モジュール4本。**うち2本が import 不能**（`magazine-view.js` 欠落）
- 本番 `trimmer-system.kouheikosehira.com` は Supabase 版を配信中。**画面はセッション開始時と同一**
- **④カルテ作成 → ⑤確認へ進めない**（編集領域が横 463px 必要／iPhone 13 は 390px）
- モックに在る「＋ 新規カルテを作成する」が `src/index.html` から欠落（マスター4回指摘の入口）

### 次回やること

**`docs/ops/plan.md` 第2章の順に、F1 の該当8件を潰す。** 各件で必ず「検査を先に作って赤を見る」。

1. `scripts/guard/isolation.mjs` 新設（条件A・B）→ #1 #2 #4 #5 #6
2. `backend/js/magazine-view.js` を `git show '6685df5^:src/js/magazine-view.js'` から復元 → #7
3. `test/backend-import.test.mjs` 新設（**先に走らせて赤を見てから** #7 を直す）→ #8
4. `src-dist-guard.config.json` の死んだ項を削除し、drift-guard を「実在しない場所を指したら EXIT 1」に → #9
5. `rm -rf dist && npm run check` の赤を確認してから扱いを決める（勝手に緩めない）→ #10
6. 各件を `docs/ops/solved-F1.md` に3出力で記録 → `failure-check-F1-end.md` → `gate --end` が EXIT 0 → `phase` を `F2` へ

**未作成のもの**: `docs/ops/template.md`（ひな形4部。第3部は failures.md 28件を分類にたたむ作業が先）、
`docs/watch.md`（経過観察・空欄スタート）。

**マスター判断待ち**: 担当トリマーの既定文2か所（`src/index.html:1953` と `:2076`。
トリマーが何も書かなくても飼い主の画面に長文が出る）／463px の直し方2択／
`photo-trim-action.jpg` の出所／本番のダミー犬3頭と孤児 owner／**資格情報3種のローテーション
（実顧客データ投入前に必須）**／Konva 4カンバスの出力形式。
