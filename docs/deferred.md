# あと回しの記録（F1〜F3 期間中）

> ルール④: フェーズ中に見つけた問題は**原則あと回し**。
> 例外は「直さないと次の画面へ行けないもの」だけで、その場合も**進める状態までしか直さない**。
> ルール⑤: ここに残したものは **F3 完了後**に「直す／放置する」をマスターが決める。

**ここは記録だけの場所。ここに書いた時点では、直していない。**

| # | 見つけた日 | 場所 | 何が起きているか | 先へ進めるか | 状態 |
|---|---|---|---|---|---|
| 1 | 2026-08-24 | `src/index.html` カルテ入力画面 | 爪・歯の解剖図が表示されない（消した UNVERIFIED 画像を指している）。`nail-diagram.png` と `teeth-diagram.jpg` は `src/assets/` に在るが、画面のどこからも参照されていない | 進める | あと回し |
| 2 | 2026-08-24 | `src/index.html` 全画面の上部 | `01 ログイン / 02 カルテ検索 / 03 カルテ作成 / 04 顧客カルテ` のタブが常に出ている。試作品の見た目で、本番の動線ではない | 進める | あと回し・マスター判断 |

## 書き方

1行 = 1件。**直した日は書かない**（このフェーズ中は直さないから）。
「先へ進めるか」が **進めない** のものだけ、ルール④の例外として**進める状態までだけ**直し、
その1件は「例外対応」と書いて、何をどこまで直したかを1行足す。

## 追記（2026-08-25）

| # | 見つけた日 | 場所 | 何が起きているか | 先へ進めるか | 状態 |
|---|---|---|---|---|---|
| 3 | 2026-08-25 | `src/index.html` 顧客カルテ画面 | 「次回のおすすめご来店時期」は**マスター指定で残す**（`D-20260825-38`）。ただし文言が固定値。どこから値を取るかは F3 で決める | 進める | F3 で確認 |

## 追記（2026-08-25・F1 の隔離検査が見つけた「置いてあるだけのファイル」）

`node scripts/guard/isolation.mjs` の条件A が、`src/index.html` からどこにも繋がっていない
ファイルを 11 件挙げた。**1件も消していない。** どれも F2 / F3 で使う予定か、マスター判断待ちで、
消すと後で「あったはずのものが無い」になる。ここに番号付きで登録して、F3 完了後に棚卸しする。

登録されていないものは検査が逃がさない（無条件の免罪符を作らないため・`AGENTS.md` D-18 偽-3）。

| # | 見つけた日 | 場所 | 何が起きているか | 先へ進めるか | 状態 |
|---|---|---|---|---|---|
| 4 | 2026-08-25 | `src/manifest.json` / `src/assets/app-icon.png` | PWA の設定とアイコンが `src/index.html` から結線されていない（`<link rel="manifest">` が 0 件）。ホーム画面に追加しても名前もアイコンも出ない | 進める | あと回し（`docs/ops/plan.md` 第6章にも記載） |
| 5 | 2026-08-25 | `src/assets/icon-ear.png` `icon-nail.png` `icon-skin.png` `icon-spa.png` `icon-weight.png` `body-side.png` | カルテ入力画面のアイコン5点と犬体図の側面図が、現在の画面から参照されていない。F2 で正UI に載せるときに使う想定 | 進める | あと回し・F2 で使う |
| 6 | 2026-08-25 | `src/assets/konva.min.js` | 手描きの描画ライブラリ（Konva v10.3.0・同梱・`AGENTS.md` の確定スタック）。F1 で UI を切り離した時点で読み込み元が無くなり、現在は未参照。描画の結線は F3 | 進める | あと回し・F3 で使う |

## 追記（2026-08-25・F1 の完了時照合が挙げたもの）

`docs/ops/failure-check-F1-start.md` の「何をすれば防げるか」16件のうち、
**F1 の完了条件（A・B・C）に関わらないもの**をここへ移した。直していない。

| # | 見つけた日 | 場所 | 何が起きているか | 先へ進めるか | 状態 |
|---|---|---|---|---|---|
| 7 | 2026-08-25 | `src/js/ui.js:102` | どの犬でも「4歳 / 2.79kg」と出る。`src/js/dummy.js` は犬ごとに `age` と `weight` を持っているのに使っていない。`F-20260821-06`（既定値が残る）と同型 | 進める | あと回し・F2 で直す |
| 8 | 2026-08-25 | `package.json` の `verify:*` | 飼い主に届く中身を見ていた検査が、UI をはがしたとき一緒に消えた。**消えたのはファイル9本**（`delete` / `draft` / `empty-pet` / `invitation` / `m6` / `portal` / `report-roundtrip` / `screens` / `xss`）。**この欄は当初「7本（`m6`/`roundtrip`/`empty`/`xss`/`portal`/`all`/`preview`）」と書いていたが誤り**——`all` と `preview` はファイルではなく npm の集約スクリプトで、実体で消えた `delete`・`draft`・`invitation`・`screens` の4本が記録から漏れていた（`bad-scenarios-F3.md` #6 が指摘）。復元は `docs/ops/verify-restore-F3.md` が正。復元元は `git show 6685df5^:scripts/verify-*.mjs` | 進める | **F3 で作り直す**。いま **1/9**（`portal` のみ・CI で 14/14 PASS） |
| 9 | 2026-08-25 | `package.json` の `test` | 実 PostgreSQL に SQL を流す唯一の検査 `verify:migrations` が、`npm test` から呼ばれていない。`supabase/migrations/` を触ったとき、人の記憶だけが担保（`F-20260821-24`） | 進める | あと回し・F3 で SQL を触る前に |
| 10 | 2026-08-25 | 削除の順序（F3 で作り直す導線） | 写真は **Storage を片付けてから DB 行を消す**。逆にすると RLS の条件が崩れ、写真が誰からも取れなくなる（`D-20260824-34`）。実装は `git show 6685df5^:src/js/ponchi-app.js` の `purgePetAssets` / `purgeOwnerAssets` | 進める | **F3 で削除導線を作るとき必ず参照** |

## 追記（2026-08-25・F2 開始時）

| # | 見つけた日 | 場所 | 何が起きているか | 先へ進めるか | 状態 |
|---|---|---|---|---|---|
| 11 | 2026-08-25 | `src/index.html` `.body-marking-stamps`（犬体図のスタンプ帯） | `overflow-x: auto` で、部品の中だけ横に流れる（`🔴 赤み` 等が 373px 並ぶ／枠は 282px）。**元からある設計で私が足したものではない**が、マスターは「横スクロール禁止」と指示している（`D-20260825-39`）。ページ全体の横スクロールは発生しない | 進める | **マスター判断待ち** |

## 追記（2026-08-25・F2 の作業中に見つけたもの）

| # | 見つけた日 | 場所 | 何が起きているか | 先へ進めるか | 状態 |
|---|---|---|---|---|---|
| 12 | 2026-08-25 | ④カルテ作成 → ⑤⑥顧客カルテ | **④で書いた「今日はおとなしくしていました。」が⑥に届かず、既定文「今月もとってもお利口に…」が出る。** 実測: `npm run walk` の `04` と `05-06` の画像を比べると別の文章。仮データ段階では画面間の受け渡しが無いため。F3 の結線表（`plan.md` 第4章）で扱う対象だが、**飼い主に届く内容が違う**ので記録する | 進める | **F3 で必ず確認**（`D-12`「押せた ではなく 同じ値で届いた で見る」） |
| 13 | 2026-08-25 | ④の担当メッセージ欄（`src/index.html:1963`）と⑤確認の器（`:2085`） | **担当メッセージの既定文が最初から入っていた。** 消し忘れると、誰も書いていない手紙が担当トリマーの名前で飼い主に届く（`F-20260821-14`・実際に届いた事故がある）。意匠モック由来で、絵として見せるために入れてあったもの | — | **直した**（2026-08-26・マスター指示）。入力欄は空にし、何を書く欄かは placeholder で示す。⑤の器も空にした。機械: `npm run check` の `no-sample-text`（`src/` に見本の文が無いこと）と `verify:edit` の 17（入力欄が空で始まる） |
| 14 | 2026-08-25 | ④カルテ作成の各所 | 写真が無い箇所に `alt` の文字（「爪の解剖図」「歯の頭骨解剖図」「ポンチ 仕上がり写真」等）が出る。`src=""` は落としたので**ページURL混入は解消**したが、**絵としては文字が並ぶ**。仮データに写真が無いため | 進める | **マスター判断待ち**（空スロット表現にするか、仮画像を置くか） |

## 追記（2026-08-25・F3 開始時のバッドシナリオから）

| # | 見つけた日 | 場所 | 何が起きているか | 先へ進めるか | 状態 |
|---|---|---|---|---|---|
| 15 | 2026-08-25 | `scripts/guard/checkout.mjs` | 終了時の検査は、まっさらな作業場で `npm ci` → `build` → `check` → `test` は走らせるが **`npm run walk` を走らせない**。F3 の完了条件は「実データで D-14 の2問」＝ walk が撮る絵なので、**絵が撮れない状態のまま「まっさらでも何も失われていない」と言えてしまう**。実際 `bad-scenarios-F3.md` #8 はこの穴を通って生き延びていた（`solved-F3.md` #8 で walk 自体は直したが、**検査に組み込んではいない**）。組み込むならブラウザの用意まで含めて設計が要るので、あと回しにする | 進める | **あと回し・F3 完了後に棚卸し** |
| 16 | 2026-08-25 | `backend/js/magazine-view.js:322` | `img.src = has ? src : ''` で、写真が無いとき **`src` に空文字**を入れている。`img.src` は空文字のとき**現在のページURL**を返すので、`D-20260824-30 #3`（空の写真スロットがページURLを指す）の再発条件そのもの。F2 が `src/index.html` から11件消した欠陥を、**レンダラが実行時に作り直している**。いまは枠を `hidden` にしているため見えず実害は出ていないが、**⑥を結線する前に直す**（属性ごと出さない形にする） | 進める | **直した**（2026-08-25・4-1 の結線前）。`src` に入れず属性ごと出さない形にした。機械: `npm run verify:edit` の 15 |
| 17 | 2026-08-25 | `backend/js/supabase-staff.js`（`dist/backend/js/supabase-staff.js`） | `my.html`（飼い主）を戻したことで `backend/js` が配られるようになったが、この1本は**トリマー側**の部品で、読み込む側（`src/index.html` の結線）がまだ無いため未到達。**消してはいけない**——②一覧の `mapPet`/`mapOwner` と招待の導線がここに在り、`plan.md` 第4章の結線表が指している。`index.html` を backend へ繋いだ時点で到達する | 進める | **F3 の結線で解消する** |
| 18 | 2026-08-25 | `.env.example` | `scripts/lib/local-stack.mjs` の接続先を差し替え可能にした（`D-20260825-44`）際、鍵側の変数名 `SUPABASE_LOCAL_ANON_KEY` を `.env.example` にも足そうとしたが、**`.env.example` は F3 の範囲外**で逸脱監視が止めた（正しい判定）。**変数は動く**（既定値は従来どおり）が、見本ファイルに名前が載っていない。`SUPABASE_LOCAL_URL` のほうは元から在る（10行目）。棚卸しのときに1行足す | 進める | あと回し・F3 完了後 |
| 19 | 2026-08-25 | `.github/workflows/ci.yml` | CI の初回実行で警告が出た——`actions/checkout@v4` と `actions/setup-node@v4` は **Node 20 を対象**にしており、ランナー側で Node 24 に強制されている（`Node.js 20 is deprecated`）。**いまは警告で、実行は成功している**が、いずれ失敗に変わる。`@v5` 系へ上げれば消える見込み。**渡した後に効いてくる類**（`plan.md` 第7章 7-1）なので、棚卸しで扱う | 進める | あと回し・F3 完了後 |
| 20 | 2026-08-25 | `worker/src/index.js:733`（`renderAppPage`） | **`/edit`（トリマー画面）が、削除済みのテンプレートを読んでいる。** `renderAppPage` は `fetchAssetHtml(env, '/ponchi-v2.html')` を読むが、`ponchi-v2.html` は `6685df5`「古いUIをはがし…」で削除され**リポジトリのどこにも無い**（`find` で0件）。テンプレートが無ければ `502 Template Not Found` を返す実装。あわせて注入するスクリプトが `/js/supabase-{vendor,auth,staff}.js` のままで、実体は `backend/js/` へ移っている（`my.html` は `#7` で直したが、ここは未修正）。**`bad-scenarios-F3` #6 の9本のうち8本が `/edit` を開くため、この1件が8本の復活を塞いでいる。** 正UI（`src/index.html`）へ向け直すのは結線そのものなので `plan.md` 4-1 で扱う。**デプロイ済みの実物が同じ状態かは未確認**（この環境から外部へ到達できない） | 進める | **直した**（2026-08-26・4-1 の結線で `/index.html` へ向け直した）。**本番も確認した**（2026-08-26・`docs/ops/prod-check-F3.md`）——**502 ではなく 200 で、旧UI `ponchi-v2.html` を配っていた。** 削除済みテンプレートを読む版がそもそもデプロイされていなかったため。この心配はデプロイした時点で消える。機械: `npm run verify:prod` |
| 21 | 2026-08-25 | `worker/src/index.js`（`createAppStateScript`） | `/edit` が注入する `window.__SCREEN__` / `__VIEW__` / `__OWNER__` / `__PET__` / `__REPORT__` / `__OWNER_LIST__` を、**いま読む側が1つも無い**（`grep` で `backend/js` `src/js` とも0件）。読んでいたのは `6685df5` で消えた `ponchi-app.js` / `ponchi-engine.js`。**死んだ注入**だが、`renderAppPage` を呼ぶ10経路すべてに関わるため、正UI の結線が固まってから一括で外す（`A-5` の対象。いま外すと変更が広がりすぎる） | 進める | **F3 4-1 の結線が固まってから外す** |
| 22 | 2026-08-25 | `worker/src/index.js` の KV モード経路（`/p/{slug}` `/edit/p/{slug}/{reportId}` ほか8経路） | `renderAppPage` を正UI へ向け直したこと（4-1）で、**KV モードの経路も正UI を配るようになった**。正UI は Supabase 前提なので KV のデータは描けない。ただし**悪化ではない**——`ponchi-v2.html` は `6685df5` で削除済みで、**変更前は全経路が `502 Template Not Found`** だった。KV モードは「閉鎖のうえ残置・切り戻し用」（`D-20260823-09`）で、切り戻しの実効性が下がったままである点は残る。**切り戻しが要る事態になったら、`6685df5^` から KV 用の UI ごと戻す**必要がある | 進める | あと回し・F3 完了後にマスターが「直す／放置」を決める |
| 23 | 2026-08-25 | `src/js/ui.js` の `showPetKarte()` | 犬を選ぶ（`/edit/p/{petId}`）と**カルテ作成（screen-3）に入る**が、その犬の**過去カルテを開く導線が無い**。`mapPet()` は `months`（過去カルテの一覧）を持って来ているのに、正UI に受け皿が無いためである。旧UI では `.archive-new-btn` と過去一覧が同じ画面に在った。**次の画面へは行けるので進める**（重要ルール④）。④保存・確定を結線するときに、同じ画面で一緒に作る | 進める | **直した**（2026-08-26・マスター指定「③で犬を選んだら過去一覧を出す」）。④カルテ作成の見出し直下に、その犬の**確定済み**カルテを新しい順で並べた。**1件も無ければ帯ごと出さない**（`D-10`）。機械: `test/ui-past-reports.test.mjs`（3件・直しを戻すと3件とも赤） |
| 24 | 2026-08-25 | `src/index.html` の歯の pill 6か所 | **ボタンの表示と、保存されて飼い主に届く値が違っていた。** 表示「ちょっと付着💦」→ 届く値 `ちょっと歯石💦` ／「グラつき😟」→ `グラつきあり😟` ／「除去準備✋」→ `除去準備開始✋`（6つ中3つ）。**意匠モックの時点でずれており**、`src/` で混入したものではない。原因は HTML 側で保存値を `App.selectTeeth(this, '…')` の第2引数に**二重に書いていた**こと | — | **直した**（2026-08-26・マスター指示）。引数を廃し、**押されたボタンの表示そのものを保存値にした**。重複が無くなったので構造的に二度とずれない。機械: `verify:roundtrip` の 1b（合否で見る） |
| 25 | 2026-08-25 | 正UI（`src/index.html`）の削除の入口 | **カルテを削除する入口が、どの画面にも無い。** `backend/js/supabase-storage.js` の `deleteReportAssets` / `purgePetAssets` は在り、`plan.md` の結線表にも載っているが、**意匠モックに削除のボタンが無い**ため置き場所が決まらない。入口を思いつきで足すのは `D-18` 偽-7（言われた入口を直さず、思いついた入口を1つ足す）なので足していない。`verify:delete` は製品と同じ関数を実ブラウザから呼んで、**削除の道そのものは毎回確かめている**（写真の実体が service_role で数えて 0 になること）| 進める | **マスター判断待ち**（2026-08-26 に「何を削除するのか具体例で言え」と問われたので、`docs/ops/delete-scope.md` に3段階を実例で書いた。置き場所の決定はそれを見てから） |
| 26 | 2026-08-25 | `src/js/ui.js` の `applyReport()` | **下書きから戻せない項目が1つある。** カットの長さとスタイルは、⑥が読む形（`trimming.comment`）に「8mm (標準ふんわり) / テディベアカット」のように**まとめて**入れているので、そこから2つの選択に戻す規則が無い。戻せないものを推測で埋めない（`D-10`）。下書きから復帰したとき、この2つだけ選び直しになる。**次の画面へは行けるので進める**（重要ルール④） | 進める | あと回し・F3 完了後の棚卸し |
| 27 | 2026-08-26 | `src/js/ui.js` の `resumeDraft()` / `applyReport()` | **前回の続きの読み込みが、いま打っている文字を上書きすることがある。** 読み込みは非同期で、終わると画面に下書きの中身を書き戻す。犬を開いた直後（1秒未満）に書き始めると、その文字が消える。`verify:m6` が実際にこれで2回落ちた（検査側は書く前に待つ形に直した）。**消えるのは1秒未満に打った分だけで、戻ってくるのはその人自身の下書き**なので、記入が失われる事故ではない。直すなら「人が触った項目には書き戻さない」印を持たせる | 進める | あと回し・F3 完了後の棚卸し |
| 28 | 2026-08-26 | 正UI（`src/index.html` / `src/js/ui.js`）の③カルテ作成の見出し | **確定したあとに「03 カルテ作成」へ戻ると、見出しが「ポンチ カルテ入力」——仮データの犬の名前になる。** `X` で確定 → 1タッチで戻る、で再現（`docs/ops/walk-D14-F3.md` の絵 `mistakes/08`）。犬を選び直して入ったときは正しく「X カルテ入力」と出る（`mistakes/03`）ので、**確定を経由したときだけ**起きている。**D-14 の問2が ✕ になった唯一の原因。** 戻ること自体は1タッチでできるが、預かっていない犬の名前が出ているため、直したかった操作を安心して続けられない。**見出しだけが古いのか、書けば別の子に入るのかは未調査**（D-14 はコードを読まない規則のため、絵の判定と切り離してある）。書けば別の子に入るのなら、これは**別の犬のカルテに書き込む事故**になり得る——**先に調べる価値がある**と考える | 進める | **直した**（2026-08-26）。`showReport()` で見出しをその犬に合わせ、HTML の初期値から意匠モックの犬の名前を外した。**書き込み先の事故ではなかった**——`commitReport` は URL の `petId` を使うので、そこで書いても正しい子に入る。見出しだけが古かった。機械: `verify:m6` の `⑤c`（赤を見てから直した）。撮り直した絵で `D-14` 問2 が ○ になった |
| 29 | 2026-08-26 | 飼い主に届いたカルテの日付 | **届いたカルテの右上が `2026.07.16`。撮った日は 2026-08-26。** ⑦に書いた文は今回書いたものが出ているので、**開いているカルテ自体は今回のもの**。日付の出どころが別に在る可能性がある（`docs/ops/walk-D14-F3.md` の絵 `correct/08`）。`docs/ops/key-parity-F3.md` で **`date` と `isoDate` は「出どころが無いキー」6件**に挙げてあり、そこと同じ話の可能性が高い | 進める | あと回し・F3 完了後の棚卸し |
| 30 | 2026-08-26 | 本番（`https://trimmer-system.kouheikosehira.com`） | **片づいた（2026-08-27）。** ①F3 完了版を本番へ出した（`verify:prod` **4/4 PASS**）。手段は GitHub の画面から押す `deploy` ワークフローで、鍵は Secrets に入っており **AI は受け取っていない**（`A-1`）。②**ダミーの掃除も完了**——マスターが本番の管理者画面から犬3頭（ポンチ/ムギ/レオ）→ 飼い主3件（A/B/「あ」）の順で削除。**service_role の再発行は不要だった**（管理者のログインだけで消せる。当初「新しい秘密鍵が要る」と書いていたのは誤り）。確認は3つの一覧が「登録されていません」を返したこと＝**サーバの答え** | — | **完了** |
| 31 | 2026-08-26 | `.github/workflows/ci.yml` | `npm run verify:prod` を **CI に入れていない。** いま入れると CI が恒常的に赤くなる（本番が旧版のため・`#30`）。入れるなら「デプロイ後に走らせる job」か、`#30` が解消してから。**入れていないので、次のセッションが本番のズレに自動で気づくことは無い**——気づかせたいなら手で `npm run verify:prod` を走らせる | 進める | あと回し・`#30` の解消後 |
| 32 | 2026-08-26 | `.gitignore` | **鍵3種（`.cftoken` `.sbtoken` `.srkey`）を `.gitignore` が塞いでいない。** `git check-ignore` が 0 件を返す——`*.key` は `.srkey` に当たらない（末尾が `.key` ではない）。**履歴にもツリーにも入っていないことは確認済み**（`git log --all -- <各ファイル>` が 0 件）だが、次のセッションが同じ場所へ降ろせば `git add -A` 一回で入る。`.gitignore` は **F3 の範囲外**で逸脱監視が止めた（正しい判定）ため、**範囲内でより強い側**——`scripts/guard/no-secrets.mjs` を `npm run check` に入れ、CI が毎回止める形——に置いた。`.gitignore` への追記そのものは棚卸しでマスターに諮る | 進める | **あと回し・マスター判断**（機械強制は済んでいる） |
| 33 | 2026-08-26 | `backend/js/supabase-staff.js` の `saveReport` / `worker/src/api-schemas.js` の `updateReportSchema` | **`#29`（届く日付が下書き作成日のまま）の原因を特定した。** 確定時の PATCH は `{ data }` しか送らず、`updateReportSchema` は `.strict()` で `data` のみ——**日付を送る口が構造的に無い。** さらに DB 側は `revoke update on reports` のうえ `grant update (data)` だけなので、**トリマーのトークンでは `report_date` を書き換えられない**（意図的に狭い設計）。walk の絵が `2026.07.16` だったのは、`supabase/seed.sql` の draft fixture（`50000000-…a2`・`report_date '2026-07-16'`）を `findDraft` が拾って再開し、確定したため。**直すには「施術日をどう決めるか」をマスターが決める必要がある**——(a) 下書きを作った日（＝いまの挙動。施術当日に書き始めるなら正しい）／(b) 確定を押した日／(c) ④に日付の入力欄を足す（意匠モックに無いので `D-18` 偽-7 に触れる）。(b)(c) はどちらも**マイグレーション**（`finalize_report` の引数追加か列の grant 追加）を伴う＝Level 3。`F-20260823-27` の Guardrail が「日付の検査を追加すること」と書き残したまま未整備だったのが、この形で再発している | — | **仕様と決まった**（2026-08-26・マスター判断）。**施術日 = 下書きを作った日**。バグではないので実装変更なし。walk の `2026.07.16` は `seed.sql` の draft fixture を再開したもので、本番の挙動ではない |
| 34 | 2026-08-27 | Cloudflare Worker `shiota0823` の `workers.dev` サブドメイン | **私の最初の記録は誤りだった（同日中に訂正）。** `https://shiota0823.rahiseko.workers.dev/my` が Cloudflare の “There is nothing here yet” を返すのを見て「配信が消えた」と書いたが、**Worker は生きている**——独自ドメイン `trimmer-system.kouheikosehira.com` の `/api/config` が `backend:"supabase"` と `sb_publishable_jdfWplK…` を返す（`wrangler.supabase.toml` の値と一致）。つまり**その Worker が本番ドメインを配信している**。`workers.dev` のほうが出ないだけで、動いていないわけではなかった。**1つの画面の文字から、確かめずに結論を書いた**（`F-20260826-42`）。ただし配っている中身は**F1 より前のまま**で、それは `#30`（デプロイ）の話 | 進める | **訂正済み・追う必要なし** |
| 35 | 2026-08-27 | `src/index.html` の⑤雑誌テンプレート（`2248` 付近） | **⑤の器に見本の数値が残っている**（`2.79 kg (適正)` ／ 偽の推移グラフ ／ `前回（2.67kg）比: +120g`）。**実データのときは届かない**——`renderMagazine` が `container.innerHTML = TEMPLATE` で器ごと差し替えるため（`backend/js/magazine-view.js:542`）。ただし差し替わる前に一瞬見えるうえ、静的配信（F2 の絵）ではそのまま出る。P-1 では触らなかった——`walk` の絵の意味が変わる恐れがあるため（`watch.md` W-2）。**P-3 の棚卸しで、`#85`（alt 文字だけの枠）`#86`（解剖図）とまとめて片づける** | 進める | あと回し・P-3 |
| 35 | 2026-08-27 | Supabase プロジェクト `shiota1`（Free プラン） | **バックアップが1つも無い。** Free には日次バックアップも PITR も含まれない（公式）。**マスター判断で、いまは用意しない**（`D-20260827-46`）——本番に在るのはダミーだけで、失って困る実データが無いため。**発火条件: 本番に実顧客のカルテを1件でも入れる前。** そのとき Pro へ上げる（提供元が毎日取る・画面から戻せる）か、GitHub で毎日ダンプを取る（0円。ただし**ダンプに実顧客の情報が入る**ので置き場所の判断が要る）かを決める。監視のほう（`verify:prod` の日次実行）は 2026-08-27 に入れた | 進める | **マスター判断・発火条件つき** |
| 36 | 2026-08-27 | `backend/js/supabase-admin.js`（③削除の一覧と確認） | **同姓同名を区別できない。マスター指摘。** ①飼い主の一覧は**名前しか出していない**（`pickOwnerForDelete` の `note: ''`）。②犬の一覧は「名前＋飼い主名」だが、飼い主も同名なら同じこと。③**確認の入力が「名前を打つ」**なので、同名の相手には**両方一致してしまい、確認として働かない**（`confirmDestructive`）。取り違えると**取り返しがつかない**（その飼い主の犬とカルテと写真が全部消える）。**DB 側は問題ない**——1件ずつ固有の UUID を持ち、名前が同じでも別物として扱われる。外部連携にも影響しない。**危ないのは人が選ぶ画面だけ**。直し方の案: (a) 一覧に見分けが付く情報を足す（登録日／その飼い主の犬の名前）、(b) 確認の入力を名前ではなく**その行だけの短い符号**（id の先頭6文字を画面に出して打たせる）にする。(a)+(b) を両方やると、同名でも取り違えない。**実顧客を入れる前に片づけるべき**——同姓同名は珍しくない | 進める | **未着手・マスター指摘**（実顧客投入前） |
