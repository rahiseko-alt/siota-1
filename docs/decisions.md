# Decision Log (docs/decisions.md)

> **運用ルール**
> - マスターへの確認事項・その回答・私（Claude）が代わりに判断した事項を、**その場で流さず**ここに記録する。
> - セッションを跨いでも残る。`docs/handoff.md` から必ずここへ辿れるようにする。
> - Append-Only。過去の決定は削除・上書きせず、覆す場合は新しい行を足して「Supersedes #N」と書く。
> - この台帳が作られる前（〜2026-08-22）の決定は `docs/handoff.md` の本文と履歴コミットに散っている。遡及記録はしない。

---

## 記録フォーマット

```markdown
### [D-YYYYMMDD-NN] タイトル

- **Date**: YYYY-MM-DD
- **Kind**: master-decision（マスターが決めた） / claude-judgment（私が代わりに判断した） / open（未決）
- **Question**: 何を確認したか / 何を判断したか
- **Answer**: マスターの回答、または私の判断とその理由
- **Impact**: これが決まらないと何が止まるか（open の場合）／何が変わったか（決定済みの場合）
```

---

## 決定事項・未決事項一覧

### [D-20260823-01] 完成の定義

- **Date**: 2026-08-23
- **Kind**: master-decision
- **Question**: 「完成」をどこまでと定義するか
- **Answer**: 実店舗で使える状態。トリマーが実際に犬のカルテを書き、飼い主がスマホで読める。意匠は当面 V1 のままでよい
- **Impact**: （旧計画・削除済み） の全体受け入れ条件を規定する

### [D-20260823-02] 現行本番の扱い

- **Date**: 2026-08-23
- **Kind**: master-decision
- **Question**: 現行の本番（`trimmer-system.kouheikosehira.com`・KVモード）はどうするか
- **Answer**: Supabase 版へ切り替える。「そのために今までの作業がある」との指摘
- **Impact**: F6（独自ドメイン切替）が計画のゴールに含まれる

### [D-20260823-03] 実データの投入時期

- **Date**: 2026-08-23
- **Kind**: master-decision
- **Question**: 実際の顧客データはいつ入れるか
- **Answer**: 実顧客データは過去に一度も入れたことがなく、KV に入っているものも全てダミー。新たにダミーデータを入れて検証する
- **Impact**: F1 でダミーデータを投入して検証する。KV → Supabase のデータ移行は不要（既に `docs/design.md` にも「移行しない」の記載あり、今回の回答で再確認）

### [D-20260823-04] 業務動線

- **Date**: 2026-08-23
- **Kind**: master-decision
- **Question**: クライアントに渡す操作フローは何か
- **Answer**: URL を開く → 作成済みアカウントでログイン → 犬の名前を選ぶ → カルテ作成 → 確認 → 顧客ページ。この動線は `design/mock-4step.html` に既に実装されている「正」の UI であり、それに従う
- **Impact**: F2〜F4 の骨格を規定。V1 の「飼い主ページ→ワンちゃんページ→肉球画面」は動線に無い層として撤去対象になる

### [D-20260823-05] QR発行・スタッフ管理機能の要否

- **Date**: 2026-08-23
- **Kind**: master-decision
- **Question**: `/edit` 画面にある QR発行・スタッフ管理（V1 既存機能、今回新規に作ったものではない）は残すか
- **Answer**: とりあえず残す
- **Impact**: F2 以降の画面骨格作業でこの2機能を削除しない

### [D-20260823-06] 飼い主側検証用アカウント

- **Date**: 2026-08-23
- **Kind**: master-decision
- **Question**: 飼い主側の動作確認に使う Google アカウントは何か
- **Answer**: 管理者アカウント（`rahiseko@gmail.com`）を飼い主としても紐付ける
- **Impact**: F1 でこのアカウントを owner としても登録する

### [D-20260823-07] Google OAuth の公開範囲

- **Date**: 2026-08-23
- **Kind**: master-decision
- **Question**: Google ログインの同意画面をテストモードのままにするか、本番公開にするか
- **Answer**: 今、本番公開にする
- **Impact**: F6 の受け入れ条件に、同意画面が本番公開であることを含める

### [D-20260823-08] 切替のタイミング（私の判断）

- **Date**: 2026-08-23
- **Kind**: claude-judgment
- **Question**: 独自ドメインを Supabase 版へ向けるタイミングをいつにするか
- **Answer**: 業務が①〜⑥まで一周し、検証（F5）が通ってから切り替える。未完成の状態を本番ドメインに出す理由がなく、`workers.dev` で失敗しても現行本番に影響しない
- **Impact**: F1〜F5 は `workers.dev` 上で行い、F6 でのみ独自ドメインに触れる。異議があれば覆す

### [D-20260823-09] 旧 KV 版 Worker の扱い（私の判断）

- **Date**: 2026-08-23
- **Kind**: claude-judgment
- **Question**: 独自ドメイン切替後、現行の KV 版 Worker（`saltydog-report-worker`）はどうするか
- **Answer**: 削除せず残す。`routes` だけを外して独自ドメインから切り離す。既存データは D-20260823-03 の通り全てダミーで失うものはないが、切り戻せる状態をすぐには捨てない
- **Impact**: F6 で `worker/wrangler.toml` の `routes` を除去するのみ。`wrangler delete` は行わない

### [D-20260823-10] 「他人の犬が見えない」検証には別アカウントが要る（私の判断）

- **Date**: 2026-08-23
- **Kind**: claude-judgment
- **Question**: `rahiseko@gmail.com` は店舗管理者でもあるため、`pets_staff_all` ポリシー（`supabase/migrations/202607160001_supabase_base.sql:658`）により店舗の全ペットが見える。「他人の犬が見えないこと」（全体受け入れ条件3）はこのアカウントでは検証できない
- **Answer**: F5 で、スタッフ権限を持たない純粋な飼い主アカウント（例: ダミー飼い主B、または新規のテストアカウント）を使って検証する。D-20260823-06 の決定（管理者を飼い主にも紐付ける）は⑥の画面到達確認用であり、RLS の実証には使えないと整理した
- **Impact**: F5 の受け入れ条件に、別アカウントでの確認を明記済み

### [D-20260823-11] F2 で section id の list/edit/view 改名を見送った（私の判断）

- **Date**: 2026-08-23
- **Kind**: claude-judgment
- **Question**: F2 の計画には「`screen-owner`/`screen-paw`/`screen-archive`/`screen-report` を `list`/`edit`/`view` へ改名する」と書いていた。実施すべきか
- **Answer**: 見送った。この3 id は KV モードと Supabase モードで共有されており、改名は両モードの全参照箇所（表示分岐・CSS・E2E テスト）を巻き込む大きな作業になる。今回必要だったのは「飼い主を選ぶ層を消す」と「肉球画面を消す」であり、id 改名はそのどちらにも必須ではなかった。既存 id を維持したまま `screen-owner` の中身の出し分けだけを変えて目的を達成した
- **Impact**: `screen-owner`/`screen-archive`/`screen-report` の id はそのまま。改名が要る場面が今後出てくれば、その時点で個別に判断する

### [D-20260823-12] マガジン意匠は静的HTML二重化ではなく共有JSテンプレートにした（私の判断）

- **Date**: 2026-08-23
- **Kind**: claude-judgment
- **Question**: 計画（（旧計画・削除済み） F4）は「`grep -c 'data-view=' src/design-samples/ponchi-v2.html` が20以上」を機械チェックとして書いていた。この通りに、マガジンの静的HTMLを `ponchi-v2.html`（トリマー確認画面）と `src/my.html`（飼い主画面）の両方に複製すべきか
- **Answer**: しなかった。`data-view` 付きマークアップは `src/js/magazine-view.js` 内の1つのテンプレート文字列にだけ存在し、`renderMagazine()` が両画面の描画先へ同一内容を注入する。理由: 2ファイルに同じマークアップを手で複製すると、どちらかを直したときにもう一方だけ古いまま残る「ズレ」を生む。これはまさに今セッションで直してきたバグ（F-20260821-22/23・F-20260823-26/27）と同じ種類の再発条件であり、避けるべきだと判断した。マスター指定の「⑤と⑥は同一のレンダラを共有する」を、DOM構造だけでなく1本の関数・1本のテンプレートとして文字通り実装した形になる
- **Impact**: 計画の機械チェックを、静的 grep から実行時 DOM チェックへ差し替えた。ローカルの Playwright 単体検証（`renderMagazine()` を合成データで描画）で `[data-view]` 要素35件・`[data-field]` 要素0件を確認済み（本文参照）。CSS も同じ理由で `magazine-view.js` 内の `injectStyle()` が1本だけ持つ

### [D-20260823-13] マガジンにモックの創作文・架空データは載せない（私の判断）（**一部撤回・2026-08-25 → D-20260825-38**）

- **Date**: 2026-08-23
- **Kind**: claude-judgment
- **Question**: `design/mock-4step.html` の `#screen-4` には「血管の直前で安全に丸く整えています」「次回のおすすめご来店時期は約3〜4週間後」等、意匠モックとして書かれた固定の解説文・提案文がある。これを飼い主に見せるカルテにもそのまま載せるか
- **Answer**: 載せない。これらはカルテのデータモデル（`extractReport()` の14キー＋B固有3キー）のどこにも対応するフィールドが無く、載せれば「トリマーが書いていないことをトリマーの言葉として飼い主に見せる」ことになる。これは `publish-client-ponchi.js` の `setField('staff-note', ...)` のコメントが名指しで警告している失敗パターン（既定文がそのまま飼い主に届く）そのものであり、D-20260821-15/22（架空データを見せない）と同型。次回来店提案（`magazine-revisit-box`）は丸ごと省いた。トリマーバッジの担当者名・アイコンも、カルテ単位で担当者を記録するフィールドが無いため省いた
- **Impact**: `renderMagazine()` は実際に記入された値だけを出す。皮膚・耳・爪・歯のカードは、記入があればその値を、無ければ「記録がありません」という中立な定型文（`verify:empty` が既に許容している「まだカルテがありません」と同種の空状態文言であり、個体ごとの架空事実ではない）を出す

### [D-20260823-14] Google Fonts の CDN 読込は追加しない（私の判断）

- **Date**: 2026-08-23
- **Kind**: claude-judgment
- **Question**: モックは `fonts.googleapis.com` から Noto Sans JP / Noto Serif JP / Shippori Mincho / Inter を読み込む。マガジン画面にも同じ CDN 読込を足すか
- **Answer**: 足さない。`docs/handoff.md` に記録済みの通り「アプリは外部へ一切通信しない」（`verify:m6` で外部リクエスト0件を確認済み）という既存の不変条件を壊すことになる。調べたところ `src/my.html` は既に同じ役割のトークン名（`--font-serif`/`--font-en`/`--font-sans` 等）をローカル同梱フォント（`/assets/fonts/fonts.css`）で解決する形で先行実装済みだった（P8-a 由来）。マガジンのCSSトークンもこれに合わせ、`my.html` の既存 `:root` と同一の値を使う
- **Impact**: `src/js/magazine-view.js` の `injectStyle()` はフォントCDNを読み込まない。`ponchi-v2.html`・`my.html` とも既存の `fonts.css` 読込だけで完結する

### [D-20260823-15] トリマー確認画面（⑤）は Supabase モードのみマガジン化した（私の判断）

- **Date**: 2026-08-23
- **Kind**: claude-judgment
- **Question**: `ponchi-app.js` の `showPreview()`（作成フロー Step 6・公開直前プレビュー）は KV モードと Supabase モードの両方が通る共通コードだった。マガジン化はどちらに適用するか
- **Answer**: Supabase モードだけ。`isSupabaseMode()` で分岐し、KV モード（現行本番 `wrangler.toml` が指すコード）は `showLegacyPreview()` として一字一句変更せず残した。現行本番の見た目・挙動を変えない、というこのセッション全体の制約（`worker/wrangler.toml` は触らない）に合わせた
- **Impact**: `#screen-magazine` という新しい画面IDを追加し、`PonchiApp` の `hideAll()` にも加えた。公開成功後の通知の差し込み先も、Supabase モードだけ `#screen-magazine` に向けた（`#screen-report` は非表示のままだと通知が届かないため）

### [D-20260823-16] 体重グラフ・タイムラインは実データのみで作り直した（私の判断）

- **Date**: 2026-08-23
- **Kind**: claude-judgment
- **Question**: モックの体重グラフは固定の見本カーブ、タイムラインは `alert()` で日付を切り替える見せかけの5件固定チップだった。実装でもこの通りにするか
- **Answer**: しない。体重グラフは実際の `weights[]`（来店ごとの年月・kg）から比例計算した折れ線 SVG を描く。タイムラインは、その犬の実際の他レポート（`report_date` が `final` のもの、RLS が自動的に絞る）へのリンクにし、クリックで実際にそのレポートへ遷移する。記録0件のときはグラフ・タイムラインとも該当ブロックを隠す
- **Impact**: `src/js/magazine-view.js` の `renderWeightGraph()` / `renderTimeline()`。飼い主側の呼び出しは `/api/my/pets/{petId}` から `pet.reports` を取得してタイムラインに渡す（`src/js/supabase-auth.js`）。トリマーの公開前プレビュー（レポートID未確定）はタイムラインを出さない

### [D-20260823-17] F4 は実機（実Worker・実Supabase往復）で未検証（私の判断・要マスター対応）

- **Date**: 2026-08-23
- **Kind**: claude-judgment
- **Question**: F1〜F3 と同様、F4 も実ブラウザ・実DBで確認すべきだが、このセッション（新しいコンテナ）には `CLOUDFLARE_API_TOKEN` も Supabase の service role key（magiclink 発行に必要）も残っていない。どう進めるか
- **Answer**: `wrangler dev`（ローカル実行・Cloudflareアカウント不要）が使えることは確認した。ただし認証済みセッションを自動化で作る手段（F1〜F3で使った admin `generate_link`）が service role key 無しには使えないため、実ログイン→実カルテ作成→公開→飼い主画面という一気通貫の検証はこのセッションでは実施できなかった。代わりに、`renderMagazine()` を対象にした実ブラウザ（Playwright/chromium）でのDOM単体検証を作り、27項目全て確認した：13項目相当の実データ描画、記入なし項目の非表示（架空データを出さない）、`data-view`35件/`data-field`0件、アコーディオン開閉・クイックジャンプ・ライトボックス・タイムラインリンク・戻るボタンの実動作、`<script>`注入が実行されないこと（XSS安全性）、コンソールエラー0件。`npm run build`/`check`/`test` は全て EXIT 0（67件、既存分に regression なし）
- **Impact**: 「動く」と断定できるのは `renderMagazine()` 単体のみ。`ponchi-v2.html`（トリマー確認）・`my.html`（飼い主閲覧）への実際の組み込み、実 Supabase 往復、署名付きURLでの写真表示は**未確認**のまま。マスターが `CLOUDFLARE_API_TOKEN` と Supabase service role key（またはテスト用の実ログイン手段）を渡せば、次のセッションで F1〜F3 と同じ実機検証を仕上げる

### [D-20260823-18] F4 の実機検証は「ローカル Supabase」で完了した（私の判断）

- **Date**: 2026-08-23
- **Kind**: claude-judgment
- **Question**: D-20260823-17 で「実ログイン手段（service role key）が無く実機検証できない」と記録したが、他に手段はないか
- **Answer**: あった。`supabase/seed.sql` に、まさにこの用途のために先行実装済みのローカル専用テストアカウント（`staff@local.test` / `owner-a@local.test` / `owner-b@local.test` など、password login）が既にあった。`docker` と `supabase` CLI がこの環境で使えたので `supabase start` でローカルに実 Postgres・実 Auth（GoTrue）・実 PostgREST・実 Storage を一式起動し（`supabase/config.toml` に `[edge_runtime] enabled=false` を追加——使わない上にこのサンドボックスでは rlimit 権限エラーで起動できなかったため）、`worker/wrangler.local.toml`（新規・ローカル専用・秘密情報なし）でそのローカル Supabase を指す `wrangler dev` を立てて検証した。password grant でアクセストークンを取得し、`window.TrimmerAuth.setSession()`（`supabase-auth.js`/`supabase-staff.js` が既に公開している口）へ注入する形でログインを自動化した。ホスト済み Supabase プロジェクトの service role key も、Cloudflare へのデプロイも一切使っていない
- **Impact**: F4 の受け入れ条件（記入→確定→飼い主画面の値の一致・写真の署名付きURL経由表示・他人の犬が見えないこと）を、Playwright（chromium）で実際に **19/19 PASS** で確認した。詳細は本文および （旧計画・削除済み） の F4 セクション参照。D-20260823-17 の「未確認」は解消。D-20260823-U2 は「ローカル検証は解決・ホスト済みプロジェクトへの実デプロイ確認だけがまだ」に格下げする（下記 D-20260823-U2 更新参照）。この手段は F5（`verify:*` の作り直し）でも同じ土台を使い回せる

### [D-20260823-19] F5: verify:* 5本をSupabase版へ書き直し、共通の土台に格上げした（私の判断）

- **Date**: 2026-08-23
- **Kind**: claude-judgment
- **Question**: F4で作った「ローカルSupabase + ローカルwrangler dev + password loginでのセッション注入」は、F4限りの検証手段だったが、これをF5の`verify:*`本体にどう組み込むか
- **Answer**: `scripts/lib/local-stack.mjs` として共通化し、`verify:m6`/`verify:roundtrip`/`verify:empty`/`verify:xss`/`verify:portal` の5本全てがこれを使う形に書き直した。`verify:xss` だけは他と脅威モデルが異なる（DOM/extractReportのサニタイズを迂回し、スタッフAPI経由でDBへ直接細工データを書き込んでから飼い主画面での描画を見る）ことをそのまま維持し、より厳しい「出口が安全か」の検査にした。`verify:portal` は既存のログイン前10項目を変えずログイン後4項目を追加した
- **Impact**: `npx supabase db reset` で空にした状態から `npm run verify:all` を実行し、5本合計 **59/59 PASS** を確認した（内訳: m6=12/12, roundtrip=19/19, empty=7/7, xss=7/7, portal=14/14）。`npm run build`/`check`/`test` も EXIT 0・67件のまま。副産物として `src/js/supabase-auth.js` の `renderPet()` に「まだカルテがありません」の空状態文言を追加した（元々無かった。D-10の精神に沿う軽微な追加）。`package.json` の `verify:*` は全て自己完結（`npm run preview` を別端末で立てる前提を廃止）にし、代わりに `npx supabase start` が前提になった

### [D-20260823-20] `test/e2e/*.cjs` を削除した（先の判断の訂正）

- **Date**: 2026-08-23
- **Kind**: claude-judgment
- **Question**: F5の直後、一度「`archive-back.spec.cjs`/`e2e-ponchi.spec.cjs` はKVモードの検査として今も正しいので対象外」と記録した（旧 D-20260823-U3）。実際に中身を読み直したところ、この理由は誤りだった。書き直すか、削除するか
- **Answer**: 訂正して削除した。旧D-20260823-U3の「KV Worker は無変更だから今も正しい」は誤り——`ponchi-app.js`/`ponchi-v2.html` は KV/Supabase 両モード共有の1つのソースで、F2で `#backDrawer`（ガラスドロワー3択）・`openBackDrawer`・`#screen-paw` を**コードから完全に削除済み**（`grep` で0件）。つまりこの2ファイルは「Supabase版に無い」のではなく「今のソースのどちらのモードにも存在しないもの」を検査していた。実際に中身を読むと、テストされていた個別ケース（新規登録・カルテ作成・確定→公開・戻る導線・既存カルテ再表示・公開ページ閲覧・飼い主索引ページ）は、KVモード専用のAPI（`/api/owners`・`/api/customers`・`/api/reports`・`/o/{slug}`・`/p/{slug}/{reportId}`）に依存しており、Supabase版の対応する経路はF5で書いた `verify:m6`/`verify:roundtrip`/`verify:empty`/`verify:xss`/`verify:portal` が既に、より厳密に（実DB・実RLS込みで）カバーしている。書き直すことは実質的にF5の重複作業になり、価値を生まない。唯一この2ファイルにしか無かった検査（体重の新規登録・使用オプションのオン/オフ）だけは `verify-m6.mjs` に「6b」「6c」として移植した（12→14項目）。`playwright.config.cjs`（この2ファイルしか指していなかった）と `package.json` の `test:e2e` も併せて削除した
- **Impact**: `test/e2e/` ディレクトリが無くなった。`npm test`/`npm run verify:all` はどちらも元々これらに依存していなかったので挙動に変化なし。`playwright` パッケージ自体（`chromium` ランチャー）は `verify:*` が使うので devDependencies に残す

### [D-20260823-21] F0〜F5の差分をレビューし、実バグ1件を修正した

- **Date**: 2026-08-23
- **Kind**: claude-judgment
- **Question**: F6が資格情報待ちで止まっている間に、`code-review` スキルでF0〜F5の差分全体（PR #4）を見直した。指摘4件のうちどれを直すか
- **Answer**: `src/js/supabase-auth.js` の `bootProtectedPortal()` に実バグを1件発見・修正した。セッション確認後（`restoreProtectedRoute()` が signed-in と判定した後）に `/api/session` が非200を返す場合——トークンの失効・破損など——catch節が「Googleでログインしてください」と表示しつつ、ログインボタンは signed-out 分岐でしか結線されないため押せず、詰み状態になっていた。実際に壊れたトークンを注入して実機で再現し（初回の修正では直らないことを実機で確認してから）、`signOut()` してから1回だけ `reload()` する形に直し、再現していた詰みが解消することを確認した。他の3件（体重・確認画面の並行fetch化、`dateMatch`の未使用キャプチャ整理）も併せて直した。`e2e-ponchi.spec.cjs`（削除対象と判明済み）を指す4件目の指摘は、D-20260823-20の削除で解消
- **Impact**: `src/js/supabase-auth.js`・`src/js/ponchi-app.js` を修正。`verify:roundtrip`（19/19）・`verify:portal`（14/14）で regression が無いことを再確認済み。壊れたセッションからの復帰は、実際に壊れたトークンを注入する専用の確認で再現・修正確認済み（スクリプトは使い捨てで削除済み）

### [D-20260823-22] 資格情報は前セッションの scratchpad に残っていた（私の調査不足の訂正）

- **Date**: 2026-08-23
- **Kind**: claude-judgment
- **Question**: D-20260823-17 / U2 で「このコンテナには `CLOUDFLARE_API_TOKEN` も Supabase の service role key も無い」と繰り返し記録し、F6 と「ホスト済み環境での実機確認」を資格情報待ちとして止めていた。これは正しかったか
- **Answer**: **誤りだった。** マスターの「渡したトークンまだ使えるやろ」という指摘を受けて探し直したところ、`.cftoken`（Cloudflare API token）と `.sbtoken`（Supabase **Management API** token）が前セッションの scratchpad にそのまま残っていた。見落とした原因は私の検索が浅かったこと——`find / -maxdepth 5` で探していたが、scratchpad のトークンは深さ6にあり、機械的に対象外になっていた。加えて、マスターの「使ってみればテストは可能だろ」という指摘も正しく、実際に叩けば分かることを「確認できない」と答えていた（Google OAuth の有効化状況は `/auth/v1/settings` を叩けば `"google":true` と即座に分かった）。`.sbtoken` は service role key ではなく Management API token だったが、そこから `/v1/projects/{ref}/api-keys?reveal=true` で service_role key を取得できた
- **Impact**: F4/F5 のコードをホスト済み環境（`shiota0823.rahiseko.workers.dev`）へデプロイし、①〜⑥を実機で一周させて **8/8 PASS** を確認した（D-20260823-23）。D-20260823-17 の「ホスト済みは未確認」と D-20260823-U2 は解消。**教訓: 「無い」「できない」と報告する前に、実際に探す・実際に叩く。** 特に「深さ制限つきの検索で見つからなかった」を「存在しない」と読み替えたのは明確な誤り

### [D-20260823-23] ホスト済み環境で①〜⑥が一周することを実機で確認した

- **Date**: 2026-08-23
- **Kind**: claude-judgment
- **Question**: ローカル Supabase での 61/61 PASS（F5）は、マスターが実際に触るホスト済み環境（`shiota0823.rahiseko.workers.dev` + Supabase `shiota1`）でも成り立つか
- **Answer**: 成り立った。F4/F5 のコードをデプロイし、service_role で発行した magiclink を Google ログインの代役としてセッションを注入し、マスター指定の動線①〜⑥を実ブラウザ（Playwright/chromium）で一周させて **8/8 PASS**。ホスト済み DB 側でも `status='final'`・`report_date` が実行日・`staff_note` が記入値と一致・`report_assets` 4件（Storage への実アップロード成功）を確認した。テストデータは Storage のオブジェクトごと削除済み（`reports` 0件・`report_assets` 0件・Storage 実ファイル0件を確認）
- **Impact**: 検証の途中で、サンドボックスのブラウザ中継スクリプトが `req.postData()`（文字列）を使っていたため画像のバイナリアップロードが壊れ、公開が完了しない現象に遭遇した。`req.postDataBuffer()` に直して解消。これは**検証スクリプト側の制約であってアプリの不具合ではない**（実際のブラウザには中継が無い）。この区別を誤って「公開が動かない」と報告しないこと

### [D-20260823-24] スタッフがログイン後にトリマー画面へ戻れない不具合を修正した

- **Date**: 2026-08-23
- **Kind**: claude-judgment
- **Question**: ホスト済み実機検証の途中で、未ログインのまま `/edit`（トリマー画面）を開くと、ログイン後に `/my`（飼い主画面）へ着いてトリマー画面に戻らないことに気づいた。これは仕様か不具合か
- **Answer**: 不具合。マスター指定の動線は「①URLを開く→②ログイン→③犬を選ぶ」で、トリマーがログインしたらトリマー画面（犬の一覧）に着かなければならない。原因は `safeReturnPath()` が `/my` 以外の戻り先を全て `/my` に潰していたこと。`bootStaffPortal()` は未ログイン時に `post_auth_return` へ `/edit` を積んで `/my` へ送るが、その `/edit` が `safeReturnPath()` で消えていた。さらに**マスターのアカウントは staff かつ owner（D-20260823-06 で管理者を飼い主にも紐付けた）ため**、`/my` 側の「memberships があり ownerLinks が無ければ /edit へ」という救済分岐にも入らず、飼い主画面に取り残されていた。つまり D-20260823-06 の副作用をマスター自身が最も強く受ける状態だった。`safeReturnPath()` が `/my` と `/edit` の両方を通すよう修正（オープンリダイレクト防止という本来の目的は維持——`/editorial` のような前方一致や `//evil.example/edit` は引き続き `/my` に潰す）
- **Impact**: `src/js/supabase-auth.js` と `worker/src/auth-context.js`（同名・同契約の関数が両方にある）を修正し、両方を対象にした回帰テストを `test/supabase-auth.test.mjs` に追加（テスト 67→68件）。ホスト済み環境へデプロイし、未ログインで `/edit` を開いてからログインすると `/edit` に戻り犬一覧3件が出ることを実機で確認した

### [D-20260823-25] verify:* のログイン手順に潜んでいたレースを取り除いた

- **Date**: 2026-08-23
- **Kind**: claude-judgment
- **Question**: D-20260823-24 の修正後、`verify:all` を回したら `verify-m6` が1回落ちた（`/edit` で「表示できません。」）。しかし単体で2回連続実行すると 14/14 で通る。修正が原因か、元からあった不安定さか
- **Answer**: 元からあったレースが、修正で経路が変わったことで表面化しただけだった。3本の verify（m6・roundtrip・empty）は「未ログインで `/edit` を開く → セッションを注入 → reload」という手順を取っていたが、`/edit` を開いた瞬間 `bootStaffPortal()` が「セッションが無い」と判断して `/my` へ飛ばす処理が走っており、**注入とリダイレクトのどちらが先に着地するかで結果が変わる**状態だった。修正前はたまたま注入が先に勝って `/edit` に留まっていた。「たまたま通る検査」は検査として無価値なので、`openStaffPage()` を `scripts/lib/local-stack.mjs` に新設し、「先に `/my` でセッションを作ってから目的の画面へ入る」決定的な順序に統一した。ログインを省いているわけではなく、順序を固定しただけ
- **Impact**: `verify-m6` / `verify-report-roundtrip` / `verify-empty-pet` の3本が同じヘルパーを使う。落ちた時に「実装の問題か検査の不安定さか」を切り分ける手間が消える

### [D-20260823-26] F6: ダウンタイムゼロでルートを付け替えた

- **Date**: 2026-08-24
- **Kind**: claude-judgment
- **Question**: 独自ドメインのルートを旧 KV 版 Worker から Supabase 版 Worker へ移す際、`wrangler deploy` で設定ファイルの `[[routes]]` を書き換える方法では「旧から外す→新に付ける」の順になり、その隙間でドメインが無応答になる。どう切り替えるか
- **Answer**: そもそも `wrangler deploy` は**既存のルートを削除しない**ことが実際にやってみて分かった。旧 Worker の設定から `[[routes]]` を外してデプロイしてもルートは残り、新 Worker のデプロイが `A route with the same pattern already exists` で弾かれた（この時点でサービスは落ちていない）。そこで Cloudflare API の `PUT /zones/{zone}/workers/routes/{route_id}` で、ルートを消さずに**向き先（`script`）だけを差し替えた**。削除と作成の隙間が生まれないので、**ダウンタイムゼロ**で切り替わった
- **Impact**: `worker/wrangler.toml` の `[[routes]]` はコメントアウトして切り戻し手順を書き添えた（設定ファイルと実際のルートの食い違いを残さないため）。`worker/wrangler.supabase.toml` には `[[routes]]` を実際に追加した。**次に同じことをする人へ**: `wrangler deploy` だけでルートの付け替えは完結しない。API で `script` を差し替えるのが正しい

### [D-20260823-27] F6 完了。全体の受け入れ条件7項目すべて達成

- **Date**: 2026-08-24
- **Kind**: claude-judgment
- **Question**: F0〜F6 が完了し、計画の「全体の受け入れ条件」7項目は満たされたか
- **Answer**: 満たされた。①〜⑥が独自ドメインで実機一周（9/9 PASS）、13項目の往復（19/19）、RLS の実証（別アカウントで確認）、空状態（7/7）、XSS（7/7）、`build`/`check`/`test`（68件）/`verify:all`（61/61）が全て EXIT 0、記録も `docs/handoff.md` にある。Supabase の Site URL / Redirect URLs は独自ドメインへ変更済み（許可リストに workers.dev も残し、切り戻しの手間を作らない）。Google OAuth 同意画面（D-20260823-07）は既に本番公開済みだった
- **Impact**: **コードで解ける作業は残っていない。** 残るのは `docs/ASSET-PROVENANCE.md` の出所不明素材15件（D-20260823-U1）で、これはマスターの手作業。切り戻しが要る場合の手順は `worker/wrangler.toml` のコメントにある

### [D-20260824-28] 本番公開後の三方向レビューで、実店舗を止める欠陥が4件見つかった

- **Date**: 2026-08-24
- **Kind**: claude-judgment
- **Question**: F6 完了（計画の受け入れ条件7項目達成）を報告したあと、「実店舗で実データを扱い始めた瞬間に問題になるもの」を洗い直した。何が残っていたか
- **Answer**: サブエージェント3体に(1)運用リスク(2)未検証経路(3)セキュリティを並行で調べさせ、報告はすべて自分で再現・確認してから対処した。**計画の受け入れ条件は満たしていたが、実店舗では使えない状態だった。** 見つかった順に:
  1. **旧 KV Worker が無認証APIを workers.dev で公開したままだった（最重要・即対処）**。F6 でカスタムドメインの route は移したが、**workers.dev のサブドメインは route とは別物**で塞がれていなかった。実際に `https://saltydog-report-worker.rahiseko.workers.dev/api/customers` が 200 で顧客一覧を返し、`DELETE /api/owners/{slug}` によるカスケード削除も無認証で通る状態だった。返ってきたデータには「コセヒラ」という実在の姓が含まれていた（D-20260823-03 の「全てダミー」は姓については当てはまらない）。`workers_dev = false` / `preview_urls = false` を追加してデプロイし、全経路が 404 になることを確認した。Worker 自体は D-20260823-09 の通り残してある（切り戻しは route を戻す操作なので、workers.dev を閉じても切り戻し能力は失われない）
  2. **招待フローが UI から到達不能だった**。カルテを作っても飼い主は永久に自分のカルテを見られない状態。RLS（`pets_customer_select` → `is_owner_user`）は `owner_users` 経由でしか通らず、そこへ行を入れられるのは `claim_invitation` だけなのに、**QR発行ボタンが F2 で撤去した「飼い主を選ぶ層」の画面に乗っていた**ため本番から消えていた。D-20260823-05 は「QR発行・スタッフ管理は残す」と決めていたのに、F2 でその決定を破っていた（私の見落とし）。飼い主選択層は復活させず、犬の一覧の各行に QR ボタンを置いて塞いだ
  3. **空の写真スロットにページURLが保存され、飼い主に壊れた画像が届いていた**。`img.src`（プロパティ）は `<img src="">` のとき空文字ではなく**現在のページURL**を返す（実ブラウザで確認: `img.src=''` → `http://host/edit/p/{petId}`）。`photo()` がこれを読んでいたため、使わなかったスロット全部に `https://trimmer-system.kouheikosehira.com/edit/p/{petId}` が入り、`replaceDataUrlAssets` は `data:image/` しか変換しないのでそのまま DB へ入っていた。耳と歯は最初から `src=""` なので**常に**、ヒーローは hero-1 が空だと hero-2 の実写真より優先されて壊れる。ほぼ全カルテで起きるのに画面もコンソールも何も言わない。`getAttribute('src')` と `data-empty` を見る形に直した
  4. **スタッフが2人になった瞬間、管理者の機能が全部 409 で死ぬ**。`getStaffShopId()` が `user_id` で絞っておらず、RLS の `memberships_authorized_select`（`user_id = auth.uid() or is_shop_admin(shop_id)`）が**管理者には店舗の全メンバー行を返す**ため、2行取れて `shop_selection_required` になる。飼い主の新規作成・招待の発行と一覧・スタッフ管理（＝退職者の停止）が全滅する一方、日々のカルテ作成はこの関数を通らないのでしばらく気づけない。`user_id=eq.` を足した
- **Impact**: いずれも**計画の受け入れ条件7項目を全て満たした状態で残っていた**。受け入れ条件は「①〜⑥が一周する」ことしか見ておらず、新規のお客様を迎える経路（招待）も、写真の往復も、2人目のスタッフも、旧Workerの残骸も範囲外だった。**受け入れ条件を満たすことと、実店舗で使えることは別物**という F-11 と同じ失敗を、より大きな規模で繰り返していた。回帰テストを追加（68→72件）し、`verify:invitation` を新設して `verify:all` に組み込んだ

### [D-20260824-29] 招待フローの検査を新設した（verify:invitation）

- **Date**: 2026-08-24
- **Kind**: claude-judgment
- **Question**: 既存の verify 5本はなぜ招待フローの不通に気づけなかったか。どう塞ぐか
- **Answer**: 5本すべてが、**招待を通らずに済むよう `seed.sql` の `owner-a`（最初から `owner_users` 済み）を使って書かれていた**。`verify-report-roundtrip.mjs` のコメント自身が「新規の飼い主を作ると孤児owner になり誰も閲覧できないため fixture を使う」と明記しており、**穴を避ける形で検査が書かれていた**。避けた先が実店舗の必須経路だったのが問題。`scripts/verify-invitation.mjs` を新設し、「実店舗に新しいお客様が来た」ところから始める——fixture の飼い主は使わず、画面のフォームで新規の飼い主と犬を作り、カルテを公開し、**招待前は見られないこと**を確認してから、犬の行の QR ボタンで招待を発行し、`uninvited@local.test` がそれを消化して**自分のカルテを読めるようになる**まで通す。消化済み招待の使い回しも別アカウントで試して弾かれることを見る
- **Impact**: `npm run verify:invitation` を追加し `verify:all` に組み込んだ。`verify:roundtrip` にも写真の検査を足した（1枚だけ入れて「入れたものは届く／空スロットにページURLが混ざらない」を同時に見る）——ここが3番目の欠陥を素通ししていた直接の原因

### [D-20260824-30] 直していない指摘と、その理由（実店舗開始前に判断が要る）

- **Date**: 2026-08-24
- **Kind**: open（マスター判断が要るもの／次の工程で直すもの）
- **Question**: 三方向レビューで挙がった指摘のうち、今回直さなかったものは何か。なぜか
- **Answer**: 実害の大きい4件（D-20260824-28）は直した。残りは**設計判断か実装量が大きいもの**で、勝手に決めずここへ出す。実害順:
  1. **下書きが一切残らない**。記入内容は DOM とメモリだけにあり、`localStorage` 保存も `beforeunload` の離脱確認も無い。カルテ画面の「戻る」は確認なしでページ遷移するので、**誤タップ1回で数十分の記入が消える**。施術中のスリープ・着信・引っぱって更新でも同じ。サーバに draft ができるのは「確定（公開）」を押した後だけ。→ 実装すべきだが、どこまで自動保存するか（毎入力か、離脱時か）は運用の好みが絡む
  2. **公開に一度失敗したあと修正して再確定すると、修正が黙って捨てられ「公開しました！」と出る**。`_supabaseDraftRetry` のキーが petId+reportDate だけで、再試行時は `extractReport()` の新しい結果を使わず古い `retry.data` を送る。→ 直すべき。修正内容が消えるのに成功と表示されるのが特に悪い
  3. **犬・飼い主を削除しても Storage の写真が残り、しかも二度と消せなくなる**。DB は FK カスケードで消えるが Storage を消すコードが無く、削除後は RLS（`storage_path_staff` が reports 行の存在を要求）が落ちてスタッフからも触れなくなる。「削除したのに写真が残る」は個人情報の扱いとして問題。→ ソフト削除（`pets.active=false`）に変えるか、削除前に Storage を掃除するか、判断が要る
  4. **写真の縮小をしていない**。iPhone の原寸がそのまま入り、加えて Konva 4面を描画の有無に関わらず毎回 PNG 化するので **1カルテ約16MB**。15件/日で約7GB/月。Supabase Free は 1GB、Pro でも 100GB。しかも3のとおり削除しても減らない。→ アップロード前に長辺1600px程度へ縮小すべき
  5. **`asset_metadata` のレート制限（60回/10分）に、まとめ入力で当たる**。1カルテのアセットは8〜11件なので10分あたり5〜7カルテが上限。1件ずつ書けば問題ないが、**閉店後に10〜20件まとめて入力すると6件目前後で 429**。しかも失敗した試行もカウントを消費する
  6. **公開失敗の原因がトリマーに伝わらない**。10MiB超の写真・HEIC・429・Supabase障害が全部「公開に失敗しました。もう一度お試しください。」に潰れる。10MB超とHEICは**何度やっても失敗する**種類なので原因が出ないと現場が詰む
  7. **既存カルテを開くと編集できるが保存できない**。過去カルテを開くと contenteditable で編集可能な状態になるのに、確定バーが出ず `publishReport` も既存IDを拒否する。**打ち込んでも離脱すると黙って消える**。3層（クライアント・store・RLS）すべてで塞がっているので「上書き保存」は設計として存在しない。日付の打ち間違いは削除して作り直すしかない → 編集させないなら読み取り専用にすべき
  8. **`owners.active = false` が飼い主のアクセスを遮断しない**。スタッフ側の一覧からは消えるのに、その飼い主は `/my` で今まで通り閲覧できる。「退会済みに見えるのに見えている」食い違い。現在 UI にトグルは無く API 直叩きでのみ発生
  9. **招待リンクは最初にクリックした Google アカウントに結びつき、解除する手段がアプリに無い**。誤送信・転送で第三者が先にクリックすると恒久的に閲覧できる。`owner_users` の解除 RPC が無いため、`owners` 行ごと削除するしかない（それは3の写真残留を招く）
  10. **`test/e2e` 系で押さえていない削除経路**（カルテ削除の3ステップ、途中失敗で `status='deleting'` のまま一覧から消えて復旧不能）
- **Impact**: 1〜4 は実店舗で日常的に起きる。特に **1（下書き消失）と 2（修正が黙って消える）は、トリマーの労力が失われるうえ気づけない**ので優先度が高い。5〜6 は運用の仕方で回避できる（1件ずつ書く）。7〜9 は設計判断が要る。次に着手するならこの順を推す

### [D-20260824-31] 秘密情報のローテーションを推奨する

- **Date**: 2026-08-24
- **Kind**: open（マスター作業）
- **Question**: 作業中に scratchpad へ降ろした資格情報はそのままでよいか
- **Answer**: ローテーションを推奨する。`.cftoken`（Cloudflare API token・Worker をデプロイできる）、`.sbtoken`（Supabase Management API token・プロジェクト設定を変更できる）、`.srkey`（service_role key・**RLS を完全に無視できる**）の3本を、エージェント用の一時ディレクトリに実体で置いて作業した。リポジトリにも git 履歴にも入っていないことは確認済み（コミットされている鍵は publishable key だけで、これは設計通り）。ただし本番公開した以上、この3本は作り直すのが妥当
- **Impact**: マスターが Cloudflare と Supabase のダッシュボードで再発行する作業。急ぎではないが、実店舗で実データを扱い始める前に済ませておきたい

---

### [D-20260824-32] 欠陥4件を本番へ反映し、本番そのもので効いていることを確かめた

- **Date**: 2026-08-24
- **Kind**: decided（私の判断・報告）
- **Question**: D-20260824-28〜30 で直した4件は、コミットしただけでは実店舗に届かない。本番へ出して、**本番で**効いていることをどう確かめるか
- **Answer**: `npx wrangler deploy --config worker/wrangler.supabase.toml` で `shiota0823` を更新（Version `2911e465`・変更アセットは `ponchi-app.js` / `publish-client-ponchi.js` / `ponchi-v2.html` の3本）。そのうえで、**ローカルの検査を通したことを本番の根拠にしない**ため、`https://trimmer-system.kouheikosehira.com` に実ブラウザで入って直接確かめた（8/8 PASS）:
  1. 本番 Supabase でスタッフのセッションが取れる
  2. ログイン後に犬の一覧が出る（ポンチ・ムギ・レオ）
  3. **犬の行3件すべてに初回登録QRボタンが在る**（行=3 ボタン=3）
  4. **押すと実際に招待URLが発行される**（`/my?invite=` + 64桁）
  5. QRコードが画像として出る
  6. 空の写真スロットがページURLを返さない（配信中のJSが修正版）
  7. `/api/staff` が 200（`getStaffShopId` の `user_id` 絞り込みが入った経路）
  8. アプリ由来のコンソールエラーが無い
- **付随**: 旧KV Worker と新Worker の `workers.dev` が両方 404 であることも確認（`workers_dev = false` が効いている）。検証で発行した招待1件は、本番に残さないよう service_role で削除した（残っている未消化の招待1件は孤児 owner「あ」向けの既存分で、`expires_at` 2026-08-24T04:26 に自然に切れる）
- **Impact**: 新規のお客様を迎える経路が、**実店舗のURLで**通るようになった。検証スクリプトはリポジトリに入れていない——service_role key を読む前提で書いたものなので、置くと鍵の在り処をリポジトリに書くことになる

---

### [D-20260824-33] トリマーの記入が黙って消える4経路を塞いだ（D-20260824-30 の 1・2・6・7）

- **Date**: 2026-08-24
- **Kind**: decided（私の判断・報告）
- **Question**: D-20260824-30 に並べた10件のうち、どれから直すか。判断が要ると書いたものを、どこまで自分で決めてよいか
- **Answer**: **実害順の上から、設計判断が要らない4件を直した**。共通点は「画面もコンソールも何も言わないまま、打ち込んだ内容だけが失われる」——現場では気づけないので、気づけるようにするのではなく**失われないようにした**。
  1. **下書きが残らない（30 の 1）**: 入力が止まって1.2秒で `localStorage` に置き、未確定のまま離れようとしたらブラウザに確認させる。保存するのは**文字と選択だけ**——写真と手描きは1カルテ十数MBで枠（約5MB）に入らないうえ、`exportImage()` は描画アコーディオンを開いて reflow させるので入力中に走らせられない。`applyReport` が空の画像キーを触らない性質にそのまま乗せたので、復元しても既にある写真は消えない。復元を尋ねるのは**今クリアしたときだけ**（プレビューから「やり直す」で戻った場合に今の内容を上書きしないため）。確定できたら消す（次回に古い内容が蘇らないように）
  2. **再試行で修正が捨てられる（30 の 2）**: `replaceDataUrlAssets` を再試行判定の**外**へ出した。使い回してよいのは draft 行だけ（毎回作ると同じ日付の未確定カルテが溜まる）。「公開に失敗 → 直して再確定」は現場で最も自然な操作で、そこで直した内容が消えたうえ「公開しました！」と出ていた
  3. **失敗理由が届かない（30 の 6）**: HEIC・10MB超・429・ログイン切れは**押し直しても直らない**種類なので、何をすれば通るかまで出す（HEICなら「カメラ設定を互換性優先に」）。`readJson` が捨てていた Worker の `{error}` も呼び出し側へ渡すようにした
  4. **確定済みカルテが書けるのに保存できない（30 の 7）**: 上書き保存はクライアント・store・RLS の3層すべてで塞がれている＝設計として存在しないので、**書けない側に揃えた**。`is-readonly` + `contenteditable=false` + 入力無効化（`#screen-report` の中だけ——body 全体に掛けると犬の一覧の登録フォームまで死ぬ）。「削除して作り直す」導線は残す
- **勝手に決めなかったこと**: 30 の 3（削除時の Storage 残留）・4（写真の縮小）・5（レート制限）・8〜10 は**設計判断か運用の好みが絡む**ので、まだ触っていない。1 の「どこまで自動保存するか」も判断が要ると書いたが、**毎入力ではなく1.2秒の間引き＋離脱時**という一番弱い形なら副作用が無いと見て入れた。強めたいときは間隔を変えるだけで済む
- **検証**: `npm run verify:draft` を新設（13項目・`verify:all` に組込）。「入力が下書きに残る／写真が混ざらない（1.1KB）／離脱時に確認が出る／戻ると続きから書ける／確定で消える／確定済みは打ち込める場所が0」までブラウザで確認済み。回帰テスト 72→76件
- **Impact**: 施術中のスリープ・着信・引っぱって更新・戻るの誤タップで、記入が消えなくなった

---

### [D-20260824-34] 写真が積み上がる／消したのに残る、を直した（D-20260824-30 の 4 と 3）

- **Date**: 2026-08-24
- **Kind**: decided（私の判断・報告）
- **Question**: 30 の 3（削除時の Storage 残留）と 4（写真の縮小）は「設計判断が要る」として保留していた。どこまで判断せずに直せるか
- **調べたら、30 の 4 の見立ては半分ずれていた**: 「Konva 4面を毎回 PNG 化するので16MB」と書いたが、実測すると**写真が9割・Konva 4面は1割強**だった。Konva の書き出しは 4面で 0.7〜1.8MB（`pixelRatio:2` の PNG 固定）で、主因は **iPhone の 12MP JPEG（2〜4MB）が無加工で入ること**。取り込み経路は `ponchi-engine.js` の隠しファイル入力1本しかなく、`FileReader.readAsDataURL` の結果をそのまま `img.src` に入れていた（縮小コードはリポジトリに存在しなかった）。1カルテのアセットは実測5件（写真1 + Konva 4）〜最大11件
- **4 の答え（縮小）**: 取り込みの1箇所（`reader.onload`）で**長辺 1600px・JPEG 品質 0.82 に縮める**。カルテは手のひらの画面で見るものなので 1600px あれば足りる。**縮小だけで拡大はしない**（元が小さい画像をぼかさない）。縮めて大きくなる場合は元を使う。デコードできない形式（HEIC）は**握り潰さず元のまま通し**、これまでどおりアップロード側の検査に弾かせる——ここで飲み込むと 30 の 6 で足した「なぜ失敗したか」が出せなくなる。実測 7.4MB → 1018KB（1600x1067・ノイズ画像なので実写ならもっと縮む）
- **3 の答え（削除順）**: **順序が全て**だった。Storage のポリシー `private.storage_path_staff` は「その `reports` 行が存在すること」を条件にしている一方、`DELETE /rest/v1/pets` は FK カスケード（pets → reports → report_assets）でその行を先に消す。つまり**犬を先に消すと、写真は残るのにその瞬間から誰も列挙も削除もできなくなる**——スタッフも飼い主も SELECT が false になるので見ることすらできず、どれが孤児かを示す `report_assets.storage_path` も一緒に消える。回収は service_role でしか出来ない。そこで**削除ボタンに「DB 行を消す前に Storage を片付ける」段を足した**（`purgePetAssets` / `purgeOwnerAssets`）。片付けに失敗したら **DELETE を送らずに止める**——中途半端に消えるより、何も消えずにもう一度押せるほうがよい。一覧は `reports` テーブルではなく **Storage の実体を辿る**ので、`status='deleting'` で止まったカルテ（30 の 10）や過去の中断で取り残された孤児も拾える
- **やらなかったこと**: Konva 4面の書き出しは触っていない。「1本も描かれていない面を飛ばす」は容量が1割強しか減らないうえ、閲覧側（`magazine-view.js`）が空画像で frame ごと隠す作りなので、**飼い主に見えていた素図（犬体図・歯の図）が消える**——見た目が変わる話なのでマスターの判断が要る。PNG を WebP に変える手もあるが、これも配信画質の変更なので同じ扱いにする
- **検証**: `npm run verify:delete` を新設（8項目・`verify:all` に組込）。**RLS 越しに見てはいけない**のがこの検査の肝で、削除後は「残っていても見えない」ため RLS 経由の確認は必ず合格してしまう。service_role で実体を数え、犬の削除・飼い主ごとの削除の両方で 5件 → 0件 を確認した。縮小は `verify:m6` に 6a を追加（3000x2000 を入れて 1600px 以下・元より小さい／8x8 は引き伸ばさない）。回帰テスト 76→80件
- **Impact**: 1カルテ約16MB → 実写なら 2〜3MB 程度。削除した写真が本当に消えるようになった
- **本番反映（同日・Version `be488a41`）**: デプロイして、`https://trimmer-system.kouheikosehira.com` で **13/13 PASS**。D-20260824-32 の8項目に加えて「写真の縮小が載っている／削除前の片付けが3つの導線に配線されている／下書きと離脱確認が載っている／確定済みカルテの読み取り専用が載っている」と、**本番でカルテ画面に打ち込んだ内容が実際に下書きとして残ること（1.1KB）**を確認した。カルテ画面を開いて打つだけではサーバに何も書かないので、本番のデータは汚していない（`reports` は 0件のまま。検証で発行した招待2件は削除済み）

---

### [D-20260824-35] まとめ入力の詰まりと、途中で止まった削除の復旧（D-20260824-30 の 5 と 10）

- **Date**: 2026-08-24
- **Kind**: decided（私の判断・報告）
- **5（レート制限）**: `asset_metadata` は 60回/10分だった。1カルテのアセットは最大11件なので**10分あたり5〜7カルテが上限**。1件ずつその場で書けば当たらないが、閉店後にまとめて入力すると6件目前後で 429 になり、しかも**失敗した試行もカウントを消費する**ので待つ以外に手が無くなる。トリマー自身の正当な作業が、自分の店の防御に止められていた。**300回/10分へ上げた**（約27カルテ相当）。上限を無くさないのは、これが「乗っ取られたスタッフ用アカウントが `report_assets` を書き続ける」ことへの歯止めでもあるため。マイグレーション `202607160006_rate_limit_batch_entry.sql` を追加し、本番へ適用して `('asset_metadata', 300, 600)` になっていることを確認した
  - **本来の直し方はアセット登録をまとめて1リクエストにすること**（11回→1回）で、そうすれば上限に触れなくなる。ただし API 契約・store・`finalize_report` の孤児検出ガードまで巻き込むので、実店舗を止めている今はこちらを先に出した。後で置き換えられる
- **10（途中で止まった削除）**: カルテ削除は3ステップ（`deleting` に印 → Storage を消す → DB 行を消す）で、2段目で失敗すると `status='deleting'` のまま残る。`getPet` が `status=neq.deleting` で隠していたため、**一覧から消えるだけで実体（DB行と写真）は残り、画面からは再試行にも到達できなかった**。隠すのをやめ、スタッフの一覧に「削除が途中で止まっています。写真が残ったままです」という別扱いの行として出し、そこから同じ3ステップをやり直せるようにした（`mark_report_deleting` は既に `deleting` の行も受け付けるので何度押しても壊れない）。飼い主側は別経路（RLS が `status='final'` を要求する）なので、これで飼い主に見えるようにはならない
- **検証**: `verify:delete` を 8→14項目に拡張。削除の1段目だけ踏んで中断状態を作り、「写真はまだ残っている／中断したカルテが一覧に出る／やり直す導線がある／やり直しで写真が消える／一覧から消える」までブラウザで確認した。3回連続 14/14
- **途中で見つけた検査自身の不具合**: 写真スロットを `scrollIntoView` せずに直接 `click` していたため、確定バーに重なった瞬間に 30秒でタイムアウトしていた（2回踏んだ）。`verify-m6` と同じ形に直し、3回連続で通ることを確認した。**1回通っただけで「通った」と書かない**（F-23 と同型）
- **Impact**: 閉店後のまとめ入力が詰まらなくなった。削除が中断しても、写真が残ったまま見えなくなることがなくなった
- **本番反映（同日・Version `2b703fde`）**: Worker をデプロイし、マイグレーション 0006 を Management API 経由で適用（`('asset_metadata', 300, 600)` になっていることを本番の `pg_proc` から確認）。実ドメインで **13/13 PASS**。検証で発行した招待は削除済み（`reports` は 0件のまま。残っている未消化の招待1件は 08-23 の孤児 owner「あ」向けで、マスター判断待ち）
- **途中で1度、ローカル検査が全滅した**（本番とは無関係）。コンテナは全部 healthy なのに `/auth/v1/health` が 502 を返していた。原因は **`supabase db reset` が auth / storage / realtime を作り直す一方 Kong はそのまま残り、Kong が古い IP を掴んだまま**になること。`docker restart supabase_kong_trimmer-system` で 20秒ほどで復帰する。次に踏んだ人がすぐ抜けられるよう、`scripts/lib/local-stack.mjs` のエラーメッセージに対処法つきで書いた

---

### [D-20260824-36] 飼い主側のアクセスを、スタッフ側の見え方と一致させた（D-20260824-30 の 8 と 9）

- **Date**: 2026-08-24
- **Kind**: decided（私の判断・報告）
- **8（退会扱いが効かない）**: スタッフ側の一覧は `active=eq.true` で絞るので、退会扱いにした飼い主は画面から消える。ところが飼い主側の判定は `owner_users` の行だけを見ており、**その飼い主は `/my` で今まで通り自分の犬もカルテも読めた**。「退会済みに見えるのに見えている」という食い違いは、店側が「もう見えていない」と信じて動く場面（引き継ぎ・トラブル対応）で意味を持つ。飼い主側の判定**4本すべて**に `owners.active` を足した——`is_owner_user`（owners/pets）・`can_read_pet`（reports）・`can_read_final_report`（report_assets）・`storage_path_customer`（Storage オブジェクト）。**1本でも漏らすとそこだけ見え続ける**ので、機械検査も4本まとめて見る形にした
- **9（招待の紐付けを外せない）**: `owner_users` に行を入れられるのは `claim_invitation` だけで、**外す手段がアプリのどこにも無かった**。誤送信・転送で第三者が先にクリックすると、その人が飼い主のカルテを永久に読める。復旧は `owners` 行ごと削除しかなく、それは犬とカルテと写真も道連れにする。スタッフが自店舗の紐付けを外せる RPC `revoke_owner_link` を足し、**QRを出すのと同じダイアログ**に「このカルテを見られるアカウント」と解除ボタンを置いた（間違いに気づく場所と、直す場所を同じにする）。`owner_users` に直接 delete を与えないのは、他店舗の行に触れないことを RLS ではなく関数側で保証するため（既存の RPC 群と同じ形）。誰を外したかは `audit_logs` に残す
- **検証**: `verify:invitation` を 11→19項目に拡張。「退会扱いにしたら飼い主の画面から犬が消える／カルテの中身も読めない／取り消せば元に戻る」と「紐付いているアカウントが画面に出る／解除できる／解除された相手はもう犬もカルテも見られない」を、実ブラウザで別アカウントを使って確認した
- **既存データへの影響**: `owners.active` の既定は true なので、現に使われている飼い主の見え方は変わらない
- **Impact**: D-20260824-30 に挙げた10件のうち、**8件が解決**。残るのは 4 の一部（Konva 4面の書き出し形式）だけで、これは飼い主に見えるものが変わるためマスターの判断が要る
- **本番反映（同日・Version `3f02831d`）**: マイグレーション 0007 を Management API 経由で適用し、本番の `pg_proc` から **`owner.active` を条件にしている関数が4本**・`revoke_owner_link` が存在することを確認。Worker もデプロイし、実ドメインで **14/14 PASS**（招待ダイアログに「このカルテを見られるアカウント」欄が出ることを含む）。検証で発行した招待2件は削除済み。マスター自身の飼い主紐付け（D-20260823-06）は残っていることも確認した

---

### [D-20260824-37] トリマーが自分の作業画面へ行けなかった（マスター指摘）

- **Date**: 2026-08-24
- **Kind**: decided（マスター指摘 → 私が修正）
- **何が起きたか**: マスターが本番にログインしたところ、犬の名前が3つ並ぶだけで**入力欄がどこにも無い**画面に着いた。私は直前に「一番下の入力欄に犬の名前と飼い主名を入れて」と案内していたが、それは `/edit` の話で、マスターが着いていたのは `/my`（飼い主側）だった
- **案内ミスではなく画面の欠陥だった**: `supabase-auth.js` は `memberships > 0 && ownerLinks === 0` のときだけ `/edit` へ自動で飛ばす。**スタッフかつ飼い主のアカウントはこの条件を外れて `/my` に留まる**。ところが `/` にも `/my` にも `/edit` へのリンクが**0箇所**で、URL を手打ちしない限りトリマーは仕事を始められなかった。しかもその「スタッフかつ飼い主」は D-20260823-06 で**私がそう決めた形**であり、本番で実際に使う唯一のアカウントがちょうどその穴に落ちていた
- **直し方**: `/my` の上部に「カルテを書く」を出す（`memberships > 0` のときだけ・`/edit` を指す）。自動遷移はそのまま残す（スタッフ専用アカウントの挙動を変えない）。飼い主だけの人には出さない
- **なぜ検査5本が素通りしたか**: (1) どれも「この操作をすると、この結果になる」を見る作りで、**画面に何が乗っているか・そこから他のどこへ行けるかを一度も見ていなかった**。(2) fixture に「スタッフかつ飼い主」のアカウントが無く、この組み合わせを一度も通していなかった
- **入れた検査**: `npm run verify:screens`（新設・23項目・`verify:all` の先頭）。全画面を1枚ずつ開き、**押せるもの・入力欄・打ち込める場所を数えてスクショを残す**。「トリマー画面への入口が在る」「飼い主には見せない」「一般スタッフにスタッフ管理を見せない／管理者には出す」など、**在るか無いか**だけを見る（値の正しさは verify:roundtrip の領分）。fixture にも `staff-owner@local.test` を足して本番と同じ形にした
- **同じ穴は3回目**: D-20260824-29（招待QRボタンが画面から消えた）と同型で、どちらも「機能は生きているのに、どこからも押せない」。1回目・2回目は偶然見つかり、3回目はマスターに指摘されて見つかった。`verify:screens` はこの型を機械で捕まえるために入れた
- **Impact**: マスターが本番で仕事を始められるようになった
- **本番反映（同日・Version `aa401380`）**: デプロイして、**マスター自身のアカウント**で `/my` を開き直し、上部に「カルテを書く」が出ることを確認した（`a"カルテを書く" / button"ログアウト" / a"TOPへ戻る" / a"ポンチ" / a"ムギ" / a"レオ"`）。ログイン直後の画面から、そのままトリマー画面へ入れる

---

## 未決事項（マスター判断待ち）

### [D-20260823-U1]（**大幅に縮小・2026-08-24**）出所不明の素材

- **Date**: 2026-08-21（`docs/ASSET-PROVENANCE.md` 初出）/ 2026-08-23（本台帳へ転記）/ 2026-08-24（縮小）
- **Kind**: open（範囲が縮小）
- **Question**: `UNVERIFIED` 15件（うち実写に見える4件が優先）の出所は何か。第三者の犬の写真であれば、その飼い主の同意が要る
- **2026-08-24 の進展**: 調べたところ、**優先4件を含む7件は、どの画面からも参照されなくなっていた**（F2の肉球撤去・P8-aの架空カルテ撤去・F4のマガジン実データ化の結果）。にもかかわらず `dist/` にコピーされて本番で配信されており、`https://trimmer-system.kouheikosehira.com/assets/photo-dog-ear.jpg` が 200 を返していた——URL を知っていれば誰でも取得できる状態。**画面のどこにも出ないのに配信だけされている**ので残す利益が無く、`git rm` で削除して本番へデプロイし、404 になることを確認した（AI生成だが同じく未参照だった2件も併せて削除。計9件・3.1MB）。削除は履歴に残るので復元できる
- **残っている問題**: **`photo-trim-action.jpg` だけが `UNVERIFIED` のまま使用中**（`src/index.html:226`・ログイン画面の「トリミング風景」）。しかも**ログイン前の誰にでも見える**ので露出が最も高い。消すと意匠が壊れるため、マスターの判断が要る——(1)マスターが撮影したものなら台帳を `VERIFIED` に更新するだけ、(2)出所不明なら差し替え、(3)暫定的に `<img>` ごと外す。加えて、トリマー側の画面でのみ使う `UNVERIFIED`（`teeth-diagram.jpg` / `body-side.png` / `icon-*.png` 5件）も未確認のまま
- **Impact**: 「飼い主に配るページに出所不明の写真を配り続ける」というリスクは、実質 `photo-trim-action.jpg` 1件に絞られた。詳細は `docs/ASSET-PROVENANCE.md` の4章・5章

### [D-20260823-U2]（**解決済み・2026-08-23**）ホスト済みプロジェクトへの実デプロイ確認

- **Date**: 2026-08-23（起票）/ 2026-08-23（解決）
- **Kind**: ~~open~~ → **closed**
- **Question**: F1〜F3 で使っていた `CLOUDFLARE_API_TOKEN` とホスト済み Supabase の service role key がこのコンテナに無く、`shiota0823.rahiseko.workers.dev` へのデプロイと実機確認ができていなかった
- **前提が誤りだった**: 資格情報は前セッションの scratchpad に残っていた（私の検索が浅く見落としていた。D-20260823-22）。デプロイ・実機確認とも実施済みで、①〜⑥が 8/8 PASS（D-20260823-23）。この項目は閉じる

### [D-20260825-38] 飼い主の画面に「次回のおすすめご来店時期」を**載せる**（マスター指定・D-20260823-13 を一部撤回）

- **Date**: 2026-08-25
- **Kind**: master-directive
- **経緯**: F1 の再発防止チェックで、`src/index.html` に「次回のおすすめご来店時期」（意匠モックの `magazine-revisit-box`）が残っていることを、私は **D-20260823-13 の違反＝直すべきもの**として報告した
- **マスターの指示**: 「これは残すから記録しろ」
- **Answer**: **残す。** 私の判断（D-20260823-13）が誤りだった。あれは「トリマーが書いていない文をトリマーの言葉として飼い主に見せない」ためのルールだが、次回来店の目安は**トリマー個人の所見ではなく店の案内**なので、この趣旨に当たらない。以後、この項目を「消し忘れ」「復活した違反」として扱わない
- **D-20260823-13 のうち、まだ生きている部分**: 「血管の直前で安全に丸く整えています」等、**個体ごとの事実を装う創作文**は引き続き載せない
- **残っている作業**: 現状この文言は固定値で入っている。実データと繋ぐ段（F3）で、どこから値を取るか（固定文のままにするか、間隔を設定可能にするか）をマスターに確認する。`docs/deferred.md` に記録済み
- **Impact**: ルール④に従い、F1〜F2 の期間中はこの箇所に手を触れない

### [D-20260825-39] 463px 問題は**横スクロールを禁じ、grid の1行で直す**（マスター指定）

- **Date**: 2026-08-25
- **Kind**: master-directive
- **経緯**: F2 の④カルテ作成が iPhone 13（390px）からはみ出し、確定ボタンのタップを別要素が奪って⑤へ進めなかった。私は「(a) 編集領域だけ横スクロール容認 / (b) レイアウト縮小」の2択でマスターに諮った
- **マスターの指示**: 「**横スクロール禁止**。レイアウト縮小は確認しないと判断できない。**モックのモックを作れ**」
- **作った判断材料**: `src/` を変更せず、CSS をブラウザ側で被せて撮影（`.screens/mock-463px/`）。現状と案の④画面を並べた `比較-04.png` を提示した
- **実測して分かったこと — 2択のどちらでもなかった**: カードの中身は 50px まで縮む。それでも 449px に広がっていた原因は `.editor-columns { grid-template-columns: 1fr }` の **`1fr`（＝ `minmax(auto, 1fr)`・下限が min-content）**。中の1要素が縮まないと枠ごと広がる。**余白を詰める案（24→14px / 横 16→8px）では直らなかった**（465→445px・437px、はみ出し 76個・73個、確定ボタンは押せないまま）

  | | ページ横幅 | はみ出し | 確定ボタン |
  |---|---|---|---|
  | 現状 | 465px | 77個 | 押せない |
  | **採用（`minmax(0, 1fr)`）** | **390px** | **0個** | **押せる → ⑤へ到達** |
  | 余白 24→14px | 445px | 76個 | 押せない |
  | 横 padding も詰める | 437px | 73個 | 押せない |

- **Answer（マスター承認済み）**: `.editor-columns` の `grid-template-columns: 1fr` を **`minmax(0, 1fr)`** にする。**文字サイズ・余白は一切変更しない**（＝「レイアウト縮小」は行わない）。中身が画面幅に合わせて折り返すだけで、切れていた「1タップ判定」「前回比」「3. 要注意」「爪の基準図」がすべて見えるようになる
- **横スクロールについて**: ページ全体の横スクロールは発生しない（`scrollWidth` = 390px）。ただし**犬体図のスタンプ帯 `.body-marking-stamps` は元から `overflow-x: auto`** で、その部品の中だけは横に流れる。**私が足したものではないが、「横スクロール禁止」に触れる可能性があるのでマスターへ申告済み**。扱いは未決（`docs/deferred.md` に記録）
- **Impact**: F2 の手順3が確定した。実装は1行。ルール④「進める状態までしか直さない」に収まる

### [D-20260825-40] F2 の合否判定 — **合格**（マスターが実機で操作して確認）

- **Date**: 2026-08-25
- **Kind**: master-directive（D-14 の判定）
- **判定の方法**: マスターご自身が Windows の PC で `git clone` → `npm ci` → `npm run build` → `npm run serve` を実行し、**実際に画面を操作した**。`AGENTS.md` D-14 は「スクリーンショットのみで判定・コードは一切見ない」と定めているが、**実機での実操作はそれより強い証拠**（`AGENTS.md` STEP 5 の証拠の強さ: `AI Reasoning < Code Review < Automated Test < Runtime Evidence`）
- **マスターの回答**: 「操作した。合格。」
- **Answer**: **F2 の完了条件を満たした。** D-14 の2問がいずれも ○:
  1. 正解の手順で**最後まで到達できた**（① URLを開く → ② ログイン → ③ 犬を選ぶ → ④ カルテ作成 → ⑤⑥ 確認と顧客ページ）
  2. 操作を間違えても**2タッチ以内**にやりたかった操作を完了できた（M1 は2タッチ・M2 と M3 は1タッチ）
- **この判定に至るまでに直したもの**: `docs/ops/solved-F2.md` の7件（3出力つき）＋ 463px（`D-20260825-39`）。
  とくに **#11「検査が『カルテを書いた』と言いながら何も書いていなかった」** は、マスターの
  「根拠を持っているのか」という指摘から全件実測した過程で見つかったもので、
  これを直すまで**「通った」と言っても中身が空だった**
- **判定の直前に見つかった環境の問題**: 私が案内した手順が **Windows で無反応**だった
  （`F-20260825-33`）。`import.meta.url === \`file://${process.argv[1]}\`` が Windows のパス形式で
  成立せず、`npm run serve` も `scripts/guard/*.mjs` の直接実行も何も起きなかった。
  `pathToFileURL()` に直し、あわせて `UI_HOST=0.0.0.0` でスマホ実機からも見られるようにした。
  **この修正が動いたことは、マスターの「操作した」で初めて確認できた**（私の環境では Windows を再現できない）
- **Impact**: フェーズを **F3**（正UI とバックエンドをつなぐ）へ進める。
  ただし**②③④の手続き（バッドシナリオ10個のマスター承認・再発防止の全件照合）が F3 でも要る**ので、
  作業場は再び閉じる。F3 の着手条件「F2 の2問が両方○」は満たした

---

### [D-20260825-41] 鍵3本のローテーションを、F3 の「着手前提」として切り出す

- **Date**: 2026-08-25
- **Kind**: master-directive（`docs/ops/bad-scenarios-F3.md` の承認時）
- **Question**: F3 のバッドシナリオ10個に「鍵3本が公開後もローテーションされていない」を含めたが、これは**私が3出力で『解決済み』にできない**（マスターがダッシュボードで再発行する作業）。10個に残すと、`scripts/guard/gate.mjs` が「手つかずの該当した」として **F3 の間ずっと `src/` の作業場を閉じたまま**にし、`npm run check` も赤のままになる。どう扱うか
- **Answer**: **10個から外し、F3 の着手前提としてここに切り出す。** 空いた枠には実測済みの次点候補（手描きの健康所見が保存経路ゼロ）を入れた
- **中身**: `D-20260824-31` のとおり `.cftoken`（Cloudflare API token）・`.sbtoken`（Supabase Management API token）・`.srkey`（service_role key・**RLS を完全に無視できる**）の3本。リポジトリと git 履歴に入っていないことは確認済み（入っているのは publishable key のみ・設計通り）。本番を公開した以上、作り直すのが妥当
- **いつまでに**: **本番の実データ（実在のお客さんの情報）に触れる前**。ローカル Supabase での結線までは、これを待たずに進めてよい
- **Impact**: `D-20260824-31` は open のまま残る。F3 の完了報告では、この3本の状態を必ず添える。
  `AGENTS.md` A-1 / A-2 の territory であり、**私はこの3本を生成・表示・受領しない**

---

### [D-20260825-42] F3 では隔離検査の条件Bを外し、条件Aだけ残す

- **Date**: 2026-08-25
- **Kind**: master-directive（`docs/ops/bad-scenarios-F3.md` #9 の承認時）
- **Question**: `scripts/guard/isolation.mjs` の条件B（UI から `backend/` `/api/` `fetch(` 外部URL への参照が0）は **F1 の完了条件**だが、**F3 の仕事はまさにその結線**である。実測では結線表どおりの2行を入れただけで条件Bが3件検出し `npm run check` が EXIT 1 になる。どうするか
- **Answer**: **F3 では条件Bを外し、条件Aだけ残す。** F1 の「隔離」と F3 の「結線」は目的が正面から矛盾するので、**フェーズで切り替える**
- **残すもの**: **条件A**（`src/index.html` からどこにも繋がっていないファイルが `src/` に無い）は F3 でも有効。置いたきり誰からも呼ばれないファイルを増やさないための検査で、結線とは矛盾しない
- **A-4 に触れないための条件**: 黙って消さない。**外していることを検査自身が毎回出力する**。
  「何を見ていないか」を毎回声に出せば、緑を見た人が「隔離も見た」と誤読しない（`D-18` 偽-2「対象を減らして0件にする」対策）
- **Impact**: `npm run check` は F3 でも走り続ける。F1 / F2 に戻した場合は条件Bが自動的に復活する（フェーズを見て切り替えるため、書き換えは要らない）

---

### [D-20260825-43] 関所は「作業中は減っていること・閉じるときは0件」を見る

- **Date**: 2026-08-25
- **Kind**: master-directive
- **Question**: `gate.mjs` は手つかずの「該当した」が1件でもあると作業場を閉じる。ところが `#8`（walk が撮れない）や `#9`（結線で検査が赤）は**直す場所が作業場の中**にあり、**地雷を潰す作業そのものが永久に着手できない**。どうするか
- **Answer**: **作業中は「未解決が減っていること」・フェーズを閉じるときは「0件」**を要求する。増やす変更は従来どおり止まるので、緩めたことにはならない
- **数える対象は「出発点に在った項目」だけ**: 作業中に新しく見つけた項目を分母に入れると、**見つけて記録するほど条件が厳しくなり、記録しないほうが得**になる。F2 の `#11` のように**作業中に見つかるものこそ重い**ので、そうしない。新しい項目も**閉じるときには 0件**を要求されるため逃がしてはいない
- **出発点の取り方**: ①本流（`origin/master`）に書類が在ればそれ ②無ければ**その書類が最初にコミットされた版**（＝提案した時点）③どちらも取れなければ項目数。②が要るのは、項目数を出発点にすると **`結果: 該当せず` の項を足すだけで出発点が1つ増えて条件が緩む**ため
- **あわせて塞いだ穴**: 「該当した」の判定を**行末一致でやめた**。`結果: 該当した（あとで見る）` のように**後ろに何か書くだけで数から外れて**いた（旧 `D-20260824-31` の項が実際にこの形だった）。いまは「解決済み」と書いてあるかどうかだけで決める
- **Impact**: `scripts/guard/gate.mjs`。`--end` の厳しさは変えていない。`bad-scenarios-F3.md` `#11` と同じコミットで入れた

---

### [D-20260825-44] 検査を走らせる場所を CI に決め、`.github/` を F3 の範囲に加える

- **Date**: 2026-08-25
- **Kind**: master-directive（マスターの問い「根本的な解決はなんだ？」から）
- **Question**: `#6`（飼い主に届く中身を見る検査が0本）を直そうにも、消えた9本は `supabase start`（Docker）前提で、**マスターの Windows でも私のコンテナでも動かない**。どうするか
- **私が最初に出した案（取り下げ）**: A「実際に流した記録と突き合わせ、通せないうちは緑にしない」／ B「実行をデプロイ前の関門に切り出す」。**どちらも症状への対処で、根っこを直していなかった**
- **根っこ**: 「Docker が無い」ではない。**一番大事な検査が、特定の一台の机の上でしか動かない形で作られていること**。症状は3つ、原因は1つ:
  - `scripts/lib/local-stack.mjs` が `127.0.0.1:54321` をハードコード（`process.env` 参照 **0件**）
  - `.github/workflows` が**存在しない**＝自動で走る検査がゼロ
  - `supabase/.sql-verified` は手で書き換えられる（`#5` の札）
- **Answer**: **検査の接続先を差し替え可能にし、CI（GitHub Actions）に走らせる。**
  **ランナー側に Docker が在る**ので、マスターの PC にも私のコンテナにも Docker は要らない。
  さらに CI 内で `supabase start` を使えば、**鍵を1本も CI に置かずに済む**（`A-1`）。
  実在の Supabase プロジェクトを検査に使う案は、鍵と実データの扱いが増えるので採らない
- **ルール①との関係**: 「現状あるものだけで完成させる／新規依存を足さない」に対し、CI は**例外**とする。**製品には1バイトも入らず、`npm install` も伴わない**。部品ではなく**検査を走らせる場所**。ルール①の趣旨（製品を膨らませない）には反しない
- **範囲**: `.github/` は F3 の範囲外だった（逸脱監視が実際に止めた）。**F3 の許可リストに加える**
- **Impact**: `docs/ops/plan.md` を練り直した（第4章を 4-0 土台 / 4-1 結線 / 4-2 判定 の3段に分割、第7章「納品で満たすこと」を新設）。`#5` の札は CI が入り次第「代用品」に格下げする

---

### [D-20260827-45] 鍵3種のローテーションが済んだ（マスター実施）

- **Date**: 2026-08-27
- **Kind**: decided（`D-20260824-31` の決着）
- **Question**: 作業中に降ろした資格情報3本を、実顧客データの前にどう始末するか
- **Answer**: **マスターが画面で3本とも処理した。私は値を一切受け取っていない（A-1）。**
  1. **Cloudflare の API トークン** → **Roll**（値だけ作り直し。権限は同じ、古い値は死んだ）。
     消さずに Roll にしたのは、本番デプロイでこの権限が要るため
  2. **Supabase の Management API トークン**（`sbp_…`・`0823`）→ **すでに Expired**（期限切れ）だった。
     危険は消えた状態で、片づけとして削除
  3. **service_role** → 画面の **「Disable JWT based API keys」** で旧式2本（`anon` / `service_role`）を停止し、
     あわせて新式の秘密鍵（`sb_secret_…` の `default`）も削除
- **止まらない根拠（先に確かめた）**: 本番が使うのは `SUPABASE_URL` と `sb_publishable_jdfWplK…` の2つだけ
  （`worker/wrangler.supabase.toml`）。`service_role` / `sb_secret_` を読む箇所は
  **worker のコードに1つも無い**（使うのは `verify:delete` / `verify:admin` がローカルの Supabase に対して数えるときだけ）
- **戻せるか**: 旧式の停止は公式に「再有効化できる」と明記されている。新式の秘密鍵は必要になった時点で作り直す
- **Impact**: `plan.md` 7-3 が ✅ になり、第7章の形式的な条件は**全項目 ✅**。
  次はデプロイ（`deferred` #30）——その前提だった「鍵が先」は解けた

---

### [D-20260827-46] バックアップは、実顧客データが入るまで用意しない（マスター判断）

- **Date**: 2026-08-27
- **Kind**: decided（マスター判断）
- **Question**: 納品前診断 HIGH-4「バックアップ・監視・エラー通知が文書化ゼロ」にどう応えるか
- **調べた事実**: **Supabase の Free プランには日次バックアップも PITR も無い**（[公式](https://supabase.com/docs/guides/platform/backups)）。
  設定で有効化できるものではなく、**Pro（月 $25 程度）以上に上げるか、自前で毎日ダンプを取るかの選択**になる。
  いまのプロジェクト `shiota1` は Free。つまり**控えは1つも取られていない**
- **Answer**: **いまは用意しない。** マスター判断:「まだ納品前なので、有料でその仕組みを完成させることは不可。バックアップはまだ要らない段階である」
- **なぜ妥当か**: 本番に入っているのは**ダミー犬3頭と孤児 owner だけ**で、失って困る実データが1件も無い。
  控えが要るのは**実際のお客さんのカルテが入った瞬間から**であって、それ以前に費用をかける理由が無い
- **いつ効いてくるか（発火条件）**: **本番に実顧客のカルテを1件でも入れる前。**
  そのときに Pro へ上げる（提供元が毎日取る・画面から戻せる）か、GitHub で毎日ダンプを取る（0円だが
  **ダンプに実顧客の情報が入る**ので置き場所の判断が要る）かを決める。`docs/deferred.md` #35 に発火条件つきで残した
- **監視のほうは今日入れた**: `verify:prod` の日次実行（`.github/workflows/watch-prod.yml`）。
  これは費用がかからず、**本番と master のズレ**を毎朝見る
- **クライアントの意向（マスター経由・同日）**: 「バックアップは**納品後、使いながら考える**」
- **追加指示（同日）**: 「A（手元に落とす）でよいが、**機能としては付けない**。理由は見積もりに入っていないから。
  ただし **CSV として DL できるようにした準備だけ**しておくこと」
  → `scripts/export-csv.mjs` を置いた。**画面にボタンは作っていない**（製品の機能ではない）。
  鍵は実行する人が渡す（`A-1`）。中身は標準出力に出し、ファイルを勝手に作らない（`A-2`）。
  写真は出ない（`asset://` の参照で実体は Storage）。手順は `docs/runbook.md` 2-B

### [D-20260827-47] 爪の基準図（6.2MB）を繋ぐかは、マスターが決める

**決めた人**: 私（保留の判断）。**マスターの判断が要る**ものとして残す。

カルテ入力画面の「爪のチェック」には、`<img alt="爪の解剖図">` という **`src` の無い枠**が
置いてあった。絵は出ず、「爪の解剖図」という**文字が並ぶだけ**で、基準図の役に立っていない。

- **歯のほうは繋いだ**。`teeth-diagram.jpg` は **92KB**。台帳（`docs/ASSET-PROVENANCE.md`）が
  「残存・使用中（トリマー側の画面）」と書いている当のもので、繋いだことで**記録が事実になった**
- **爪のほうは繋がない**。`nail-diagram.png` は **6.2MB** ある。カルテを書くたびに携帯へ落ちるので、
  外で使うトリマーの通信量と待ち時間に直接効く。台帳は「圧縮を検討する。ただし
  **状態判断に使う図なので、画質を落とすかは用途の確認が先**」と書いている
- **空の枠だけは外した**。役に立っていないものを、役に立つふりのまま置かない

**マスターに決めてほしいこと**（どれか1つ）:
1. **繋ぐ**（6.2MB のまま）。画質はそのまま。読み込みは遅くなる
2. **縮めてから繋ぐ**。血管の位置が判定できる範囲で軽くする——**どこまで落としてよいかは、
   使う人にしか決められない**
3. **繋がない**。基準図なしで判定する（いまの状態）

### マスターの判断（同日）: **1（そのまま繋ぐ）**

繋いだ。**画質は落とさない**——血管の位置を見て3段階を判定する図なので、
縮めて血管が見えなくなれば図が在る意味が無い。

重さへの手当ては `loading="lazy"` だけにした。**配るものは変えず、要るときまで落とさない**
（②爪のチェックまで画面を送ってから読み込まれる）。画質には触れないので、判断の 1 のまま。

### 続き（同日・マスター発言）: **この絵は後日差し替える**

いまの `nail-diagram.png` は暫定。差し替えるのは**ファイルだけ**で、HTML は触らない
（`src/assets/nail-diagram.png` を置き換えれば済む形にしてある）。

差し替えるときに一緒にやること:
- `docs/ASSET-PROVENANCE.md` の出どころを書き換える。**いまの1枚は AI 生成・`UNVERIFIED`**
  で、公開前の宿題として挙がっているもの（`A-3`）。差し替えで**この宿題が1つ片づく**
- 差し替え後の重さを見る。軽い絵になれば `loading="lazy"` は残しておいても害はない

### [D-20260827-48] ログイン画面の写真はマスター撮影（`VERIFIED`）。あわせて画像は全部1命令で差し替えられるようにする

**マスターの回答（2026-08-27）**: 「この写真はあなたがサロンで撮ったものか」→ **イエス**。

`photo-trim-action.jpg` は `D-20260823-U1` から未決だった1件で、**ログイン前の誰にでも
見える**ためこのリポジトリで最も露出が高い素材だった。選択肢1（台帳を `VERIFIED` に
更新するだけ）で決着。差し替えも撤去も不要。

**確認の仕方**: 実物をマスターに見せて1問だけ聞いた。ファイルの中に C2PA の署名が無く、
**機械では決められない**ため——ここは人にしか答えられない。

### あわせてのマスター指示: 「回答にかかわらず、画像はすべて交換可能にしろ」

出どころが片づいたかどうかとは別に、**製品に出る画像はどれも「あとで自分のものに
差し替える」もの**として扱う。手で差し替えると2か所つまずく:

1. **拡張子**。手元が `.png` で置き場所が `.jpg` だと、同じ名前で置いても食い違う
2. **参照の数**。1枚が複数の場所から参照されている（`photo-trim-action.jpg` は3か所）。
   1か所だけ直すと**片方だけ変わる**

だからこうした:

- **役割の台帳** `scripts/lib/image-roles.mjs` —— 役割の名前・実体・**どの画面のどこに
  出るか**・**誰が見るか**。「誰が見るか」を必須にしたのは、**差し替えの緊急度がそこで
  決まる**から（ログイン前に見えるものと、トリマーしか見ないものは違う）
- **差し替えの命令** `node scripts/swap-image.mjs <役割> <新しいファイル>` ——
  拡張子が違えば名前を寄せ、**参照している画面を全部書き換える**
- **機械** `npm run check` の `image-inventory` —— 台帳と実体を突き合わせる。
  **台帳に無い画像は画面に出せない**（載せ忘れを人の注意力で防ぐのをやめる・`D-13` の型）

**出どころ（`ASSET-PROVENANCE.md`）は自動で書き換えない**（`A-3`）。差し替えれば
出どころも変わるが、それは機械には分からない。命令の最後に「人が直せ」と出す。

**画質も勝手に変えない。** 縮めるかは用途によって答えが違う（`D-20260827-47`）。

### [D-20260827-49] 段のタブ（`01/02/03/04`）は残す。実運用で使う

**マスターの回答（2026-08-27）**: 「このタブは実際の運用で使いますか」→ **使う**。

`docs/deferred.md` #2 は 2026-08-24 から「**試作品の見た目で、本番の動線ではない**」と
書いたまま止まっていた。**その記述が誤りだった**——マスターは使う。残す。

### 使うと決まったので、直したものが1つ

「04 顧客カルテ」は、確定前に押すと**空の雑誌**が出ていた。`screen-4` の中身は
`renderMagazine` が `innerHTML` ごと差し替えるまで意匠の器なので、描く前に押すと
**見出しだけその犬の名前で、中身が空**になる。`D-12`「押せた ではなく 届いた」で
見れば、これは届いていない——②のタブに空の一覧が出た穴（`verify:m6` が見つけた）と
まったく同じ形である。

**タブを使わない前提なら放置できたが、使うなら本番の品質**なので直した。
中身が無いときは器を案内に置き換える:
「このカルテはまだ確定していません。『03 カルテ作成』で書いて、確認へ進むとここに出ます」

**黙って何もしないのは選ばなかった**——押しても反応が無いのは、空の雑誌が出るのと
同じくらい人を止める。

**静的配信（Supabase を積んでいない経路）では何もしない。** あちらは意匠を見るための
経路で、器を消すと `walk` の絵の意味が変わる（`docs/watch.md` W-2）。

### [D-20260827-50] 犬体図のスタンプ帯の横スクロールは、保留・このまま

**マスターの回答（2026-08-27）**: 「折り返す／このまま／実機で見てから」→ **保留でこのまま**。

`docs/deferred.md` #11。④カルテ作成の犬体図の下、`🔴 赤み` などのボタンが並ぶ帯が、
**帯の中だけ**横に流れる（中身 373px ／ 枠 282px・`overflow-x: auto`）。

**`D-20260825-39`「横スクロール禁止」との関係**: ページ全体は流れず、部品の中だけ。
マスターが「このまま」と決めたので、**指示の対象外**として扱う。私が繰り返し差し戻さない。

**やるとしたら何をするか**は決めてある——`flex-wrap: wrap` で折り返す。
横に流さず2段にするだけで、**ボタンは1つも減らない**。CSS 1〜3行。
実機で触って気になったとき、この記録を見れば作業がすぐ分かる。

**残る心配（記録だけしておく）**: スクロールバーを隠してある（`scrollbar-width: none`）ので、
**右側にボタンがあることに気づかない**可能性がある。気づかなければ、その所見は
「無いもの」として記録される。実店舗で使い始めたら、ここを一度見る価値がある。

### [D-20260827-51] F4（棚卸しと納品の仕上げ）へ進む。範囲は2ファイルだけ広げる

**マスターの回答（2026-08-27）**: 「F3 を閉じて次へ進めるか」→ **進める**。
続いて「バッドシナリオ10個でよいか」→ **承認する**。

**進め方を順序どおりにした。** 門番はフェーズを切り替えても、その段の成果物2つが
揃うまで作業場を開かない。**先に `docs/ops/phase` を書き換えると何も触れなくなる**ので、
書類 → 承認 → 実施 → 切り替え、の順で通した。

1. `docs/ops/failure-check-F4-start.md`（失敗47件・決定52件の照合）
2. `docs/ops/bad-scenarios-F4.md`（10個）→ **マスター承認 → 10個すべて実施**
   （**該当した 4件 / 該当せず 6件**）
3. `docs/ops/phase` を `F4` に

**範囲は F3 に2ファイルだけ足した**——`.gitignore` と `.env.example`。
どちらも F3 の範囲外で**実際に2回止められた実物**である。
「もう終わりだから全部触ってよい」にはしない（`D-18` 偽-3「無条件の免罪符を作らない」）。

### バッドシナリオが実際に掘り出したもの

**#1（古い記録を根拠にする）が欠陥を1つ見つけた。** `worker/src/index.js` の
コメント**7か所**が、もう存在しない注入（`__SCREEN__='owner'` 等）と、削除済みの
テンプレート名（`ponchi-v2.html`）を説明したままだった。**コードは正しく、説明文だけが
嘘をついている**状態で、`grep` した次の人を必ず誤らせる。同日直した。

**#2 は消す手を止めた。** 未参照ファイル7点について、参照する側ではなく
**記録・ルールが名指ししている側**を数えたところ、`konva.min.js` が **9件**
（`AGENTS.md` の確定スタック表を含む）。消していれば規則と実体が食い違っていた。

**#9 は手順書の穴だった。** 「実顧客を入れる前に見ること」が記録にはあるのに、
`docs/runbook.md` に**チェック項目として存在しなかった**。2-D として足した。

### [D-20260827-52] バックアップの件は決着済み。再提起しない

**マスターの指示（2026-08-27）**: 「すでに説明した。**クライアントの意向は絶対である。**
この件に関して話題に出すな。」

`D-20260827-46` は**決定**である。クライアントの意向は「納品後、使いながら考える」。
それが答えであって、**未決の宿題ではない**。

**私の誤り**: `docs/runbook.md` 2-D の表で、この決定を「**未**」と書いた。
決まっているものを未決の欄に置けば、見るたびに承認待ちに見え、**同じ話を何度も持ち出す**
ことになる。実際そうなった。決定は「決着済み」と書く。

**以後の扱い**:
- リスクの説明はもう済んでいる。**繰り返さない**
- 実顧客投入前のチェック表（`runbook` 2-D）でも「決着済み」として扱う
- **マスターから聞かれたときだけ答える**

**一般化**（同じことを繰り返さないため）: マスターが判断を下した項目を、
私の側の心配を理由に「未」「要検討」の欄へ置き直さない。**決定は決定として記録する。**
心配が残るなら、それは私の記録（`watch.md`）に置くもので、マスターへの問いに戻さない。

### [D-20260828-53] W-1 の規則化を却下し、「壊して赤にならない検査は壊れているとみなす」を置く

- **日付**: 2026-08-28
- **決めた人**: マスター
- **問い**: `docs/watch.md` W-1（検査が実際の仕組みと違う所を見る・**16回**発生）を
  `AGENTS.md` の規則へ昇格させるか。昇格の4条件のうち3つを満たし、
  4つ目（機械判定可能）が `empty-pass` で**一部**（5/16）になった、と諮った。
- **回答**: 「もはやルールじゃダメだろ。**16回中16回盛れない方法にしろ。**
  監視以前にそういう事をやる時点の整備が必要だろう、監視は機械がダメなら
  フックにするか毎回見に行くか、**壊してみて赤にならないなら検査自体が壊れてるとする**か」
- **決定**:
  1. **規則にしない。** 規則は「気をつける」の言い換えで、16回のうち何回止まるかを言えない
  2. **形を見る機械も、それだけでは足りない**（`empty-pass` は 5/16）。形を足しても
     次の形は予測できないので、**形の追加で 16/16 には到達しない**
  3. 代わりに**定義を置く**——**「壊して赤になったところを見ていない検査は、
     壊れているものとして数える」**。形に依存しないので逃げ道が無い
- **落としたもの**: `docs/ops/proof-of-red.md`（台帳・182件）と
  `scripts/guard/proof-of-red.mjs`（`npm run check` から呼ぶ）。
  検査を1件ずつ**名前で**載せる——数だけの上限は、別の検査を消して枠を空ければ通るため。
  台帳に無い検査は赤／台帳が実体に無い検査を指していても赤（**3方向とも実測で確かめた**）。
- **残っていること**: 182件すべてが未確認。埋め方は「毒見」（`proof-of-red.md` 末尾）。
  Docker が要るので CI に置く。

### [D-20260828-54] `delivery-ready` に「1項ごとに埋められない理由」を足す（選択肢 A）

- **日付**: 決定は 2026-08-28。**記帳は 2026-08-29**（マスター指示「**判断Aは記帳しろ**」）
- **決めた人**: マスター
- **問い**: 台帳の残り13件は、実測の結果「**埋めるべきでない**」もの（守りが二重／待ちを
  言い直しているだけ／いまの画面では症状が起こせない）だった。理由は1項ごとに書いてあるが、
  `delivery-ready.mjs` の除外は**ファイル単位**でしか書けず、機械はその理由を読めずに数え続ける。
  **このままでは F4 が構造上閉じられない。** 諮った選択肢は2つ:
  - **A（こちらを推した）**: 機械に**1項ごとの理由欄**を足す。理由は既に全件書いてあるので機械側だけの作業
  - **B**: 該当の検査そのものを直す。**製品の守りに触る**うえ、検査を減らす方向なので
    `A-4`（検査を消して緑にしない）と紛らわしい
- **回答**: **A**
- **記帳が遅れた理由（隠さずに書く）**: この判断は 2026-08-28 の時点で
  `docs/handoff.md` `0-N` にしか書かれておらず、**この帳面に項が無かった**。
  引き継ぎは次のセッションでは読まれないので、そこにしか無い判断は**次の人には無いのと同じ**
  （`D-19` と同じ形）。2026-08-29 の F4 完了照合でそれを見つけ、マスターに諮って記帳した。
- **回答の言葉を引用していない理由**: A を選んだときのマスターの発言が手元に残っておらず、
  あるのは「A を選んだ」という**私の側の要約だけ**である。他の項のように発言を引用すると
  創作になる（`F-20260825-29` の型）ので、**選んだ事実だけを書く**。
  記帳そのものの指示（2026-08-29）は「**判断Aは記帳しろ**」。
- **落としたもの**: `scripts/guard/delivery-ready.mjs` の「1項ごとに埋められない理由」欄。
  **黙らせる道具にしないための歯止めを4つ**入れ、すべて検査で裏づけた
  （`test/delivery-ready.test.mjs`）:

  | 歯止め | 動き |
  |---|---|
  | 理由が **20字未満** | 理由と認めない（短い言い訳で外せない） |
  | **実体に無い検査**を指している | 「古い」として赤 |
  | **すでに証明済み**の項に理由が付いている | 「矛盾」として赤 |
  | — | **何件をどの理由で外したかは、通ったときの出力に必ず出る** |

- **境界**: ここに足してよいのは「**埋めるべきでないもの**」だけ。
  埋められるのに面倒だから、は該当しない。迷ったら**まず壊し方を書いて実測する**。
  実際 `verify-invitation :: 5.` は 2026-08-29 に**理由ではなく実測で消えた**
  （2層を同時に剥がす壊し方を作り、赤にした）。**理由つきで外すことは「証明した」ことではない。**

### [D-20260829-55] CI が全滅した件は決着。原因は設定変更の誤りで、再発しない

- **日付**: 2026-08-29
- **決めた人**: マスター
- **問い**: `F-20260828-57`（run #156〜162 が全ジョブ数秒で失敗し、runner が一度も
  割り当てられなかった件）。こちらでは原因を特定できず、最有力候補（Actions の無料枠 /
  spending limit）は**アカウント所有者にしか残量が見えない**ため確かめられなかった。
  納品条件 7-1（渡した後も自動で検査が走る）に直接効くので、マスターへ上げていた。
- **回答**: 「**気にするな、設定を変えたら間違えたからだ。解消すみだし、再発しない**」
- **決定**:
  1. **原因はマスターによる設定変更の誤り。** すでに解消しており、再発しない
  2. **この件はもう持ち出さない。** `D-20260827-52` と同じ扱い——決定であって宿題ではない
- **裏づけ（こちら側の実測）**: `master` の run #191 と PR #33 の run #192 が、
  どちらも `check` / `verify` / `walk` の**全 job 成功**。現象は出ていない
- **`F-20260828-57` の扱い**: Status を **解決済み**にした。ただし
  **「切り分けが確かめていない前提の上に積まれた」という失敗そのものは残る**——
  再発防止（`How to prevent`）は原因が分かっても価値が変わらないので、書き換えない

### [D-20260829-56] C-8（フォント統一）は「全文明朝体」に再指示された

- **日付**: 2026-08-29
- **決めた人**: マスター
- **問い**: C-8 の第1段（`index.html` だけシステムフォントと不一致だった箇所を、
  他3ファイルに合わせてシステムフォント統一）を報告したところ、飼い主画面の
  「次回のおすすめご来店時期」ブロックのフォントを問われた。見出し（`.magazine-revisit-title`）は
  明朝体（`--font-serif`）、本文は `body` 既定のゴシック体（`--font-sans`）という
  混在があった
- **回答**: 「動線は希望通りになっている。全文明朝体で進めろ。明朝体にも種類あるだろ」
- **決定**:
  1. **全文を明朝体に統一する。** `--font-sans`/`--font-en` を `--font-serif` と
     同じフォント列（`"Hiragino Mincho ProN", "Yu Mincho", YuMincho, "Times New Roman", serif`）に
     揃え、`src/index.html`/`src/my.html`/`src/admin.html`/`backend/js/magazine-view.js` の
     4ファイルと、Canvas 直書きの2箇所（`src/js/ui.js` の犬体図ラベル・印の文字）に適用した
  2. **「種類がある」への回答**: 具体的な明朝体（Hiragino Mincho ProN / Yu Mincho）を
     名指しした。ただし**この2書体を含む日本語フォントは同梱していない**
     （`docs/ASSET-PROVENANCE.md`・ファイルサイズの都合）ため、実際に描画されるのは
     端末のシステム明朝体——Mac/iOS のみ確実、Windows は Yu Mincho（環境により無ければ
     フォールバック）、Android・Linux 等はシステムに明朝体が無いことが多く、
     ブラウザ既定の serif（欧文フォールバックの可能性あり）に落ちる。
     Google Fonts 等の外部CDN読込は `D-20260823-14`（外部通信ゼロが不変条件・`verify:m6`が実測）
     に反するため使えない。**全端末で同じ明朝体を保証したい場合は、日本語フォントを
     ファイルとして同梱する（容量増）という選択が別途要る**——今回はその選択はせず、
     既存のシステムフォント方式のまま明朝体へ切り替えた
- **裏づけ**: `npm run build`/`check`/`test`（EXIT 0）、`verify:*` 全12本（208/208）、
  `gate.mjs --end`/`delivery-ready.mjs` とも通過。`npm run walk` の絵で全画面が
  明朝体で統一されたことを確認

### [D-20260829-57] PR マージ後のデプロイは、毎回の個別承認を要らないことにした

- **日付**: 2026-08-29
- **決めた人**: マスター
- **問い**: `#41`（本番404の修正）・`#42`（全文明朝体）とも、マージ後に
  「デプロイしてよいか」を毎回確認していた。`deploy.yml` は元々
  「デプロイは戻しにくい外向きの操作で、実行前にマスターの承認が要る」という
  注記つきで作られている（`workflow_dispatch` のみ・自動起動なし）
- **回答**: 「おｋ。毎回マージは行ってよい。設定を変えろ」
- **決定**:
  1. **PR がマージされたら、デプロイの都度確認は挟まない。** Claude Code は
     `deploy.yml` を自分の判断で起動してよい
  2. **`workflow_dispatch` という起動方式自体は変えていない。** プッシュ契機の
     完全自動化（`on: push`）はしていない——起動するのは人間（マスター）か
     Claude Code のセッションで、CI が勝手に走るわけではない
  3. `deploy.yml` の冒頭コメントを更新し、この決定を反映した
- **Impact**: 以後のセッションは、PR マージ後に「デプロイしてよいか」を聞かずに
  `deploy.yml` を起動し、`wrangler deploy` と `verify:prod` の結果を報告する
  （失敗時は当然報告・対応する）。マージ自体の承認は引き続き扱いが変わらない
  （このプロジェクトはそもそも PR 作成からマージまで Claude Code が一貫して行ってきた）

### [D-20260829-58] 「次回のおすすめご来店時期」の値の出どころ（第6章 `#3`）

- **日付**: 2026-08-29
- **決めた人**: マスター
- **問い**: `docs/ops/plan.md` 第6章 `#3`「飼い主画面『次回のおすすめご来店時期』の
  値の出どころ」——(a) 固定文のまま (b) 来店間隔を設定可能にする、のどちらか
- **回答**: 「デフォルトは30日後、別途修正できるようにする。修正はデフォルト自体の
  修正も犬ごとの修正も可能とする。」（(b) を採用。ただし細部はこの回答で確定）
- **決定**:
  1. 効果的な次回来店日 = カルテの来店日 +（その犬の上書き日数があればそれ、
     無ければ店舗の既定日数）。店舗の既定日数は `shops.default_revisit_days`
     （既定値30・1〜3650）、犬ごとの上書きは `pets.revisit_days_override`
     （nullable・1〜3650）——`supabase/migrations/202608290010_revisit_interval.sql`
  2. **店舗の既定日数は管理者だけが編集できる**（RLS `shops_admin_update`）。
     `admin.html`（`backend/js/supabase-admin.js`）に「④ 店舗設定」を新設
  3. **犬ごとの上書きは⑤カルテ確認画面から編集できる**（スタッフ限定・
     `backend/js/magazine-view.js` の `revisit-edit` ブロック、`opts.onRevisitDaysChange`
     が渡っているときだけ表示）。⑥飼い主画面には編集欄を出さない
  4. ⑤⑥とも同じ `renderMagazine()` が計算・表示する（マスター指定の「同一レンダラ」
     方針を踏襲）。⑥（飼い主）が店舗の既定日数を読むために、RLS に
     `shops_customer_select`（自分の犬が居る店舗に限る）を新設した——
     `shops_staff_select` はスタッフ限定で飼い主には使えないため
- **裏づけ**: `npm run build`/`check`/`test`（EXIT 0）、新設 `scripts/verify-revisit-interval.mjs`
  （11/11）、`mutate-run.mjs` の壊し8種で12件の `check()` のうち11件を実測で赤にした
  （`docs/ops/proof-of-red.md` 26回目）。残り1件（余計なダイアログが出ていないかの
  安全網）は未証明のまま理由つきで記帳。`gate.mjs --end`/`delivery-ready.mjs` とも通過

### [D-20260830-59] チェックアウト時、次セッションの開始点を「動作確認」に指定

- **日付**: 2026-08-30
- **決めた人**: マスター
- **問い**: `F-20260829-61`（デプロイ事故）復旧の報告後、チェックアウトを指示
- **回答**: 「チェックアウト、次回は動作確認から」
- **決定**: このセッションでの確認は `curl` によるAPI層（REST/RLS）までで、
  ⑤（スタッフ）⑥（飼い主）の実画面を人の目で開いて確認する「動作確認」は
  行っていない。**次のセッションは、新しい作業に着手する前に、まずこの動作確認を
  最初に行うこと**（`docs/handoff.md` 0-X に詳細）
- **Impact**: 次セッションの着手順が固定された。第6章の残り5件（`#5` `#6` `#11`
  `#22` `#35`）の質問は、動作確認の後に行う

### [D-20260902-60] `konva.min.js` は削除する（第12章 `#6`）

- **日付**: 2026-09-02
- **決めた人**: マスター
- **問い**: 未使用の `konva.min.js`（182KB）を、依存として残すか削除するか
- **回答**: 「canvas じゃ無いと上手くいかないから コンバをやめた経緯」
- **決定**: 削除する。描画は素の Canvas 2D で作ってあり、Konva に戻す予定は無い。
  1度も読み込まれないまま本番へ配っていたので、2026-08-24 に未参照の画像7件を
  消したのと同じ理由（`https://.../assets/konva.min.js` が誰でも取れる状態）で落とす
- **Impact**: `src/assets/konva.min.js` を `git rm`。`docs/ASSET-PROVENANCE.md` の
  「2. ライブラリ」は「同梱なし」になった。履歴からは戻せる

### [D-20260902-61] 犬体図のボタンの帯は、折り返して画面に収める（第12章 `#11`）

- **日付**: 2026-09-02
- **決めた人**: マスター
- **問い**: `.body-marking-stamps` が画面幅に収まらず横スクロールになる件
- **回答**: 「ボタンの帯を変えれば良いだろ、画面に収まるように。しかし、他の崩れが
  ないように先に調査して、崩れないようにやれ」
- **決定**: 横スクロール（`overflow-x: auto`）をやめ、`flex-wrap: wrap` で折り返す。
  **Supersedes `D-20260827-50`**（「保留・このまま」）
- **裏づけ**: このクラスは⑤犬体4面図の2本の帯だけが使う（他画面に無いことを確認）。
  実ブラウザで幅 320 / 390 / 768px を測り、3幅とも**横スクロールが消え、
  画面外に切れるボタンが 0 件、ページ自体の横スクロールも無い**ことを実測
- **Impact**: 「フケ」「全部消す」が画面の外に隠れなくなった。狭い画面向けの
  文字縮小（`@media`）は、新しい `.mark-mode-btn` / `.mark-undo-btn` にも効かせた

### [D-20260902-62] 旧 KV 版 Worker と KV モードのコードを、全部削除する（第12章 `#22`）

- **日付**: 2026-09-02
- **決めた人**: マスター
- **問い**: 旧 KV Worker（`saltydog-report-worker`）を、切り戻し用に残すか削除するか
- **回答**: 「22は削除」→「全て削除」（Worker だけでなく KV 関連すべて、の意）
- **決定**: **Supersedes `D-20260823-09`**（「切り戻せるように Worker は残す」）。
  切り戻し先を持たない代わりに、**誰も通らない道に無認証の API が生き続ける状態**を
  無くす。実際 2026-08-24 に、この Worker の workers.dev 経由で
  `/api/customers` が誰でも 200 で読め、`DELETE /api/owners/{slug}` の
  カスケード削除まで無認証で通ることが見つかっている（`docs/decisions.md` の
  2026-08-24 の記述）。**設定で閉じているだけの無認証 API は、設定が戻れば開く。**
- **消したもの**（リポジトリ側）:
  - `worker/wrangler.toml`（KV 版 Worker の設定・唯一の切り戻し手順が書かれていた）
  - `worker/src/index.js` の KV モード全経路（`/api/customers` `/api/owners`
    `/api/reports`、`/p/*` `/o/*` `/edit/*` の KV 版、`readJSON`/`writeJSON` ほか
    到達しなくなった26関数）。**1168行 → 507行**
  - `worker/src/data-stores/kv-data-store.js` と `create-data-store.js`
    （2つの backend を選び分ける工場。既定値が `'kv'` だったので、
    `DATA_BACKEND` の設定漏れが**黙って KV に落ちる**造りでもあった）
  - `DATA_BACKEND` 変数（3つの wrangler 設定から）とコード側の分岐
  - KV→Supabase の移行ツール一式（`scripts/migrate-kv-to-supabase.mjs`、
    `scripts/lib/kv-export-parser.mjs`、`scripts/lib/supabase-migration-target.mjs`、
    `test/kv-migration.test.mjs`、`test/fixtures/kv-export.json`）。移行は完了済み
  - `test/worker-unit.test.mjs`（5件・**全件が KV ルートの検査**）。
    **検査を弱めて緑にしたのではない**（`A-4`）——検査していた機能そのものを、
    マスターの決定で消したため。Supabase 側の検査は1本も減っていない
- **まだ残っている作業**: **Cloudflare 上の Worker 実体の削除**。資格情報を私は
  受け取らないので（`A-1`）、マスターが Cloudflare の画面で
  `saltydog-report-worker` を削除する（Workers & Pages → 該当 Worker →
  Settings → Delete）。リポジトリからは配れなくなっているので、
  消し忘れても新たに更新されることはない
- **裏づけ**: `npm run build` / `npm test`（199件）/ `npm run check` いずれも EXIT 0。
  さらに**ローカルの実 Supabase を起動して `verify:*` を15本すべて実測**
  （portal / edit / xss / roundtrip / empty / screens / delete / draft / invitation /
  m6 / admin / photo / revisit / options / first-run）、**全部 PASS**。
  つまり KV の経路を全部落としても、飼い主に届くところまで壊れていない

### [D-20260902-63] 未参照のアイコン5点は削除する（第12章 `#5`）

- **日付**: 2026-09-02
- **決めた人**: マスター
- **問い**: `icon-ear/nail/skin/spa/weight.png`（各 512x512・計約 640KB）を、
  ④カルテ入力の見出しに置くか、削除するか
- **回答**: 「5個のアイコンはすべて削除」
- **決定**: 削除する。**どの画面からも一度も参照されていない**まま、`dist/` へ
  コピーされて本番で配信されていた（URL を知れば誰でも取れる）。
  意匠モック `design/mock-4step.html` も番号の丸だけで、この5点は最初から未採用。
  2026-08-24 に未参照の画像7件を消したのと同じ理由（`D-4`「出所未確認の素材を
  外部公開物へ転載禁止」）。5点はいずれも `UNVERIFIED`（出所未確認）だった
- **Impact**: `src/assets/` から `git rm`。`docs/ASSET-PROVENANCE.md` の
  「`icon-*.png` は残存・使用中」という記載は**誤りだった**ので訂正した。
  公開前の宿題（`UNVERIFIED` の出所確認）からも5件が外れ、残るは
  `teeth-diagram.jpg` と `body-side.png` の2件。`body-side.png` は
  画面に到達しているので残す。履歴からは戻せる

### [D-20260903-64] 常連の6枚目は「前回の続き」から書き始める

- **日付**: 2026-09-03
- **決めた人**: マスター
- **問い**: 5回来店してカルテが5枚たまっている犬の、6枚目をどう始めるか
- **回答**: 「カルテを作成するを押すと、自動的に前回のカルテのコピーが表示される。
  日時やコースなど毎回必ず変わる項目は空欄で、変わらないであろう項目は
  5回目のコピーになっている、という状況から6回目のカルテ作成を始められるようにしろ」
- **取捨の内訳**（マスター判断・同日）:

  | 空にする | 引き継ぐ |
  |---|---|
  | 来店日 / コース / トリマーからのメッセージ / 体重 / ⑦使用オプション / 写真3種 | 爪（前足・後ろ足）/ 耳（右・左）/ 歯の状態 / BCS / ベスト体重 / 犬体4面図の印 |

  写真とメッセージを空にするのは、**撮り直さず書き直さずに確定すると、前回のものが
  今回のカルテとして飼い主に届く**ため。犬体図の印を引き継ぐのは、しこり・イボが
  前回からの継続所見で、「消えていれば消す／増えていれば足す」という使い方をするため。

- **一度作って消している機能である**。「前回を複製」（`cloneAndCreate()`）は
  `6fecedc` / PR #55 で削除された。理由は2つあり、**どちらも今回踏んでいない**:
  1. `alert('【複製完了】…読み込みました')` と言いながら**何も複製していなかった**
     → 今回は、実際に引き継いだ項目だけを帯に名指しする（`carryOverLabels()`）
  2. ②一覧から④へ直行し、`/edit/p/{petId}` 経由の初期化（下書き再開・**店舗の
     使用オプション一覧の取得**）を飛ばしていた。「⑦使用オプションが出ない」の
     実害はここから出ていた
     → 今回は②一覧にボタンを置かず、**正規経路 `showPetKarte()` の中**で引き継ぐ。
        `renderOptionChips` → `resumeDraft` の順序も崩していない

- **造り**:
  - `findLastFinalReport(petId)`（`backend/js/supabase-staff.js`）— 直近の確定カルテを1件返す
  - `carryOverReport(previous)`（`src/js/ui.js`）— 引き継ぐ項目だけを取り出す純関数。
    画面も通信も触らないので1項目ずつ検査できる
  - 流し込みは**既存の `applyReport()` をそのまま使う**（新しい流し込みを書かない）
  - **下書きが1件でもあれば下書きが勝つ**。書きかけを前回の内容で上書きしない
  - **引き継いだ状態は保存しない**。開いただけでカルテが生えるのを防ぐ
  - 確定にも `__marks` を載せる（`commitReport()`）——`bodyMarkingImage` は焼いた PNG で、
    そこから印は復元できない。載せないと**次の回で犬体図を引き継げない**

- **裏づけ**: `test/ui-carry-over.test.mjs`（17件・**直す前のコードで17件すべて赤**を実測）。
  新設 `npm run verify:carry-over`（40件）を**実 Supabase・実ブラウザ・人と同じクリック**で
  5枚目→6枚目→7枚目まで通し、40/40 PASS。壊し方3種で20件が赤になることを実測
  （`docs/ops/proof-of-red.md` 27回目）。`npm run build` / `npm test` / `npm run check` は EXIT 0

- **実ブラウザでしか出なかった不具合を1件、この作業中に見つけて直した**:
  引き継ぎの帯の `.carry-over { display: flex }` がブラウザ既定の
  `[hidden] { display: none }` を上書きしており、引き継ぎが起きていない初回の犬にも
  空の帯が出ていた。単体テストでは出ない型（`D-14`「絵で判定する」の理由そのもの）

### [D-20260903-64] 体重の記録を、スタッフにも飼い主にも届くようにする（第12章 `#45`）

- **日付**: 2026-09-03
- **決めた人**: マスター
- **問い**: ④の「前回比」が確定カルテ5枚ある犬でも「前回の記録なし」と出る件
  （放置リスト `#45`）を直すか
- **回答**: 「納品して問題あるなら直せ」→（不具合が2つあると報告して）「両方直す」
- **見つかっていた不具合**:
  1. **④の「前回比」が常に「前回の記録なし」**。`App.currentDog.prevWeight` に値が
     入る経路は仮データ（`src/js/dummy.js`）だけで、実データでは常に `null`。
     体重を毎回空で始める設計は「前回値は前回比で見える」ことを前提にしているので、
     前提そのものが崩れていた
  2. **⑥飼い主の「体重推移」が常に点1つ**。`renderWeightGraph()` はカルテ1枚の
     `weights`（＝その回の1件）を描いていた。`polyline` は2点未満では線を引けないので、
     **推移は一度も飼い主に届いていなかった**。既存の `verify:roundtrip` の
     `14b.` は「箱の中に要素が在るか」しか見ないので、点1つでも緑になっていた
- **決定・直し方**: **保存形式（`reports.data`）は変えない。** 体重履歴は
  **読み出すときに足す**（`shopDefaultRevisitDays` と同じ扱い）。過去のカルテに
  後から書き足す必要が無く、`extractReport()` も `key-parity` の台帳も動かない。
  - `supabase-data-store.js`: `listWeightHistory()` と純関数 `weightHistoryFromReports()`。
    **量らなかった回は落とす**——0kg として線に乗せると、書いていない体重が
    谷として飼い主に届く（`D-10`）
  - `worker/src/index.js`: カルテ1枚を返す2か所（⑤スタッフ・⑥飼い主）に
    `weightHistory` を付ける。取れなくてもカルテ本体は返す
  - `magazine-view.js`: グラフは `report.weightHistory` を優先。無い経路では従来どおり
  - `ui.js`: `resumeDraft()` が**下書きの有無にかかわらず**前回の確定カルテを取り、
    `prevWeight` に入れる。前回比の描画は `renderWeightDiff()` に切り出した。
    **まだ量っていないときは差ではなく「前回 4.4kg」と出す**——`0 - 4.4` を
    「-4400g ▼」と出すと痩せたように見える
- **裏づけ**: 新規 `test/weight-history.test.mjs`（7件）＋`ui-carry-over` に5件。
  `verify:carry-over` を 40件 → **49件**に拡張し、**5つの壊し方で新しい9件すべてを
  1件ずつ赤にした**（`proof-of-red` 29回目）。実 Supabase で `verify:*` 16本すべて PASS。
  ⑤の実画面の絵で**線が引かれていること**を目視（`.human/carry-over/8_*.png`）
- **途中で直した検査の弱さ**: 「カルテが1枚も無い犬では『前回の記録なし』のまま」は、
  1回目の壊し方では赤にならなかった。**画面に出ていた文字が HTML の初期値のままで、
  「計算して記録が無かった」と「一度も計算していない」を区別できていなかった**
  （`docs/watch.md` W-1 の型）。検査を弱めるのではなく、`resumeDraft()` が
  **前回が無かったときも必ず描き直す**ように製品側を直して、見分けられるようにした
- **Impact**: 第12章 `#45` は片づいた。トリマーは前回との差を見ながら量れる。
  飼い主の「体重の推移」に、初めて線が引かれた

---

### [D-20260903-65] 徹底調査で見つけた7件を直す（配線・実挙動・見落とし・イレギュラー・脆弱性）

- **日付**: 2026-09-03
- **決めた人**: マスター（調査の指示）／直す範囲は「納品して問題あるなら直せ」（同日）に従った
- **問い**: 「配線は全てつながっているのか、コードや緑/赤しか見てない箇所は正しく動くのか、
  致命的な見落としはないか、人間として操作して目的の動作はすべて完了するか、
  イレギュラーな操作をして壊れる事は無いか、脆弱性はないか、を徹底リサーチして記録に残せ」
- **やり方**: 敵対的に3体の検証を並走させ（配線の網羅／致命的な見落としと恒真の検査／
  脆弱性・RLS・XSS・レート）、私自身は実機で人と同じ操作をした。
  すべて**実 Supabase・実 RLS・実ブラウザで再現してから**直し、
  直したあとに**壊して赤になることを実測**してから検査に足した。
- **見つけて直した7件**（詳細は `docs/ops/audit-2026-09-03.md`）:

  | # | 何が起きていたか | 直し方 |
  |---|---|---|
  | A-1 | ④の **BCS・両耳・歯が、選ばれて見えるのに1つも保存されない**。既定が全部「良好・問題なし」の側なので、トリマーが信じて触らないほど記録が消える。同じ画面のドックは「未記入」と言っており、画面の中でも食い違っていた | `src/index.html` の既定 `is-active` を4つ外した。選ばれるまで、どれも点けない |
  | A-2 | **⑤確認から段のタブ「03」を押すと、組み立てていない④が開く**。⑦使用オプションの帯ごと消え、引き継ぎも前回比も出ない。確定すると必ず⑤に着くので**本番の動線で踏める** | 「1つの印に2つの意味」が原因。`setDogIdentity()`（移動しない）を切り出し、関所は `karteInputReady` を見る |
  | A-3 | **下書きを再開すると、前回の所見が黙って今回の所見になる**。1回目は帯が出るが、画面を1回タップして下書きができると、2回目以降は**帯だけが消えて値が残る**。完了バッジは「8/8」と言う | 持ち回りの印は増やさず、**手元の前回のカルテと突き合わせて、いま同じものだけを名指しで言う**（`carryOverStillSame()`） |
  | A-4 | 管理者「カルテ修正」で、**確定カルテ8枚の犬でも「前回の記録なし」**。`resumeDraft()` を通らないので HTML の初期値が残っていた | 直しているのは過去のカルテなので**比べる相手が違う**。推測で埋めず「カルテ修正中（前回比は出しません）」と言う |
  | A-5 | **確定カルテが0枚の犬で、今日量った体重が捨てられる**。`report.weightHistory \|\| data.weights` の **`[]` は真** | `pickWeightSeries()` に切り出し、空の配列を「無い」として扱う |
  | A-6 | 前回のカルテを読めなかったとき**黙る**。組み替える前は一言出ていた。常連が初回の子と同じ画面になる | 失敗した事実だけ持ち帰ってドックに出す。記入は止めない |
  | A-7 | `__marks` を「確定のときに落とす」と書いたコメントが**実装と逆**（`AGENTS.md` A-5） | コメントを直した |

- **越境は1件も無い**。攻撃11通りを実測（他店・他人の飼い主・退会後・招待未消化・
  下書き要求・トークン無し）。効いている本質は **Worker が `service_role` を持たず、
  常に呼び出し元のトークンで RLS を通ること**。`verify:xss` 24/24 PASS。
  `weightHistoryFromReports()` は `{ym,date,kg}` を組み直すので、他カルテの `data` は
  出口に出ない（実測）
- **裏づけ**: 新規 `test/ui-default-state.test.mjs`（2件）／`weight-history` に3件／
  `supabase-store` に1件（体重履歴のクエリ形）／`verify:m6` に2件。
  **4通りの壊し方で新しい8件すべてを赤にした**（`proof-of-red` 30回目）。
  `npm test` 149+55+29 / `npm run check` 全緑 / `verify:*` 17本すべて PASS
- **`#46` は同日に埋めた**（`proof-of-red` 31回目）: ⑥飼い主の線を見る自動検査が
  1本も無く、「⑤と⑥は同一レンダラだから飼い主にも届く」という**推論**だけだった。
  `verify:roundtrip` に前回の来店（3.2kg・確定）を置き、**飼い主の画面で `polyline` の
  点を数える** `14c.` `14d.` を足した。点は「2以上」ではなく**ちょうど2**で見る
  ——`status=eq.final` の門を外して下書きが混ざったときに気づけるように。
  2通りの壊し方（⑥のビューへ渡さない／worker の応答から落とす）で**どちらも赤**。
  **埋める前は、同じ壊し方で 34/34 PASS のままだった**
- **直さずに記録したもの**: `#47`（読み出しの増幅）／`#48`（壊れた `petId` で
  ブラウザのエラー画面）。マスター判断が要る3件は `audit-2026-09-03.md` 第5章
- **過剰に直していないか**（マスター指示の独立検証④）: 体重に `999999` を入れると
  `+999994600g ▲` と出るが、`type=number` に人が打てる範囲の話で壊れてはいないので
  **直していない**。浮動小数点も80万組を検算して不一致0だったので `Number.EPSILON` 対策はしない
- **Impact**: 飼い主に届く健康の記録から、**3種類の嘘**（見えているのに保存されない／
  前回のものが今回として届く／量ったのに捨てられる）が消えた

---

### [D-20260904-66] 1アカウント＝1役割。スタッフ兼飼い主は仕組みとして用意しない

- **日付**: 2026-09-04
- **決めた人**: マスター
- **問い**: スタッフ兼飼い主のアカウントは、ログイン後どちらの画面を既定にするか
- **回答**: 「**レアケースだから想定する必要なし。別のアカウントを発行するから
  仕組みとして用意しない。**」
- **意味**: **1つのログインアカウントは1つの役割しか持たない。**
  スタッフのアカウントはスタッフの画面へ、飼い主のアカウントは飼い主の画面へ。
  兼務する人は**アカウントを2つ持つ**。振り分けに「両方持つ人」の分岐は要らない。

- **⚠️ これは2度目の回答である。** 一度答えの出ている問いを私がもう一度聞いた。
  1度目は `D-20260823-06` 以降の一連（`docs/decisions.md:216` `:374`）で、
  「マスターのアカウントが staff かつ owner だから穴に落ちた」という**症状の側**は
  記録されていたが、**「その形を今後も前提にするのか」という決定として残っていなかった**。
  記録が症状止まりだと、次に読む人（＝私）が同じ問いを立て直す。
  **決定は、症状ではなく決定として残す。**

- **同時に指摘された私の書き方の誤り**: 質問文に「飼い主リンクが0のスタッフ」と書いた。
  これは `owner_users` の行数（内部の変数名 `ownerLinks`）をそのまま出したもので、
  **読む人には意味が通らない**。しかも指しているのは「自分の犬をこの店に預けていない
  トリマー」＝**ふつうのスタッフ全員**で、特別なケースですらなかった。
  つまり**判断を仰ぐ必要のない話を、内部の語で判断待ちにしていた**。
  マスターへの問いは、**画面と人の言葉**で書く（`D-9` と同じ精神）。

- **`D-20260823-06` の位置づけが変わる**: あれは「管理者を飼い主にも紐付ける」決定で、
  ⑥の画面に到達して確認するための便宜だった。**その形を本番の前提にはしない。**
  以後、兼務アカウントの救済（`[data-staff-link]` の出し分けなど）は
  **作らない・増やさない**。

- **消す対象（入口の作り直しと同時に行う。いま単体では消さない）**:

  | 場所 | 何 |
  |---|---|
  | `backend/js/supabase-auth.js` | 兼務者を `/my` に留めて `[data-staff-link]` を出す分岐 |
  | `src/index.html` / `src/my.html` | `[data-staff-link]` の器 |
  | `supabase/seed.sql` | `staff-owner@local.test` とその紐付け |
  | `scripts/lib/local-stack.mjs` | `FIXTURE.staffOwnerEmail` |
  | `scripts/verify-screens.mjs` | 5〜8（兼務者の入口を見る4件） |
  | `test/supabase-auth.test.mjs` | `:295-296`（seed に兼務アカウントが在ることを要求） |

  **単体で先に消さない理由**: いま消すと、置き換わる新しい振り分けがまだ無いまま
  **検査だけが4件減る**（`A-4`「検査を弱めて緑にしない」に触れる）。
  入口を1本にする作業の中で、**新しい振り分けの検査と入れ替える**。

- **【追記 2026-09-04・実際にやったこと】** 入口を1本にする作業（`ca79196` 以降）で、
  上の表のうち**製品側の3件は消した**（`supabase-auth.js` の分岐 / `src/my.html` の
  `data-staff-link` / `verify-screens` の兼務者向け4件）。
  **検査の土台3件（`seed.sql` の `staff-owner@local.test` / `FIXTURE.staffOwnerEmail` /
  `test/supabase-auth.test.mjs` の fixture 確認）は、消さずに残した。**
  理由: 兼務の口座は「**スタッフ権限があれば必ず作業画面へ**」を試すのに**いちばん
  きつい形**で、飼い主リンクも持つ人で確かめられる。仕組みとして用意しないことと、
  検査で使わないことは別。新しい `verify:screens` の `5.`〜`8b.` はこの口座を使う。
  **上の表は「消す対象」として残っているので、そのまま読むと判断を誤る**
  （`docs/watch.md` W-8「古い記録は、次の人の判断をそのまま狂わせる」）。この追記が正。

- **Impact**: 入口の作り直しで決めることが1つ減った。振り分けは
  「スタッフの権限を持つか / 持たないか」の**1本の分岐**でよい。
