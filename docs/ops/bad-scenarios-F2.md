# F2 バッドシナリオ（10個）

承認: 未

> **F2 の完了条件（`AGENTS.md` D-14）**
> **1** 正解の手順で**最後まで到達できた** ／ **2** 操作を間違えても**2タッチ以内**にやりたかった操作を完了できた
> 判定は**スクリーンショットのみ**。**コードは一切見ない。**
>
> 選ぶ基準は1つだけ: 「これを1つ見落としたら、**1 か 2 のどちらかが満たせなくなるか**」。
> ならないものは出さない。10個に届かなくても水増ししない（マスター指定）。
> ルールは2行で書く（`AGENTS.md` D-17）。**人間の行だけ読めば、承認の可否は判断できる。**
>
> **未実行**（承認前）。結果はすべて `未`。

---

### 1. 新しいお客さんの犬を、作る場所がどこにも無い — 結果: 未
- **人間**: 一覧に並ぶのは既にいる犬だけ。初めてのお客さんが来たとき、カルテを始める入口が画面に無い。
- **AI**: `design/mock-4step.html` に `App.createNewKarte()` のボタンが 2件在るのに `src/index.html` は「＋」「新規」が **0件**。実体は `src/js/ui.js:127` に生きており、押す場所だけが無い（`D-20260824-37` と同型・4回目）。確かめ方: `grep -c 'createNewKarte' design/mock-4step.html src/index.html`

### 2. 戻る手段が、消す予定のタブに乗っている — 結果: 未
- **人間**: 間違えたときに戻る道が、上の「01 02 03 04」の帯しかない。その帯は「本番の動線ではない」として消す候補に入っている。消したら戻れなくなる。
- **AI**: `src/index.html` の戻り導線は `btn-step`（`:1661-1664`）と `goToStep()` のみ。その `01〜04` タブは `docs/deferred.md` #2 で撤去候補・マスター判断待ち。**完了条件2 がタブの存廃に丸ごと依存している。** 確かめ方: `grep -nE '戻る|goToStep' src/index.html` の結果がタブ以外にあるか数える

### 3. ④だけ直して、他の画面のはみ出しを見ていない — 結果: 未
- **人間**: カルテ作成の画面は直せても、他の画面が同じように画面からはみ出していたら、そこで止まる。
- **AI**: `D-20260825-39` で採用した `minmax(0, 1fr)` は `.editor-columns`（④）だけに効く。②一覧・⑤確認の `grid` は未計測。確かめ方: 全画面で `document.scrollingElement.scrollWidth > 390` を数える

### 4. 写真が出ず、文字だけが並ぶ — 結果: 未
- **人間**: 画面に写真が出ず、「ポンチ 仕上がり写真」のような**文字**がそのまま出る。絵で合否を決めるのに、絵が壊れて見える。
- **AI**: `src/index.html` の `<img>` 14件のうち **11件が `src=""`**（爪・歯の解剖図、仕上がり写真、皮膚被毛、耳、肉球ほか）。`src=""` は `img.src` プロパティが**現在のページURL**を返す形で、`D-20260824-30` #3 の再発条件そのもの。確かめ方: `grep -oE '<img[^>]*>' src/index.html` で `src=""` を数える

### 5. ⑤と⑥が同じ画面なので、「最後まで着いた」が言えない — 結果: 未
- **人間**: 「確認」と「お客様のページ」が同じ見た目なので、写真を見ても**どちらに着いたのか分からない**。
- **AI**: 画面は `screen-1`〜`screen-4` の **4枚しか無い**のに動線は6段階。⑤も⑥も `goToStep(4)` で同じ `screen-4`（設計どおり・`plan.md` 第4章）。**完了条件1「最後まで」の終点が絵で判別できない。** 確かめ方: `grep -oE 'id="screen-[0-9]*"' src/index.html`

### 6. 「2タッチ」の数え方を決めていない — 結果: 未
- **人間**: 「2タッチで戻れた」の**タッチが何を指すか**決まっていない。決まっていないと合格も不合格も言えない。
- **AI**: `scripts/walk-human.mjs` の mistakes モードは `M1-0` `M1-1` と番号を振るが、スクロール・入力・ダイアログを1タッチに数えるかの定義が無い。確かめ方: `.human/mistakes/_操作ログ.txt` を読み、番号の付け方が一意に決まるか見る

### 7. `src` を直したのに、画面は古いまま — 結果: 未
- **人間**: 直したはずなのに、写真を撮ると直っていない。配信しているのは別の場所だから。
- **AI**: `npm run serve` / `walk` が配るのは **`dist/`**（`scripts/serve-ui.mjs:14`）。`npm run build` を挟まないと `src/` の変更は画面に出ない。`dist/` は `.gitignore` 管理外。確かめ方: `src/index.html` を1文字変えて `build` せずに `walk` し、写真に反映されるか見る

### 8. 1回通っただけで「通った」と書く — 結果: 未
- **人間**: たまたま1回うまくいったのを根拠にしない。
- **AI**: `F-20260821-23`（2回目で必ず落ちた）と `D-18` 偽-11。`walk` は自前でサーバを立てる（`walk-human.mjs:33`）。確かめ方: **同一コミットで `npm run walk` を3回連続**実行し、3回とも同じ結果になるか見る

### 9. 絵で決めるはずの合否を、コードを読んで決める — 結果: 未
- **人間**: 画面の写真だけで決めると決めたのに、中身を読んで「大丈夫そうだ」と判断してしまう。
- **AI**: `D-14` と `D-18` 偽-12（人間の合否を機械の合否に置き換える）。`npm run check` / `npm test` が緑でも完了条件の根拠にしない。確かめ方: 完了報告の根拠が `.human/` の画像だけになっているか、報告文を読み返す

### 10. モックに無いものを足す／モックに在るものを落とす — 結果: 未
- **人間**: 良かれと思って自分でボタンや文言を足さない。逆に、モックに在るものを黙って落とさない。
- **AI**: `plan.md` 第3章「意匠モックが正。自分の発案を混ぜない」・`D-18` 偽-7。#1 は**落とした側**の実例。確かめ方: `design/mock-4step.html` と `src/index.html` の `onclick` 集合を差分で比べる

---

## 水増しを避けて落とした候補

- **担当トリマーの既定文2か所**（`src/index.html:1953` `:2076`）— 飼い主に届く**中身**の問題で、到達性にも戻りやすさにも影響しない。マスター判断待ちとして `failure-check-F1-end.md` に記載済み
- **`.body-marking-stamps` の横スクロール** — 部品の中だけで、ページ全体は 390px に収まる。`docs/deferred.md` #11 に登録済み・マスター判断待ち
- **PWA が未結線** — ホーム画面に追加したときの話で、動線の到達性を崩さない（`docs/deferred.md` #4）
- **`ui.js:102` の「4歳 / 2.79kg」固定** — 表示内容の誤りで、到達も戻りも妨げない（`docs/deferred.md` #7）

## 承認後にやること

1. `承認: 未` を `承認: 済` に書き換える
2. 10個を**実際に実行**し、各項の `結果: 未` を `該当せず` / `該当した` に書き換える
3. 1つでも `該当した` が残る間は、F2 を閉じない（`node scripts/guard/gate.mjs --end` が止める）
