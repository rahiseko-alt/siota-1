# F2 再発防止チェック（開始時）

対象: `docs/failures.md` 全 **32件**（`grep -c '^### \[F-2026' docs/failures.md`）
参照: `docs/decisions.md` 全 **41件**（`grep -cE '^### \[?D-2026' docs/decisions.md`）
実施: 2026-08-25
根拠は**見たファイル名か、実行した命令とその出力**にする。「たぶん大丈夫」は書かない。

---

## この時点の実測（すべて実行した）

| 命令 | EXIT | 中身 |
|---|---|---|
| `npm run build` | **0** | 34件 |
| `npm run check` | **0** | guard / 隔離 src / 隔離 dist / src↔dist parity / design 隔離 |
| `npm test` | **0** | 64件 |
| `npm run walk` | **1** | ④→⑤で `locator.tap: Timeout 30000ms`（463px 問題・`D-20260825-39` で直し方が決定済み） |

**F2 の完了条件**（`AGENTS.md` D-14）: **1** 最後まで到達できた ／ **2** 間違えても2タッチで戻れた。
**判定はスクリーンショットのみ。コードは一切見ない。**

---

## 起こり得るもの（F2 の完了条件を崩し得るもの）

| 失敗ID / 決定ID | 何が起きたか（1行） | いま起こり得るか | 既に起こっていないか | 根拠（実行した命令と出力） |
|---|---|---|---|---|
| **D-20260824-37** | 機能は生きているのに、どこからも押せない | **○** | **既に起きている（4回目）** | `grep -oE 'onclick="App\.[a-zA-Z]+' design/mock-4step.html src/index.html` を突き合わせると、**モックにあって src に無いのは `createNewKarte` と `selectKarte` の2つ**。`selectKarte` は `src/js/ui.js:30` の `card.onclick` で結線済みだが、**`createNewKarte` は実体（`ui.js:127`）が生きたまま押す場所が 0件**（`grep -cE '＋|新規' src/index.html` = 0） |
| **F-20260821-11** | 緑を完了の根拠にし、機能そのものを確かめなかった | **○** | **×（まだ起きていない）** | `build`/`check`/`test` が EXIT 0 の一方で `walk` は EXIT 1。**この3本の緑を F2 の根拠にしてはいけない**（完了条件は D-14 の2点だけ） |
| **F-20260821-23** | 新設した検査が2回目の実行で必ず落ちた | **○** | **未確認** | `walk-human.mjs:33` が自前で `startUiServer(PORT)` を立てる。in-process なので workerd 残留は起きないが、**3回連続 EXIT 0 を一度も取れていない**（現在 EXIT 1 のため） |
| **F-20260821-08** | PWA が未結線 | **○** | **既に起きている** | `grep -c 'rel="manifest"' src/index.html` = 0。ただし**ホーム画面追加の話で、動線の到達性は崩さない**（`docs/deferred.md` #4） |
| **F-20260821-06** | 画面の見出しがデモ既定値のまま出る | **○** | **既に起きている** | `src/js/ui.js:102` がどの犬にも「4歳 / 2.79kg」を入れる。`dummy.js` は犬ごとに `age`/`weight` を持つ。**表示内容の誤りで、到達も戻りも妨げない**（`docs/deferred.md` #7） |
| **F-20260821-14** | 誰も書いていない定型文が飼い主に届く | **○** | **既に在る（種として）** | `src/index.html:1953` の `<textarea id="editor-trimmer-letter">` と `:2076` に同じ長文が初期値で入る。**マスター判断待ち**（`failure-check-F1-end.md`）。F2 の完了条件には当たらない |
| **D-20260824-30 #3** | 空の写真スロットにページURLが入り、壊れた画像が飼い主に届く | **○** | **種は在る** | `grep -oE '<img[^>]*>' src/index.html` の 14件中 **11件が `src=""`**。`img.src` プロパティは `src=""` のとき現在のページURLを返す。**いまは保存しないので実害は無いが、F3 で繋いだ瞬間に再発する。** F2 では「絵が壊れて見える」問題として現れる |
| **F-20260821-22** | 意匠モックを本番の実体の場所にそのまま置いた | **○（今回は意図的）** | **方針としては正しい** | `plan.md` 第3章が「意匠モックが正」と指示。`onclick` 集合の差分で **src 側だけの追加は 0件**＝私の発案の混入は無い |
| **F-20260825-28 / -29** | 「機械で確認」の条件を機械を作る前に完了と判定した／該当しない判定になる仕様を書こうとした | **○** | **×** | F2 の完了条件は**機械では測れない**（絵で人間が決める）。だからこそ**「walk が緑になる仕様」に寄せる誘惑**が正面にある。`D-18` 偽-12 |
| **F-20260825-30 / -31** | 自作の検査が、正常系で誤検出・誤読した | **○** | **×** | F2 で `walk-human.mjs` に手を入れるなら、**赤くなることと緑のままであることの両方**を本物の入力で確かめる |

---

## 起こり得ないもの（F2 の範囲では成立しない）

| 失敗ID | 何が起きたか（1行） | 根拠 |
|---|---|---|
| F-20260821-01 | `check` が実体の無い場所を指し、一度も動いていなかった | F1 で解決（`solved-F1.md` #9）。`src-dist-guard.config.json` は 2項目とも実在し、実在しない場所を指したら EXIT 1 |
| F-20260821-02 | `--test-isolation=none` でテスト全滅 | `grep -c 'test-isolation' package.json` = 0。`npm test` は 64件が実際に走る |
| F-20260821-03 / -19 | 実在しない対象へ「必ず通せ」と指示し続けた | F1 で解決。`AGENTS.md` が指す `npm run *` のうち実在しない3本には「F3 で作り直すまで存在しない」と明記済み |
| F-20260821-04 / -05 | KV の `/api/config` 欠落／存在しない reportId を fetch | `node scripts/guard/isolation.mjs src` = EXIT 0。**UI に `/api/` も `fetch(` も 0件**（F1 の条件B） |
| F-20260821-07 | 外部 CDN 依存 | 同上（条件B が外部URLを 0件と保証。`dist` 側も同時に検査） |
| F-20260821-09 / -10 | 移設元を見ないと決着しない | vibe-base への参照は打ち切り済み（`AGENTS.md` D-14 末尾） |
| F-20260821-12 / -13 / -17 / -18 / -25 | 保存・復元の欠落／stored XSS／エスケープの文脈違い／fetch のレシーバ | **F2 に保存経路が無い**。`isolation.mjs` 条件B が UI からの通信を 0件で保つ。これらは **F3 で繋ぐ段の論点**（`docs/deferred.md` #8 に検査の復活を登録済み） |
| F-20260821-15 | カルテ0件の犬に架空の履歴 | 仮データ（`dummy.js`）で動く段階なので、実データの空状態は F3 の論点 |
| F-20260821-16 | 直せる不具合を「判断が要る」と称して投げ返した | `docs/deferred.md` 11件はすべて「先へ進めるか」を判定済み。463px は**2択で諮ったうえ、実測して3つ目の答えを出した**（`D-20260825-39`） |
| F-20260821-20 / -21 | 受入基準の読み替え／実行しなかったことを指摘が無かったと報告 | F2 の完了条件は語ではなく**絵**。読み替える余地が構造的に小さい |
| F-20260821-24 / F-20260823-01 | マイグレーションが一度も実行されていない | F2 で SQL は触らない（`scope.mjs` の F2 allow に `supabase/` が無く、触ると EXIT 1） |
| F-20260823-26 / -27 | 日付結合の欠落／日付ピッカーの既定値 | `grep -c 'type="date"' src/index.html` = 0。日付入力が現 UI に無い |

---

## 起こり得るもの — 何をすれば防げるか（1件1行）

1. **D-20260824-37**: 「＋ 新規カルテを作成する」を `design/mock-4step.html` から `src/index.html` へ移す（`plan.md` 第3章 手順2）。**モックの該当ブロックを移すのであって、自分でボタンを設計しない。**
2. **F-20260821-11**: F2 の完了報告に `build`/`check`/`test` の EXIT 0 を**根拠として書かない**。根拠は `.human/` の画像だけにする。
3. **F-20260821-23**: `npm run walk` を**同一コミットで3回連続**実行し、3回とも同じ結果になることを確認してから「通った」と書く。
4. **D-20260824-30 #3**: `src=""` の `<img>` 11件について、F2 では**絵として壊れて見えるか**だけを見る。直すなら空スロットの表現に寄せる（`src` 属性を持たせない）。**F3 で保存に繋ぐ前に必ず再確認する**と `docs/deferred.md` に残す。
5. **F-20260825-28 / -29**: 完了条件を「walk が緑」に読み替えない。**walk は画像を撮る道具であって、合否を出す道具ではない**（D-14）。
6. **F-20260825-30 / -31**: `walk-human.mjs` に手を入れるなら、**正常系（緑のまま）と異常系（赤くなる）の両方**を実際に走らせて確かめる。
7. **戻り導線（バッドシナリオ #2）**: `01〜04` タブを撤去するかはマスター判断（`docs/deferred.md` #2）。**F2 では撤去しない**——撤去すると完了条件2の唯一の手段が消える。
8. **463px（`D-20260825-39`）**: `.editor-columns` を `minmax(0, 1fr)` にする。**文字サイズ・余白は変更しない。** 直したら `npm run build` を挟んでから `walk`（`dist/` が配信元のため）。

---

## F2 で「直さない」と決めているもの（ルール④）

`docs/deferred.md` に **11件**（#1〜#11）。うち F2 で使う予定のものが #5（アイコン6点）、
マスター判断待ちが #2（01〜04タブ）・#11（スタンプ帯の横スクロール）。
**このフェーズ中は触らない。** F3 完了後にマスターが「直す／放置」を決める。
