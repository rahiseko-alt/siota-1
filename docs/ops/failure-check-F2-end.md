# F2 再発防止チェック（完了時）

対象: `docs/failures.md` 全 **34件** ／ 参照: `docs/decisions.md` 全 **42件**
実施: 2026-08-25
前提: `docs/ops/failure-check-F2-start.md`（開始時）の項目を、1件ずつ現況で見直したもの

---

## この時点の実測（すべて実行した）

| 命令 | EXIT | 中身 |
|---|---|---|
| `npm run build` | **0** | 34件 |
| `npm run check` | **0** | guard / 隔離 src / 隔離 dist / src↔dist parity / design 隔離 |
| `npm test` | **0** | **64件** |
| `node scripts/guard/solved.mjs F2` | **0** | 「該当した」7件すべてが3出力で裏づけ済み |
| `npm run walk` | **0** | ①〜⑥ 5コマ（**3回連続**で同じ結果） |
| `npm run walk mistakes` | **0** | 8コマ |

**F2 の完了条件（D-14）は、マスターが実機で操作して合格と判定した**（`D-20260825-40`）。
スクリーンショットより強い証拠（Runtime Evidence）。

---

## 開始時に「起こり得る」と書いたものの現況

| 失敗ID / 決定ID | 開始時の判定 | いまどうなったか |
|---|---|---|
| **D-20260824-37**（押す場所が無い） | 既に起きている（4回目） | ✅ **潰した。** モックのブロックを逐語で移し、実ブラウザで押せることを確認（`grep -c createNewKarte src/index.html` = **1**）。**自分でボタンを設計していない** |
| **F-20260821-11**（緑を完了の根拠にする） | 起こり得る | ✅ **繰り返していない。** 完了の根拠は `.human/` の画像と**マスターの実機操作**。`build`/`check`/`test` の緑を根拠にしていない |
| **F-20260821-23**（2回目で落ちる） | 未確認 | ✅ **確認した。** `npm run walk` を**同一コミットで3回連続**実行し、3回とも EXIT 0・5コマ |
| **D-20260824-30 #3**（空スロットにページURL） | 種は在る | ✅ **潰した。** `src=""` を属性ごと削除（11件 → **0件**）。実ブラウザで `img.src` が `http://127.0.0.1:8831/` → **空文字**になることを確認 |
| **F-20260821-22**（モックをそのまま置く） | 方針としては正しい | ✅ **逸脱なし。** `onclick` 集合の差分で **src 側だけの追加は 0件** |
| **F-20260825-28 / -29**（緑になる仕様に寄せる） | 起こり得る | ⚠️ **一度やりかけた。** 提案した10個を確かめずに承認を求めた（`F-20260825-32`）。マスターの指摘で全件実測し、**3件が該当せず**・**11個目が発覚**。該当しなかった3件も消さずに残した |
| **F-20260825-30 / -31**（自作の検査が誤判定） | 起こり得る | ⚠️ **また起きた。** 測定スクリプトが `MODULE_NOT_FOUND` を「違反を捕まえた」と誤読（`F-20260825-32`）。直してから測り直した |
| **F-20260821-08**（PWA 未結線） | 既に起きている | ⏸ あと回し（`docs/deferred.md` #4）。到達性を崩さない |
| **F-20260821-06**（既定値が残る） | 既に起きている | ⏸ あと回し（`docs/deferred.md` #7）。到達性を崩さない |
| **F-20260821-14**（誰も書いていない定型文） | 種として在る | ⏸ **マスター判断待ち**（`docs/deferred.md` #13）。**新規カルテにも既定文が入る**ことを実測で確認した |

---

## F2 の作業中に**新しく**起きた失敗

| ID | 何が起きたか | 状態 |
|---|---|---|
| **F-20260825-32** | 「見落とし10個」を根拠を確かめずに提案し、うち3個は該当しなかった。**11個目（検査が入力していない）は提案に入っていなかった** | CLOSED（全件実測して根拠を付けた） |
| **F-20260825-33** | 「これで試せます」と案内した手順が、**マスターの環境（Windows）では何も起きなかった** | CLOSED（`pathToFileURL()` に修正・**マスターの実機で動作確認済み**） |

**どちらも「自分が動かせる環境でしか確かめていない」型。**
`F-20260825-33` は `AGENTS.md` D-15 に「どの AI でも実行できるコマンド」と書きながら
**どの OS でも、を書いていなかった**ことが原因だった。

---

## いま「起こり得る」もの — F3 で最初に見るべきもの

| # | 何が起こり得るか | 根拠 |
|---|---|---|
| 1 | **④で書いた内容が⑥に届かない** | `docs/deferred.md` #12。実測で確認済み（④「今日はおとなしくしていました。」/ ⑥ 既定文）。**F3 の結線表そのもの**。`AGENTS.md` D-12「押せた ではなく 同じ値で届いた で見る」 |
| 2 | **飼い主に届く中身を見る検査が1本も無い** | `docs/deferred.md` #8。`verify:*` 7本が削除済みで、`AGENTS.md` D-9〜D-12 の機械強制が**いま存在しない**。復元元は `git show 6685df5^:scripts/verify-*.mjs` |
| 3 | **削除の順序（Storage → DB）を知らずに削除導線を作る** | `docs/deferred.md` #10。`D-20260824-34`。逆にすると写真が誰からも取れなくなる |
| 4 | **`finalize_report` の `null` を成功として扱う** | `AGENTS.md` D-2。例外を投げないので握ると draft のまま残る |
| 5 | **SQL を一度も実行せずに置く** | `docs/deferred.md` #9。`verify:migrations` が `npm test` から呼ばれていない |

---

## 対の指標（緑の隣に、逃がした数を並べる）

| | 件数 |
|---|---|
| F2 で**解決**した（3出力あり） | **7 件**（+ 463px） |
| F2 で**保留**にした | **1 件**（#2 戻る導線・`deferred` #2 に番号を添えた） |
| `docs/deferred.md` の**あと回し** | **14 件**（F2 開始時 11 → +3） |
| `docs/failures.md` | 全 **34件** / OPEN **1件** |
| `docs/watch.md`（経過観察） | **未作成**（`plan.md` 第5章が「空のままのフェーズは見落としを疑う」としている） |
| `docs/ops/template.md`（ひな形4部） | **未作成** |

**F2 は「正UI だけで動線が通ることを人間が確かめられた」フェーズ**であって、
アプリが完成したフェーズではない（ルール②）。実データはまだ1件も流れていない。
