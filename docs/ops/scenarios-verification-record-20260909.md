# シナリオ台帳210件・実操作検証の記録（2026-09-09）

> マスター指示（2026-09-09）「検証用サブエージェントに、人間と同じ操作で実際に画面のスクショを
> 受け入れ条件として検証させろ。禁止事項はコードを見ること自体を禁止。実操作とスクショ以外の
> 行為を一切禁止。それで全シナリオやれ」を受けて実施した210件（`docs/ops/scenarios.md`）の結果を、
> **問題無し／問題あり／途中までは確認した（内容も）／確認できなかった**の4区分に整理した記録。
>
> **正の場所はあくまで `docs/ops/scenarios.md`。** この文書は読みやすくするための整理であり、
> 個別の結果を直すときは台帳側を直すこと（この文書は台帳の**要約**という位置づけ。
> 数字が食い違ったら台帳が正）。
>
> **③と④の分け方**: 「実際に操作して、何か具体的な事実（無いことの確認を含む）が得られたか」で分けた。
> テストデータが空だった、環境に写真置き場が無かった等で**何も分からなかったもの**は④。
> 得られなかった目的のかわりに、**別の事実**（例:「その機能自体が無い」「反応が全員同じだった」）
> が分かったものは③。

## 内訳

| 区分 | 件数 |
|---|---|
| ① 問題無し（OK） | 143 |
| ② 問題あり（NG） | 11 |
| ③ 途中までは確認した（内容も） | 18 |
| ④ 確認できなかった | 38 |
| 合計 | 210 |

---

## ① 問題無し（OK・143件）

台帳どおりに動いた。番号のみ列挙する（内容は `docs/ops/scenarios.md` の各行参照）。

- **お店の人・ふつう（S・47件）**: S-1〜S-34, S-36〜S-48（S-35は③・S-49/50は③へ）
- **お店の人・危険（SD・30件）**: SD-1〜7, SD-10〜12, SD-15〜20, SD-25, SD-26, SD-28, SD-30〜33, SD-42, SD-44, SD-45, SD-47〜50
- **飼い主・ふつう（O・28件）**: O-3〜8, O-10, O-16, O-27, O-28, O-30〜36, O-38〜43, O-45〜48, O-50
- **飼い主・危険（OD・30件）**: OD-1〜4, OD-8〜13, OD-21〜23, OD-28〜32, OD-35〜38, OD-40〜46, OD-48
- **未ログイン（G・8件）**: G-1〜6, G-9, G-10

---

## ② 問題あり（NG・11件）

放置リスト（`docs/ops/plan.md` 第12章）`#53` `#55`〜`#61` に対応。

| # | ゴール（そうなってほしい結果） | 実際に起きたこと | 放置リスト |
|---|---|---|---|
| `SD-9` `SD-23` | 体重・メッセージは未入力なら確定できない | 空欄のまま「確定」できてしまう | `#55` |
| `SD-22` | 体重に現実的な上限がある | `9999`と打っても止まらず、そのまま飼い主に届く | `#56` |
| `SD-21` | 同じ犬・同じ日は1枚 | 2枚目も確定でき、過去カルテに同日が2つ並ぶ | `#57` |
| `SD-24` | 他人の個人情報が紛れ込まない | メッセージ欄に他の実在客の名前を書いても無警告で確定 | `#58` |
| `SD-8` `SD-46` | 同時編集は起きない／検知される | **2人が同時に同じ確定済みカルテを保存すると、後の人の内容で無警告に上書き。履歴も残らない**（2ブラウザで実演・再現） | `#59` |
| `SD-27` `SD-29` | 修正時に写真を足せる | `invalid report context`で保存失敗。写真だけStorageに迷子で残る | `#60` |
| `SD-43` | 招待QRの発行に回数制限がある（台帳の想定） | **10回連続発行しても止まらなかった**（想定と実物が食い違っていた） | `#61` |
| `O-37` | 「戻る」で愛犬の一覧に戻る | 1回では戻らず、その犬のカルテ一覧に留まる（2回目で戻る） | `#53` |

---

## ③ 途中までは確認した（内容も・18件）

目当ての結論には届かなかったが、**実際に操作して何らかの具体的な事実**は得られた。

| # | ゴール | ここまで確認できた内容 |
|---|---|---|
| `S-35` | マイクで話した内容が文字に入る | マイクでの実音声入力はできない環境。ただし**マイクボタン・テキスト入力欄自体は画面に存在する**ことは確認した |
| `S-49` | 招待を取り消すと入れなくなる | **「取消」ボタンの存在は確認した**（S-48のモーダル内）。招待発行APIがレート制限（429）にかかり、取消→再アクセス不可までは確認できなかった |
| `S-50` | 飼い主の結び付きを解除できる | S-49と同じ理由で未完了。**解除対象の使い捨て飼い主とその犬は用意済み**（次回そのまま使える） |
| `O-20` | 歯の写真が表示される | 写真の実体が無い状態でアクセスすると**アプリはエラーバナーで明確に失敗を示し、クラッシュはしない**ことを確認した |
| `OD-5` | 削除予定のカルテを開くと見えてしまう | **「削除予定」という待機状態を持つ機能自体が管理画面に存在しない**ことを確認した（削除は即時・不可逆） |
| `OD-6` | 他人の写真URLを直接開いても見えない | **本人・他人・無認証のすべてが同一の400エラー**になることは確認した。ローカルにStorageの置き場が無く「守りが効いて弾いた」のか「単に環境に無いだけ」かは区別できなかった |
| `OD-7` | 写真URLを転送しても、期限内なら他人が開ける | OD-6と同じ制約の中で、**写真の取得は毎回閲覧者本人のログイン情報を使っており、URL自体に使い回せる合鍵は付いていない**ことは確認した（部分的な傍証） |
| `OD-14`〜`OD-18`, `OD-20`（6件） | 招待の悪用（使い回し・期限切れ・推測・大量発行）が起きない | **お店の人・管理画面の「顧客新規作成」画面からは招待発行の入口が見当たらなかった。**ただし【曖昧】これは誤りの可能性が高い——同じ日の別バッチ（S-48・SD-43）は**実際に招待QRを発行できている**（別の入口＝犬の詳細画面から）。飼い主側からの探索だけでは見つけられなかっただけで、機能自体は存在する。次回は招待の入口をS/SDバッチと同じ場所から辿って再確認が要る |
| `OD-25` | 退店後にログインすると見えてしまう | **「退店（解約）」の操作自体が管理画面に存在しない**ことを確認した |
| `OD-26` | 犬を譲渡した後、前の飼い主に見えてしまう | **「譲渡（飼い主の付け替え）」の操作自体が管理画面に存在しない**ことを確認した |
| `OD-33` | 写真の名前に細工した文字を入れても実行されない | **アップロード自体はエラーなく受け付けられる**が、**保存後のカルテには`assets: []`のまま残らない**（＝そもそも保存が成立していない）ことを確認した |
| `OD-39` | 監査ログが漏れない | **管理画面に監査ログ画面が無く、それらしいAPIも404**であることを実際に叩いて確認した（漏れる対象自体が存在しない） |
| `OD-50` | 通信を盗み見られても中身が読まれない | ローカルはHTTPなので本番相当のTLS確認はできないが、**認証トークンがURLやクエリ文字列に含まれていないこと**は確認した |

---

## ④ 確認できなかった（38件）

操作のしようが無かった、または得られた情報が実質ゼロだったもの。

**テストデータに記入例が無く、表示内容そのものが確認できなかった（15件）**
`O-9` `O-11` `O-12` `O-13` `O-14` `O-15` `O-17` `O-18` `O-19` `O-21` `O-22` `O-23` `O-24` `O-25` `O-26` `O-29`
（体重・爪・耳・歯・BCS・犬体図・仕上がり写真・使用オプション・メッセージのいずれも、練習用の犬に記入が無かった。`O-21`は`O-20`と同じ写真に依存し、単独では何も得られなかった）

**招待の実物が手元に無く、拾って使う側の操作ができなかった（3件）**
`O-1` `O-2` `G-8`

**負荷試験・大量アクセスは指示により対象外（1件）**
`OD-19`

**物理的な状況・人間の行動で、操作として再現できないもの（3件）**
`OD-24`（端末紛失）／`OD-27`（SNS投稿）／`OD-34`（写真40枚を用意できず重さを再現できない）

**マスターの作業・検索エンジン等、この場では扱えないもの（2件）**
`OD-47`（鍵の管理）／`OD-49`（検索エンジン）

**長時間の経過が必要で、1回の操作では確認できないもの（2件）**
`SD-13`（自動ログアウトの有無。トークンの仕組みからの推測はあるが実際に長時間放置して確かめてはいない）／`SD-14`（退職の概念自体が組織運用の話）

**本番配信・組織運用の話で、ローカルでは再現しようが無いもの（8件）**
`SD-34` `SD-35` `SD-36` `SD-37` `SD-38` `SD-39` `SD-40` `SD-41`（本番のダミー犬・鍵の入れ替え・デプロイ手順・ロールバック等）

**書き込み操作にあたり、指示により対象外としたもの（1件）**
`O-49`（新規カルテの追加）

**未ログイン画面に導線自体が無く、開けるURLが無かった（1件）**
`G-7`

（内訳: 15+3+1+3+2+2+8+1+1 = 38）

---

## まとめ

- **210件のうち143件は台帳どおりで問題無し**、**11件は実際の不具合**として放置リストへ。
- **経路が途中で止まる（行き止まりになる）ものは1件も無かった。**
- **③の`OD-14`〜`18`・`OD-20`は、記録として要注意。** 「招待機能が無い」という記述は誤りの疑いが強く、同日の別シナリオが実際に招待を発行できている。次にこの6件を扱うときは、まずこの矛盾を解消してから判定し直すこと。
