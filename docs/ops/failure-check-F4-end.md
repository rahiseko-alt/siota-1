# F4 再発防止チェック（完了時）

対象: `docs/failures.md` 全 **60件**（`grep -c '^### \[F-2026' docs/failures.md`）
参照: `docs/decisions.md` 全 **55件**（`grep -c '^### \[D-2026' docs/decisions.md`）
　　　**この照合で、記録そのものの欠陥を2件見つけた**（下の「記録の側の欠陥」）。
実施: 2026-08-29
根拠は**見たファイル名か、実行した命令とその出力**にする。「たぶん大丈夫」は書かない。

> **開始時（`failure-check-F4-start.md`）は 47件 / 53件だった。**
> F4 の中で失敗が **13件**、決定が **1件** 増えている（内訳は下の「F4 で新しく出した失敗」）。

---

## この時点の実測（すべて実行した）

| 命令 | EXIT | 中身 |
|---|---|---|
| `npm run build` | **0** | 36件 |
| `npm run check` | **0** | guard / 隔離 / src↔dist / design / 削除順序 / 繋ぎ方 / 検査台帳 / キー対応 / 見本文 / 画像台帳 / 鍵 / **空で合格していないか** / **壊して赤になったか** |
| `npm test` | **0** | **191件**（unit 100 / schema 56 / migration 6 / backend 29）＋ `test:sql`（SQL 10本を実 PostgreSQL に流して照合） |
| `node scripts/guard/delivery-ready.mjs` | **0** | 客に当たる経路 11本すべて証明済み |
| `node scripts/guard/checkin.mjs` | **0** | 「始めてよい」 |

### CI（**手元の緑ではなく、CI が出した緑**）

`master` の先端 `7c18c46` に対する **run #191**（2026-08-28T23:57 UTC・全 job 成功）:

| job | 結果 | 中身 |
|---|---|---|
| `check` | **成功** | `build` / `check` / `test` |
| `verify` | **成功** | 土台2本 ＋ 飼い主・トリマー・XSS・往復・空・画面・削除・下書き・招待・動線・管理者・写真の往復 |
| `walk` | **成功** | `D-14` の写真2組（正解の手順 / 間違えたとき） |
| `mutate` | skipped | `workflow_dispatch` のときだけ走る設計（意図どおり） |

---

## 開始時に「起こり得る」とした失敗が、いま起こり得るか

開始時の表の全行に、**実行した命令か、開いたファイル**で答える。

| 失敗ID / 決定ID | いま起こり得るか | 根拠（実行した命令と出力） |
|---|---|---|
| **F-20260827-46**（先送りの記録が古びる） | **○（機械は無い。人の手順として残っている）** | `docs/watch.md` **W-8**「行を読んだら、その行が名指しするファイルを1つ開く」。F4 の棚卸しはこの手順で回した。**機械化はできていない**——記録が現実と合っているかは、機械には判定できない |
| **F-20260827-47**（掃除で生きている値を落とす） | **起こらなかった（機械が在る）** | F4 の掃除で `npm test` が毎回通っている。`test:backend` の `backend-import` 29件が「読める形か」を見る。実測 EXIT 0 |
| **F-20260825-40 / -41 / -43**（検査が恒真・待たない・仕組みと違う所を見る＝ W-1 の型） | **まだ起こる。F4 でさらに 6回出した** | `F-20260828-48` `-49` `-50` `-51` `-55` `-56`。**うち2回は機械（`empty-pass`）が見つけた**——F3 まではゼロだった。ただし規則への昇格は**マスターが却下**し（`D-20260828-53`）、代わりに**「壊して赤になったところを見ていない検査は壊れているものとして数える」**という定義を置いた（`docs/ops/proof-of-red.md`）。**W-1 の役割は「何回起きたか」から「183件のうち何件を確かめたか」へ変わった** |
| **F-20260827-45**（検査が一度も実行されていない） | **起こらない** | `verify-inventory`（`npm run check` 内・実測「台帳 OK」）と `sql-verified`（`npm test` 内・実測「SQL 10本を照合」）。`docs/watch.md` W-7 も「いまは全部が辿れる」 |
| **F-20260826-42**（確かめずに「無い」と結論する） | **○（この段でも要る）** | F4 で実際に効いた: `F-20260828-57` は**この型を踏んで2時間積んだ**。逆に今回のチェックインでは「検査3本が赤」を**原因を当てずに実行して**、部品が入っていないだけと確かめている |
| **F-20260825-33**（自分が動かせる環境でしか確かめない） | **状況が変わった。下の「環境が変わった」を見ること** | 開始時は「**この環境には Docker が無い**」だった。**いまは在る**（`dockerd` 起動 → `docker ps` が応答 → `npx supabase start` が完了）。手元でも `verify:*` を走らせられる。**ただし納品条件 7-2「運用に Docker を要求しない」は変わっていない** |
| **F-20260821-24 / F-20260823-01**（マイグレーションが一度も実行されない） | **起こらない** | `npm test` の `test:sql` が **SQL 10本を実際の PostgreSQL に流して**照合する。実測「✅ SQL は、いまの中身のまま実際の PostgreSQL を通っている」。札を手で書き換える逃げ道は `sql-verified.mjs` が塞いでいる |
| **F-20260821-11**（受入基準を通すこと自体を目的にする） | **まだ起こる。F4 の中で1回起きた** | `docs/handoff.md` `0-L` にマスターの指摘が残っている——「**赤にすることが目的ではない。目的はなんだ？**」。台帳の数を追っていた。歯止めとして `0-M`/`0-N` に「**埋めるべきでないものは理由を書いて外す**」を入れ、`delivery-ready.mjs` に**1項ごとの理由欄**を機械として足した（`docs/handoff.md` `0-N` の「マスター判断 A」。**この判断は `decisions.md` に無い**——下の「記録の側の欠陥」2） |
| **F-20260821-14 / -12 / -13**（見本の文が届く／書いたのに消える） | **起こらない（機械が3枚）** | `no-sample-text`（実測「見本の文は無い」）＋ `key-parity`（実測「⑥14キー / ④8キー・対応 OK」）＋ CI の `verify` job の往復2本（run #191 成功） |
| **D-20260824-34**（削除の順序） | **起こらない** | `npm run check` の実測「✅ 削除の順序 OK（Storage を片付けずに DB を消す場所は 0 件）」＋ CI `verify` の削除の検査 |
| **F-20260821-09**（XSS Critical の箇所が特定できていない・**OPEN**） | **○（OPEN のまま。経路は増えていない）** | `Status: OPEN` は変わっていない。条件は「`innerHTML` の経路を増やさないこと」で、F4 で `innerHTML` を足す作業は無かった。CI `verify` の XSS の検査は成功している |
| **F-20260825-37**（別の層で「戻すと赤」を採る） | **起こらない** | F4 で足した3出力（`docs/ops/solved-F4.md`）は、いずれも**直した層そのもの**で赤を採っている。`node scripts/guard/solved.mjs` が `gate.mjs --end` から呼ばれ、①と③に同じ症状の行が無ければ止める |
| **D-20260827-45**（鍵3種はローテーション済み） | **—（済・機械も入った）** | `.gitignore` は F4 で塞いだ（`deferred` #32）。`npm run check` の実測「✅ 鍵は入っていない（鍵ファイル名3種 / 追跡ファイル175本を走査）」 |
| **D-20260827-46**（実顧客を入れる前にバックアップ） | **—（発火条件は手順書に載った）** | `docs/runbook.md` の `## 2-D. 実顧客のカルテを本番へ入れる前に見ること` の5項目。3出力は `docs/ops/solved-F4.md` の `#9`。**まだ発火していない**（実顧客は入れていない） |

---

## F4 で新しく出した失敗（13件）

| 失敗ID | 中身（1行） | Status | 機械で塞いだか |
|---|---|---|---|
| `F-20260827-44` | 「写真つき美容レポート」に写真を入れる口が1つも無かった | 解決済み | CI `verify` の「写真の往復」 |
| `F-20260827-45` | RLS の検査が一度も実行されていなかった | 解決済み | `verify:migrations` が `rls.sql` を流す |
| `F-20260827-46` | 先送りの記録が影響範囲を1つしか書いていなかった | 解決済み | **機械なし**（`docs/watch.md` W-8 の手順） |
| `F-20260827-47` | 掃除で同じ名前の生きている値を落とした | 解決済み | `npm test`（コミット前に止まった） |
| `F-20260828-48` | 「消えた」を見る検査2件が、一覧を引けていなくても緑だった | 解決済み | **`scripts/guard/empty-pass.mjs`**（`npm run check`）。3出力あり |
| `F-20260828-49` | その `empty-pass` 自身が、数える対象を取り違えていた | 解決済み | `test/empty-pass.test.mjs` |
| `F-20260828-50` | 「マイグレーションが当たっている」が DB 無しでも緑（同じ検査で2度目） | 解決済み | `verify:migrations` の書き直し |
| `F-20260828-51` | 体重の**グラフ**が描かれなくても誰も気づかなかった | 解決済み | `verify:roundtrip` にグラフの項を追加 |
| `F-20260828-52` | 「他人には見えない（RLS）」の検査が、他人の犬が丸見えでも緑 | **対応中** | 下の「まだ塞げていないもの」1 |
| `F-20260828-53` | CI の記録コミットが、無視ファイルを見せない `git status` に頼っていた | 解決済み | `--ignored` を付けた |
| `F-20260828-54` | `img.src = ''` の壊し方が、狙った2件には見えていなかった | 解決済み | 壊し方を属性側に直した |
| `F-20260828-55` | `verify-m6` の「②b」が `true` の直書きだった | 解決済み（**壊し方はまだ**） | 下の「まだ塞げていないもの」2 |
| `F-20260828-56` | `verify-admin` の「18.」が2つの独立した理由で常に合格 | 解決済み（**壊して赤はまだ**） | 下の「まだ塞げていないもの」2 |
| `F-20260828-57` | CI 即死の切り分けが、確かめていない前提の上に2時間積まれた | **未解決** | 下の「まだ塞げていないもの」3 |
| `F-20260828-58` | 壊し方を1つ足したら、製品は無傷なのにテストだけが3回に2回赤 | 解決済み | 3出力あり（サンドボックスで壊す） |
| `F-20260828-59` | 確定が id 無しを返したとき、画面はそのまま `/null` に着く | 解決済み | `test/report-commit-guard.test.mjs`。3出力あり |

> 表は16行あるが、`F-20260827-45` `-46` `-47` の3件は**開始時にはもう記録されていた**
> （開始時の書類が「F4 で現に踏み得るもの」として名指ししている）。
> **F4 の作業の中で新しく増えたのは 13件**——`-44` と `-48`〜`-59`。
> 数え方: `comm -13 <(git show f2f1fdd:docs/failures.md | grep -o '^### \[F-[0-9-]*\]' | sort) <(grep -o '^### \[F-[0-9-]*\]' docs/failures.md | sort)`

**F4 で増えた決定は2件**: `D-20260827-52`（バックアップの件は決着済み）と
`D-20260828-53`（W-1 の規則化を却下し、「壊して赤にならない検査は壊れているとみなす」を置く）。

---

## まだ塞げていないもの（正直に書く）

1. **台帳 183件のうち 41件は、壊しても赤になるか確かめていない**（`npm run check` の実測
   「壊して赤になったところを見た 142件 / まだ見ていない 41件」）。
   ただし **「客に当たる経路」11本は全件証明済み**で、残りは
   ①判定できない2本 ②F4 の後に回す1本 ③**1項ごとに理由をつけて外した13件**。
   `node scripts/guard/delivery-ready.mjs` が EXIT 0 で「F4 を閉じてよい」を返す。
   **数字（142/183）は「まだ全部を確かめていない」を隠さないために残してある。**

2. **`F-20260828-55` / `-56` は、機械の直しだけで「壊して赤」を見ていない。**
   直した本人が「直った」と言っているだけの状態で、`D-18` の3出力は揃っていない。
   台帳では未証明側（41件）に入っている——**隠していない**。

3. **`F-20260828-57`（CI 即死）は、現象が消えたが原因は分かっていない。**
   - 現象: run #156〜162 が全ジョブ数秒で失敗し、runner が一度も割り当てられなかった
   - **いま**: `master` の run #191 が **全 job 成功**（2026-08-28T23:57 UTC）
   - **原因は特定できていない。** 最有力だった「Actions の無料枠 / spending limit による停止」は
     **アカウント所有者（マスター）にしか残量が見えない**ので、ここでは確かめられない。
     **「直った」ではなく「いまは出ていない」と書く。** 月が替われば同じ止まり方をしうる
   - **これは製品の欠陥ではなく、検査を走らせる場所の問題**。ただし納品条件 7-1
     （渡した後も自動で検査が走る）は CI が前提なので、**納品の可否に直接効く**

4. **W-1 の型は F4 でも 6回出た**（`-48` `-49` `-50` `-51` `-55` `-56`）。
   規則への昇格はマスターが却下し、代わりに「壊して赤にならない検査は壊れている」という
   **定義**を置いた。**定義は置いたが、183件のうち 41件がまだその定義で未確認**——
   つまり W-1 は**閉じていない**。数え方が「事故の回数」から「残りの仕事」に変わっただけ。

5. **`F-20260821-09`（XSS Critical の箇所）は `Status: OPEN` のまま。**
   経路は増えていないが、特定はできていない。

6. **記録の書き方を、機械が縛っていない**（上の「記録の側の欠陥」1）。
   今回は1件だったので直せたが、**次に形が崩れても同じように気づけるとは限らない**。
   `failures.md` / `decisions.md` の見出しの形を `npm run check` で見る機械は無い。

---

## 記録の側の欠陥（この照合で見つけた2件）

**全件照合は「失敗が再発しうるか」を見る作業だが、今回は記録そのものが2か所壊れていた。**
どちらも中身ではなく**形**の問題で、だからこそ人の目には映らず、機械にも数えられていなかった。

### 1. 決定が1件、数える命令から見えていなかった（**直した**）

`docs/decisions.md` の最後の1件だけ、見出しが `## D-20260828-53 — …` と
**他の54件と違う形**（他は `### [D-2026…] …`）で書かれていた。
`failure-check-F4-start.md` が使っている `grep -c '^### \[D-2026'` は
**この1件を数えていない**——開始時の「53件」も、本書が最初に書いた「54件」も、
**実際より1件少なかった**。

- **直した**: 見出しを他の54件と同じ形にした。`grep -c` が **55** を返す
- **なぜ効くか**: `D-8`（失敗は1つの帳面にだけ書く）は「書く場所」を1つにしたが、
  **書き方**は機械で縛っていない。形が違う記録は、置いてあっても**在ることにならない**

### 2. マスターの判断が1件、`decisions.md` に無い（**直していない**）

`docs/handoff.md` `0-N` の「**マスター判断 A**」——`delivery-ready.mjs` に
1項ごとの理由欄を足す、という判断——は、**引き継ぎファイルにしか無い**。
`decisions.md` に項が無く、番号も付いていない。

- **直していない理由**: 決定の記録には**マスターが実際に言った言葉**が要る（他の項はすべて
  引用が入っている）。手元にあるのは「A を選んだ」という**私の側の要約だけ**なので、
  私が文面を作れば `F-20260825-29`（該当しない判定になる仕様を書く）と同じ型になる
- **引き継ぎは揮発する。** `docs/handoff.md` の冒頭にあるとおり、`## 0` より下は過去の記録で、
  次の人は読まない。**判断がそこにしか無ければ、次の人には無いのと同じ**（`D-19` と同じ形）
- **マスターへ**: この判断を `decisions.md` に残してよいか、残すなら文言をいただきたい

---

## 環境が変わった（開始時の前提が1つ古くなっている）

`failure-check-F4-start.md` は「**この環境には Docker が無い**（実測: `docker ps` が socket に繋がらない）」
「緑を主張してよいのは CI が出した緑だけ」と書いた。**この容器では Docker が動く。**

```
$ dockerd &            → API listen on /var/run/docker.sock
$ docker ps            → 応答する
$ npx supabase start   → 完了（127.0.0.1:54321）
```

**だからといって `F-20260825-33` が消えたわけではない。** 変わったのは
「**この容器では**手元でも実測できる」ことだけで、**次の容器では在るとは限らない**。
納品条件 7-2（**運用に Docker を要求しない**）は変えていない——手元の必須3本
（`build` / `check` / `test`）は Docker 無しで通る。

---

## F4 を閉じてよいか

**機械の答え**: `node scripts/guard/gate.mjs --end` が本書の作成をもって EXIT 0
（それ以前は「③ 再発防止（完了）が無い」の1件だけが残っていた）。

**人の答え**: 閉じてよい。ただし**上の「まだ塞げていないもの」5件は、閉じても消えない**。
とくに **3（CI 即死の原因が未特定）** は納品条件 7-1 に直接効くので、
**マスターに上げる**（`docs/handoff.md`）。
