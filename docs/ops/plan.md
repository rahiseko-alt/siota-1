# SALTY DOG カルテ — 整理整頓プラン（F1 / F2 / F3）＋ 監視の仕組み

> **F3 完了まで有効。** セッションを跨いでも破棄しない。
> 実行開始時に `docs/ops/plan.md` としてリポジトリへ置き、そちらを正とする。

## Context

本番は動いているが、**マスターが実際に使うと指定した動線どおりに進めない**。
私は「動線を直した」と報告していたが直しておらず、行き止まりに自分の思いつきで
ボタンを1つ足していただけだった。検査が9本すべて緑でも、人間は前に進めなかった。

原因は、UI とバックエンドが混ざっていて、**画面の良し悪しをバックエンドの緑で隠せる**こと。
そこで **UI を切り離し、UI だけで動線を完成させてから、繋ぎ直す。**
新しく作るのではなく、**いま在るものを並べ替える。**

さらに、私が計画から逸れることが繰り返し起きているので、**逸脱を機械が止める**。

---

## 重要ルール（マスター指定・全フェーズに適用）

| # | ルール |
|---|---|
| **①** | **現状あるものだけ**で F1〜F3 を完成させる。新しく作らない |
| **②** | **納品物の完成 ≠ フェーズの完成。** F3 が終わっても「売れる状態」ではない |
| **③** | このフェーズの目的は **現状の整理整頓** である |
| **④** | 期間中に見つけた問題は **原則あと回し**。例外は「直さないと先に進めないもの」だけで、その場合も**進める状態までしか直さない** |
| **⑤** | あと回しは `docs/deferred.md` に残し、**F3 完了後**に「直す／放置する」を決める |

**④の判断基準**: 「これを直さないと次の画面に行けないか？」だけで決める。
行けるなら直さず、`docs/deferred.md` に1行書いて先へ進む。

---

## フェーズ

### F1 — UI とバックエンドの隔離

**完了条件**
- `src/` に UI 以外が無い
- **UI から `backend/` への参照が 0**（機械で確認）
- `npm run build` / `check` / `test` が EXIT 0

**現在地: ほぼ完了**
```
src/      = UI だけ       index.html / js/ui.js / js/dummy.js / manifest.json / assets
backend/  = バックエンド   supabase-auth / supabase-staff / supabase-storage / vendor-entry
scripts/serve-ui.mjs      API も認証も無い静的配信（繋いでいたら壊れて分かる）
```
残: 隔離の機械確認を `npm run check` に足す。

### F2 — 正UI のみで動線を完了させる

**動線（マスター指定・意匠モックが正）**
```
① URLを開く → ② ログイン → ③ 犬の名前を選ぶ → ④ カルテ作成 → ⑤ 確認 → ⑥ 顧客ページ
```

**完了条件（D-14・スクショのみで判定・コードは見ない）**
1. 正解の手順で最後まで到達できる
2. 操作を間違えたとき、2タッチ以内にやりたかった操作を完了できる

**現在地: ④まで。⑤へ進めない。**
カルテ入力画面だけ横に **463px** 必要（iPhone 13 は 390、SE は 320）。
はみ出した結果、確定ボタンの上に別の要素が重なりタップを奪う。
→ **ルール④の例外。⑤に進める状態までだけ直す。**

**道具**: `npm run walk` / `npm run walk mistakes`（判定しない。絵を出すだけ）

### F3 — 正UI とバックエンドをつなぐ

**完了条件**: F2 の2点が**実データでも**同じように通る。
**着手条件**: F2 の2点が両方 ○ になってから。それまで触らない。

---

## 監視の仕組み（サブエージェント3本 ＋ Hook）

`.claude/agents/` に定義し、`.claude/settings.json` の Hook で発火させる。
**Hook は「サブを呼んだ証拠が在るか」を見て、無ければ止める。** 呼び忘れを防ぐため。

### ① 逸脱監視サブ `plan-guard` — 発火: **常時**

- **役割**: いまの行動が `docs/ops/plan.md` の**現在のフェーズの範囲内か**だけを見る。
- **Hook**: `PreToolUse`（`Edit` / `Write` / `Bash`）
  → `scripts/hooks/plan-guard.mjs` が、`docs/ops/phase` に書かれた現在フェーズと、
  そのフェーズで触ってよい場所を突き合わせ、**範囲外なら止めて理由を返す**。
- 例: F2 中に `backend/` を触ろうとしたら止める。F2 中に新しい依存を足そうとしたら止める（ルール①）。

### ② 提案サブ `bad-scenarios` — 発火: **フェーズ開始直後**

- **役割**: そのフェーズの**バッドシナリオを10個**出し、マスターに提案する。
- **制約（マスター指定・厳守）**
  - **本質的かつ単純な見落としに限定する**
  - **レアケース・影響度の小さい事柄を想定してはならない**
  - 「1つでも外すと、そのフェーズの完了条件が満たせなくなる」ものだけ
- **承認後**: 10個を**実際に実行**し、それぞれ**該当しないこと**を確かめてからフェーズを進める。
- **成果物**: `docs/ops/bad-scenarios-F{n}.md`（提案 → 承認印 → 実行結果）
- **Hook**: `PreToolUse` が、そのフェーズの成果物に**承認印が無い間は編集を止める**。

### ③ 再発防止サブ `failure-matcher` — 発火: **フェーズ開始直後 と 完了直後**

- **役割**: `docs/failures.md` と `docs/decisions.md` を照合し、
  **同じ失敗が起こり得ないか／既に起こっていないか**を確認する。
- **成果物**: `docs/ops/failure-check-F{n}-{start,end}.md`
- **Hook**: 開始時のものが無ければ編集を止める。完了時のものが無ければフェーズを閉じさせない。

### 置き場所

```
.claude/agents/plan-guard.md          逸脱監視
.claude/agents/bad-scenarios.md       バッドシナリオ提案
.claude/agents/failure-matcher.md     再発防止
.claude/settings.json                 Hook の配線
scripts/hooks/plan-guard.mjs          範囲外を止める
scripts/hooks/phase-gate.mjs          ②③の成果物が揃うまで止める
docs/ops/plan.md                      この計画（正）
docs/ops/phase                        現在のフェーズ（F1 / F2 / F3 の1行）
docs/deferred.md                      あと回しの記録
```

---

## 実行順

1. `docs/ops/` 一式と `.claude/` の3サブ・Hook を置く（**この作業自体は F1 の一部**）
2. Hook が実際に止めることを、わざと範囲外を触って確かめる
3. F1 を閉じる（②③のサブを回してから）
4. F2 開始 → ②が10個提案 → 承認 → 実行 → 動線を通す
5. F3

## 共通ゲート

`npm run build` / `npm run check` / `npm test` が EXIT 0。
フェーズの終わりに `docs/ops/plan.md` の「現在地」を更新する。
