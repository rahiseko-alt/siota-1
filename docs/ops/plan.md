# SALTY DOG カルテ — 完成までの実装計画（正・1本のみ）

> **納品まで有効。** セッションを跨いでも、AI が替わっても破棄しない。
> ルールの正は `AGENTS.md`（D-15）。この文書は**計画の正・1本のみ**。
> **2026-08-25 に練り直した**（マスター指示）。**2026-08-29 に再統合した**（マスター指示・下記）。
>
> **2026-08-29 の統合（重要）**: `docs/ops/delivery-plan.md`（P-1〜P-3）と `docs/deferred.md`
> （あと回しの記録37件）を、この1本に吸収した。**以後、計画ファイルはこれだけ**（`D-8`）。
> 旧2ファイルは廃止済み。**古いコメントや過去の記録が「docs/deferred.md #N」を指している場合、
> それは下の「第12章 放置リスト」の同じ番号を指す**（番号は変えていない）。

---

## いまやる番（機械が読む・毎セッション更新）

> `node scripts/guard/checkin.mjs` が実行時にこの行を画面へ出す。
> `checkout.mjs` は、このセッションでこの行を更新したかを見る（更新が無ければ EXIT 1）。

**いまやる番: ④完了バッジを本物の8項目チェックに作り直した（`0-AG`・PR #57を更新）。マスター指示「1、項目全てが対象だ。」＋「含めない（測定・評価項目だけを完了の対象にする・推奨）」を受け、`updateCompletionStatus()`を新設——コース／体重／ベスト体重／BCS／爪のチェック（前後とも）／耳のチェック（左右とも）／歯のチェック／担当からのメッセージの8項目を毎回すべて読み直して判定し、全ての入力経路（ボタン・セレクト・テキスト入力・音声入力・下書き再開・新規カルテ表示）から呼ぶよう配線した。写真・犬体図の印・使用オプションはマスター指示により対象外。build/check/test全緑、CIと同じ14本のverify:*も`db reset`直後に全緑、使い捨てPlaywrightスクリプトで8項目が個別に埋まって初めて全項目入力完了(8/8)になることを実機確認済み（詳細は`docs/handoff.md` 0-AG）。次のセッションがまずやること: 1) PR #57のCI確認・マージ・デプロイ。2) マスターに④店舗設定を開いてもらい使用オプション一覧が空かどうか確認（`0-AF`から持ち越し・まだ回答なし）・空なら手動で追加し直してもらう。3) それでも④に選択欄が出なければ、コード側にまだ見つけていない不具合が残っている——再調査が要る。4) `.btn-invite`（初回登録QR）の常時表示設計・「＋新規カルテを作成する」ボタンの扱いはマスター判断待ち。5) `0-Y`から持ち越しのPR #52分（ピンチ修正・次回来店時期）の本番⑤⑥目視確認。6) 残る5件（`#5` `#6` `#11` `#22` `#35`）の`AskUserQuestion`**

---

## 0. ゴールの定義

- **人間**: マスターが実店舗で1頭分のカルテを最後まで作り、飼い主がスマホでそれを見られる。
  そして**渡した後も、壊れたら壊れたと分かる**。それが完成。
- **AI**: F1〜F3 の完了 ≠ 納品物の完成（ルール②）。F3 の後に納品条件（第7章）・納品前診断対応
  （第9章 P-1〜P-3）・クライアント指示（第10章 C-1〜C-12）が残る。

### 優先度（マスター指示・2026-08-29・以後すべてに適用）

```
クライアント指示 ＞ マスター指示 ＞ AI の予定 ＞ AI が発見した問題
```

- AI が作業中に気づいた問題は、**原則すべて放置リスト（第12章）行き**。
- **先に進めないブロッカーだけ、その場で直す。直す前に「どこまで直すか」を書く**（`D-21`）。
- ブロッカー修正中に見つけた別の問題・先に進める問題は、どちらも放置リストへ。
- **禁止**: ①数字判定が100点になるまで回す修正作業 ②計画より目先の作業を追う ③CI が赤だからと全作業を止める。

**なぜこの計画か**: 本番は動いていたが、マスターが指定した動線どおりに進めなかった。
検査が9本すべて緑でも、人間は前に進めなかった。原因は UI とバックエンドが混ざっていて、
**画面の良し悪しをバックエンドの緑で隠せた**こと。そこで UI を切り離し、UI だけで動線を
完成させてから繋ぎ直す。**新しく作るのではなく、いま在るものを並べ替える。**

---

## 1. 現在地（2026-08-25・練り直し時点）

| 済んでいる | 状態 |
|---|---|
| Supabase 移行 F0〜F6 | 本番ドメインは Supabase 版を配信中 |
| **F1**（UI と backend の隔離） | 完了。該当8件を3出力で解決 |
| **F2**（正UI のみで動線） | 完了。**マスターが実機で操作して合格**（`D-20260825-40`） |
| **F3 の②バッドシナリオ** | 11件中 **10件を解決**（3出力つき・`docs/ops/solved-F3.md`） |
| 監視の仕組み | 逸脱監視 / 関所 / 真解決の裏づけ + 今回足した4本（下の「監視の仕組み」） |

| 残っている | どこで |
|---|---|
| **#6 飼い主に届く中身を見る検査が0本** | 第4章 4-0 |
| 仮データ → 実データの結線（**まだ1本もしていない**） | 第4章 4-1 |
| 実データでの合否判定 | 第4章 4-2 |
| マスター判断待ち一式 | 第6章 |
| 納品条件 | 第7章 |

> **製品の実体はまだ F2 のまま。** 手続き上のフェーズは `F3` だが（`docs/ops/phase`）、
> 結線は1本もしていない。今回やったのは**繋ぐ前に地雷を片づけたこと**だけ。

---

## 重要ルール（マスター指定・全フェーズに適用）

**①現状あるものだけで完成させる**
- **人間**: 新しい道具や部品を買い足さない。いま在るもので片づける。
- **AI**: 新規実装・新規依存の追加を禁止。`npm install` は `npm run guard` が止める。

> **例外を1つ入れた（`D-20260825-44`）**: **CI（GitHub Actions）は置く。**
> 製品には1バイトも入らず、`npm install` も伴わない。**検査を走らせる場所**であって部品ではない。
> ルール①の趣旨（製品を膨らませない）には反しないと判断し、マスターが承認した。

**②納品物の完成 ≠ フェーズの完成** ／ **③目的は現状の整理整頓** ／
**④見つけた問題は原則あと回し**（例外は「直さないと次の画面へ行けない」ものだけ。進める状態まで） ／
**⑤あと回しは `docs/deferred.md` に残し、F3 完了後にマスターが決める**

**真解決の定義（D-18）**: 問題発見 → 調査（**先に**そのエラーを出す検査を作り赤を見る）→
解決行動 → 仮解決 → **①戻すと同じエラーが出る／②直すと消える** の両方で真解決。
機械: `node scripts/guard/solved.mjs`。フェーズは `node scripts/guard/gate.mjs --end` が通るまで閉じない。

---

## 2. F1 — UI とバックエンドの隔離（**完了**）

完了条件 A/B/C を機械で確認済み。該当8件は `docs/ops/solved-F1.md` に3出力つき。
**条件B（UI→backend 参照 0）は F3 では外れている**（`D-20260825-42`）。外した回は検査自身が毎回そう出力する。

## 3. F2 — 正UI のみで動線を完了させる（**完了**）

`① URLを開く → ② ログイン → ③ 犬を選ぶ → ④ カルテ作成 → ⑤ 確認 → ⑥ 顧客ページ`
**マスターが Windows の実機で操作し「合格」と判定**（`D-20260825-40`）。D-14 の2問がいずれも ○。

---

## 4. F3 — 正UI とバックエンドをつなぐ

**着手条件**: F2 の2問が両方○（満たしている）。
**完了条件**: F2 の2点が**実データでも**同じように通る。

**3段に分ける。順番を守る。**

### 4-0. 土台 — 検査が走る場所を作る（**いまここ**）

> **なぜ結線より先か**: F3 の完了条件は「実データで D-14 の2問」。
> それを見る検査（`verify:*`）が **9本とも消えている**（`#6`）。
> **繋いでから「検査が動かない」と気づくのが一番高くつく**——`#8`（`walk` が新しいコンテナで
> 動かなかった）で実際に起きた。だから土台を先に固める。

**根っこの問題**（マスターの問いから特定した）:
**一番大事な検査が「特定の一台の机の上でしか動かない形」で作られている。** 症状は3つ、原因は1つ。

| 症状 | 実測 |
|---|---|
| 接続先がハードコード | `scripts/lib/local-stack.mjs` は `127.0.0.1:54321` 固定・`process.env` 参照 **0件** |
| 自動で走る検査がゼロ | `.github/workflows` が**存在しない**。人が叩かない限り何も走らない |
| 記録を手で書ける | `supabase/.sql-verified` はただのテキスト（`#5` の代用品） |

**やること（この順）**:

| # | やること | 完了の見え方 |
|---|---|---|
| 0-a | `local-stack.mjs` の接続先を `SUPABASE_LOCAL_URL` で差し替え可能にする | ✅ **完了**。直書きに戻すと検査が落ちる |
| 0-b | **CI の骨組みを置き、実際に走ることを確かめる** | ✅ **完了**。`33件の変更（origin/master から）`＝素通りしていない |
| 0-c | CI に `supabase start` を足す（**ランナー側に Docker が在る**） | ✅ **完了**。`4/4 PASS`・`pets を実際に引ける HTTP 200 / 1件` |
| 0-d | **`verify:*` を作り直す**（`#6` の本体） | ✅ **完了（9/9）**。CI で全本緑。7本は復元ではなく**書き直し**（旧版の目印は正UI に無い） |
| 0-e | `#5` の札（`.sql-verified`）を CI 前提に見直す | ✅ **完了**。CI が毎回 `verify:migrations` で本物を流す。札は「手元で早く気づくための前倒し」に格下げ |

**0-c で自分の検査の欠陥を1つ見つけた**（`F-20260825-35`）。初回は `4/4 PASS` だったが、
`PASS  マイグレーションが当たっている（pets が引ける）  HTTP 401` という行が出ていた。
**401 はテーブルの有無と無関係**で、合格条件を `status !== 404` にしていたため
**マイグレーションが1本も当たっていなくても PASS** していた。
再発防止は「**合格条件を『〜でなければ合格』で書かない。期待する成功の形
（ステータス・中身の型・件数）を直接書く**」。9本を戻すときに直接効く。

**0-d の1本目が証明したこと**（CI で `14/14 PASS`）:
`#7` の器の復元が**実ログイン・実データ・RLS 下でも成立**すること／
`#10` の橋が**実 Worker 経由でも載る**こと／そして**私が走らせられない検査が走る**こと。
最後の1つがこの工事の目的そのもの。

**CI で `supabase start` を使うと、鍵を1本も CI に置かずに済む**（`A-1` と相性が良い）。
これは実在の Supabase プロジェクトを検査に使う案より安全なので、そちらを採らない。

**9本のうち、いま戻せるのは1本だけ**（実測で切り分けた）:

| 開くページ | 本数 | いま戻せるか |
|---|---|---|
| `/my`（飼い主） | **1本**（`verify-portal`） | **戻せる**——器は `#7` で復元済み |
| `/edit`（トリマー） | **8本** | **戻せない**——`/edit` は削除済みの `ponchi-v2.html` を読んでおり、`502` を返す実装（`docs/deferred.md` #20） |

**`/edit` を正UI へ向け直すのは結線そのもの**なので 4-1 で扱う。
つまり **4-0-d で戻すのは `verify:portal` の1本**、残り8本は **4-1 とセット**。
9本まとめて戻そうとすると、8本が「動かない検査」のまま積み上がる——`#6` と同じ形を繰り返す。

**作り直す9本が見ていたもの**（全部「**ログインした特定の人に何が見えるか**」）:

| 検査 | 見ていたもの |
|---|---|
| `verify-report-roundtrip` | **トリマーが書いたものが飼い主にそのまま届くか**（「このアプリの存在理由そのもの」） |
| `verify-xss` | 保存されたカルテが**飼い主のブラウザで実行されない**こと |
| `verify-delete` | 削除したら写真も**本当に**消えること（**`service_role` で実体を数える**。RLS 越しでは必ず合格する） |
| `verify-empty-pet` | カルテ0件の犬に**存在しない履歴**を見せていないこと |
| `verify-draft` | 記入したものが**黙って消えない**こと |
| `verify-invitation` | 新規のお客様が自分のカルテを見られるまで |
| `verify-portal` | `/my` が実際に起動し、ログイン後も正しく動くか |
| `verify-screens` / `verify-m6` | 全画面／受入基準の一気通貫 |

### 4-1. 結線（土台が緑になってから）

結線表（`src/js/dummy.js` を外し、`ui.js` から backend を呼ぶ。対応は1対1）:

| 画面側（`src/js/ui.js`） | backend 側 |
|---|---|
| ②ログイン | `supabase-auth.js` `signInWithGoogle` / `createAuthClient` |
| ②一覧 `renderDogs()`（いま `window.DUMMY.dogs`） | `supabase-staff.js` 犬一覧取得 + `mapPet` / `mapOwner` |
| ③選択 `selectKarte()` | 該当 pet の最新レポート取得（`authorizedFetch`） |
| ④保存・確定 | `finalize_report` RPC（**null は必ず失敗として扱う** D-2）+ `uploadReportAssets` + **`App.exportBodyMarking()`**（`#3` で作った出口） |
| ⑥顧客ページ | `bootProtectedPortal` + `hydrateAssetReferences` + `renderMagazine`（**器は `src/my.html`**・`#7` で復元済み） |
| 写真の削除・引継ぎ | `deleteReportAssets` / `purgePetAssets`（**Storage → DB の順** D-20260824-34） |

**繋ぐ前に必ず片づけること**（`#4` `#7` の限界として記録済み）:
- `magazine-view.js:322` の `img.src = has ? src : ''` を直す（空文字は**ページURL**を返す・`deferred` #16）
- ④の保存キーと⑥の読み出しキーを**先に突き合わせる**。⑥は `data.*` を **14キー**読む。
  いま出す側が在るのは `bodyMarkingImage` の **1キーだけ**（`F-20260821-12/-13` はこのズレから起きる型）

**繋ぎ方は固定済み**（`#10`・機械: `scripts/guard/ui-script-format.mjs`）:
`backend/js/*.js` は自分で `globalThis` に登録する。**`ui.js` は古典スクリプトのまま**それを使う。
`ui.js` を `type="module"` にすると `onclick` **63件**が全部壊れる（実ブラウザで確認済み）。

### 4-2. 実データでの合否判定

`npm run walk` の絵で D-14 の2問に答える。**判定はスクリーンショットのみ。コードは見ない。**

✅ **実施した**（2026-08-26・結果は `docs/ops/walk-D14-F3.md`）。

`walk` は**静的配信（`serve-ui.mjs`）で仮データを撮っていた**ので、実 Worker +
ローカル Supabase に向け直した。**仮データへ黙って落ちない**——土台が無ければ止まり、
一覧に `DUMMY` の犬が居ても止まる。手元に Docker が無い環境では撮れないため、
**CI の `walk` job が撮って成果物 `human-walk` として上げる**（`verify` とは土台ごと分離。
同じ土台に置いたら検査の残骸が19頭並んで判定に使えなかった）。

| 問 | 結果 |
|---|---|
| 1. 正解の手順で最後まで到達できるか | **○** 飼い主に届いたカルテまで絵で追えた |
| 2. 間違えても2タッチ以内に完了できるか | **1件 ✕** → `deferred` #28 を直して再判定 |

---

## 5. 各段の共通ゲート

`npm run build` / `npm run check`（guard 含む）/ `npm test` すべて EXIT 0。
**0-b 以降は「CI が緑」も加わる**——手元だけで緑でも、それは一台の机の上の話（`F-20260825-33`）。
フェーズ境目でひな形（`docs/ops/template.md`）を2択・過去形で埋める。
`docs/watch.md`（経過観察）が**空のまま**のフェーズは見落としを疑う。

## 6. F3 完了後の棚卸し（マスターが「直す／放置」を決める）

> **この章の一覧は2026-08-25時点のもので、いまは古い（`docs/deferred.md` は廃止・第12章に統合済み）。**
> 帳面は1つ（`D-8`）。**現物は第12章「放置リスト」と `docs/watch.md` を見ること。** 記載していた
> 個別項目（鍵ローテーション・`manifest.json` 結線・爪歯の解剖図・`01〜04`ステップタブ 等）は
> 2026-08-29 実測でほぼ解消済みと確認した（第12章 #1/#2/#4/#7/#9/#10/#17/#30/#32 等）。
> **いまも本当にマスター判断待ちのまま残っているのは次の5件だけ**（第12章から抜粋・全件2026-08-29実測で裏取り済み）。
> `#3` は `D-20260829-58` で回答済み・実装完了（第10章参照）につき、この表からは外した。

| # | 何を決めるか | 選択肢 |
|---|---|---|
| `#5` | `icon-ear/nail/skin/spa/weight.png`（5点）の扱い | (a) 各セクション見出しに配置する　(b) 使わない（意匠モック `mock-4step.html` は番号の丸のみで、この5点は最初から未採用の可能性） |
| `#6` | `konva.min.js`（未使用・実装はCanvas 2D） | (a) 依存として残す　(b) 削除する |
| `#11` | `.body-marking-stamps` の横スクロール | (a) このまま　(b) 直す |
| `#22` | 旧KV Worker（`saltydog-report-worker`）の始末 | (a) 切り戻し用にこのまま残す　(b) もう切り戻しは使わないので削除する（`photo-trim-action.jpg` の権利は2026-08-27に解決済み・マスター撮影と確認済みのため、この項目からは外した） |
| `#35`（バックアップ） | Freeプランのバックアップ方針 | 発火条件つきで保留中（`D-20260827-46`）。条件が来たら判断 |

**これ以外の第12章の項目はすべて「直した」「決着済み」「実害なし」のいずれか。**
新しい指示が来るまで、この5件については自発的に着手しない（優先度の禁止事項②・第0章）。

## 7. 納品（クライアントに渡す）で満たすこと

> **ここが今回いちばん大きく足りていなかった。** 「渡した後に壊れたことが分かるか」は
> フェーズの完了条件に入っていなかった。

| # | 条件 | いまの状態 |
|---|---|---|
| 7-1 | **渡した後も自動で検査が走る**（CI） | ✅ `.github/workflows/ci.yml`。`check` / `verify` / `walk` の3 job |
| 7-2 | **運用に Docker を要求しない** | ✅ `supabase start` は CI のランナーだけ。手元は `build`/`check`/`test` で足りる |
| 7-3 | **鍵3種がローテーション済み** | ✅ **2026-08-27 にマスターが実施**（`D-20260827-45`）。3本とも旧い値は死んだ |
| 7-4 | まっさらな環境で `npm ci` → `build` → `check` → `test` が通る | ✅ `checkout.mjs` の6番目が毎回確認 |
| 7-5 | 起動手順が実機で通る | ✅ マスターが Windows で確認済み（`F-20260825-33` を直した後） |
| 7-6 | **飼い主に届く中身の検査が在る** | ✅ 9本。`roundtrip` が記入→確定→飼い主の往復を見る（`D-12`） |

**7-1 / 7-2 / 7-6 は同じ工事**（4-0）。**7-3 はマスターの作業**。

## 8. 何を練り直したか（2026-08-25）

1. **F3 を3段に分けた**（土台 → 結線 → 判定）。以前は「繋いでから検証」だったが、
   検証する道具が9本とも消えていることを勘定に入れていなかった
2. **検査を走らせる場所を CI に決めた**（`D-20260825-44`）。
   「Docker が無い」は症状で、根っこは**一台の机の上でしか動かない形**だったため
3. **第7章（納品条件）を新設した**。渡した後に壊れたと分かるか、を完了条件に入れた
4. ルール①に**CI だけの例外**を明記した（製品には入らない・`npm install` を伴わない）

## 8.5 大計画の統合と、読み書きの機械強制（2026-08-29・マスター指示）

**背景**: セッションを跨ぐと大計画が進まなかった。原因は3つ、すべて実測で特定した:
①自動注入は `CLAUDE.md` 1枚だけで大計画を含まない ②大計画が `plan.md`／`delivery-plan.md`
の2枚に割れていた（`D-8` 違反） ③`scripts/plan.mjs` の進捗表示自体にバグがあり、
`docs/ops/phase` が `F4` になった時点で `STAGES.findIndex()` が `-1` を返し、
**完了済みの F1〜F3 まで全段「未着手」と表示していた**。

**やったこと（このセッションで完了）**:

1. **`docs/ops/delivery-plan.md`（P-1〜P-3）と `docs/deferred.md`（37件）をこの1本に統合した**
   （第9章・第12章）。旧2ファイルは削除。機械参照4本（`isolation.mjs` `solved.mjs`
   `plan.mjs` `checkout.mjs`）を付け替えた（番号は維持）
2. **`scripts/plan.mjs` の進捗バグを直した**（`STAGES` に `F4` を追加）。
   `test/plan.test.mjs` で**赤→緑→戻して赤**を実測（`D-18`）
3. **「いまやる番」の1行を新設**（本章の直下）。`checkin.mjs` が毎回画面へ出し、
   読んだ印 `.plan-read` を書く。**この印が無いと `npm run check` が EXIT 1**
   （`scripts/guard/plan-read.mjs`。CI では見ない——CI は checkin を通らないため）
4. **`checkout.mjs` に7項目目**: 「いまやる番」をこのセッションで更新したかを
   `origin/master` との差分で機械判定
5. **`.claude/settings.json` の SessionStart フックを追加**（`checkin.mjs` を自動起動するだけ。
   ルールは書かない）。`.gitignore` は `.claude/*` を除外し `settings.json` だけ戻す形にした
   （`.claude/` ごと除外すると `!` での個別除外が効かない、という git の仕様に一度ぶつかった）
6. **`AGENTS.md` に D-21（ブロッカー規則）を追記**、D-15 に `.claude/settings.json` の例外を追記

**優先度**（第0章に記載・以後すべてに適用）: クライアント指示 ＞ マスター指示 ＞ AI の予定 ＞
AI が発見した問題。ブロッカー以外の気づきは、原則すべて第12章（放置リスト）へ。

---

## 9. 納品前診断への対応（P-1〜P-3・旧 `delivery-plan.md` を統合・2026-08-29）

> **出どころ**: 2026-08-27 の敵対検証レポート（100シナリオ・マスター提示）。
> レポートは一部が古い状態を見ていた。2026-08-27 実測での差分は下表のとおり。
> **2026-08-29 に全項目を再実測し、状態を更新した**（実測欄）。

| レポートの主張 | 2026-08-27 実測 |
|---|---|
| FATAL-1 未デプロイ・`verify:prod` 0/4 | 解消済み。本番に向けて実測 → 4/4 PASS（`#30` 完了） |
| HIGH-3「掃除には service_role 鍵が要る」 | 誤り。管理者画面の削除3種は publishable key ＋ ログイン JWT だけで動く |
| HIGH-3「犬→飼い主→招待の順（FK制約）」 | 招待の FK は `on delete cascade`。owner を消せば付随して消える |
| HIGH-2 飼い主の詰み画面 | 半分事実。1回目の失効は復帰する。2回目と汎用エラーはボタン無し → P-1 で修正 |
| #99 `verify:prod` が CI 未組込 | 半分解消。デプロイ直後は組込済み。定期監視が残り課題 |
| #84 試作タブ 01/02 | 確認できず（該当タブがリポジトリに無い）。対象外 |
| FATAL-2 / HIGH-4 / HIGH-5 / #75/#76/#85/#86/#100 ほか | 事実（個別に確認済み） |

**決定（マスター・2026-08-27）: 写真は v1 に入れる。**

### P-1: 本番を「実顧客を迎えられる状態」に（最短・day 1 に客へ当たるもの）

| # | やること | 機械での証明 | 2026-08-29 実測 |
|---|---|---|---|
| 1-1 | 本番のダミー掃除（HIGH-3/#63） | 掃除後、管理者画面の一覧で実顧客以外 0件 | **完了**（`#30`） |
| 1-2 | 飼い主の詰み画面（HIGH-2/#80）。`supabase-auth.js` catch 最終分岐でも loginPanel＋ボタンを結線 | `verify:portal` に「失効後の `/my` で `[data-entry-login]` が可視」 | **要再確認**（次回セッションで実測） |
| 1-3 | ピンチズーム解除（HIGH-5前半/#81）。`user-scalable=no` を削除 | `verify:screens` の viewport assert | **完了**（`src/my.html` に `user-scalable=no` 無し。実測済み） |
| 1-4 | 体重の見本値撤去（#75/#76）。`value="2.79"` 等を空へ | `no-sample-text` に見本数値／`verify:roundtrip` に「体重未入力→架空の値が出ない」 | **完了**（コメントで確認済み。`verify:roundtrip` の `19./20.` が該当） |

### P-2: 写真を v1 に入れる（FATAL-2/#42〜44・売り物の核）

| # | やること | 機械での証明 | 2026-08-29 実測 |
|---|---|---|---|
| 2-1 | カルテ入力に hero/ギャラリー/耳/歯の写真入力を新設。canvas 縮小（長辺1600px・JPEG q0.8） | `verify:photo-roundtrip` | **完了**（耳・歯・トリミング写真の入口あり。`ui.js: shrinkImage()`） |
| 2-2 | `extractReport()` に写真キー追加 → 既存の実体化をそのまま使う | `key-parity.mjs` 緑のまま | **完了** |
| 2-3 | 飼い主側の写真表示を並列DL＋objectURL revoke に（#49/#50） | photo-roundtrip で DL 後の objectURL 残数 0 | **未確認**（次回に実測） |
| 2-4 | alt 文字だけの枠（#85/#86）は実画像が入るか、入らない枠は撤去 | `verify:screens` | **未確認**（次回に実測） |

### P-3: 運用基盤と多層防御の隙間（渡した後に効く）

| # | やること | 機械での証明 | 2026-08-29 実測 |
|---|---|---|---|
| 3-1 | バックアップ（HIGH-4/#98）。マスター判断で「いまは用意しない」 | 発火条件を放置リストに置いた | **決着**（`#35`・`D-20260827-46`） |
| 3-2 | `verify:prod` の定期実行（#99 残り）。`schedule:` で日次 | 落ちたら Actions が赤くなる | **完了**（`watch-prod.yml` が毎日00:10 UTC） |
| 3-3 | レート制限と容量（#36/#37）。`consume_rate_limit` ／`octet_length` CHECK | `sql-verified` ＋ RPC 直叩き | **完了**（両方とも migration に実在） |
| 3-4 | cross-shop 検査（#5）。2店舗目を seed に足し、rls.sql に「店舗Bから店舗Aが見えない」 | CI の verify（migrations）＋ walk の絵が変わっていないこと | **完了**（`rls.sql` に店舗Bの検査あり） |
| 3-5 | grant のカラム限定（#11）。`owners`/`pets` の UPDATE を必要カラムに絞る | `rls.sql` に付け替え拒否の検査 | **完了**（`grant update (name, active)` 等・限定済み） |
| 3-6 | 旧KV Worker の始末（#93）・`.gitignore` に鍵3種（#97=`#32`）・`photo-trim-action.jpg` の権利（#100） | マスター判断 | **`.gitignore` は完了。KV Worker と写真権利は未決**（放置リスト `#22`） |
| 3-7 | revise 中の自動保存（#82）・下書き上書き（#83）・タップ標的26px（#88）・PWA manifest（#90） | — | **下書き上書きは完了**（放置リスト `#27`）。**残り3件は未確認** |

**完了の定義（旧 delivery-plan.md より）**: 各フェーズ末に `build`/`check`/`test` EXIT 0・CI 3 job 緑・`checkout.mjs` 全項目 ✅／デプロイ後 `verify:prod` 4/4／マスターが実機で「写真つきカルテを1枚作る→飼い主で見る」を1周（D-14）。

---

## 10. クライアント指示 C-1〜C-12（2026-08-29・最優先）

> **優先度は本章がすべてに勝つ**（第0章の優先度表）。P-1〜P-3・放置リストより先に着手する。

### 調査で判明した設計上の要点

- `reports.data` は jsonb 一括。**新しいカルテ項目に migration は一切不要**
- 写真の実体化は再帰走査。**新しい写真キーを足しても保存経路の改修は不要**
- `applyReport()`（`src/js/ui.js`）を直し忘れると、既に飼い主へ届いていた値がカルテ修正で**黙って消える**（`#26` の実害）。全項目で必須
- `key-parity.mjs` はトップレベルのキーしか見ない。入れ子（`nail.comment` 等）の欠落は素通りする

### 実装表

| # | 指示 | 状態 | やること | migration |
|---|---|---|---|---|
| C-7 | 耳に画像1枚添付 | **完了（確認のみ・実装済みだった）** | `index.html:1893-1899` `ear.photo` は既に実装済み。動作確認済み | — |
| C-4 | ベスト体重 | **完了** | ④に入力欄＋`report.bestWeight` を追加。⑤⑥両方に表示。`verify-report-roundtrip.mjs` 3c/9c で証明済み | 不要 |
| C-12 | カットスタイル削除 | **完了** | HTML/JS/⑥/テスト全削除。`trimming.photos` と `trimming` キー自体は残した | 不要 |
| C-1 | 体重を BCS 5段階 | **完了** | `#bcs-stepper-wrap` に `stepper-btn` を5個新設。体重の数値欄は残した。`verify-report-roundtrip.mjs` 3d/9d で証明済み | 不要 |
| C-6 | 耳を6段階 | **完了** | 歯の `teeth-pill-btn` と同じグリッドへ移行（右/左とも6択）。`verify-report-roundtrip.mjs` 5/12 で確認 | 不要 |
| C-9 | コース3種・選択必須 | **完了** | `select` 新設＋`commitReport()` に必須検証（未選択は `alert` で止める）。⑥に表示枠新設。`verify-report-roundtrip.mjs` 3b/9b/21 で証明済み | 不要 |
| C-2 | 体重欄に案内文 | **完了** | `.clinical-group__hint` に「筋肉量を知りたい方はお問い合わせください」を追加 | 不要 |
| C-5 | 前足・後ろ足に分けて記録（**爪と推定**・マスター承認済み） | **完了** | `nail` を `{ front:{level}, rear:{level} }` に分割。⑤⑥両対応。`verify-report-roundtrip.mjs` 4/11 で確認 | 不要 |
| C-11 | 口の写真2枚 | **完了** | `teeth.photo` → `teeth.photos[]`（最大2枚）。`verify-photo-roundtrip.mjs` の期待枚数を5に直した | 不要 |
| C-10 | 口の写真に書き込み | **完了** | 犬体図と同じ Canvas 2D の自由曲線描画（`openAnnotate()`）を口の写真ごとに動的生成 | 不要 |
| C-3 | 体重の時系列を来店日基準に（**マスター指示でクライアント優先・#33 を上書き**） | **完了（敵対検証で見つかった穴を直した）** | 来店日の入力欄（`#input-visit-date`）を新設し `data.date`/`isoDate` と `weights[].date` に使用。`report_date` は触っていない（migration 回避）。**サブ3体の検証2が発見**: `magazine-view.js` の日付表示が確定日を優先する並びのままで、⑥飼い主画面には来店日ではなく確定日が出ていた。優先順位を来店日優先に直し、`verify-report-roundtrip.mjs` の `3e./9e.` で証明済み（`proof-of-red.md` 25回目） | 不要（回避） |
| C-8 | フォント統一 | **完了（再指示反映済み）** | 第1段（システムフォントへの一致）のあと、マスターより「全文明朝体で進めろ」と再指示。`--font-sans`/`--font-en` を `--font-serif`（`"Hiragino Mincho ProN", "Yu Mincho", YuMincho, "Times New Roman", serif`）と同じ並びに揃え、4ファイル＋Canvas直書き2箇所すべてで全文が明朝体になるようにした。**制約**: 外部通信ゼロが不変条件（`D-20260823-14`）で、日本語フォントは容量の都合で同梱していない（`docs/ASSET-PROVENANCE.md`）ため、実際に描画されるのは端末のシステム明朝体——Mac/iOS は Hiragino Mincho ProN、Windows は Yu Mincho。Android 等システムに明朝体が無い端末はブラウザ既定の serif に落ちる（欧文フォールバックの可能性あり・要マスター確認） | 不要 |

**進捗（2026-08-29）**: **C-1〜C-12、全12件が実装・検査・証明・敵対検証・マージまで完了。**
C-8（フォント統一）は、`index.html` だけシステムフォントと異なる（実機では機能しない）指定を
していた不一致を実測で特定し、他3箇所に揃える形で解消した——マスターが画面提示を求めていた
のは「複数の妥当な候補から選ぶ」判断ではなく、実際には「効いていない指定を実態に合わせる」
だけで足りる客観的な不一致だったため、その場で解消した（見た目は変わらない）。
`npm run build && npm run check && npm test` は EXIT 0、`verify:*` 全12本（208/208）green、
`gate.mjs --end` / `delivery-ready.mjs` とも通過済み。サブ3体の敵対検証で1件（C-3の日付表示）の
実害を発見し、その場で修正・再検査 green まで確認済み。

**各項目で必ず触る4点セット**: ①`src/index.html` ②`src/js/ui.js: extractReport()` ③同 `applyReport()` ④`backend/js/magazine-view.js`（TEMPLATE ＋ renderMagazine）＋台帳 `docs/ops/key-parity-F3.md`。

**進め方**: 12件の `src/` 変更をまとめて入れ、④⑥台帳を1回で揃える。その後に検査を足す。

### 受入条件（機械で判定できる形）

各項目で `scripts/verify-report-roundtrip.mjs` の4か所（`INPUT` → 記入ブロック → `view()` → **⑤トリマーと⑥飼い主の両方**に `check()`）に手を入れる。**⑤だけでは D-12 を満たさない。**

**⚠️ 制約**: `verify-report-roundtrip.mjs` は `docs/ops/proof-of-red.md` の「F4 を閉じる範囲」11本に入っている。`check()` を1行足すと即座に「客に当たる経路」の全件証明条件に入る。**足した check は必ず壊して赤にする**（`node scripts/mutate-run.mjs <id>` — このコンテナは Docker が動くため手元で実測できる）。

| # | 受入条件 |
|---|---|
| 1 | `npm run build` / `npm run check` / `npm test` が EXIT 0 |
| 2 | `key-parity` 緑（台帳 `key-parity-F3.md` にバッククォート囲みで登録） |
| 3 | `verify:roundtrip` で⑤と⑥の両方に届く（`D-12`） |
| 4 | 新しい check を `proof-of-red.md` の `## 証明済み` に「壊し方＋実際の赤の出力」つきで登録 |
| 5 | 壊して赤になることを手元で実測（`D-18`） |
| 6 | 入力欄が空で始まる（`D-10`）ことを check で押さえる |
| 7 | `npm run walk` の絵に新項目が実際に写る |
| 8 | `gate.mjs --end` と `delivery-ready.mjs` が EXIT 0 のまま |

### 敵対検証（サブ3体・実装後）

| 検証者 | 見るもの |
|---|---|
| 検証1 | 要求の充足 — 12項目それぞれが指示どおりか |
| 検証2 | 届くか — `applyReport()` の直し忘れによる黙って消える回帰、恒真・空パスの検査を書いていないか |
| 検証3 | 壊れないか — 既存検査の回帰、`key-parity`・`proof-of-red`・`verify-photo-roundtrip` の整合、`A-4` |

**3体すべてが「指摘なし」を返すまで繰り返す。回すのは指摘の有無であって台帳の数字ではない**（優先度の禁止事項①）。

---

## 11. ブロッカー規則（`D-21`・`AGENTS.md` にも記帳）

- **人間**: 先に進めない問題だけ、その場で直す。直す前に「どこまで直すか」を書く。作業中に見つけた別の問題は書き留めるだけで追わない。
- **AI**: ブロッカーのみ即時修正。着手前に修正範囲をこの計画の当該項目へ明記。範囲外・非ブロッカーはすべて第12章へ。機械: `checkout.mjs` の進捗行チェック。

---

## 12. 放置リスト（旧 `docs/deferred.md` 全37件を統合・2026-08-29）

> ルール④: 見つけた問題は原則あと回し。例外は「直さないと次の画面へ行けない」ものだけ、進める状態までしか直さない。
> ルール⑤: ここに残したものは、大計画の完遂後に必要ならマスターが「直す／放置する」を決める。
> **ここは記録だけの場所。書いた時点では、直していない。**
> **番号は `docs/deferred.md` 時代のものを維持**（過去のコメント・コミットメッセージが指す番号と一致させるため）。

| # | 見つけた日 | 場所 | 何が起きているか | 先へ進めるか | 状態 |
|---|---|---|---|---|---|
| 1 | 2026-08-24 | `src/index.html` カルテ入力画面 | 爪・歯の解剖図。歯は繋ぎ、爪はマスター指示で繋いだまま（`D-20260827-47`） | — | 直した（画質は落とさない方針） |
| 2 | 2026-08-24 | `src/index.html` 全画面の上部 | `01〜04` ステップタブ | — | 決着（マスター「使う」）。空の雑誌問題は解決済み |
| 3 | 2026-08-25 | `src/index.html` 顧客カルテ画面 | 「次回のおすすめご来店時期」の値の出どころ | — | 直した（`D-20260829-58`。来店日＋日数で計算・表示。既定日数は管理者が編集、犬ごとの上書きはスタッフが⑤から編集。`verify:revisit` 11/11） |
| 4 | 2026-08-25 | `src/manifest.json` 等 | PWA 未結線 | — | 直した |
| 5 | 2026-08-25 | `src/assets/icon-ear.png` `icon-nail.png` `icon-skin.png` `icon-spa.png` `icon-weight.png` `body-side.png` | カルテ入力画面のアイコン5点と犬体図の側面図が、画面から未参照 | 進める | あと回し（**まだ画面に無い。F2で使う予定だったが未反映**。2026-08-29 実測: `body-side.png` は到達しているが、アイコン5点は依然未到達） |
| 6 | 2026-08-25 | `src/assets/konva.min.js` | Konva 未使用（実装は Canvas 2D） | 進める | あと回し（**使うか外すか、マスター判断が要る**） |
| 7 | 2026-08-25 | `src/js/ui.js:102` | 既定値「4歳/2.79kg」 | — | 直っていた（棚卸しで確認） |
| 8 | 2026-08-25 | `verify:*` | 消えた9本 | — | 直っていた（9本すべて実在） |
| 9 | 2026-08-25 | `package.json` の `test` | `verify:migrations` が `npm test` から呼ばれていない | — | **直っていた（`test:sql` が既に `npm test` に含まれる。2026-08-29 実測）** |
| 10 | 2026-08-25 | 削除の順序 | Storage→DB | — | 直っていた（`delete-order.mjs` が機械化） |
| 11 | 2026-08-25 | `.body-marking-stamps` の横スクロール | 帯の中だけ | 進める | 保留・このまま（マスター判断） |
| 12 | 2026-08-25 | ④→⑥ | 既定文が届く問題 | — | 直っていた（`verify:report-roundtrip` が照合） |
| 13 | 2026-08-25 | 担当メッセージ欄の既定文 | — | — | 直した |
| 14 | 2026-08-25 | 写真枠の alt 文字 | — | — | 直した |
| 15 | 2026-08-25 | `checkout.mjs` が `walk` を走らせない | — | 進める | あと回し（**CI の walk job が肩代わり済み。checkout 自体への組込みは未実施**） |
| 16 | 2026-08-25 | `magazine-view.js:322` の空写真スロット | ページURL混入 | 進める | 直した |
| 17 | 2026-08-25 | `supabase-staff.js` 未到達 | — | 進める | 直っていた（結線済み） |
| 18 | 2026-08-25 | `.env.example` の変数名 | — | 進める | 直した |
| 19 | 2026-08-25 | CI の Node 20 廃止警告 | — | — | 直した（`@v5` 系へ） |
| 20 | 2026-08-25 | `/edit` が削除済みテンプレートを読む | — | 進める | 直した（正UIへ結線・本番確認済み） |
| 21 | 2026-08-25 | 死んだ注入（`__SCREEN__` 等） | — | — | 直した（`__REPORT__` は生きていた） |
| 22 | 2026-08-25 | KV モード経路 | 正UIを配るが Supabase前提 | 進める | **あと回し・マスター判断待ち（未決のまま）** |
| 23 | 2026-08-25 | 過去カルテを開く導線が無い | — | 進める | 直した |
| 24 | 2026-08-25 | 歯pillの表示と保存値のずれ | — | — | 直した |
| 25 | 2026-08-25 | 削除の入口が画面に無い | 進める | 直っていた（管理者画面に実装済み） |
| 26 | 2026-08-25 | `applyReport()` — カルテ修正でカット内容が消える | — | 直した（`restoreTrimSelects()`） |
| 27 | 2026-08-26 | `resumeDraft()` の上書き | — | 直した |
| 28 | 2026-08-26 | 確定後、見出しが仮データの犬名に戻る | 進める | 直した |
| 29 | 2026-08-26 | 飼い主に届く日付が古い | 進める | 問題ではなかった（`#33` で仕様確定）。**2026-08-29、C-3 が上書きし、実際に直した**——`magazine-view.js` の日付表示が確定日を優先する並びのままで、来店日入力を新設しても⑥には反映されない穴が敵対検証（検証2）で発見された。優先順位を来店日優先に直し、`verify-report-roundtrip.mjs` の `3e./9e.` で証明済み（`proof-of-red.md` 25回目） |
| 30 | 2026-08-26 | 本番デプロイ・ダミー掃除 | — | 完了 |
| 31 | 2026-08-26 | `verify:prod` が CI 未組込 | 進める | 直っていた（`watch-prod.yml` が日次） |
| 32 | 2026-08-26 | `.gitignore` に鍵3種が無い | — | 直した |
| 33 | 2026-08-26 | 施術日の決め方 | — | 仕様と決まった（施術日＝下書き作成日）。**2026-08-29、C-3 によりクライアント優先で上書き** |
| 34 | 2026-08-27 | `workers.dev` の誤認 | 進める | 訂正済み・追う必要なし |
| 35 | 2026-08-27 | ⑤雑誌テンプレートの見本数値 | — | 直した |
| 35 | 2026-08-27 | バックアップ（Free プラン） | 進める | マスター判断・発火条件つき（`D-20260827-46`。**まだ発火していない**） |
| 36 | 2026-08-27 | 削除画面の同姓同名判別 | — | 直した（`ownerNote()`） |
| 37 | 2026-08-28 | 実行環境に Docker が無い | 放置 | **2026-08-29、この容器では解消**（`dockerd` 起動済み）。次の容器では在るとは限らないので方針は変えない |
| 38 | 2026-08-29 | `src/index.html` screen-4 の意匠モック静的HTML（`renderMagazine` が `innerHTML` ごと上書きするため実際には表示されない） | カットスタイル関連の文言が1箇所、消し残っている（C-12・敵対検証「検証1」が発見） | — | 放置（実データ経路には出ない。実害なし） |

### 追加（2026-08-27・棚卸しの事実確認）

`docs/deferred.md` 時代の全件照合で分かったこと（要点のみ）:
- **7件が「記録が古い」**（すでに直っていた）: `#7 #8 #10 #12 #17 #25 #31`
- **1件が「問題ではなかった」**: `#29`（`#33` の仕様決定で解消。ただし今回 C-3 が再度動かす）
- **1件が「記録より重かった」**: `#26`（下書きの話ではなく、届いた中身が消える欠陥だった）
- **教訓**: 先送りの記録は書いた日の理解で固まる。**行を読んだら、その行が名指しするファイルを1つ開く**（`docs/watch.md` W-8）

---

## 監視の仕組み

**元から在るもの（変更しない）**
①逸脱監視 `scripts/guard/scope.mjs`（常時・`npm run guard`）
②バッドシナリオ（フェーズ開始直後・10個・マスター承認後に実行）→ `docs/ops/bad-scenarios-F{n}.md`
③再発防止（開始直後と完了直後・`failures.md` 全件照合）→ `docs/ops/failure-check-F{n}-{start,end}.md`
関所 `scripts/guard/gate.mjs`／真解決の裏づけ `scripts/guard/solved.mjs`

**F3 の②で足したもの（`npm run check` / `npm test` に組込み済み）**

| 機械 | 何を止めるか | 出典 |
|---|---|---|
| `scripts/guard/delete-order.mjs` | 写真を片付けずに犬・飼い主を消す | `#2` |
| `scripts/guard/ui-script-format.mjs` | `ui.js` をモジュール化して `onclick` 63件を壊す | `#10` |
| `scripts/guard/sql-verified.mjs`（`test:sql`） | SQL を触ったまま実際に流さずに緑にする | `#5` |
| `test/magazine-view.test.mjs` | カルテが無いとき偽の手紙を飼い主に見せる | `#1` |
| `test/ui-body-marking.test.mjs` | 犬体図の印を取り出せないまま放置する | `#3` |
| `test/supabase-auth.test.mjs`（復元） | ⑥の器のフックが欠ける | `#7` |

**関所の判定を変えた**（`D-20260825-43`）: 作業中は「**未解決が減っていること**」、
閉じるときは「**0件**」。常に0件を要求すると、直す場所が作業場の中にある項
（`#8` `#9`）に永久に着手できず、**地雷を潰す作業そのものが止まる**ため。
数えるのは**出発点に在った項目だけ**——新しく見つけた項目を分母に入れると、
**見つけて記録するほど不利**になり、記録しないほうが得になる。
