# F4 再発防止チェック（開始時）

対象: `docs/failures.md` 全 **47件**（`grep -c '^### \[F-2026' docs/failures.md`）
参照: `docs/decisions.md` 全 **52件**（`grep -c '^### \[D-2026' docs/decisions.md`）
実施: 2026-08-27（マスター判断「進める」を受けて）
根拠は**見たファイル名か、実行した命令とその出力**にする。「たぶん大丈夫」は書かない。

---

## この時点の実測（すべて実行した）

| 命令 | EXIT | 中身 |
|---|---|---|
| `npm run build` | **0** | 36件 |
| `npm run check` | **0** | guard / 隔離 src・dist（条件Aのみ）/ src↔dist parity / design 隔離 / 削除順序 / 繋ぎ方 / 検査台帳 / キー対応 / 見本文 / **画像台帳** / 鍵 |
| `npm test` | **0** | **150件**（59 + 56 + 6 + 29） |
| `node scripts/guard/checkout.mjs` | **0** | 6項目すべて（PR #26 マージ後に実測） |
| CI（PR #26 / #27） | **緑** | `check` / `verify` / `walk` の3 job |
| `npm run verify:migrations` | **未実施** | **この環境に Docker が無い**（実行できないので「通った」と書かない） |

**F3 の完了条件は満たしている**（`plan.md` 4-2）: `walk` の絵で `D-14` の2問に答え、
問2 の ✕ 1件（`deferred` #28）を直して撮り直し ○ になった。

---

## F4 で現に踏み得るもの

**F4 は「棚卸しと納品の仕上げ」**（`plan.md` 第6章・第7章）。作るのではなく**閉じる**段なので、
踏む失敗の型が F1〜F3 とは違う。**「もう終わりだ」という気分そのものが最大の危険**である。

| 失敗ID / 決定ID | 何が起きたか（1行） | いま起こり得るか | 根拠（実行した命令と出力） |
|---|---|---|---|
| **F-20260827-46** | 先送りの記録が**影響範囲を1つしか書いておらず**、客に当たる欠陥を「選び直しになるだけ」と書いていた | **◎ 最有力** | F4 の中心作業が**まさに `deferred.md` を根拠に「直す／放置」を決めること**。記録が古ければ判断ごと狂う。同じ棚卸しで、直っているのに「まだ在る」が **7件**出た（`#7` `#8` `#10` `#12` `#17` `#25` `#31`）。`docs/watch.md` **W-8** に登録済み |
| **F-20260827-47** | 掃除で、死んだ名前と**同じ名前の生きている値**を一緒に落とし、`/edit` が素の HTML になった | **◎** | F4 は掃除の段。未参照ファイル7点（`deferred` #5 `#6`）・KV モード（`#22`）・`checkout` への `walk` 追加（`#15`）がすべて「消す／触る」作業。`npm test` が**コミット前に**止めたのは運が良かっただけで、機械が無い側を消せば止まらない |
| **F-20260825-40 / F-20260826-41 / F-20260827-43** | 検査が**何を壊しても緑**（恒真）／保存を待たずに読む／実際の仕組みと違う所を見る | **◎** | `docs/watch.md` **W-1** に「F3 だけで7回」と記録があり、**減っていない**。F4 で足す検査も同じ型を踏み得る。**書いたら必ず戻して赤を見る**——今日足した 17+11 件はすべてそれを実施した |
| **F-20260827-45** | 検査が**書かれた日から一度も実行されていなかった**（`npm run` のどれからも辿れなかった） | **○** | `verify-inventory` と `sql-verified` が塞いだが、**新しく足す検査には効かない**。`docs/watch.md` **W-7** |
| **F-20260826-42** | 画面の文字1つから、確かめずに「配信が消えた」と結論した | **◎** | F4 は**本番に触れる判断**（旧 KV Worker の廃棄・`deferred` #22 `#93`）を含む。「無い」と書く前に、在ることを示す側を1つ当たる |
| **F-20260825-33** | 自分が動かせる環境でしか確かめていなかった（マスターの Windows で落ちた） | **◎** | **この環境には Docker が無い**（実測: `docker ps` が socket に繋がらない）。`verify:*` も `verify:migrations` も**手元では走らない**。緑を主張してよいのは CI が出した緑だけ |
| **F-20260821-24 / F-20260823-01** | マイグレーションが一度も実行されず、本番投入の1本目で構文エラー | **◎** | F4 に残っている `consume_rate_limit` の工事（納品前診断 #36）は SQL を触る。`sql-verified` は**実際に流すまで緑にしない**設計なので、**Docker のある環境でしか着手できない**。札を手で書き換えるのは `A-4` 違反 |
| **F-20260821-11** | 受入基準を通すことを目的にして、機能そのものを確かめていなかった | **◎** | F4 の終わりは「納品」。`plan.md` 第7章の 7-1〜7-6 が全部 ✅ でも、**それは渡した後に壊れたことが分かるかの話**であって、いま正しく動くことの証明ではない |
| **F-20260821-14 / -12 / -13** | 見本の文・所見が、書いていないのに飼い主へ届く／書いたのに消える | **○（機械が在る）** | `no-sample-text`（見本文＋**見本の数字**）と `key-parity` と `verify:roundtrip` の3枚。今日 `#35` の見本数値を足し、**実際に2件を捕まえてから緑にした** |
| **D-20260824-34** | 削除の順序を誤ると、写真が誰からも回収できなくなる | **○（機械が在る）** | `scripts/guard/delete-order.mjs` が `npm run check` で毎回。実測 `✅ 削除の順序 OK（0件）` |
| **F-20260821-09** | plan が指す XSS Critical の箇所が特定できていない（**OPEN**） | **○** | `Status: OPEN` のまま。`magazine-view.js` の書き込みは `textContent` と定数 `TEMPLATE` の `innerHTML` だけ——**経路を増やさないこと**が条件。F4 で `innerHTML` を足す作業（`#3` の「次回」節を器に足す等）が出たら、ここが効く |
| **F-20260825-37** | 「戻すと赤」を、**直したものとは別の層**で採ろうとした | **○** | 今日の 28 件はすべて**直した層そのもの**で赤を採った（`restoreTrimSelects` を外す／`draftTouched` を殺す／`ownerNote` を空にする／`goToStep` の判定を殺す／台帳から画像を外す） |
| **D-20260827-45** | 鍵3種はローテーション済み | **—（済）** | `plan.md` 7-3 ✅。ただし `.gitignore` は**まだ塞いでいない**（`deferred` #32）——**F4 で範囲に入った2ファイルの片方がこれ** |
| **D-20260827-46** | バックアップは実顧客データが入るまで用意しない | **—（発火条件つき）** | **F4 の中で実顧客を入れる判断が出たら、ここが発火する**。CSV の準備だけは置いた（`scripts/export-csv.mjs`） |

---

## この段で特に効かせる3つ

1. **記録を根拠にするときは、その行が名指しするファイルを1つ開く**（`W-8`）。
   F4 は記録を根拠に「直す／放置」を決める段なので、記録が古いと判断ごと狂う。
2. **消す前に、両方向から数える**（`F-20260827-47`）。
   出す側が 0 件でも、受ける側が生きていることがある。
3. **手元の緑を、緑と呼ばない**（`F-20260825-33`）。
   この環境に Docker が無い以上、`verify:*` と `verify:migrations` の緑は **CI が出したものだけ**。
