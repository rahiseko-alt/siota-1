# 1件ずつ壊した結果

**一部だけ（edit-broken-url-falls-out）。全体の記録ではない。**

実行: `node scripts/mutate-run.mjs`（**本物の土台が要る**——CI で走らせる）

- 赤になった（その壊しを検出できた）: **1件**

## 赤になった（`- <検査の名前>` ← どの壊しで）

- 検査を最後まで実行できた   ← edit-broken-url-falls-out / verify-edit.mjs
