# 納品前診断への対応計画（3フェーズ）

> **出どころ**: 2026-08-27 の敵対検証レポート（100シナリオ・マスター提示）。
> **層**: 計画。実装はフェーズごとに PR で行い、各フェーズ末に
> `checkout.mjs` → デプロイ（GitHub Actions `deploy` 手押し）→ `verify:prod` 4/4 → 実機1周。

## 0. 事実確認 — レポートと実測の差分（2026-08-27 実測）

レポートは**一部が古い状態を見ている**。計画はこの差分を反映してある。

| レポートの主張 | 実測 |
|---|---|
| FATAL-1 未デプロイ・`verify:prod` 0/4 | **解消済み（2026-08-27）**。本番に向けて実測 → **4/4 PASS**（`deferred` #30 完了） |
| HIGH-3「掃除には service_role 鍵が要る。新しい秘密鍵を発行せよ」 | **誤り。鍵は不要。** 管理者画面（PR #17・デプロイ済み）の削除3種は publishable key ＋ ログイン JWT だけで動く（`supabase-data-store.js:34-39` が付けるヘッダはこの2つのみ。RLS は `owners_staff_all` / `pets_staff_all` が staff を通す）。**マスターが本番 `/admin` から消せる** |
| HIGH-3「犬→飼い主→招待の順（FK制約）」 | 招待の FK は `on delete cascade`（`202607160001:108-109`）。「あ」の招待は 2026-08-24 に期限切れ済み。**owner を消せば付随して消える** |
| HIGH-2 飼い主の詰み画面 | **半分事実**。1回目の失効は signOut→再読込で復帰する（`auth_reload_once`）。**2回目と汎用エラーはボタン無し**のまま → 修正対象 |
| #99 `verify:prod` が CI 未組込 | **半分解消**。デプロイ直後の関門としては `deploy.yml` に組込済み。**定期監視が無い**のが残り |
| #84 試作タブ 01/02 | **確認できず**（該当するタブ・文言がリポジトリに無い）。対象外 |
| FATAL-2 / HIGH-4 / HIGH-5 / #75/#76/#85/#86/#100 ほか | **事実**（`type="file"` 0件・バックアップ文書ゼロ・`user-scalable=no`・`value="2.79"` 等を個別に確認） |

**決定（マスター・2026-08-27）: 写真は v1 に入れる。**

---

## P-1: 本番を「実顧客を迎えられる状態」に（最短・day 1 に客へ当たるもの）

| # | やること | 機械での証明 |
|---|---|---|
| 1-1 | **本番のダミー掃除**（HIGH-3/#63）。マスターが本番 `/admin` に入り、③削除で**犬3頭（ポンチ/ムギ/レオ）→ 飼い主3件（A/B/「あ」）**。screen-navi で1歩ずつ。鍵の発行は**しない** | 掃除後、管理者画面の一覧で実顧客以外 0件（マスターの画面で確認・スクショ） |
| 1-2 | **飼い主の詰み画面**（HIGH-2/#80）。`supabase-auth.js` catch 最終分岐でも loginPanel＋ボタンを結線。汎用エラーに再読込ボタン | `verify:portal` に「失効後の `/my` で `[data-entry-login]` が可視」を追加 |
| 1-3 | **ピンチズーム解除**（HIGH-5前半/#81）。`my.html:5` の `maximum-scale=1.0, user-scalable=no` を削除 | `verify:screens` に viewport の静的 assert |
| 1-4 | **体重の見本値撤去**（#75/#76）。`value="2.79"`・「前回 2.67kg」・偽SVG・雑誌側 `2.79kg` を空/「未記録」へ | `no-sample-text` に見本数値を追加／`verify:roundtrip` に「体重未入力→架空の値が出ない」 |

## P-2: 写真を v1 に入れる（FATAL-2/#42〜44・売り物の核）

| # | やること | 機械での証明 |
|---|---|---|
| 2-1 | カルテ入力（screen-3）に **hero/ギャラリー/耳/歯の写真入力**を新設。`onchange` で canvas 縮小（長辺1600px・JPEG q0.8）。**HEIC も canvas 経由で JPEG 化**（サーバの enum 変更なし・#43/#44 を同時に解消） | `verify:photo-roundtrip`（新設）: 実ブラウザで添付→確定→飼い主 `/my` に**同じ画像**が出る |
| 2-2 | `extractReport()` に写真キーを追加 → 既存の `replaceDataUrlAssets` / `uploadReportAssets` がそのまま実体化（**新 API 無し**）。キー名は `key-parity-F3.md` の「出どころ無し6キー」に合わせる | `key-parity.mjs` 緑のまま／「出どころ無し」が減る |
| 2-3 | 飼い主側の写真表示を**並列DL＋objectURL revoke**に（#49/#50） | photo-roundtrip で DL 後の objectURL 残数 0 |
| 2-4 | alt 文字だけの枠（#85/#86）は、実画像が入るか、入らない枠は撤去 | `verify:screens` |

検査を書くときの自己点検（W-1・7回目まで出ている型）: **待ちの述語は開始時に偽か／左辺と右辺は別の場所から来ているか／抽出した期待値が空なら失敗にしているか／実際に落として確かめたか。**

## P-3: 運用基盤と多層防御の隙間（渡した後に効く）

| # | やること | 機械での証明 |
|---|---|---|
| 3-1 | ~~**バックアップ**（HIGH-4/#98）~~ → **調べたら、設定ではなく費用の話だった。** Free には日次バックアップも PITR も無い。**マスター判断で、いまは用意しない**（`D-20260827-46`・`deferred` #35）——本番に在るのはダミーだけで、失って困る実データが無い。**発火条件は「実顧客のカルテを1件でも入れる前」** | 発火条件を `deferred` に置いた（そのとき復旧手順を1回実測して `runbook.md` へ） |
| 3-2 | **`verify:prod` の定期実行**（#99 残り）。`schedule:` で日次 | 落ちたら Actions が赤くなる（意図的に古い版へ向けて赤を1回見る） |
| 3-3 | **レート制限と容量**（#36/#37）。`consume_rate_limit` を SECURITY DEFINER RPC 冒頭でも呼ぶ／`reports.data` に `octet_length` CHECK（migration） | `sql-verified`（実 PostgreSQL）＋ RPC 直叩きで N+1 回目拒否 |
| 3-4 | **cross-shop 検査**（#5）。`seed.sql` に2店舗目を足し、`rls.sql` に「店舗Bスタッフは店舗Aの全テーブル 0件」を追加。**W-2: seed を触るので `walk` の絵の意味を確認** | CI の `verify`（migrations）＋ `walk` の絵が変わっていないこと |
| 3-5 | **grant のカラム限定**（#11）。`owners`/`pets` の UPDATE を必要カラムに絞る | `rls.sql` に付け替え拒否の検査 |
| 3-6 | **旧KV Worker の始末**（#93）・`workers_dev = false` 明記（#94）・`.gitignore` に鍵3種（#97=deferred #32）・`photo-trim-action.jpg` の権利（#100） | 旧Worker の削除と素材差し替えは**マスター判断**（到達不能 404 は実測済み） |
| 3-7 | revise 中の自動保存（#82）・下書き上書き（#83=W-4）・タップ標的 26px（#88）・PWA manifest（#90）は**棚卸しで仕分け**（`deferred` に載せる） | — |

## 完了の定義

1. 各フェーズ末: `build`/`check`/`test` EXIT 0・CI 3 job 緑・`checkout.mjs` 6項目 ✅
2. デプロイ後 `verify:prod` **4/4**
3. 最後にマスターが実機で「**写真つきカルテを1枚作る→飼い主で見る**」を1周（D-14 の2問）
