# F3 再発防止チェック（完了時）

対象: `docs/failures.md` 全 **39件**（`grep -c '^### \[F-2026' docs/failures.md`）
参照: `docs/decisions.md` 全 **44件**
実施: 2026-08-26
根拠は**見たファイル名か、実行した命令とその出力**にする。「たぶん大丈夫」は書かない。

---

## この時点の実測（すべて実行した）

| 命令 | EXIT | 中身 |
|---|---|---|
| `npm run build` | **0** | 35件 |
| `npm run check` | **0** | guard / 隔離 src+dist（条件A）/ parity / design / 削除順序 / 繋ぎ方 / **検査台帳** / **キー対応** |
| `npm test` | **0** | **112件** |
| **CI `verify`（`f790a91`）** | **緑** | 土台2本 ＋ **飼い主に届く中身を見る9本すべて** |

CI の9本（実出力は `docs/ops/solved-F3.md` の各項）:

| 検査 | 結果 | 見ているもの |
|---|---|---|
| `portal` | 14/14 | `/my` が起動し、自分の犬だけ見える |
| `xss` | 24/24 | 保存された値が飼い主のブラウザで実行されない（D-11） |
| `roundtrip` | 19/19 | **書いたものが同じ値で届く**（D-12） |
| `empty` | 11/11 | カルテ0件に架空の履歴を出さない（D-10） |
| `screens` | 20/20 | 各画面に在るべき入口が在り、押すと着く |
| `delete` | 6/6 | 削除で写真が**実体として**消える（service_role で数える） |
| `draft` | 7/7 | 記入が黙って消えない |
| `invitation` | 8/8 | 新規のお客様が自分のカルテを見られるまで |
| `m6` | 12/12 | 動線①〜⑥が通しで進み、間違えても戻れる |

---

## 開始時に「起こり得る」とした失敗が、いま起こり得るか

| 失敗ID | 何が起きたか（1行） | いま起こり得るか | 根拠（実行した命令と出力） |
|---|---|---|---|
| **F-20260821-14** | 「担当からの一言」が保存されず、**誰も書いていない定型文**が飼い主に届く | **⑥では起こらない。④の入力欄には種が残る** | `verify:roundtrip` の 10「飼い主: 担当からの一言」が入力した値と一致（19/19）。`verify:edit` の 14/16 が⑤確認の器に文例が無いことを見る。**④の入力欄（`src/index.html:1963`）の既定文は残っており**、`verify:edit` が毎回「残っているもの・1件」として出力する（`docs/deferred.md` #13・マスター判断待ち） |
| **F-20260821-11** | 画面が出る・押せるだけを見て「動く」と報告した | **起こらない** | `verify:roundtrip` が記入 → 確定 → 飼い主が開く、の往復を19項目で見る。`verify:m6` は「押せた」を「戻れた」と数えていたのを直し、戻り先で犬が並んでいることまで見る（`F-20260825-39`） |
| **F-20260821-12 / -13** | 記入した所見の大半が飼い主に届いていなかった | **起こらない（ただしキー名だけの機械は不十分）** | `scripts/guard/key-parity.mjs` が⑥が読む14キーと④が出す8キーを毎回突き合わせる。**この機械はキー名しか見ない**ので、`weights.ym` の欠落と `trimming.length/style` の宛先違いは通してしまった——見つけたのは `verify:roundtrip`。値が届くかは往復の検査が担当する、と役割を分けた |
| **F-20260821-21** | 数字だけを見て「通っている」と読んだ | **起こらない** | `docs/ops/verify-restore-F3.md` が9本の状態を持ち、`scripts/guard/verify-inventory.mjs` が毎回 git と突き合わせる。台帳と実体がズレたら `npm run check` が EXIT 1 |
| **F-20260825-35 / -36** | 期待する成功の形を、実際の仕組みに合わせて書かずに検査を書いた | **まだ起こる。今回4回出した** | 歯のボタン（表示と保存値が違う）／招待の航法（`networkidle` が落ち着かない）／`m6` の下書き上書き。**どれも CI が止めた**が、書く前に仕組みを測る癖はまだ付いていない。**`AGENTS.md` への昇格候補**（ルール昇格原則の「複数回発生した」に該当・マスター判断待ち） |
| **D-20260824-29** | 招待QRのボタンが画面から消え、どこからも押せなかった | **起こらない** | `verify:screens` の 13c が `.btn-invite:not([hidden])` を数える。`verify:invitation` は fixture を使わず、新しい飼い主を作って招待の発行から閲覧まで通す（8/8） |
| **D-20260823-06** | スタッフかつ飼い主が `/my` に留まり、`/edit` へ行けなかった | **起こらない** | `verify:screens` の 5〜8 が `staff-owner@local.test` で入口の存在と、押して着くことを見る |
| **D-20260824-30 の 3** | 犬を先に消すと写真が Storage に残り、誰も回収できない | **起こらない** | `verify:delete` が `service_role` で実体を数える（1件 → 0件）。`scripts/guard/delete-order.mjs` が順序を静的に見る |
| **D-20260824-30 の 1 / 7** | 誤タップ1回で数十分の記入が消える | **起こらない** | `verify:draft` 7/7。触られたら残し、離れて戻ると続きから書ける |

---

## 今回のフェーズで新しく出した失敗（4件）

| 失敗ID | 中身 | 機械で塞いだか |
|---|---|---|
| `F-20260825-37` | 「戻すと赤」を、直したものとは別の層で採ろうとした | `scripts/guard/solved.mjs` が止めた（①と③に同じ症状の行が無い） |
| `F-20260825-38` | 犬体図の印が**中身の無い画像**として保存されていた | `verify:roundtrip` の 8/15 ＋ 単体検査2件。`exportBodyMarking()` は空を返さず投げる |
| `F-20260825-39` | 「押せた」を「戻れた」と数えていた——戻り先の一覧が空だった | `verify:m6` の ★b（戻った先に犬が並んでいる） |
| （記録済み）`-35`/`-36` の再発3回 | 検査が実際の仕組みと違う所を見ていた | **機械で塞げていない。** CI が毎回止めているだけ |

---

## まだ塞げていないもの（正直に書く）

1. **`F-20260825-35`/`-36` の型が止まらない。** 今回4回出した。どれも CI が止めたので
   間違った緑は1つも出ていないが、**「書く前に仕組みを測る」を機械にできていない**。
2. **④の入力欄の既定文**（`docs/deferred.md` #13）。`verify:edit` が毎回1件として出す。
   マスター判断待ち。
3. **削除の入口が画面に無い**（`#25`）。`verify:delete` が毎回1件として出す。
4. **表示と保存値のずれ**（歯・`#24`）。`verify:roundtrip` が毎回1件として出す。
5. **`npm run walk` は仮データのまま。** F3 の完了条件（D-14 の2問を**実データの絵**で
   答える・4-2）は**未実施**。9本が緑になっても、フェーズはこれを通すまで閉じられない。
