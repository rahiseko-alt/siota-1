# F3 再発防止チェック（開始時）

対象: `docs/failures.md` 全 **35件**（`grep -c '^### \[F-2026' docs/failures.md`）
参照: `docs/decisions.md` 全 **44件**（`grep -cE '^### \[?D-2026' docs/decisions.md`）
`docs/failures.md` の **OPEN は 3件**（`grep -c OPEN docs/failures.md`）
実施: 2026-08-25
根拠は**見たファイル名か、実行した命令とその出力**にする。「たぶん大丈夫」は書かない。

---

## この時点の実測（すべて実行した）

| 命令 | EXIT | 中身 |
|---|---|---|
| `npm run build` | **0** | 34件 |
| `npm run check` | **0** | guard / 隔離 src（条件Aのみ）/ 隔離 dist（条件Aのみ）/ src↔dist parity / design 隔離 |
| `npm test` | **0** | **64件**（5 + 47 + 6 + 6） |
| `npm run verify:migrations` | **0** | 7/7 PASS（`npm test` からは呼ばれていない＝バッドシナリオ #5） |
| `npm run walk` | **1** | **chromium が無い**（playwright 1.59.1 は rev 1217 を要求 / 在庫 1194）＝バッドシナリオ #8 |

**F3 の完了条件**（`plan.md` 第4章）: F2 の2問が**実データでも**同じように通ること。
判定はスクリーンショット。つまり **`walk` が動かないうちは F3 を閉じられない**（#8 を先に直す理由）。

---

## 起こり得るもの（F3 で現に踏み得るもの）

| 失敗ID / 決定ID | 何が起きたか（1行） | いま起こり得るか | 既に起こっていないか | 根拠（実行した命令と出力） |
|---|---|---|---|---|
| **F-20260821-14** | 「担当からの一言」が保存されず、飼い主には**誰も書いていない定型文**が届く | **◎ 最有力** | **種が在る（2か所）** | `grep -n "今月もとってもお利口" src/index.html` → **1963（④の入力欄）と 2086（⑥の顧客画面）の2か所に別々の固定文**。**バッドシナリオ #1 はこの失敗の再来そのもの。** `renderMagazine(_, null)` は例外を投げず器に触れずに帰るので、固定文がそのまま残る（実行で確認） |
| **D-20260824-30 #3** | 空の写真スロットが**ページURL**を指し、壊れた画像が飼い主に届く | **◎** | **`src/` では解消・`backend/` で再生産される** | `grep -oE '<img[^>]*>' src/index.html \| grep -c 'src=""'` → **0**（F2 で解消）。ところが `backend/js/magazine-view.js:322` が **`img.src = has ? src : ''`** と実行時に空文字を入れる。**F2 で静的HTMLから消した欠陥を、繋いだ瞬間にレンダラが作り直す** |
| **F-20260821-09** | plan が指す XSS Critical の箇所が特定できていない（**OPEN**） | **◎** | **未解決のまま** | `Status: OPEN`。**F3 は保存データを飼い主のブラウザに出す段**＝この失敗が実現する場所。現状 `magazine-view.js` の書き込みは `setText`（`el.textContent`）と定数 `TEMPLATE` の `innerHTML` のみで、**いまは経路が無い**。増やさないことが条件 |
| **F-20260821-18** | エスケープ関数を、文脈の違う場所に使って無効化していた | **○** | **罠が在る** | `backend/js/magazine-view.js:295` の **`esc()` は実際にはエスケープしない**（`String(value)` を返すだけ）。現状は `textContent` 経由なので安全だが、**名前が「安全にした」と誤読させる。** `innerHTML` に一度でも使えば即座に穴 |
| **F-20260821-12 / -13** | 皮膚・歯・耳・爪の所見が、保存はされるのに**復元で静かに消える** | **◎** | **未確認** | 受け手 `magazine-view.js` は `data.skin` `data.teeth` `data.ear` `data.nail` ほか **11箇所**を読む。出す側（④）の保存形はこれから作る。**キー名が1つずれるだけで静かに消える** |
| **D-20260824-34** | 削除の順序を誤ると、写真が残るのに**誰も回収できなくなる** | **◎** | **導線がまだ無い＝これから踏む** | `grep -cniE "削除\|delete\|purge" src/js/ui.js` → **0**。守らせる検査 `verify-delete.mjs` は `6685df5` で削除済み。**バッドシナリオ #2** |
| **F-20260821-24 / F-20260823-01** | マイグレーションが一度も実行されず、本番投入の1本目で構文エラー | **◎** | **条件がそのまま残る** | 壊した SQL を入れた複製で `npm test` **EXIT 0** / `verify:migrations` **EXIT 1**（実測）。**`npm test` は `verify:migrations` を呼ばない。** バッドシナリオ #5 |
| **F-20260821-15** | カルテ0件の犬に、存在しない施術履歴が飼い主へ表示される | **○** | **受け手は正しい** | `magazine-view.js` は未記入を「記録がありません。」「未記録」で埋める設計（`:335` `:364` `:537`）。**モックの文例で埋めない**方針が実装に入っている。危ないのは④側の既定文（`F-20260821-14` と同件） |
| **F-20260821-25** | Workers の `fetch` をレシーバ付きで呼び、本番の Supabase 通信が全滅 | **○** | **解決済み・検査あり** | `worker/src/data-stores/supabase-data-store.js:29` が **`this.fetchImpl = fetchImpl.bind(globalThis)`**。レシーバを直接検査するテストも在る（`test/supabase-store.test.mjs`）。**ブラウザ側で同じ形を新規に書かないこと** |
| **F-20260823-26 / -27** | 新規カルテが常に公開できない（日付結合の欠落）／日付ピッカーが静的既定値のまま | **◎** | **未確認** | どちらも④の保存経路の話で、その経路をこれから作る。`docs/deferred.md` #3（「次回のおすすめご来店時期」の値の出どころは F3 で決める）と同じ場所 |
| **F-20260821-11** | 受入基準を通すことを目的にして、機能そのものを確かめていなかった | **◎** | **×（まだ）** | `build`/`check`/`test` が EXIT 0 でも **F3 の完了条件ではない**。完了条件は実データでの D-14 2問。しかも `check` はいま**条件Bを見ていない**（`D-20260825-42`）ので、緑の意味が F1 のときより狭い |
| **F-20260825-33** | 自分が動かせる環境でしか確かめていなかった | **◎** | **既に起きている** | `npm run walk` が**このコンテナで EXIT 1**。引き継ぎの「walk EXIT 0・3回連続」は前のコンテナでの実測。`checkout.mjs` は `build`/`check`/`test` しか走らせず **walk を見ない**ので、壊れたまま「異常なし」で終われる。**バッドシナリオ #8** |
| **F-20260825-32** | 根拠を確かめずに「見落とし10個」を提案した | **○** | **×（今回は守った）** | `bad-scenarios-F3.md` の10個は**承認前に全件実行**し、各項に命令と出力を貼った。落とした候補（`finalize` の `null`・RLS・`purgePetAssets` の順序）も**確かめたうえで**該当せずと書いた |
| **F-20260821-23** | 新設した検査が2回目の実行で必ず落ちた | **◎** | **未確認** | F3 で `verify:*` を作り直す（#6）。**1回通っただけを根拠にしない。** 前回は自前で立てた Worker を止め切れていなかった |
| **F-20260825-28 / -29** | 「ほぼ完了」と報告した／該当しない判定になる仕様を書こうとした | **◎** | **×** | #9 の緩和は**マスター判断で条件Bを外した**もので、私が「該当しない判定になる仕様」を書いたのではない（`D-20260825-42`）。**外したことを検査自身が毎回出力する**造りにして、緑の誤読を防いだ |
| **F-20260825-30 / -31** | 自作した関所が、正常系を誤判定・誤読した | **◎** | **×** | 今回 `isolation.mjs` に手を入れた。**赤（結線あり・F1）／緑（結線あり・F3）／また赤（F1 に戻す）** と、**条件Aが F3 でも赤くなること**の4通りを実際に走らせた（`solved-F3.md` #9） |
| **F-20260825-34** | マージせずに「チェックアウト完了」と宣言した | **◎** | **×** | `D-19`。`node scripts/guard/checkout.mjs` が EXIT 0 になるまで完了と言わない |
| **F-20260821-06** | 画面の見出しがデモ既定値のまま出る | **○** | **既に起きている** | `src/js/ui.js:102` がどの犬にも「4歳 / 2.79kg」を入れる。`docs/deferred.md` #7・**F3 の実データ結線で自然に消える想定**だが、消えたことを確かめる |
| **F-20260821-08** | PWA が未結線 | **△** | **既に起きている** | `grep -c 'rel="manifest"' src/index.html` → 0。`docs/deferred.md` #4。**F3 の完了条件には当たらない** |

---

## 起こり得ないもの（F3 の範囲では成立しない）

| 失敗ID | 何が起きたか（1行） | 根拠 |
|---|---|---|
| F-20260821-01 | `check` が実体の無い場所を指し、一度も動いていなかった | F1 で解決（`solved-F1.md` #9）。`src-dist-guard.config.json` は2項目とも実在し、実在しない場所を指せば EXIT 1 |
| F-20260821-02 | `--test-isolation=none` が node 22 で `bad option` | M5 で除去済み。`npm test` は EXIT 0 で 64件が実際に走っている |
| F-20260821-03 | 移設しないと決めたガードを呼び出し側が参照し続けていた | `npm run check` の4本すべてが実在のスクリプトを指し、EXIT 0 |
| F-20260821-07 | 納品物が外部 CDN の画像とフォントに依存 | フォントは `src/assets/fonts/` に同梱。`design/` 隔離も EXIT 0 |
| F-20260821-19 | 手順書が移設元の PC のパスと個人アドレスのまま | `README.md` / `handoff.md` は `git clone` からの手順に統一済み（マスターが Windows で実行して確認） |
| F-20260821-16 | 直せる不具合を「判断が要る」と称して投げ返した | 今回マスターへ上げたのは **#3（鍵の再発行＝私にはできない）** と **#9（`A-4` に触れる緩和の可否）** の2件だけで、どちらも私の手では閉じられない |
| F-20260821-10 | 移設元の失敗記録が転記できていない | 35件が `docs/failures.md` に在り、この照合表がその全件を対象にしている |

---

## この照合から F3 の作業に持ち込む条件

1. **④の保存キーと⑥の読み出しキーを、突き合わせてから書く**（`F-20260821-12/-13`）。
   `magazine-view.js` が読む **11箇所**が正。合わせるのは④の側
2. **`img.src = ''` を作らない**（`D-20260824-30 #3`）。写真が無い箇所は**属性ごと出さない**。
   `magazine-view.js:322` は**いま空文字を入れている**ので、繋ぐ前にここを直す
3. **`esc()` を `innerHTML` に使わない。** いまエスケープしていない（`magazine-view.js:295`）
4. **「押せた」ではなく「同じ値で届いた」で見る**（`D-12`・`docs/deferred.md` #12）
5. **削除は Storage → DB**（`D-20260824-34`）。順序を守らせる検査を先に作る（#2 と #6 は同じ作業）
6. **1回通っただけを根拠にしない**（`F-20260821-23`）。作り直す `verify:*` は連続実行で確かめる
