# AGENTS.md

> **中心思想**
> **既にあるものを探し、必要な差分だけ作り、過去の失敗に該当するときだけ照合し、重要な変更だけ独立した目で疑い、最後は実際に動いた事実で証明する。**
> *(最小の工程で、十分な品質を得る)*

---

## 4層ルール構造 (Rule Hierarchy)

本リポジトリのルールは以下の4階層で構成される。上位レベルのルールは下位レベルに常に優先する。

```text
LEVEL A: NON-NEGOTIABLE RULES     (絶対遵守・省略不可)
LEVEL B: DEVELOPMENT PRINCIPLES    (開発思想・基本Harness)
LEVEL C: RISK-BASED WORKFLOW      (リスク別動的プロセス・Sub-Agent)
LEVEL D: REPOSITORY-SPECIFIC RULES (プロジェクト固有ルール)
```

---

# LEVEL A — NON-NEGOTIABLE RULES (絶対ルール)

リスクレベルや作業規模にかかわらず、いかなる場合も省略・妥協してはならない。
**ルールは2行で書く**（`- **人間**:` / `- **AI**:`）。D-17 参照。

#### A-1 — 鍵とパスワードを、コードに書かない
- **人間**: 店の鍵にあたる文字列をコードや画面に出さない。漏れたら他人が全部のデータを見られる。
- **AI**: APIキー・トークン・パスワード・秘密鍵・本番接続文字列・認証情報を、生成・表示・コミット・受領しない。

#### A-2 — 本物のお客さんの情報を、練習に使わない
- **人間**: 実在するお客さんの名前や連絡先を、テストや見本や記録に使わない。
- **AI**: 実在する個人情報・顧客データをサンプル/テストデータ/ログに使わない。不必要に外部送信しない。

#### A-3 — 出どころの分からないものを混ぜない
- **人間**: どこから来たか分からない写真・音・部品を、店のものに混ぜない。
- **AI**: 著作権不明なコード・画像・音源・ライブラリ・外部データを無断混入しない。

#### A-4 — 検査が赤いとき、検査のほうを消さない
- **人間**: 赤いのは中身が悪いから。検査を消して緑にするのは、嘘をつくのと同じ。
- **AI**: テストの削除・弱体化で CI を通さない。`// eslint-disable` `# type: ignore` 等で握りつぶさない。

#### A-5 — 作業中のゴミを置いたまま「終わり」と言わない
- **人間**: 試しに置いた部品やメモを残したまま完了にしない。
- **AI**: デバッグ用の一時コード・到達不能な死んだコード・不要なログ出力を残したまま完了としない。

---

# LEVEL B — DEVELOPMENT PRINCIPLES (開発原則)

すべての開発タスクにおける基本原則。

```text
1. Goalを確認する
2. repository内を先に探す (Search First)
3. Library / Framework / SDK / API / OSS / SaaS / 特化AIを探す (Reuse First)
4. ExistingとGoalの差分をGapとして定義する (Define the Gap)
5. Gapだけを実装する (Build Only the Gap)
6. 不要な抽象化をしない (No Unnecessary Abstraction)
7. 将来用途だけの実装をしない (No Speculative Implementation)
8. 変更範囲を必要最小限にする (Minimum Change)
9. 機械判定できるものは機械に任せる (Mechanical Check First)
10. 最後は実際に動いた事実で証明する (Runtime Evidence)
```

### 本リポジトリの確定スタック (Confirmed Stack)

これは「推奨」ではなく**確定事項**である。本リポジトリは既に本番稼働しているアプリの
納品用リポジトリであり、選定はやり直さない。テンプレートの鉄板構成を持ち込まないこと。

| レイヤー | 本リポジトリの実体 |
|---|---|
| **言語・ランタイム** | **素の JavaScript**（ES2015 相当）/ Node.js 22。**TypeScript は導入しない** |
| **フレームワーク** | **無し。** React も Next.js も使わない。DOM を直接触る |
| **スタイリング・UI** | HTML 内の `<style>` と CSS 変数。**Tailwind / shadcn / UI ライブラリは使わない** |
| **描画** | Konva v10.3.0 を `src/assets/konva.min.js` に**同梱**。CDN 参照禁止 |
| **フォント・画像** | `src/assets/` に**同梱**。外部 CDN 参照禁止（`F-20260821-07`） |
| **配信・API** | Cloudflare Workers（`worker/src/index.js`）。ビルドは自作の `scripts/build-dist.mjs` |
| **データストア** | **二系統**。Cloudflare Workers KV（本番）と Supabase/PostgreSQL（移行先・未有効化） |
| **バリデーション** | Zod（唯一テンプレートから引き継いだもの） |
| **テスト** | **`node --test`**（Vitest ではない）+ Playwright（E2E・`verify:*`） |
| **Linter / Formatter** | **無し。** 導入されていない。整形は既存コードの書き方に合わせる |
| **CI / CD** | **無し。** GitHub Actions は置かれていない。検査はローカルで走らせる |

**この表から外れる変更（TypeScript 化・フレームワーク導入・Linter 追加・CDN 参照）は、
マスターの承認なしに行わない。** 「今どきの標準はこうだから」は理由にならない。

> 上表に `Next.js` `Tailwind` `Vitest` の語が残っているのは、**使わないと明示するため**。
> 移設 plan の受入基準#15 はこれらの語が 0 件になることを求めていたが、
> 目的は「旧スタックを指示していないこと」であって語の消去ではない。詳細は `F-20260821-20`。

### 標準開発Harness (Default Harness)

通常開発の基本フローは以下のみとする。ReviewやFailure Matchは条件付きでのみ追加する。

```text
EXPLORE  -->  BUILD  -->  VERIFY
```

---

# LEVEL C — RISK-BASED WORKFLOW (リスク別ワークフロー)

変更の影響度（Risk Level）およびトリガー条件に応じて、適切な安全装置を動的に適用する。

## 1. リスクレベル判定 (Risk Levels)

| Level | 種別 | 対象例 | 適用フロー |
|---|---|---|---|
| **Level 0** | **TRIVIAL** | typo, コピー修正, CSS微調整, 単純rename, コメント修正, 1行バグ修正 | `BUILD` → `VERIFY` (EXPLORE省略可) |
| **Level 1** | **NORMAL** | 単一API, 小機能, 局所バグ修正, 既存コンポーネントの軽微拡張 | `EXPLORE` → `BUILD` → `MECHANICAL CHECK` → `VERIFY` |
| **Level 2** | **IMPORTANT** | 新機能追加, 複数ファイル変更, State管理, DB設計/クエリ, 外部API連携, 既存仕様変更, 重要ビジネスロジック | `EXPLORE` → `FAILURE MATCH` → `BUILD` → `MECHANICAL CHECK` → `INDEPENDENT CRITIC` → `WRITER FIX` → `VERIFY` |
| **Level 3** | **CRITICAL** | Auth/認可, 決済/課金, DBマイグレーション, セキュリティ基盤, データ削除, 秘密情報/クレデンシャル | `EXPLORE` → `FAILURE MATCH` → `BUILD` → `MECHANICAL CHECK` → `INDEPENDENT CRITIC` → `WRITER FIX` → `AUTOMATED TEST` → `RUNTIME VERIFY` → `INDEPENDENT VERIFIER` |

---

## 2. 各ステップの詳細仕様

### STEP 1: EXPLORE (探索と差分定義)
- **目的**: 何を作るべきかではなく、**何だけを作ればよいか (GAP)** を特定する。
- **出力形式** (巨大なDiscovery Reportは作らない。原則この3点のみ):
  ```text
  GOAL:     [今回何を実現するか]
  EXISTING: [リポジトリ内・外部OSS等で既に存在・利用可能なもの]
  GAP:      [今回新しく実装・変更する最小差分]
  ```
- **省略条件**: 変更内容を1文で説明でき、既存設計を変更しない場合（Level 0）はEXPLOREを省略可能。

---

### STEP 2: FAILURE MATCH (過去失敗照合)
- **目的**: `docs/failures.md` と照合し、過去の失敗の再発を未然に防ぐ。
- **トリガー条件** (以下のいずれかに該当する場合のみ実行):
  1. **Level 2 / Level 3** の変更
  2. **既知リスク領域 (Known-Risk Area)** の変更:
     `AGENTS.md`, `.claude/`, `.github/`, CI/CD, Git/ブランチ運用, PR/マージ, セットアップ, デプロイ, シークレット管理, テスト基盤
  3. **同一失敗の2回発生 (Repeated Failure)**:
     コマンド失敗、CI失敗、デプロイ失敗、プッシュ/マージ失敗、同一バグ修正の繰り返し、ユーザーからの2回以上の同種指摘
- **担当**: 独立軽量Sub-Agent `failure-matcher`
- **入力**: `GOAL`, `GAP`, `TOUCH` (触るファイル/サブシステム), `PLAN` (予定変更方針) のみ。全文は渡さない。
- **出力**: 最大3件 (`Failure`, `Why relevant`, `Guardrail`) または `NO RELEVANT FAILURE` のみ。

---

### STEP 3: BUILD & MECHANICAL CHECK (最小実装と機械検査)
- **BUILD原則**:
  - Reuse first / Minimum change / Existing pattern first
  - 禁止: 不要なFramework/wrapper、既存helperの再発明、将来用機能、不要な依存追加、全面rewrite、無関係なrefactor
  - 目標: 「最も美しい設計」ではなく「現在のGoalを満たす最小の整合した変更」
- **MECHANICAL CHECK**（本リポジトリでの実体）:
  - Lint と Typecheck は**存在しない**（Linter 未導入・TypeScript 不使用）。代わりに以下3本。

    | コマンド | 見るもの |
    |---|---|
    | `npm run build` | dist が生成でき、相対パスが残っていないこと |
    | `npm run check` | src↔dist の対応と、`design/` が `src/` に流れ込んでいないこと |
    | `npm test` | 全6スイート 66 件 |

  - **3本とも EXIT 0 になるまで VERIFY へ進まない。**
  - 機械判定できるものは先にすべて自動実行する。LLM Reviewerに機械判定を任せない。

---

### STEP 4: INDEPENDENT CRITIC (独立レビュー)
- **目的**: 作成者 (Writer) が気付かなかった重大問題を発見する。
- **トリガー**: Level 2 / Level 3 の変更時
- **担当**: 独立した目線を持つ `independent-critic` (フレッシュコンテキスト)
- **確認項目 (3点のみ)**:
  1. `WRONG GOAL`: 本来の要求・UXを外していないか、必要なフローが抜けていないか、別の問題を解いていないか
  2. `WRONG APPROACH`: 既存解/OSSの車輪の再発明、不適切な技術選択、過剰実装・不要な抽象化がないか
  3. `REAL BUG`: Regression, データ損失, 状態不整合, Race Condition, 権限漏れ, Security, クラッシュ
- **制約**: Critic自身はコードを書かない。リファクタリングを要求しない。代案実装を書かない。
- **Writerの後処理**: 指摘を「採用」「却下」「追加調査」に分類し、採用する場合のみ最小修正する（全面書き直しは禁止）。

---

### STEP 5: REAL VERIFICATION (外部事実による実証)
- **原則**: 自己申告の推論ではなく、外部事実 (Runtime Evidence) で完了を証明する。
- **証拠の強さ**:
  `AI Reasoning < Code Review < Automated Test < Runtime Evidence`
- **本リポジトリの Runtime Evidence**（`npm run preview` を別端末で起動してから実行）:

  | コマンド | 証明するもの |
  |---|---|
  | `npm run verify:m6` | 導線が通ること（ログイン→検索→肉球→カルテ→保存→公開） |
  | `npm run verify:roundtrip` | **トリマーが書いた所見が、同じ値で飼い主に届くこと**（13項目） |
  | `npm run verify:empty` | カルテ0件の犬に、存在しない履歴を見せていないこと |
  | `npm run verify:xss` | 保存されたデータが飼い主のブラウザで実行されないこと |
  | `npm run verify:portal` | `/my` が起動し、未ログインの飼い主にログイン導線が出ること（自分で Worker を立てるので `preview` 不要） |
  | `npm run verify:all` | 上記5本 |

  **`verify:roundtrip` が本リポジトリで最も重要な検査。** 画面が出るか・押せるかではなく、
  書いたものが相手に届くかを見る。この2つは別物で、前者だけを見て「動く」と報告した失敗が
  `F-20260821-11` に記録してある。**入力欄を足したら、必ずこの検査にも足すこと。**

- **対象別検証方法**:
  - **UI / Web**: 実ブラウザ / Playwright等での実操作と表示結果
  - **API**: 実際のHTTPリクエスト/レスポンスとDB状態
  - **Database**: マイグレーション実行、読み書き、ロールバック、互換性
  - **Auth / 権限**: 許可される正常系と**拒否されるべき異常系**の双方案件
  - **File / Batch**: 実ファイルの入出力処理と生成された成果物の内容検証
- **INDEPENDENT VERIFIER**:
  - Level 3 または重要な公開・完了判定時に稼働。完了報告のコマンド・検証手順を再実行し、外部事実を確認する。

---

## 3. セットアップの2段階運用 (Setup Modes)

| モード | 対象 | 最低要件 |
|---|---|---|
| **FAST** (基本) | Prototype, PoC, UI確認, 実験, 内部利用, 短期検証 | 起動可能, Lint, 最低限テスト, Git管理, README |
| **STRICT** | Production, 公開サービス, Auth, 決済, 個人データ, DB migration | FAST要件 ＋ CI, Branch Protection, Smoke/E2E, 独立検証, セキュリティ検査 |

- 判定: `auth`, `payment`, `personal data`, `public release`, `production DB`, `migration` を含む場合は自動で STRICT、それ以外は FAST を基本とする。

---

## 4. セッションの In / Out 規約

### セッション開始 (In)
1. `docs/handoff.md` を読む。
2. 必要に応じて `docs/design.md` を読む。
3. `docs/failures.md` 全文は**読まない**（必要なときだけ Failure Matcher が参照）。

### セッション終了 (Out)
1. `docs/handoff.md` を更新する（今回やったこと、現在の状態、次回やること）。
2. 失敗が発生・解決した場合は `docs/failures.md` に追記する (append-only)。
3. コミット & プッシュ（PR / マージは指示がある場合のみ）。

---

# LEVEL D — REPOSITORY-SPECIFIC RULES (プロジェクト固有ルール)

本プロジェクト固有の制約・要件をここに追記する。

### ルール昇格原則 (Promotion Rule)
- 失敗が1件起きただけで安易にGlobal Rule（AGENTS.md）へ追加してはならない。
- 失敗はまず `docs/failures.md` に記録し、Failure Matcherで再利用する。
- **「複数回発生した」「重大事故に直結する」「機械判定可能」「汎用性が高い」** 条件を満たすもののみ、このセクションまたは上位レベルへ昇格する。

### [プロジェクト固有ルール一覧]

いずれも**実際に起きた事故から昇格したもの**で、括弧内が出典。
機械強制のあるものは、規則を覚えていなくてもコマンドが止める。

**ルールは必ず2行で書く。**
`- **人間**:` が非エンジニア向けの1行、`- **AI**:` が実行する側の1行。
人間の行に専門語を入れない。読めないルールは守られないし、マスターが判断できない。

---

#### D-1 — 本番は `src`、`design` は絵の見本
- **人間**: お客さんに見えているのは `src` の中身だけ。`design` は動かない絵の見本なので、本番に混ぜない。
- **AI**: `src/` が本番の実体。`design/` を `src/` `worker/` `scripts/` から参照しない。機械: `npm run check`

#### D-2 — 「保存しました」と出たのに保存できていない、を防ぐ
- **人間**: 保存に失敗したときは、必ず失敗と画面に出す。黙って下書きのまま残さない。
- **AI**: `finalize_report` の `null` 返却は必ず失敗として扱う。例外を投げないので、握ると draft のまま残る。

#### D-3 — `/api` に鍵が無いのは、わざと
- **人間**: 「鍵がかかっていない」のは今のところ意図した作り。勝手に鍵をかけない、見つけて騒がない。
- **AI**: `/api/*` の無認証は意図された前提。勝手に塞がない・勝手に脆弱性として報告しない。

#### D-4 — 出どころ不明の写真を、外に出さない
- **人間**: どこから来たか分からない写真は、お客さんに見せる画面に載せない。後で使えなくなる。
- **AI**: 出所未確認の素材を外部公開物へ転載しない。外部 CDN も参照しない（`F-07`）。機械: `docs/ASSET-PROVENANCE.md`

#### D-5 — 配るものは手でコピーしない
- **人間**: 配信用のファイルを手で写さない。ズレて「直したはずが直っていない」が起きる。
- **AI**: 生成物は `npm run build` 経由でのみ作る。dist への手コピー禁止。機械: `npm run check`

#### D-6 — 消す前に、まだ使われているか見る
- **人間**: 古いと思って消したものが、実はまだ使われている、が起きる。消す前に必ず確かめる。
- **AI**: 上書き・削除の前に、消える識別子を `grep -c` して生存を確認する。

#### D-7 — 自分のルールを自分で守れると思わない
- **人間**: 「気をつける」では守れない。守らせるのは機械にやらせる。
- **AI**: ルールは `.gitignore` / npm の EXIT 1 / test に落とす。文章だけで終わらせない。

#### D-8 — 失敗は1つの帳面にだけ書く
- **人間**: 失敗とやり残しは、決まった1か所にだけ書く。あちこちに書くと誰も追えない。
- **AI**: 失敗と未完了は `docs/failures.md` にのみ記す。commit hash だけで参照しない。機械: `Status: OPEN` 行

#### D-9 — 画面の目印に日本語を使わない
- **人間**: 部品の目印に日本語を使うと、掴み損ねて画面が壊れる。
- **AI**: `data-*` の値に非 ASCII を使わない。使うならセレクタに連結せず `pickByValue()` を使う（`F-12`・`F-17`）。機械: `npm run verify:xss`

#### D-10 — 飼い主の画面に、見本を出さない
- **人間**: 書いていないことは空で出す。例文やデモが出ると、お客さんはそれを本当のことだと読む。
- **AI**: `window.__VIEW__` の画面に見本・デモ・既定文を出さない。未記入は空で出す（`F-14`・`F-15`）。機械: `npm run verify:empty`

#### D-11 — うちの画面が作らない形でも、データは入ってくる
- **人間**: 「うちの画面ではこうしか作れない」は「こうしか来ない」ではない。
- **AI**: 保存済み JSON は無認証 API からも入る。入力形を信用しない（`F-17`）。機械: `npm run verify:xss`

#### D-12 — 「押せた」ではなく「同じ値で届いた」で見る
- **人間**: ボタンが押せたことは、伝わったことではない。書いた値が相手の画面に同じで出て、初めて合格。
- **AI**: 検査は「入力した値が受け手に同一で届いたか」で書く（`F-11`）。機械: `npm run verify:roundtrip`

#### D-13 — よそから持ってきた文書は、コードと同じ回数読み直す
- **人間**: 持ち込んだ手順書が、いまのこの店の話とは限らない。
- **AI**: 移設・複製した文書、特に「削除禁止」「必ず通せ」は対象の実在を確かめる（`F-03`・`F-19`）。

#### D-14 — 人が使えるかは、画面の写真だけで見る
- **人間**: 見るのは2つだけ。**最後まで行けるか**と、**間違えても2タッチで戻れるか**。
- **AI**: 判定にコードを一切読まない。`npm run walk` / `npm run walk mistakes` の画像のみで判定する。

#### D-15 — ルールを書く場所は1つだけ
- **人間**: ルールはこの `AGENTS.md` にだけ書く。2か所にあると、AI ごとに言うことが変わる。
- **AI**: `.claude/` `.codex/` `*.claude.json` `CLAUDE.md` にルールを書かない。機械: `.gitignore`

#### D-16 — いまは「整理整頓」の期間
- **人間**: 新しく作らず、散らかったものを並べ直している期間。見つけた不具合は原則あと回し。
- **AI**: 現在フェーズは `docs/ops/phase`、計画の正は `docs/ops/plan.md`。範囲外を触らない。機械: `npm run guard`

#### D-17 — ルールは2行で書く
- **人間**: どのルールも「人間向け1行」「AI向け1行」で書く。読めないルールは判断もすり合わせもできない。
- **AI**: 新規ルールは必ず `- **人間**:` と `- **AI**:` の2行にする。専門語は AI 行にだけ置く。

#### D-18 — 「調べた」「直した」「解決した」は別のもの
- **人間**: 調べただけ、症状を消しただけを「解決した」と言わない。**戻したら再び壊れることを見せて、初めて解決**。
- **AI**: 三層を混同しない（下記）。解決の主張には**赤 → 緑 → 戻して赤**の3出力が要る。機械: `node scripts/guard/solved.mjs`

### D-14 の手順 — 人間が使えるかの確認（マスター指定・2026-08-24）

**確認するのは、この2つだけ。**

1. **正解の手順で最後まで到達できるか**
2. **操作を間違えたとき、2タッチ以内に、やりたかった操作を完了できるか**

**確認方法**

- **スクリーンショットの確認のみ。**
- **コードは一切見ない。**

**なぜこの形か**

コードを読むと、人間には関係のない粗（デモ用の既定文が残っている等）を数え上げてしまう。
そういうものは人間が見れば間違いだと分かるので、確認の対象ではない。
画面だけを見れば、**進めるか / 戻れるか**という人間にとっての合否だけが残る。

`verify:*` が全部緑でもマスターが本番で詰まった（`D-20260824-37`）のは、
機械が「セレクタを掴めるか」を見ていて、**人間が到達できるか**を見ていなかったため。

**外部リポジトリ（vibe-base 等）を参照しない。** 移設は完了しており、このリポジトリ単独で
ビルド・検査・実行・失敗照合のすべてが完結する。行番号や commit hash で他リポジトリを
指す記述は失効しているものとして扱う（`F-09`）。

---

### D-16 の中身 — 整理整頓プラン（F1 / F2 / F3）マスター指定・2026-08-25

**F3 が終わるまで有効。セッションを跨いでも、AI が替わっても破棄しない。**
計画の全文は `docs/ops/plan.md`。現在のフェーズは `docs/ops/phase` の1行（`F1` / `F2` / `F3`）。

#### なぜやるか

本番は動いていたが、**マスターが指定した動線どおりに進めなかった**。
検査は9本すべて緑で、それでも人間は前に進めなかった。
UI とバックエンドが混ざっていて、**画面の良し悪しをバックエンドの緑で隠せた**からである。
だから **UI を切り離し、UI だけで動線を完成させてから、繋ぎ直す。**
新しく作るのではなく、**いま在るものを並べ替える。**

#### フェーズ

| | やること | 完了条件 |
|---|---|---|
| **F1** | UI とバックエンドの隔離 | `src/` に UI 以外が無い／UI から `backend/` への参照が 0（機械で確認）／`build`・`check`・`test` が EXIT 0 |
| **F2** | 正UI のみで動線を完了させる | D-14 の2点が両方 ○（**スクショだけで判定。コードは見ない**） |
| **F3** | 正UI とバックエンドをつなぐ | F2 の2点が**実データでも**同じように通る |

F2 の動線（**意匠モックが正。自分の発案を混ぜない**）:
`① URLを開く → ② ログイン → ③ 犬の名前を選ぶ → ④ カルテ作成 → ⑤ 確認 → ⑥ 顧客ページ`

#### 重要ルール（マスター指定・全フェーズに適用）

**①現状あるものだけで完成させる**
- **人間**: 新しい道具や部品を買い足さない。いま家に在るもので片づける。
- **AI**: 新規実装・新規依存の追加を禁止。`npm install` は `npm run guard` が止める。

**②納品物の完成 ≠ フェーズの完成**
- **人間**: F3 が終わっても「売れる状態」ではない。片づけが終わっただけ。
- **AI**: フェーズの完了条件を、納品可否や受入基準と読み替えない。

**③目的は現状の整理整頓**
- **人間**: 良くすることではなく、いま在るものを並べ直すことが目的。
- **AI**: 機能追加・設計改善・リファクタを目的に含めない。

**④見つけた問題は原則あと回し**
- **人間**: 途中で見つけた不具合はその場で直さない。直すのは「直さないと次の画面へ行けない」ものだけ。
- **AI**: 例外時も「進める状態まで」しか直さない。判断基準は「次の画面へ行けるか」だけ。

**⑤あと回しは記録に残す**
- **人間**: あと回しにしたものは全部書き残し、F3 が終わってからマスターが直す／放置するを決める。
- **AI**: `docs/deferred.md` に1件1行で記す。F3 完了後に棚卸しする。

**④の判断基準はこれだけ**: 「これを直さないと次の画面に行けないか？」
行けるなら直さず、`docs/deferred.md` に1行書いて先へ進む。

#### 監視の3本（どの AI でも同じように回す）

特定の AI のサブエージェント機能に依存しない。**役割・発火・成果物**だけを決める。
サブを起こせる AI はサブに、起こせない AI は自分で、同じことを同じ順でやる。

| | 役割 | 発火 | 成果物 |
|---|---|---|---|
| **① 逸脱監視** | いまの行動が**現在フェーズの範囲内か**だけを見る。良し悪しは見ない、場所だけを見る。自分で直さない、止めて理由を言うだけ | **常時** | — （`npm run guard` が機械で止める） |
| **② バッドシナリオ** | そのフェーズの**バッドシナリオを10個**出し、**マスターに提案する** | **フェーズ開始直後** | `docs/ops/bad-scenarios-F{n}.md` |
| **③ 再発防止** | `docs/failures.md` と `docs/decisions.md` を**全件**照合し、同じ失敗が**起こり得ないか／既に起こっていないか**を見る | **フェーズの開始直後と完了直後** | `docs/ops/failure-check-F{n}-{start,end}.md` |

**② の制約（マスター指定・厳守）**

- **本質的かつ単純な見落としに限定する。**
- **レアケース・影響度の小さい事柄を想定してはならない。**
- 判定基準はこれだけ: **「これを1つ見落としたら、そのフェーズの完了条件が満たせなくなるか？」**
  ならないものは**出さない**。10個に届かなくても水増ししない。
- **マスターの承認を受けてから** `承認: 済` と書き、10個を**実際に実行**し、
  **該当しないこと**を確かめてからフェーズを進める。1つでも該当したら進めない。

**③ の書き方**: 根拠は**見たファイル名か、実行した命令とその出力**にする。「たぶん大丈夫」は書かない。

#### 機械強制

```
npm run guard          変更した全ファイルを、現在フェーズの範囲と関所に掛ける
npm run check          guard を含む（忘れても止まる）
node scripts/guard/scope.mjs <path...>        1本だけ見る
node scripts/guard/scope.mjs --cmd "<命令>"   命令が範囲外を触らないか見る
node scripts/guard/gate.mjs --end             フェーズを閉じてよいか
```

`npm run guard` が赤いのは、たいてい**正しい**。赤を消すために範囲を広げない。
範囲外を触る必要が本当にあるなら、**マスターに言って判断を仰ぐ**。


---

### D-18 の中身 — 解決の定義と、解決に見せかける手口（マスター指定・2026-08-25）

#### 三層を混同しない

| 層 | やること | やっていないこと |
|---|---|---|
| **調べる（テスト）** | 該当するか・しないかを判定する | **何も直していない** |
| **直す（修正）** | 症状が出ないようにする | **原因は残っていてよい** |
| **解決する** | 原因を無くす | — |

- **人間**: 「検査を通した」は調べただけ。「出なくなった」は直しただけ。原因が残っていれば、また出る。
- **AI**: 報告では必ずどの層かを明示する。層を書かない完了報告は無効とする。

#### 解決の定義 — 5つすべてを満たしたときだけ「解決」と言う

1. **原因に触れた** — 症状の出口ではなく、症状を生んだ場所を変えた
2. **元の手順で再現しない** — 見つけたときと**同じ手順**でやって、出なくなった
3. **戻すと再び出る** — 直しを元に戻すと症状が戻る。**「直したのはそこだ」の証明はこれしかない**
4. **同じ原因の他の場所も消えた** — 1か所だけ塞いでいない
5. **証拠が出力である** — 実行した命令とその出力が残っている。**文章での申告は証拠ではない**

**2 と 3 を飛ばした「解決」は、ほぼ必ず偽物である。**

#### 解決に見せかける手口 — 12種（見つけ次第この表に足す）

| # | 手口 | **人間**（何をしているか） | **AI**（何が起きているか / どう潰すか） |
|---|---|---|---|
| **偽-1** | 検査を消す・弱める | 赤い検査そのものを消して緑にする | テスト削除 / skip / assert 緩和。**潰し**: 検査の本数と対象件数を記録し、減ったら EXIT 1 |
| **偽-2** | 見る範囲を狭める | 調べる場所を減らして「0件」にする | 対象拡張子・ディレクトリの絞り込み。**潰し**: 走査対象の件数を記録し、減ったら EXIT 1 |
| **偽-3** | 例外リストに載せる | 違反を「これは許可」と書き足して通す | allow-list / ignore への追加。**潰し**: 例外は `docs/deferred.md` の番号必須。件数が増えたら EXIT 1 |
| **偽-4** | 判定条件を緩める | 合格ラインを下げて通す | 閾値・比較・順序の変更。**潰し**: 「戻すと赤」の再現出力を必須にする |
| **偽-5** | 別の緑で覆う | 関係ない検査が緑なので、全体が直ったように見せる | 中身が空の検査を EXIT 0 の根拠にする。**潰し**: 各検査に「**何を保証しないか**」を書かせる |
| **偽-6** | 報告文だけで直したことにする | commit や報告に「直した」と書く。動かして見ていない | 実行出力の無い完了報告。**潰し**: 3出力が無ければフェーズを閉じない |
| **偽-7** | 指定を直さず別のものを足す | 言われた入口を直さず、思いついた入口を1つ足して「行けるようになった」と言う | 仕様（意匠モック）との差分を取らない追加実装。**潰し**: モックと `src/` の操作集合を機械で比較 |
| **偽-8** | あと回し記録に入れて終える | `deferred` に書いたら片付いた気になる | 記録は**保留**であって解決ではない。**潰し**: 報告に `deferred` の件数を必ず出す。減っていなければ「解決0件」と言う |
| **偽-9** | 症状の場所を移す | 消す・別の場所へ動かす。目の前から消えただけ | 削除・移設を解決に数えない。**潰し**: 削除は復元可能性（`git show`）と呼び出し元の生存（`grep -c`）を示す |
| **偽-10** | 壊れるところを一度も見ていない | 直したと言うが、壊れた状態を自分の目で見ていない | 「戻すと赤」を実行していない。**潰し**: 3出力を必須にする |
| **偽-11** | 一度の緑で終える | たまたま1回通ったのを根拠にする | 同一コミットで2回以上、同じ結果になることを示す |
| **偽-12** | 人間の合否を機械の合否に置き換える | 検査が緑＝人間が使える、と読む | D-14 の由来そのもの。**潰し**: 人が使えるかは `npm run walk` の画像だけで見る |

#### 機械での潰し方

`docs/ops/solved-F{n}.md` に、**該当した1件につき3つの出力**を貼る。

```
### {番号} {見出し}
種別: 解決 ／ 回避 ／ 保留      ← 回避・保留は「解決」と呼ばない
#### 直す前（赤）
（命令と出力）
#### 直した後（緑）
（命令と出力）
#### 直しを戻した（また赤）
（命令と出力）
```

`node scripts/guard/solved.mjs` が次を見る。無ければ EXIT 1。

- `docs/ops/bad-scenarios-F{n}.md` で **`結果: 該当した`** の項が、すべて `solved-F{n}.md` に在るか
- 各項に **3つの見出しと3つの出力**が在るか
- **「直す前（赤）」と「直しを戻した（また赤）」に同じ症状の行が在るか**（別のものを直していないか）
- `種別: 回避` `種別: 保留` の項に `docs/deferred.md` の番号が書いてあるか

`node scripts/guard/gate.mjs --end` がこれを呼ぶ。**通らない限りフェーズは閉じられない。**
