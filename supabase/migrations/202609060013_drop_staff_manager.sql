/* スタッフ管理の画面を削除する（マスター指示 2026-09-06）。

   `D-20260823-05` で「とりあえず残す」と決めたまま、**押す場所が1つも無い**状態で
   残っていた（`grep` で呼び出し0件・実測）。お店の人を招待する／利用を止める、の
   2つだけの画面で、招待は `/edit` の犬の一覧からも発行できる。
   **押せない画面は無いのと同じ**（`D-12`）なので、画面ごと削除した。

   この関数を呼ぶ経路は `/api/staff` の PATCH だけで、それも一緒に削除した。
   残しておくと「誰も呼ばないのに `security definer` で書き込める関数」になる。 */

begin;

drop function if exists public.update_staff_membership(uuid, uuid, boolean);

commit;
